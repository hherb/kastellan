# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# Editors / IDEs
.idea/
.vscode/
.claude/settings.local.json
.claude/worktrees/
*.swp
*.swo

# Direnv / env
.envrc
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Logs / runtime artefacts
*.log
*.pid
/run/
/state/
/data/
/scratch/

# Generated docs site (built fresh in CI, never committed)
/dist/

# Python (only ever inside workers/python-exec/)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# browser-driver uses a system venv + pip (NOT uv), so a stray uv.lock here is a
# local artefact — ignore it. (gliner-relex's uv.lock IS tracked; it uses uv sync.)
/workers/browser-driver/uv.lock

# Local agent artefacts
audit-*.jsonl
2026-05-09-000520-since-a-system-reboot-after-update-to-newest-apt.txt

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
docs/essay-medium-draft.md
