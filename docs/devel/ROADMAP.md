# kastellan — Development Roadmap

Sequenced feature list. Items are added whenever a feature decision is made
and ticked off as they ship. Order reflects expected build sequence — earlier
items unlock later ones.

> **How to update.** When we agree on a new feature, append it to the most
> appropriate phase (or a new phase) at the position that respects
> dependencies. When a feature ships, change `[ ]` → `[x]` and condense its
> entry to a **terse one line** with the commit/PR hash — enough to document
> the build sequence, no more. Pending `[ ]` items keep their full design
> context. **Pure refactors, test-module lifts, file splits, clippy/CI gates,
> flake fixes, and isolated bug fixes are NOT recorded here** unless they're
> load-bearing for remaining work — git history and the handover archives
> (`handovers/archive/`) are the durable record for those.

---

## Phase 0 — Skeleton & First Sandboxed Worker (Linux)

- [x] Cargo workspace, AGPL-3.0 license, README, .gitignore — `140eec5`
- [x] `kastellan-core` (bin+lib stub) — `140eec5`
- [x] `kastellan-sandbox` crate skeleton (trait + policy struct) — `140eec5`
- [x] `kastellan-supervisor` crate skeleton — `140eec5`
- [x] Architecture & threat-model doc skeletons — `140eec5`
- [x] Linux bwrap backend (`linux_bwrap.rs`): unshare-all, FS bind, --clearenv, --setenv, die-with-parent, new-session, as-pid-1 — `eae3df4`, `f2411ec`
- [x] AppArmor `unprivileged_userns` workaround: `scripts/linux/install-bwrap-apparmor-profile.sh` + `LinuxBwrap::probe()` — `eae3df4`
- [x] Sandbox negative tests (/etc/passwd + /home invisible, listed paths visible, net unreachable, relative paths rejected) — `eae3df4`
- [x] `kastellan-protocol` crate: JSON-RPC 2.0 server/client over stdio (MCP-stdio compatible) — `f2411ec`
- [x] `workers/shell-exec`: argv allowlist, no shell interpretation (`KASTELLAN_SHELL_ALLOWLIST`) — `f2411ec`
- [x] `core::tool_host::spawn_worker`: spawn worker under sandbox, return connected protocol Client — `f2411ec`
- [x] End-to-end test: core → bwrap → shell-exec → JSON-RPC echo + POLICY_DENIED + METHOD_NOT_FOUND — `f2411ec`

## Phase 0 hardening — Defence in depth (Linux)

- [x] Landlock LSM as second FS-allowlist layer in the worker (ABI v6) — `3210f70`, `97d4465`
- [x] seccomp-bpf syscall filter — per-profile allow-list (`Strict` kills `socket()`, `NetClient` permits) — `3210f70`, `97d4465`
- [x] Worker prelude crate (`workers/prelude`): `serve_stdio` calls `lock_down()` before serving — `3210f70`
- [x] `tool_host` derives lockdown env (`KASTELLAN_LANDLOCK_RW` / `KASTELLAN_SECCOMP_PROFILE`) so callers can't skip worker-side layers — `3210f70`
- [x] cgroup v2 CPU/memory caps via `systemd-run --user --scope` (MemoryMax + MemorySwapMax=0 + CPUQuota + TasksMax); probe fails closed without a live `systemd --user` — `3cea642`
- [x] Policy-driven `cpu_quota_pct` / `tasks_max` + `setrlimit(RLIMIT_CPU)` `cpu_ms` enforcement (cross-platform `prelude/rlimit.rs`) — closes #6, 2026-05-14
- [x] Per-task `Workspace` RAII type (`<root>/<task_id>/{in,out,tmp}`, single owner, `extend_policy` wiring) — `9333311`
- [x] Spawn timeout / wall-clock kill (`WorkerSpec.wall_clock_ms`, watchdog thread, `kill(-1)`-fanout guard) — `57edfb2`

## Phase 0b — macOS Port (Seatbelt)

> Done before adding more workers, to stop Linux-isms leaking through the codebase.

- [x] `macos_seatbelt.rs`: SandboxPolicy → `.sb` (TinyScheme) generator; strict profile denies unrestricted mach-lookup (#1) — `2fa46a2`
- [x] `sandbox-exec` invocation + `setsid` fresh-session isolation (#2) — `2fa46a2`
- [x] setrlimit CPU via shared `prelude::rlimit` (mem/wallclock deferred to container backend / parent watchdog) — 2026-05-14
- [x] Network containment via `(deny network*)` + allowlist — `2fa46a2`
- [x] All sandbox containment + e2e tests mirrored green on macOS — `2fa46a2`

## Phase 0 cont. — Service supervisor

- [x] Linux `systemd --user` unit generator + `systemctl --user` driver (`supervisor/src/systemd_user.rs`) — 2026-05-10
- [x] macOS LaunchAgent plist generator + `launchctl bootstrap` driver (`supervisor/src/launchd_agents.rs`) — 2026-05-08
  - [x] `run_launchctl` bounded by `bounded_command::run_capped` (20s) so a churn-degraded GUI domain can't hang a lifecycle call at 0 CPU — 2026-06-25 (residual read-only `print` calls: [#353](https://github.com/hherb/kastellan/issues/353))
- [x] Core daemon `ServiceSpec` (`specs::core_service_spec`) + cross-OS `default_probe()` + e2e against the real binary — 2026-05-09
- [x] `kastellan.target` that brings up Postgres + core — native systemd `.target` (Linux) / readiness-based bundle (macOS); inference is an external health-checked dependency, workers are core-owned (spawned on demand). `TargetSpec` + `Supervisor::{install,start,stop,uninstall}_target` + `specs::kastellan_target_spec()`; `ServiceSpec.after`/`part_of` ordering fields; gated `target_smoke` e2e ran live against `systemctl --user` — branch `feat/kastellan-target-bring-up`, 2026-06-06
- [x] Auto-restart with backoff on worker crash (Option K). `ServiceSpec.restart_backoff: Option<RestartBackoff { max_delay_sec, steps }>` (additive, `#[serde(default)]`, `None` = old constant-`RestartSec=5` behaviour). systemd backend emits `RestartSteps`/`RestartMaxDelaySec` (252+; older systemd warns-but-loads) inside the `keep_alive` block; macOS launchd warns-and-ignores at install (no equivalent knob — same posture as `after`/`part_of`). core+postgres specs wired with a 5s→300s/8-step curve. Builder test modules lifted to siblings to stay under cap — branch `feat/restart-backoff`, 2026-06-07
- [x] **One-command operator install** — `kastellan-cli install` / `uninstall [--purge]` takes a freshly-built tree to a running, supervised, per-user Kastellan (Postgres + daemon under `systemd --user` / launchd). Copies all binaries (atomic) into a flat `~/.local/lib/kastellan/` prefix + assets into `~/.local/share/kastellan/`, shells out to idempotent `kastellan-db-init --username $USER` (peer-auth match), writes a tunable `~/.config/kastellan/kastellan.env` (mode 0600) carried by `ServiceSpec.environment_files` → systemd `EnvironmentFile=` (launchd folds it into plist `EnvironmentVariables` — no silent drop), defaults to Ollama `gemma4:26b-a4b-it-q8_0` + `embeddinggemma` (memory-fit-checked `ollama pull` when local), enables linger (Linux), restart (stop→start) so reinstalls apply new artifacts, and verifies (PG socket + both services `active`, polled to 90s). Resolves HANDOVER open-question #6 (production install convention). `core/src/install/{plan,run}.rs` + `core/src/bin/kastellan-cli/install.rs`. PRs [#316](https://github.com/hherb/kastellan/pull/316) + review fixes — **DGX-verified post-merge (build + reinstall + `--no-start` + `secret list` all green)**, 2026-06-20
  - [x] **Operator config survives a reinstall — `kastellan.env.local`** — closes [#458](https://github.com/hherb/kastellan/issues/458) (branch `fix/458-operator-env-overlay`, PR **OPEN** 2026-08-09, gated on both hosts + live-accepted). `install` regenerates `kastellan.env` from CLI flags every deploy, so hand-added keys were dropped and tuned values reverted **silently** — which on 2026-08-06 removed `KASTELLAN_MAIL_ENDPOINT` and cost the deployed bot its whole mail capability for two days: `mail.*` never registered, the planner fell back to `shell-exec` filesystem probing, and the only symptom was the bot answering *"I do not have permission to access your files"*. No code defect anywhere. **The design rests on a measurement**: `EnvironmentFile=` overrides `Environment=`, later files win, `-<missing>` starts fine — verified on the DGX with a transient unit, so one overlay listed second fixes lost keys *and* reverted values, and the regenerated file can also override the unit's own `KASTELLAN_EGRESS_FORCE_ROUTING=1` (hence part 3). `ServiceSpec.environment_file: Option<PathBuf>` → `environment_files: Vec<EnvFileRef{path, optional}>`, applied in order; `optional` is a real field because launchd's `read_to_string(...)?` hard-errors on the absence that is `kastellan.env.local`'s normal state. `parse_env_file`/`merge_env` lift out of the macOS-only `launchd_agents::builders` into a `cfg`-free `supervisor/src/env_file.rs` (#511's `atomic_write` precedent) — **and the DGX gate proved the point: predicted 3067, landed 3069, the +2 being those two tests running on Linux for the first time**, having never compiled on the DGX or in CI. Part 2: pure `diff_env_files` + `kastellan.env.bak` (written only on a non-empty diff, so a later clean install cannot clobber it) + a warning naming every dropped/changed key — **key names only, never values**, since the operator reads values from the backup. Part 3: a disabled force-routing now emits a `warn!` + `egress.force_routing_disabled` audit row instead of nothing (`main.rs` had no `else` arm at all), phrase as a `const` per #516/#524/#525. **Five of six review findings were defects in the PLAN TEXT**, transcribed faithfully and invisible to test counts and clippy — worst being `diff_env_files` comparing a repeated key's *first* value where systemd and our own `merge_env` take the *last*, so `old=A=1\nA=2` vs `new=A=1` (a real revert) reported **nothing at all**. The whole-branch review then found three cross-task defects per-task reviews structurally could not: `README.md` still taught the retracted behaviour, `upgrade_from_git.sh` could not read the overlay it now recommends, and `uninstall --purge` destroys overlay + backup while the daemon recommends `--purge`. **Live-accepted**: with the settings in *both* files, one install printed `dropped:` ×4 + `changed:` ×1, wrote the backup, left the overlay's mtime untouched, and `/proc/<MainPID>/environ` then carried all four keys plus the `-ctx64k` tag and `FORCE_ROUTING=1` — the exact 2026-08-06 sequence, survived. DGX **3069 / 0 / 53**, clippy clean; Mac supervisor 87/0 + `install::` 42/0 + aarch64 cross-lint clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-08-08-operator-env-overlay*`.
  - [x] **The installer's worker list had silently lagged the workspace by five binaries** — closes [#504](https://github.com/hherb/kastellan/issues/504) (PR [#520](https://github.com/hherb/kastellan/pull/520), merged `41b21f36`). `optional_binaries()` was written when there were six workers; `mail` (#483), `email-in` (#496), `web-research` and both brokers were never added, and since the daemon discovers workers as `current_exe()`-relative siblings, **each was undeployable regardless of configuration** — one startup `ERROR` line, otherwise a completely healthy-looking install. Confirmed live on a fresh deploy of `main` (`"worker misconfigured; skipping","tool":"web-research"`), together with the worse half: the `cp` that worked around it for the mail worker during #492 left the deployed binary dated 2026-08-01 and no install has refreshed it since. The durable half of the fix is a drift guard in **`tests-common::installable`** — every binary **any workspace member builds** must be installed or in an explicit `NOT_INSTALLED` list *with a written reason*. Workspace-wide, not `workers/*`: a new `core`-level binary would go missing exactly the way `kastellan-worker-mail` did. It lives in `tests-common` because `linux-check.yml` is compile-only apart from `cargo test -p kastellan-tests-common`, making it the one guard that runs on **every PR**; a defect whose character is surviving checks that are not looking for it has to be caught the day a worker is added. The guard deliberately does not assert *which* list a binary lands in — only that the call was made consciously. Filed while writing the exemptions: [#519](https://github.com/hherb/kastellan/issues/519) — `kastellan-microvm-run` resolves from `$PATH`, not as an exe-relative sibling, so adding it to the list would copy it where the resolver never looks (recorded as an explicit exemption, not papered over). **Review round (`984dc53a`)** caught the guard having the same disease it was written to cure: it knew only two of Cargo's three binary-discovery modes, so a crate adding a bare `src/bin/*.rs` with no `[[bin]]` section would have passed green while the binary went uninstalled — dormant only because every such file in the tree happens to carry an explicit declaration. Discovery now covers `autobins` and dedupes candidates by claimed **path** before name (`prelude` renames its `src/bin` files, so a name-only check invents phantom targets), the hand-rolled line scanner was replaced with the `toml` parser already in the workspace (it read a commented-out `# "workers/retired",` as a live member — the #479 shape), and `optional_binaries()`'s docstring no longer claims a missing binary always costs "one `ERROR` line": the two broker sidecars log *nothing* when absent, and `search-broker` backs two tools rather than one. Mutation-checked: a bare `phantom_tool.rs` now fails the guard and passed before. DGX **3000 / 0 / 53**, clippy clean.

## Phase 0 cont. — Postgres bring-up

- [x] Local Postgres via PGDG apt + user-level supervisor unit (`scripts/linux/install-postgres.sh`, PG 18; macOS via Homebrew) — 2026-05-09
- [x] Localhost-only UDS, peer auth, dedicated `kastellan` role, locked-down `initdb` (`kastellan-db-init`, idempotent) — 2026-05-09
- [x] `pgvector` extension; full-text search via native `tsvector`+GIN; graph storage via relational `entities`/`relations` behind a `Graph` trait — 2026-05-09 (closes #9/#10 won't-fix)
- [x] `db/migrations/` skeleton (`memories`/`tasks`/`entities`/`relations`/`audit_log`/`secrets`); `vector(1024)` (bge-m3 dim) — 2026-05-09
- [x] `sqlx` embedded `MIGRATOR` run at core startup, fail-closed — 2026-05-09
- [x] Secrets at rest: AES-256-GCM + OS keyring (`db::secrets`, AAD-bound, `Zeroizing`); migration 0004 — closes #12, 2026-05-10

## Phase 0 cont. — Audit log

- [x] Non-superuser `kastellan_runtime` role + DB-layer REVOKE on `audit_log` (append-only enforced by Postgres); migration 0002 — 2026-05-10
- [x] Append-only audit writer at the `tool_host::dispatch` chokepoint; migration 0003 NOTIFY trigger; runtime-pool `SET ROLE` on every connection — closes #11, 2026-05-10
- [x] JSONL on-disk mirror under `~/.local/state/kastellan/` (`audit_mirror::spawn_mirror`, daily rotation, fsync per write) — 2026-05-10
- [x] CLI viewer: `kastellan-cli audit tail` (no DB connection required) — 2026-05-10

## Phase 0 cont. — LLM router stub

- [x] OpenAI-compatible HTTP client (`kastellan-llm-router`, `Router::send`, reqwest + rustls) — Option J, 2026-05-10
- [x] Local backend pointer (vLLM/SGLang :8000 Linux, Ollama :11434 macOS; `KASTELLAN_LLM_*` env) — 2026-05-10
- [x] Frontier backend pointer — unwired (`PolicyDeniedFrontier`) until the Phase-5 policy gate; key sourced from `db::secrets`, never env — 2026-05-10

## Phase 1 — Memory & Loop

- [x] **Dispatcher chokepoint invariant** — every tool/channel/routine action enters core through `tool_host::dispatch()`; `WorkerCommand` newtype seals direct worker calls (module-private), pinned by a `compile_fail` doctest — Option M, 2026-05-10 (seal tightened #16, 2026-05-13)
- [x] **`memory::recall` — semantic + lexical lanes** via pgvector + `tsvector`/GIN, fused with Reciprocal Rank Fusion — Option N, 2026-05-10
- [x] **Graph lane in `memory::recall`** — `memory_entities` join table (migration 0007) + 1-hop `Graph::neighbors` expansion fused alongside semantic/lexical; `GRAPH_FANOUT_CAP_PER_SEED=32` — Option P, PR #41 (`76fe940`), 2026-05-13
- [x] **Embedding router** — `Router::embed` + `core::memory::embed_query`; OpenAI-compat `/embeddings`, dim-validated against `EMBEDDING_DIM` — Option O, 2026-05-12
- [x] **Scheduler (CASSANDRA Phase 1)** — tick → claim task → LLM plan → CASSANDRA review → step dispatch loop; lanes/leases/`FOR UPDATE SKIP LOCKED`/NOTIFY triggers, `agent_prompts` SHA-256 ledger; migrations 0005/0006 — merged `93da413`, 2026-05-11 (stub ConstitutionalGuard + DeterministicPolicy held for the observation phase)
- [x] **`ToolHostStepDispatcher`** — `ToolRegistry`/`ToolEntry` host-side allowlist, spawn-per-step through `tool_host::dispatch`, deny-by-default `shell_exec_entry` — 2026-05-11
- [x] **Scheduler audit-row coverage** — spec §7 task-lifecycle rows (`task.running`/`task.<state>`/`task.finalize`), step short-circuit rows (`step.unknown_tool`/`step.spawn_failed`), crash-recovery `task.crashed`+finalize, producer-side `task.submitted`/`task.cancelled`/`task.finalize` from the CLI — 2026-05-12 → 2026-05-14
- [x] **`cli_ask_e2e` full-chain pin** — real CLI → daemon → sandboxed worker → Postgres, mock LLM only; the canonical end-to-end regression for the whole loop — 2026-05-11
- [x] **First real `ConstitutionalGuard` rule** — substring screen over the 5 constitutional principles → `Verdict::ConstitutionalBlock` — PR #67 (`67d29a0`), 2026-05-15
- [x] **First real `DeterministicPolicy` rule** — data-classification invariants (ceiling≥floor / step≥floor / step≤ceiling) → `Verdict::Block`; paired `ask --classification-floor` — 2026-05-15
- [x] **Observation / rule-iteration harness** — `observation::capture` fixture captures (SCHEMA_VERSION), `plan.formulate` carries full Plan, `observation replay` re-runs the CASSANDRA chain offline with verdict deltas, lenient plan parser for fenced LLM output — 2026-05-13 → 2026-05-15
- [x] **Constitutional refusal state** — `Plan.refused` + `Outcome::Refused` + terminal `tasks.state='refused'`; migration 0012 — closes #23, PR #59 (`f1fea54`), 2026-05-14
- [x] **Automatic classification-floor inference** — CLI keyword classifier + agent raise-only `Plan.floor_request`; `ClassificationFloorSource` provenance, inner-loop `max(producer, agent)`; runner rejects forged `agent_raised` (#71) — PR #70 (`4ddfe3b`), 2026-05-16
- [x] **Memory layers — L1 always-in-context index** — `MemoryLayer{Meta..Digest}` + `layer` column (migrations 0013/0014); `load_l1` row/byte caps; L0-writer policy enforced in code — PR #69 (`eb8e4bd`), 2026-05-15
- [x] **L0 seed data loader** — TOML meta-rules → validated L0 rows, idempotent seeding, `load_l0_active`; starter `seeds/memory/l0_meta_rules.toml` — 2026-05-16
- [x] **Prompt assembler (L0 + L1 + base)** — `assemble_system_prompt` + `SystemPromptBuilder` trait wired into every plan iteration; drift-detection keys in `plan.formulate` — 2026-05-16
- [x] **Recall-lane wiring** — `RecallBuilder` composes `embed_query` + `recall` into the assembled prompt (degrade-and-warn); first production consumer of `Router::embed` + `recall` — PR #79 (`7553404`), 2026-05-17
- [x] **L1 promotion writer** — operator `memory l1 {add,list,remove}` + agent `Plan.l1_insight`; validator + source-agnostic dedup — PR #82 (`eb6b8a8`), 2026-05-18
- [x] **Worker lifecycle policy** — `Lifecycle::{SingleUse, IdleTimeout}` (warm-keep, post-completion-only caps, `stateless` contract); `WorkerLifecycleManager` trait + Single/Idle/Composite managers; passive crash detection + restart backoff — spec + slices 1–2, PR #83 (`b7dba3a`), 2026-05-18 (hardening #84/#85/#86 followed)
  - [x] **Worker manifest plumbing** (item 11; resolves lifecycle spec open question 1 — Rust consts, not on-disk TOML) — `WorkerManifest` trait + per-worker impl (`ShellExecManifest`/`GlinerRelexManifest`) + static `WORKER_MANIFESTS` driving a pure `assemble_registry`; `build_tool_registry` reduced to allowlist-prefetch + `ResolveCtx`; `current_exe()`-relative sibling binary discovery (env override wins, set-but-invalid override fails closed, gliner exempt). Behaviour-preserving — PR #187 at `2e3d0c5`, 2026-06-05
  - [ ] **Slice 3 (operator surface + SIGTERM grace)** — `kastellan-cli supervisor status` for warm workers + cap state; formal SIGTERM-grace-then-SIGKILL teardown via `grace_period_seconds`; proactive SIGCHLD crash detection. Low priority until a worker needs one of these.
- [x] **GLiNER-Relex worker** (first `IdleTimeout` consumer) — Python package + Rust manifest/typed client; CPU/CUDA/MPS device resolution; Apple-`container` backend variant — slices 1–2.5 + macOS slice, PRs #88/#103/#118, 2026-05-18 → 2026-05-25
  - [ ] **operator-CLI macOS validation** (operator action): install Postgres locally (`brew install postgresql@17 && brew services start postgresql@17`) and rerun `KASTELLAN_GLINER_RELEX_ENABLE=1 cargo test -p kastellan-core --test gliner_relex_e2e -- --nocapture` to exercise the full PG-backed lifecycle path on darwin. Python `_resolve_device` is already cross-validated; this is the lifecycle-manager validation. Half-hour once PG is installed.
- [x] **Entity extraction v2** — single-pass GLiNER-Relex call; `EntityExtractor` trait, quarantine-by-default (`entities.quarantine`/`name_norm`, migrations 0015/0016), extraction runs before recall — PR #91 (`f12b460`), 2026-05-19. (v1 `HybridEntityExtractor` was superseded; design preserved at `docs/superpowers/specs/2026-05-18-entity-extraction-graph-lane-design.md`.)
- [x] **Memory-write-time entity auto-linker** — `link_memory_entities` threaded through L0/L1 writers; one shared extractor across query- and write-time — PR #92 (`d58ecc9`), 2026-05-19
- [x] **Operator quarantine-review CLI** — `entities {list,show,approve,reject,merge}`; approving an entity makes `recall(GRAPH_ONLY)` return its memories — PR #93 (`028e541`), 2026-05-20. Completes the graph-lane chain end-to-end (v2 extractor → auto-linker → review CLI).
- [x] **Relation-label vocabulary** — `relation_kinds` (migration 0017, 19 seeds) fed to the worker so triples are no longer silently dropped; `RelationKindsCache` — PR #100 (`5bcd060`), 2026-05-21
- [x] **Vocabulary management CLIs** — `relations kinds {add,remove,list}` (PR #109) + `entities kinds {add,remove,list}` (PR #110) over the lookup tables via `connect_admin_pool` — 2026-05-22
- [x] **`relations show <entity-id>`** — outbound + inbound edge walk (recursive CTE, depth-capped, diamond-deduped), text/JSON output — PR #113 (`9a46e18`), 2026-05-23
- [x] **macOS Apple `container` micro-VM backend** — `SandboxBackendKind` per-worker selection; closes the macOS memory-enforcement gap Seatbelt can't cover (GLiNER-Relex now enforces `mem_mb` on darwin) — spike + slices 1/2/2.5, PRs #105/#106/#108/#118, issue #55, 2026-05-21 → 2026-05-25
- [ ] **`context_manager`**: token-budget + task-completion + wall-clock reset triggers
- [ ] **Reset snapshot writer** (compact context → memory before reset)
- [x] **Worker-output prompt-injection guard (slice 1)** — `cassandra::injection_guard` 22-entry catalogue + `screen`; on Block, `tool_host::dispatch` substitutes a redacted placeholder and writes a `policy/injection.blocked` audit row carrying only SHA-256 — closes Item 30, PR #141 (`62905ae`), 2026-05-28. (#142 chat-template false-positives deferred until a `web-fetch`/MCP worker exists.) **Slice 2 candidates (deferred per YAGNI):** Review tier (0.45–0.70 band), `kastellan-cli policy review` surface, heuristic/combinatorial scoring, multilingual catalogue, per-tool policy.
- [x] **Opaque secret references (slice 1)** — `Vault` (TTL'd `RwLock<HashMap>`) + `SecretRef` opaque newtype + `substitute_refs_in_params`; substitution in `tool_host::dispatch` is fail-closed, plaintext never in audit rows (#147); `KASTELLAN_BOOTSTRAP_SECRETS` daemon-startup materialization — closes Item 31, PR #146 (`bc36e4c`), 2026-05-29. Pre-req for the Phase-5 frontier gate. **Slice 2 (deferred):** CLI↔daemon IPC + `kastellan-cli secrets materialize`; per-task vault scoping; embedded substitution.
- [x] **L3 skill arc** (crystallise → approve → pin → invoke) — the GenericAgent skill import: distil a successful trajectory into a parameterised JSON-RPC tool-call template stored as an L3 `memories` row, recalled and re-invoked on the next similar task. **Complete end-to-end on `main`:**
  - Crystallisation writer — agent emits `Plan.l3_skill`; validated, SHA-256-deduped, stored `layer=3 trust:untrusted` with a `dispatch_count >= 1` grounding gate; `memory l3 {list,remove}` — PR #173 (`6eb966e`), 2026-05-31
  - Trust enum + approval gate — `SkillTrust{Untrusted|UserApproved|Pinned}` (fail-safe parse); pure `evaluate_approval` (re-validate + `secret://` scan + tool-existence vs the `registry.loaded` snapshot, fail-closed); `memory l3 {approve,revoke}` — PR #176 (`bbcc7b3`), 2026-05-31
  - Recall surfacing — `<skills>` planner block for `UserApproved`/`Pinned` only (reference, no invoke), SQL trust push-down — PR #177 (`4b978d8`), 2026-06-01
  - Operator invocation — `memory l3 run` substitutes `{{params}}` → live-registry re-validation → sandboxed dispatch; dry-run by default, no CASSANDRA review on the operator path — PR #178 (`d862e6e`), 2026-06-03
  - Autonomous door — agent `Plan.invoke_skill` expanded before CASSANDRA review; gated on a new `pinned` tier (strict subset of operator-runnable); `memory l3 pin` — PR #181 (`6e10a81`), 2026-06-04
  - Daemon reroute (#179) — `memory l3 run` enqueues an `l3_run` task executed daemon-side against the single live `ToolRegistry` (the Postgres `tasks` queue + `LISTEN/NOTIFY` IS the operator→daemon command channel), retiring the in-process path and its env-divergence cliff — PR #186 (`67bc474`), 2026-06-05, **#179 CLOSED**
- [x] **Developer onboarding manual** — `docs/devel/manual/` (10 chapters + index, ≤2 A4 pages each) — PR #119 (`99bbfab`), 2026-05-25
- [x] **Large-tool-result handoff cache** — in-memory per-task content-addressed `HandoffCache` (`core/src/handoff.rs`). Oversized `Ok` results (serialized JSON > `DEFAULT_RESULT_BYTE_CAP` = 64 KiB) are stashed in the dispatcher layer (`ToolHostStepDispatcher::dispatch_step`, after `tool_host::dispatch` returns — the sealed chokepoint is untouched) and replaced with a `{handoff_ref, byte_len, summary_head}` placeholder. A reserved `handoff`/`fetch` built-in, intercepted before registry lookup (no worker spawn), returns clamped slices (`MAX_FETCH_BYTES` = 256 KiB). `task_id` threaded through `StepDispatcher`; entries purged at task terminal in the lane runner; per-task byte budget + global `MAX_TRACKED_TASKS` backstop bound memory. Blocked injection outputs are never stashed (they arrive as the tiny `injection_blocked` placeholder, under cap); operator `memory l3 run` path (`task_id <= 0`) passes through verbatim. In-memory (not the unwired `Workspace` scratch) per the design; `web-fetch`'s 100 KiB text cap is the realistic worst case. — branch `feat/handoff-cache`, 2026-06-08. Review follow-ups (2026-06-09): the stash branch now has real-worker dispatcher coverage (`scheduler_step_dispatch_e2e::dispatcher_stashes_oversized_ok_result_only_for_positive_task_id` — shell-exec echo > 64 KiB, asserts placeholder + cache round-trip + purge + the `task_id = 0` passthrough gate + the `handoff.stashed` audit row), closing [#198](https://github.com/hherb/kastellan/issues/198); the global backstop now `warn!`s when it evicts a bucket; the fetch intercept documents why it (unlike stash) is ungated on `task_id`. Planner-surface follow-up (2026-06-09, PR [#200](https://github.com/hherb/kastellan/pull/200)): `assemble_system_prompt` now emits an always-present, drift-proofed `<handoff>` block (`render_handoff_block()` in `core/src/prompt_assembly/assemble.rs`, interpolating the source-of-truth `HANDOFF_TOOL`/`HANDOFF_METHOD_FETCH` constants plus the byte caps `SUMMARY_HEAD_BYTES`/`MAX_FETCH_BYTES`, with a unit test cross-checking the placeholder fields, the real `fetch(...)` return shape, the fetch params, and both caps) teaching the planner the placeholder shape + the `fetch` step protocol — the mechanism is no longer inert. Deferred: per-tool `result_byte_cap` override (YAGNI); on-disk store.
- [x] **Registry-driven `<tools>` planner block** — the planner LLM is now told which tool workers exist. New `WorkerManifest::tool_doc() -> Option<ToolDoc>` (defaulted `None`); `ToolDoc{name,method,summary,params}` static compiled-in literals (trusted, unescaped). `assemble_registry` collects docs **only** in the `Resolution::Register` arm → a Disabled/Misconfigured worker is neither dispatchable nor advertised. All 7 workers implement `tool_doc()` (methods verified against live handlers); registry drift guard pins `doc.name == name()`. Threaded `build_tool_registry → Arc<[ToolDoc]> → PgSystemPromptBuilder::with_tool_docs → assemble_system_prompt`; rendered between `<recalled>` and `<handoff>`. `agent_planner.md` rewritten to point at `<tools>` and drop the old "there is no web-search tool / do not search the web" prohibitions (the sole reason the Matrix "I have no web search tool" refusal happened). Advertisement only — no dispatch/allowlist/reviewer change, no new dependency. Subagent-driven TDD (6 tasks); final opus review READY TO MERGE (0 Critical/Important). Mac-green (core lib 1154/0/1, workspace build exit 0, `clippy -p kastellan-core --all-targets -D warnings` clean). Operator still needs SearxNG + `KASTELLAN_WEB_SEARCH_ENDPOINT` + the allowlist row for a live web answer. — branch `feat/planner-tools-block`, PR [#437](https://github.com/hherb/kastellan/pull/437), 2026-07-11. Spec/plan `docs/superpowers/{specs,plans}/2026-07-11-registry-driven-tools-block-planner-prompt*`.
- [x] **Memory two-tier write path: `put_doc()` vs `put_doc_light()`** — `db::memories::insert_memory_light(executor, body, metadata, layer)`: a thin named delegate to `insert_memory_at_layer` with `embedding = None` (no new SQL, no migration), inheriting the L0 `PolicyViolation` guard. Documents the degradation contract (lexical + `metadata @>` work; semantic + graph degrade gracefully — `semantic_search` already filters `WHERE embedding IS NOT NULL`). PR [#195](https://github.com/hherb/kastellan/pull/195) (`39a036a`), 2026-06-07. **Deferred follow-ups:** core-side caller wiring; per-namespace caps + oldest-eviction (openhuman quotes "max 50 KV entries, max 200 docs") — fits on `memories.metadata` as the namespace selector with no schema change, but does not block this surface; a graph-lane degradation test ([#196](https://github.com/hherb/kastellan/issues/196)).
- [x] **L1 embedding population — semantic recall lane populated** — closes the [#323](https://github.com/hherb/kastellan/issues/323) forward write path. New `Embedder` async-trait seam (`core/src/memory/embedder.rs`, mirrors `EntityExtractor`): `RouterEmbedder` (delegates to `embed_query` → `truncate_to_embedding_dim` + audit row, `Err → warn!+None`) + `NoOpEmbedder`. `promote_l1` embeds **lazily after the dedup check** (a duplicate never embeds) and degrades to a NULL embedding + WARN on failure (write never blocked). Real `RouterEmbedder` threaded `spawn_scheduler`→`lane_loop`→`drain_lane`→`write_l1_promoted_row` for the agent-raised path; operator CLI `l1 add` stays NoOp. No `db` change. macOS live-PG 18: `memory_l1_promote_e2e` 12/0 (+3: embed-on-insert, lazy-on-dedup-skip, degrade-and-warn), clippy `-D warnings` clean. MERGED to `main` as `2ec853a` (PR [#324](https://github.com/hherb/kastellan/pull/324)), 2026-06-20. ~~**Deferred:** backfill / `kastellan-cli memory l1 reembed`~~ — **DONE** (next bullet, #325).
- [x] **L1 embedding backfill — `kastellan-cli memory l1 reembed`** — closes [#323](https://github.com/hherb/kastellan/issues/323) item 2 ([#325](https://github.com/hherb/kastellan/issues/325)). (Re)embeds every `layer = 1` row with `embedding IS NULL` — pre-#324 rows + operator-added `l1 add` rows (NoOpEmbedder by design) — through the **real** `RouterEmbedder`, so backfilled vectors match the forward path's on-insert ones. New db helpers reuse the existing dim/encode chokepoints: `load_unembedded_at_layer` (scan) + `set_embedding` (guarded `UPDATE … WHERE embedding IS NULL` → **idempotent + race-safe**). Core `reembed_l1_null(pool, &dyn Embedder) -> ReembedReport{scanned,embedded,skipped}` (new `core/src/memory/l1_reembed.rs`, degrade-and-warn per row, never fails the batch); CLI `memory l1 reembed` builds the embedder from `RouterConfig::from_env()`, prints `scanned=/embedded=/skipped=`. No separate audit row (each embed already logs `action='embed'`); no migration; **L1 only**. macOS live-PG 18: new `memory_l1_reembed_e2e` 3/0 + db/core units +4, clippy `-D warnings` clean. Branch `feat/325-l1-embedding-backfill`, 2026-06-21. **Review follow-ups (PR #327):** pure `reembed_batch_failed` predicate (`scanned>0 && embedded==0`) → aggregate WARN in `reembed_l1_null` + CLI **exits non-zero** on a wholly-failed batch (idempotent no-op still exits 0); +3 core units + a 4th mixed-batch e2e scenario (`memory_l1_reembed_e2e` now 4/0).
- [x] **Entity-embedding backfill + entity-similarity recall lane** — `entities.embedding` (`vector(256)`, NULL for every row, no reader) now populated + consumed by a **4th recall lane**, mirroring the L1 arc. New `db::entity_embedding` module: `load_unembedded_entities` (`(id,kind,name)` scan, quarantine-blind) + `set_entity_embedding` (guarded race-safe `UPDATE … WHERE embedding IS NULL`) + `entity_similarity_search` (top-`ENTITY_SIMILARITY_FANOUT=64` entities by cosine `<=>`, embedded + non-quarantined → their linked memories ranked by `MIN(dist)`; `include_quarantined` seam mirrors `graph_search`). Core: shared `ReembedReport` lifted to `core/src/memory/reembed.rs`; `entity_reembed::{entity_embedding_text("kind: name"), reembed_entities_null}` (degrade-and-warn, mirrors `reembed_l1_null`). `RecallModes` gains `entity`; `ALL` + the new no-seeds default `SEMANTIC_LEXICAL_ENTITY` (used by `RecallParams::new`) enable it → runs on the cli_ask path; quarantine-filtered in production; RRF-fused. CLI `kastellan-cli entities reembed` (sibling module `entities_reembed.rs`). Backfill embeds ALL entities (review-blind; the lane filters); no migration (col pre-exists from 0019); **no ANN index** (deferred) + ~~no forward embed-on-insert path~~ (**DONE** — next bullet). macOS targeted PG18: db lib 140/0, core memory lib 215/0, recall units 22/22, live e2e `entity_reembed_e2e` 4/0 (incl. quarantine exclusion) + `memory_recall_e2e` 2/0 + `memory_l1_reembed_e2e` 4/0; `cargo clippy --workspace --all-targets -D warnings` clean. MERGED to `main` as `4f4d61c` (PR [#335](https://github.com/hherb/kastellan/pull/335)), 2026-06-21. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-21-entity-embedding-recall-lane*`.
- [x] **Agent tool-loop recovery (PR [#337](https://github.com/hherb/kastellan/pull/337))** — fixes a live `plan_iteration_cap_exceeded` on a question the model knew the answer to. Three root causes (systematic debugging on the live DGX): (1) **blind replanning** — `plans_so_far_summary` collapsed failed steps to bare `"err"`, discarding `StepOutcome::Err { code, detail }`; new pure `render_step_outcome` surfaces `err: <CODE>: <detail>` (200-char clamp) to the planner + `agent_planner.md` guidance (answer directly from knowledge; only real tools exist; read the error, don't repeat a denied step; `shell-exec` argv[0] must be absolute) + `DEFAULT_MAX_PLANS_FAST` 3→5. (2) **LLM transport timeout** — the "`error sending request`" failure was the reqwest total `.timeout()` (30s) firing during a ~86s plan generation on the local 26B (a reqwest timeout displays identically to a send failure); `DEFAULT_TIMEOUT_MS` 30s→180s + `RouterError::Transport` now tags `[request timed out]`/`[connection failed]` via the pure `transport_kind_tag`. (3) **empty DGX allowlist** → seeded `/usr/bin/{cat,ls,python3}` (operator DB state). Verified live: distance question completes on plan 1; `/usr/bin/ls /tmp` runs `terminal_kind:ok` in the jail; a tool task formulates all 5 plans (no 30s cutoffs). TDD units (`render_step_outcome`, `transport_kind_tag`) + cap pins; clippy `-D warnings` clean on core/db/llm-router; deployed + restarted on the DGX. Branch `fix/agent-step-error-feedback`, 2026-06-22. **⚠ Open follow-up [#338](https://github.com/hherb/kastellan/issues/338):** ~~successful tool *output* is still fed back as just `"ok"`~~ — **DONE, next bullet.** **Review follow-ups (PR #337):** added a seam test pinning the worker-error → `map_dispatch_result` → `plans_so_far_summary` composition end-to-end; clarified the `STEP_ERR_DETAIL_MAX` clamp doc (rendered detail is ≤ cap+1 incl. the `…` marker); noted on #338 that the error `detail` (worker-supplied `rpc.message` for the RPC bucket) already shares the same injection surface, so the eventual guard must cover both halves.
- [x] **Forward entity embed-on-insert** — closes the deferred forward half of the entity-embedding arc (symmetric with #324 forward / #325 backfill, but for entities). `entity_extraction::batch_upsert::upsert_entities_and_relations` now takes a `&dyn Embedder` and embeds the entities it **just created** (`inserted == true`, the `xmax = 0` discriminator) through the shared `entity_embedding_text` chokepoint + guarded `set_entity_embedding`, so a freshly-extracted entity is searchable via the entity-similarity lane without a manual `entities reembed` run. Pure `select_new_entities(deduped, map)` selector (4 units); degrade-and-warn embed loop (an embed `None` / lost `IS NULL` race / write `Err` skips the row, never fails the upsert); embeds after the entity commit, before the relations phase. Conflict-hit (existing) entities are NOT re-embedded — still the backfill's job. `GlinerRelexExtractor` now owns an `Arc<dyn Embedder>` (`main.rs` shares the one `RouterEmbedder` across L1 + entities); `NoOpEntityExtractor` path unaffected. macOS PG18: new `entity_forward_embed_e2e` 3/0 (embed-on-insert + lane surfaces, conflict-not-reembedded, decline-leaves-NULL) + `entity_extraction_e2e` 16/0 + `entity_reembed_e2e` 4/0 + `memory_entity_link_e2e` 6/0 regressions; `cargo clippy --workspace --all-targets -D warnings` clean. No migration, pure-Rust → DGX not required. Branch `feat/entity-forward-embed-on-insert`, 2026-06-21. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-21-entity-forward-embed-on-insert*`.
- [x] **Feed successful tool output back to the planner ([#338](https://github.com/hherb/kastellan/issues/338))** — the success-half symmetric to PR #337's error-half, and the blocker for every tool-using task. `render_step_outcome` (`core/src/scheduler/inner_loop.rs`) collapsed a successful `StepOutcome::Ok(Value)` to the bare `"ok"`, so the planner never saw a step's output and re-issued the same successful step until `plan_iteration_cap_exceeded` (live: 5 identical `/usr/bin/ls /tmp` plans). **Key finding:** the injection-guard requirement was already met upstream — `tool_host::dispatch` screens every worker result over the first 64 KiB and `tool_dispatch::dispatch_step` stashes any `Ok(v)` >64 KiB to the handoff cache; since the caps are equal, every `Ok(v)` reaching the render is already screened + ≤64 KiB. **Fix (TDD):** the `Ok` arm renders a bounded head via `injection_guard::extract_scannable_text` (`STEP_OK_SUMMARY_MAX`=4 KiB) as `"ok: <head>"` (`…` on truncation), render stays screen-free by design; `prompts/agent_planner.md` updated so the planner answers from the output instead of re-running. **Security fix (final review):** the `fetch_handoff` branch returned its slice *unscreened*, and since `tool_host` only screened a stashed body's first 64 KiB, a fetch at `offset ≥ 64 KiB` could surface an unscreened tail (a hole *opened* by the render change) — closed with new `tool_dispatch/fetch_screen.rs::screen_fetched_data` (Strict/fail-closed), screening each served slice at the dispatch chokepoint → blocked `data` replaced by a withheld-note placeholder; the invariant now holds via both chokepoints. macOS: inner_loop 31/0 (+4), fetch_screen 3/0 (real Block exercised), `cli_ask_e2e` 7/0 (incl. plan-cap pin), `cargo clippy --workspace --all-targets -D warnings` CLEAN. Pure-Rust → DGX not required for the unit gate; live "ls /tmp count" acceptance is the remaining operator gate. Follow-ups [#339](https://github.com/hherb/kastellan/issues/339) (global summary budget) + [#340](https://github.com/hherb/kastellan/issues/340) (clearer blocked signal + split-slice limit). Branch `feat/338-feed-tool-output-to-planner`, 2026-06-22. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-feed-successful-tool-output-to-planner*`.
- [x] **Global budget for `plans_so_far_summary` ([#339](https://github.com/hherb/kastellan/issues/339))** — #338 follow-up hardening. #338 raised the per-step term ~2000× (bare `"ok"` → up to `STEP_OK_SUMMARY_MAX`=4 KiB), and `plans_so_far_summary` re-renders every plan's every step every iteration, so the accumulated total (`max_plans` × steps × 4 KiB, `max_plans` operator-overridable) was unbounded. **(1) Refactor:** the rendering helpers (constants + `sink_screen_blocks` + `render_step_outcome` + the per-plan mapping) lifted out of the over-cap `inner_loop.rs` (575→481) into a new pure `inner_loop/summary.rs` (286); `plans_so_far_summary` is now a thin delegate. **(2) Budget (TDD):** new `RenderedStep{text,elidable}` + pure `apply_summary_budget(&mut [Vec<RenderedStep>], budget) -> usize` elides the **oldest** successful-step output heads first (→ `OK_ELIDED_MARKER`) until total step-text bytes ≤ `PLANS_SUMMARY_BUDGET`=**32 KiB**; errors, decisions, the injection withheld-marker, and the most-recent plan's heads are preserved (what the planner answers from next, so no #338 loop regression). Guards: never elide non-elidable steps, never *grow* a tiny `"ok: 9"` (`len > marker` check), idempotent. Budget bounds the dominant (step-output) term, not the small per-step framing. macOS lib gate: summary 17/0 (+8 budget units) + inner_loop 43/0 (no regression), `cargo clippy -p kastellan-core --lib -- -D warnings` clean. Pure-Rust, no migration, no OS-gated code → DGX not required. Final whole-branch review (opus): **READY TO MERGE**, no Critical/Important. Branch `feat/339-plans-summary-global-budget`, 2026-06-23. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-plans-summary-global-budget*`.
- [x] **Clearer injection-blocked signal to the planner ([#340](https://github.com/hherb/kastellan/issues/340))** — final #338-arc follow-up, **completing the planner-feedback arc (#337→#338→#343→#339→#340)**. When `tool_host::dispatch` blocks a worker result on the output injection screen, the placeholder `{ injection_blocked, score, reason_codes }` rendered through `extract_scannable_text` (string leaves only) so the planner saw just the reason-code string (e.g. `"ok: instruction_override"`) — an unintelligible gap that could tempt a re-run, unlike the `fetch_screen` placeholder which already carries a human-readable `note`. **Fix (TDD, pure-fn):** new `core/src/tool_host/injection_placeholder.rs` (72 LOC) — `WITHHELD_NOTE` const = `"[tool output withheld: failed injection screen]"` + `injection_blocked_placeholder(score, &reason_codes) -> Value` (adds the `note` string leaf; keeps the structured fields for audit-shape parity with `fetch_screen`); `dispatch`'s Block arm calls it. `prompts/agent_planner.md` gained a "withheld step — don't re-run it" bullet; `docs/threat-model.md` gained a subsection on planner-bound-output screening (the two `tool_host`+`fetch_screen` chokepoints) **and the known split-slice limitation** (a payload split across a 64 KiB boundary / two `fetch_handoff` slices can evade single-slice screening — inherent to streaming bounded-memory screening; sandbox + egress proxy remain the real boundary). macOS: `injection_placeholder` units 3/0, `tool_host` lib 43/0, `injection_guard_e2e` 6/0 against real PG18 + Seatbelt (placeholder-shape test now also pins the `note`), clippy `--lib --tests -D warnings` clean. Pure-Rust, no migration → DGX not required. Branch `feat/340-injection-blocked-note`, 2026-06-23.
- [x] **Planner reliability on a local reasoning model — the agent could not answer ANY question about the user's email** (branch `fix/planner-disable-thinking`, PR [#505](https://github.com/hherb/kastellan/pull/505) **MERGED 2026-08-02** as `6b083553`). Driven from a real Matrix message; **four independent faults, only one in the layer everyone suspected.** (1) **Host**: an `apt upgrade` installed a new kernel but left the NVIDIA module metapackage on the old one ⇒ no `nvidia.ko` ⇒ Ollama silently ran the 26B model **100% on CPU** (`"size_vram": 0`); the `linux-modules-nvidia-**fs**` package *did* upgrade, so the tree looked current. Operator-fixed. (2) **Deployment**: `supervisor/src/systemd_user.rs` never runs `systemctl --user enable`, so **kastellan did not survive a reboot on Linux** while macOS did (`launchd_agents.rs` sets `RunAtLoad=true`) — a cross-platform-parity break; DGX hand-`enable`d at the time, **code fix shipped 2026-08-03** (PR [#509](https://github.com/hherb/kastellan/pull/509), closes [#508](https://github.com/hherb/kastellan/issues/508)): `install` enables `<name>.service` and `install_target` enables `<name>.target` — the boot entry point, whose `Wants=` pulls the members in, so members are deliberately not enabled separately — with `uninstall_target` disabling symmetrically so no dangling `default.target.wants/` link is left behind. The `Supervisor` trait now states "install implies auto-start" as a cross-platform contract both backends must honour (launchd declaratively via `RunAtLoad`, systemd imperatively via `enable`), and the installer's `loginctl enable-linger` call is documented as the other half of the same guarantee — lingering starts the user *manager*, enabling gives it something to start. **Retrospective review of #509 (PR [#511](https://github.com/hherb/kastellan/pull/511), merged before review):** no bug in the fix, five follow-ups — `write_atomic`'s destination-derived staging path was the *actual* cause of the tmp-file collision #509 patched at the test-name layer (now a per-writer `tmp_path_for()` with `create_new` and cleanup after a failed publish, in **one** `cfg`-free `supervisor/src/atomic_write.rs` shared by both backends, so its seven tests run on both hosts instead of splitting 4-on-Linux/3-on-macOS — and the same destination-derived pattern in `install::run`'s `copy_exec`/`symlink_replace`, which left the "two concurrent installs still race" claim true one layer up, fixed with it); **DGX re-gate 2965 / 0 / 53, +13 = exactly the tests added**; a `builder.rs` comment still claimed "we don't enable by default"; `--no-start` skips linger so it arms units a headless host still can't start, which only a code comment said; the contract's "after a reboot" was false on both platforms for a host nobody logs into (launchd loads LaunchAgents at *login*); and a SIGKILLed smoke test can now strand an enable symlink the manager fails on every login. Filed not fixed: [#510](https://github.com/hherb/kastellan/issues/510) — **CI never runs #508's regression guard** (`linux-check.yml` is compile-only, both tests would `[SKIP]` on `ubuntu-latest` regardless, and there is no macOS job at all, so launchd code is never even *compiled* on a PR) — plus, from #511's own review round, [#512](https://github.com/hherb/kastellan/issues/512) (`kastellan-cli install` prints systemd-only guidance and health-checks via `systemctl` on macOS) and [#513](https://github.com/hherb/kastellan/issues/513) (per-file atomicity is not per-*install* atomicity: overlapping installs can publish a mix). (3) **`llm-router`**: the local reasoning model emitted **15 094 chars of `reasoning` for 1 519 chars of plan** (222 s; inside the daemon a single call consumed a full **600 s** budget and still timed out, so raising `KASTELLAN_LLM_TIMEOUT_MS` is *not* a fix). Added `ChatRequest.chat_template_kwargs` + `RouterConfig::disable_thinking` (default ON, `KASTELLAN_LLM_DISABLE_THINKING=0` opts out) emitting `{"enable_thinking": false}` — the OpenAI-compat extension Ollama and vLLM both honour — applied in `dispatch_local` as a property of the local leg; caller value wins; unset ⇒ field not serialised ⇒ byte-identical payload. Measured **222 s → 51 s**. (4) **`plan_parser` — the fault that hid the rest**: `parse_plan_lenient` re-emitted the *strict*-path error whenever the lenient path failed, and the strict path always describes the markdown fence, so a well-formed fenced plan that merely omitted a required field was reported as `expected value at line 1 column 1` ("your output wasn't JSON") when the real error was `missing field 'steps'`. **Cost the entire session** until the raw completion was logged. Now the lenient error is returned whenever the input contained a `{`. Also: `AgentError::Decode` captured the raw completion but its `Display` renders only `detail`, so it reached nobody — the structural facts (`detail`, `raw_len`, `has_brace`, `finish_reason`, token counts) are now `warn!`-logged, with the raw head at `debug!` because a planner completion restates the user's mail verbatim and the daemon log has none of `audit_log`'s access control (`docs/threat-model.md` gained a "User data in the daemon log" subsection). And `Plan::data_ceiling` `#[serde(default)]`s to `Secret` (local planners routinely omit it on terminal plans) with a warn on every use, while **`steps` is deliberately NOT defaulted** (an empty `steps` marks a terminal plan, so defaulting it would turn a *truncated* plan into a silently-terminal one); `agent_planner.md` hardened. **Review correction (2026-08-02):** that default was originally shipped and documented as *fail-closed*; it is **fail-open**. `data_ceiling` is a **ceiling**, so the most sensitive class is the most *permissive* value — at rank 3 both I1 (`ceiling >= floor`) and I3 (`step.classification <= ceiling`) pass vacuously, and a plan omitting the field is not ceiling-constrained at all. Docs, warn text, test names and the planner prompt corrected to say so; the behavioural fix (resolve an absent ceiling against the task's `classification_floor`, plus audit provenance) is [#506](https://github.com/hherb/kastellan/issues/506) (correction shipped in PR [#507](https://github.com/hherb/kastellan/pull/507), **MERGED 2026-08-03** as `d37df573`) — `Public` is *not* the fix, since `"my email"` infers a `Personal` floor and a `Public` ceiling would re-block the very task this branch unblocked. **#507's own second review round (2026-08-03) narrowed the severity claim further:** it is not "I1 and I3 vacuous, I2 still enforced" for every plan. L3 expansion stamps each expanded step's `classification` with `plan.data_ceiling` and runs *before* the reviewer, so a plan that both omits the field and carries `invoke_skill` has **all three** invariants pass vacuously. Still confined to that review stage — nothing outside `cassandra/deterministic.rs` reads `step.classification`. DGX re-gate at `c5ba7637`: **2950 / 0 / 53**, clippy clean, 4 `[SKIP]` (GLiNER tier only), +1 = the single test added. **Live: first end-to-end answers from the real 37k-message archive** — task 122 `completed` 386 s/6 iterations, task 124 `completed` **77 s/2 iterations**. **Gates: DGX 2949/0/53** (`--nocapture`, exactly 4 `[SKIP]`, all the env-gated GLiNER tier) **+ Mac 2831/0/23**, clippy `-D warnings` clean on both, **both +11** = exactly the tests added ⇒ nothing platform-gated. Known residual: the planner passes `mail.get_message`'s `message_id` as a **string** where the worker requires `i64`, burning an iteration in nearly every run.

## Phase 2 — Channels (read-only)

> **Primary channel decided 2026-06-12 (operator brainstorm):** **Matrix, self-hosted,
> single-user, federation OFF** (E2E via `matrix-rust-sdk` + `vodozemac`, vendor-neutral, zero
> marginal cost, all platforms via Element). **Email is the cross-transport fallback** (separate
> failure domain), used for low-trust async notifications, never commands *(superseded 2026-07-28 for inbound: the shipped slice-1 channel does ingest commands, gated by DMARC + per-pairing token — see the email entry below)*. Signal (`presage`
> fragility + ban risk) and Telegram (no bot E2E, centralized) rejected as primary. Homeserver
> runs as a supervised **conduwuit** unit; hosting is operator-selectable, fail-down: Tier A
> dedicated VPS (preferred) → Tier B existing WireGuard VPS (co-host = shared blast radius with
> network ingress) → Tier C the kastellan host itself ("poor man's" default). Matrix has **no
> single-user homeserver failover** — redundancy is the cross-transport email fallback, not a
> second homeserver. Full design + co-hosting security analysis + slice decomposition:
> `docs/superpowers/specs/2026-06-12-primary-communication-channel-design.md`.

- [x] **Channel-bus abstraction (build first)** — `core/src/channel`: `Channel` trait (inbound
  `IncomingMessage` stream + outbound `send`, dyn-safe) + pure security core — fail-closed
  `PeerAuthorizer`/`StaticPairings` (`auth.rs`), `classify_inbound` (authorize→`injection_guard`
  screen under `GuardProfile::Strict`→`tasks` payload, `ingest.rs`), `reply_for_completed_task`
  (finalized task→user reply, `route.rs`) — plus the `ChannelBus` runtime (`bus.rs`) over four
  seams (`Channel`/`PeerAuthorizer`/`ChannelEvents`/`CompletedTasks`; real `PgChannelEvents` +
  `PgCompletedTasks` over the `tasks` queue + `tasks_completed` NOTIFY, no new IPC). Channel tasks
  mirror the `ask` payload so the scheduler is unchanged; unpaired peers + injection are dropped +
  audited (hash only). 18 unit tests + hermetic `FakeChannel` full-loop e2e + PG-gated real-queue
  e2e; clippy `-D warnings` clean. No live transport / no `main.rs` wiring (slice #2). Branch
  `claude/zen-bell-6bn2ze`, 2026-06-12. Plan: `docs/superpowers/plans/2026-06-12-channel-bus-abstraction.md`.
- [x] **Matrix inbound** (`MatrixChannel`, `matrix-rust-sdk`, E2E) — net allowlist scoped to the
  homeserver host:port only; single-user homeserver bring-up. (Slice #2.) **END-TO-END ROUNDTRIP LIVE**
  (branch `feat/matrix-roundtrip-wiring`, 2026-06-20): a real DM from `@horst` to `@kastellan` on
  `matrix.kastellan.dev` runs through the agent and replies (`17×23 → 391`; free-text → NL reply), on the
  **systemd** daemon. Daemon wiring (plan Task 5/6): `spawn_matrix_worker` + `daemon_spawn_config_from_env`
  (`core/src/channel/matrix.rs`) + `ChannelBus::spawn` over `DbPeerAuthorizer`/`DbPairingService`/`PgChannelEvents`/
  `PgCompletedTasks` (`main.rs`); worker auto-joins invites + bootstraps cross-signing (`sdk_live.rs`);
  `kastellan-cli matrix probe` smoke/diagnostic; installer `--matrix-*` flags + `/v1` LLM-URL fix +
  `scripts/build-release.sh`; `db` pool `DEFAULT_MAX_CONNECTIONS` 4→16 (listener-slot starvation). Self-healing worker
  respawn (`MatrixChannel::supervised`, capped backoff, shutdown-aware); allowlist scoped to the homeserver's actual
  host:port (non-443 servers reachable). **Follow-ups (none blocking):** ~~inbound messages lost while the worker is
  down/respawning~~ **FIXED 2026-06-24 ([#321](https://github.com/hherb/kastellan/issues/321)):** on restart the worker reads
  the SDK's persisted sync token and surfaces the incremental catch-up backlog (the downtime messages) instead of
  suppressing it; a fresh login (no token) still suppresses full-history replay (`sdk_live.rs` `initial_live_state` +
  `read_prior_sync_token`). ~~enable worker seccomp/Landlock (`ENFORCE_SANDBOX=0`)~~ **DONE + DEPLOYED 2026-06-24:**
  default flipped to `=1`; new `matrix_client` seccomp profile (`net_client`+`ftruncate`) + a prelude **TSYNC fix**
  (`apply_filter_all_threads`) — the filter was previously a no-op binding only the main thread, leaving the `tokio` pool
  unfiltered. ~~residual periodic worker die/respawn~~ **FIXED + DGX-CONFIRMED 2026-06-25 ([#348](https://github.com/hherb/kastellan/issues/348),
  branch `feat/348-matrix-worker-respawn-stability`, PR [#350](https://github.com/hherb/kastellan/pull/350)):** the real cause —
  found by deploying the new death-report observability — was the **initial** worker being spawned via `spawn_blocking`, whose
  pool thread tokio reaps after ~10s, tripping bwrap's `--die-with-parent` PDEATHSIG → SIGKILL (death log: `signal: 9 (SIGKILL)`,
  no OOM/rlimit/seccomp). Fix: `MatrixChannel::supervised_self_spawn` does the initial spawn on the persistent driver thread too,
  so no worker is parented to an ephemeral thread; confirmed live (initial worker stable 2+ min, zero SIGKILL). Also shipped:
  death-report observability (shared `core/src/worker_stderr.rs` drain + `WorkerClient::death_report` logs exit status + stderr)
  and defensive `sync_retry` (the live `sync()` loop retries transient returns instead of `process::exit(1)`).
  **#348 item 3 (respawn-rate alarm) — DONE 2026-06-25:** pure `channel::respawn_alarm::RespawnRateAlarm` (sliding-window
  count; fires once per storm, re-arms when it clears) wired into the supervised `drive` loop — 5 respawns within 300s logs one
  churn `warn!`. Remaining:
  egress force-routing coupling (direct `--share-net` now); in-daemon password
  materialize (keyring sync-init); ~~embedding dim mismatch (768 vs 1024 → recall degrades)~~ **FIXED 2026-06-20**
  (migration 0019: `EMBEDDING_DIM` 1024→**256**, embeddinggemma Matryoshka-truncated client-side via
  `db::memories::truncate_to_embedding_dim`; ~4× less embedding storage + faster ANN; recall semantic lane live again);
  user-side device verify.
  **Phases A–C+E done** (branch `claude/zen-bell-6bn2ze`, 2026-06-12,
  hermetic + verified anywhere): `kastellan-matrix-wire` + sandboxed-worker JSON-RPC surface
  (`matrix.init/poll/send` over the SDK seam), core `MatrixChannel` (blocking driver thread
  bridging the synchronous protocol Client to the async `Channel` trait — keeps the protocol pure
  request/response), `build_matrix_policy` (pure), config-gated `main.rs` hook (byte-identical when
  unset), and `matrix_channel_e2e` (full loop against a real fake-worker process; paired round-trip
  + unpaired-dropped). **Phase D egress-transport spike DONE** (branch
  `feat/matrix-phase-d-egress-spike`, 2026-06-19): matrix-sdk 0.8.0 landed behind `live-matrix`
  feature; AGPL license pass (225 new crates, all PASS); `ProxyBridge` (loopback-TCP↔UDS relay,
  `src/bridge.rs`); hermetic spike test (`egress_spike.rs`) confirms `CONNECT fake-homeserver.invalid:443`
  arrives at the stub via the bridge — **transport CONFIRMED: transparent tunnel via `disable_mitm`
  (worker name) + `ProxyBridge`; no CA injection; conduwuit homeserver infra already done (slice #6)**.
  SDK builder names (homeserver_url, sqlite_store, proxy, build, whoami) recorded in the spike spec.
  **Phase D live `LiveSdk` integration DONE** (this session, 2026-06-19): `workers/matrix/src/sdk_live.rs`
  (`LiveSdk` impl of the `MatrixSdk` seam — owns a tokio runtime, restore-or-password-login with persisted
  `<store>/session.json`, background sync task → bounded `VecDeque`, `poll`/`send`, reuses `ProxyBridge` for
  transport); worker `main.rs` restored to live serving (build `LiveSdk` → `rlimit` → `lock_down` → raw
  `serve_stdio`, network-init-then-lockdown order) + crate `#![allow(dead_code)]` narrowed to
  `#![cfg_attr(not(feature = "live-matrix"), allow(dead_code))]`; core `disable_mitm_for(worker_name)` pure
  predicate (browser-driver + the new `MATRIX_TOOL`) in `worker_lifecycle/force_route.rs`;
  `core/tests/matrix_live_e2e.rs` `#[ignore]` two-worker (bot+peer) send/recv round-trip. **Green
  (macOS hermetic + DGX live):** matrix worker 13/0 (`live-matrix`) / 7/0 (default), `force_route` +1, `clippy --workspace -D
  warnings` + `clippy -p kastellan-worker-matrix --features live-matrix` clean; **DGX aarch64: `--features live-matrix`
  builds, 13/0 hermetic, and the live encrypted send/recv round-trip passes** (`matrix_live_e2e` 1/0 against a throwaway
  loopback matrix-conduit homeserver). A shutdown SIGABRT (matrix-sdk's deadpool SQLite `Drop` calling `spawn_blocking`
  off-runtime) was found via the DGX e2e and fixed (`client: Option<Client>` dropped inside `runtime.block_on`).
  Post-#313-review hardening (2026-06-19): dead sync loop `process::exit(1)`s (no silent stall), `session.json` written `0600`,
  `_PASSWORD` made optional for restored sessions. `ProxyBridge` error-surfacing ([#312](https://github.com/hherb/kastellan/issues/312))
  **DONE 2026-06-20** (branch `fix/312-proxy-bridge-error-surfacing`): accept loop continues+logs+backs-off instead of
  breaking on any error (backoff is capped-exponential `backoff_delay`, resets on a healthy accept, so a wedged listener logs
  at ~1 line/5s not ~20/s); `relay` returns `std::io::Result` so UDS-connect/relay failures are logged via the worker's
  `eprintln!` seam; pure unit-tested `classify_accept_error` + `backoff_delay`. matrix worker 11/0 default + 18/0 `live-matrix`,
  clippy `-D warnings` clean (both configs). Remaining:
  full channel-worker egress-coupled production spawn (the matrix worker's sidecar spawn, plan Task 5) +
  daemon `ChannelBus` wiring + `DbPeerAuthorizer`/`DbPairingService` swap. **Task 5 must grant the matrix Landlock ruleset RW
  on the persistent store dir** (the background sync task keeps writing the SQLite state/crypto store after `lock_down`) — the
  live e2e runs with seccomp/Landlock `none`, so this is untested.
  Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-matrix-inbound-sandboxed-worker*`;
  spike spec: `docs/superpowers/specs/2026-06-19-matrix-phase-d-egress-transport-spike-design.md`.
- [x] **Homeserver supervisor unit + hardening** — conduwuit (federation OFF, loopback bind,
  token-gated→closed registration): hardened **system** systemd unit template + config template +
  `setup-conduwuit.sh` (dev/Tier-C) + `check-conduwuit-config.sh` (security-invariant verifier,
  `--self-test` green) + `docs/deploy/matrix-homeserver.md` (Tier A/B/C + co-hosting blast-radius +
  operator steps). Deliberately **not** a kastellan `ServiceSpec`: the user-level supervisor can't run
  conduwuit as the dedicated unprivileged `matrix` user, so the homeserver is a root/system unit (or
  a separate host), installed independently. Branch `claude/zen-bell-6bn2ze`, 2026-06-12. (Slice #6.)
  **DEPLOYED LIVE 2026-06-19: `matrix.kastellan.dev`** (Tier A dedicated VPS) — **Continuwuity** (conduwuit is
  archived; config-compatible fork) + Caddy auto-TLS, federation off, registration closed, accounts `@horst`/`@kastellan`.
  Runbook `docs/devel/runbooks/2026-06-19-matrix-homeserver-deploy.md`; phase scripts `scripts/matrix/vps/`. Template
  corrected to Continuwuity keys. Unblocks Phase D Task 5 (point the worker at it).
- [~] **Email inbound channel (fallback)** — **SLICE 1 MERGED 2026-07-31 (`bf8e850b`, PR #496); slices 2–3 open.** **DESIGNED 2026-07-28**, spec `docs/superpowers/specs/2026-07-28-email-fallback-channel-design.md`, slice-1 plan `docs/superpowers/plans/2026-07-28-email-fallback-channel-slice1.md`. **Supersedes the original "IMAP inbound worker" framing:** localmail now exists and already ingests the mail over IMAP, so the channel polls its `/v1` subscription instead of speaking IMAP itself — no IMAP crate to sandbox, no mail credentials in a kastellan jail, and it reuses the force-routed egress path (private netns + 1:1 sidecar) that #491/#492 built out. **The extra-CA half of that path did NOT apply here until PR [#503](https://github.com/hherb/kastellan/pull/503)** — the channel's sidecar was a *transparent tunnel* with no MITM leg, so `KASTELLAN_EGRESS_UPSTREAM_EXTRA_CA` governed only the separate `kastellan-worker-mail` TOOL. On that branch the channel intercepts unconditionally and the env var applies to it too — but note the map is **global and host-keyed**, so one entry reaches every worker resolving to that address. The 2026-06-12 security posture is kept in full (DMARC pass + per-pairing in-body token), with the gate as **pure fns in core** so every rejection still lands in `audit_log`. Two single-origin workers (inbound poller, SMTP sender) is **forced by #492**: one worker holding both self-signed localmail and a public SMTP host is `MixedAllowlist`, which fails the spawn. **Pairing is operator-only** (`pair issue-token`) — the right posture for a spoofable transport. **Correction (review, spec D8):** the original reasoning here — "the token lives on the pairing row, so an unpaired sender can never present a valid one, hence the carve-out is unreachable over email" — was **WRONG and is refuted by the code**. An unpaired sender resolves to `Ok(None)` → `Rejected`, which *does* reach the carve-out, and `try_pair` would then mint a NULL-token row, permanently disabling DMARC+token for that address. Operator-only pairing is therefore enforced by an **explicit guard**, not by construction: `bus::handle_inbound` consults the carve-out only when `msg.evidence.is_none()` (i.e. only for a transport that authenticates its own peers, e.g. Matrix), and `DbPeerAuthorizer` refuses a token-less pairing row whenever the transport *did* supply evidence. Both guards are load-bearing — do not delete them as redundant. **SLICE 1 MERGED to `main` as `bf8e850b` (PR [#496](https://github.com/hherb/kastellan/pull/496)), 2026-07-31** (code-complete 2026-07-29 on branch `feat/email-fallback-channel-inbound`; merged content code-identical to the reviewed `658924ae`): sandboxed `email-in` worker, pure DMARC+token gate + evidence enforcement at the bus authorization chokepoint, core-side `EmailChannel` over the channel-generic `PolledWorkerDriver`, hermetic e2e (`core/tests/email_channel_e2e.rs`, 8 cases incl. the header-order-bypass and skipped-id-cursor-wedge fixes), and daemon wiring (`core/src/main/email_boot.rs`, mirrors `matrix_boot.rs`): a real `EmailEgress` (force-routed 1:1 sidecar when `KASTELLAN_EGRESS_FORCE_ROUTING` is on; the same legacy `--share-net` path Matrix takes when it's off — same rule, same invariant, no divergence) and a real `AckOnlyAudit` closure writing `channel.skipped_ack_only` rows for every silently-discarded message id. A partial config or worker-spawn failure **disables the email channel with a loud `error!` and leaves the daemon running** (`spawn_email_channel -> Option<ChannelBus>`, no `Result`, so no `?` can reinstate an abort) — the fallback channel must never be able to take Matrix, the scheduler, or the graceful-shutdown path down with it; design §6 says the daemon refuses to start *the email channel*, not the daemon. Corrected from an earlier abort-on-`?` posture by the final whole-branch review; see `email_boot.rs`'s module docs. Operator env block + traps documented in `core/src/install/plan.rs::render_email_help`. **KNOWN GAP found in review — CLOSED by PR [#503](https://github.com/hherb/kastellan/pull/503) (2026-08-01; DGX gate GREEN at 2938/0/53, clippy clean):** the force-routed sidecar was a transparent tunnel with no MITM leg, so a self-signed localmail was unreachable by this channel, force-routed or not. #503 gives `spawn_net_transport` a caller-chosen `Mitm` posture, has the channel intercept unconditionally, and selects the anchor once at boot. **Two limits survive:** the anchor map is GLOBAL and host-keyed (one entry reaches every worker on that address — the DGX shares `10.0.0.3` between localmail `:8443` and SearxNG `:8888`), and interception is the *precondition* for the #3b leak scanner, **not** coverage — nothing on this path is scanned ([#501](https://github.com/hherb/kastellan/issues/501)). **Post-PR review round (2026-07-30) — 9 findings, all fixed, DGX-reverified.** Two more High-severity defects in the same families as the pre-PR ones: (a) the gate was **still bypassable** via a legal RFC 8601 `method-version` — `method = Keyword [ [CFWS] "/" [CFWS] method-version ]` (§2.2) means `dmarc/1=fail` is a legal *real* verdict, which the parser read as method `dmarc/1` and therefore did not count, so the more-than-one-dmarc rule never fired and a forged unversioned `dmarc=pass` decided (confirmed a live ADMIT against the real code on the DGX before fixing); (b) a **401/403 on `message_detail` destroyed the message**, because `is_permanent` called it permanent ⇒ acked ⇒ monotonic cursor past it, though both are retryable after an operator action. Three Medium: `email.poll` was a 250ms poll loop, not a long poll (~4 req/s ⇒ ~345k requests, TLS handshakes and `egress.allowed` audit rows per day on an *idle* inbox — `POLL_INTERVAL` now 5s, ratio test-pinned); `channel.replied` asserted deliveries that never happened, so a new **`channel.reply_undelivered`** action is written when the transport refuses (in slice 1 `EmailChannel::send` fails unconditionally, so every email reply is routed-but-undelivered until slice 2); and the per-family-bus `senders` miss logged a misleading `warn!("…dropping")` on every delivered reply (now `debug!`; bus unification filed as [#497](https://github.com/hherb/kastellan/issues/497)). Four Low, incl. a removed comment now substituting a SPACE — deleting one *welded* tokens, so `mx(x)example.net` collapsed to `mxexample.net` and matched that configured authserv-id. **Also newly documented, and not fixable at this layer:** a non-escaping MX can be made to swallow its own verdict, leaving a forged pass as the only methodspec — which is precisely why the per-pairing token, not the DMARC verdict, is what makes the gate hold. See HANDOVER for the full list. **Slice 1 is DONE and on `main` — nothing outstanding at this tier.** The authoritative DGX-native gate at `658924ae` was GREEN: **DGX aarch64 2926/0/53**, `cargo clippy --workspace --all-targets -- -D warnings` clean, only 4 `[SKIP]` (the one env-gated GLiNER tier); Mac 2808/0/23. The localmail dependency (Task 1) **merged 2026-07-30** as [hherb/localmail#223](https://github.com/hherb/localmail/pull/223) `0b6c5e05`. **Deployment is a separate gate:** the DGX's running `localmail-serve` predates that merge commit by three days, so it still serves the pre-cursor API and must be restarted before a live slice-1 deployment; the known MITM/self-signed gap above also applies to a live tier. Slices: **2** SMTP outbound + full round trip (`lettre`); **3** DGX deploy + live tier — the MITM + extra-CA half of slice 3 landed early in PR [#503](https://github.com/hherb/kastellan/pull/503), so slice 3 is now the `localmail-serve` restart, the deploy, and the live run.
- [x] DM pairing flow: short-lived single-use pairing code issued out-of-band (`kastellan-cli pair issue`, hash-only storage); the new peer presents it in-channel; binding recorded in `pairings` (migration 0018), revocable, audited. Static contact allowlists rejected (forgeable) — `DbPeerAuthorizer` gates the bus on active `(channel,peer)` rows; the pairing carve-out is compare-only + operator-gated + single-use (`claim_code` atomic). **WebAuthn deferred** (no browser/CLI client surface yet). Daemon wiring (swap `StaticPairings`→`DbPeerAuthorizer` + pass `DbPairingService`) rides slice #2 Phase D. Branch `claude/zen-bell-6bn2ze`, 2026-06-12. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-channel-pairing*`. (Pattern: ZeroClaw `security/{pairing,otp}.rs`.) (Slice #3.)
- [x] **Channel bring-up is supervised, not one-shot** — closes [#514](https://github.com/hherb/kastellan/issues/514) (PR [#516](https://github.com/hherb/kastellan/pull/516), **MERGED 2026-08-04** as `a21f8276`). Both boot modules tried exactly once and returned `None` on any failure, so a blip in the daemon's first seconds disabled the channel for the life of the process — bot deaf, every unit `active`, nothing further in the log. Live on the DGX 2026-08-03 (**12 h**; the trigger was a restart-window race, not the homeserver — the user manager was itself shutting down, so `systemd-run --scope` could not create the sidecar cgroup). New `channel::boot_supervisor::ChannelSupervisor`: unbounded retries over the existing pure `RestartBackoff`, driven by a `BootOutcome{NotConfigured|Started|Retry|Fatal}` taxonomy whose `Fatal` arm keeps statically-dead configs (`forced_localhost_homeserver` per #459, a partial `EmailConfig`) from becoming retry loops; pure `DowntimeEscalator` for the escalating "deaf for N minutes" line; `channel.started`/`channel.boot_failed` audit rows. Also closes [#502](https://github.com/hherb/kastellan/issues/502) (`SidecarHandle` gains the `Drop` its comments already claimed) — without it the retry loop would leak one egress proxy per attempt. Issue item 3 (`After=network-online.target`) **declined**: a `systemd --user` manager has no such unit (verified on the DGX). **Review round 2026-08-04:** the findings were all one class — *renaming a log line silently breaks everything that greps for it*. `scripts/upgrade_from_git.sh`'s post-deploy channel check had gone blind (both grep alternatives renamed away), `install::plan::render_email_help` still told operators to grep for `EMAIL CHANNEL DISABLED` while the supervisor emits `CHANNEL DISABLED` (its test pinned a *literal*, so it stayed green through the drift — both sides now interpolate `boot_supervisor::CHANNEL_DISABLED_LOG_PHRASE`), and `egress::persistent_net`'s comment still claimed the leak #502 had just fixed. The deploy verifier now polls (bring-up retries, so one snapshot is not a verdict) and is scoped to the current start by a pre-restart byte offset, which also kills its pre-existing stale-`✅` false green. Plus `shutdown_before_the_first_poll_starts_no_attempt` covering the loop-top shutdown guard. Gated both hosts at `e0a663df`: **DGX 2985 / 0 / 53** (`--nocapture`, 4 `[SKIP]` all the `KASTELLAN_GLINER_RELEX_ENABLE` tier) and **Mac 2864 / 0 / 23**, clippy `-D warnings` exit 0 on both. **A host reboot reproduced #514 live on the deployed pre-fix build** (2026-08-04 00:02 UTC): the matrix worker's initial sync failed `tunnel error: unsuccessful` before the network was up, the channel never started, and the bot was deaf until a manual restart that succeeded first try — that string is one of the branch's own scripted `Retry` cases, so the classification matches what production actually emits. Filed not fixed: [#517](https://github.com/hherb/kastellan/issues/517) (supervision covers *bring-up* only — a post-boot `ChannelBus` pump death is still permanent deafness with the supervisor parked, so the "silent bot ⇒ fatal config" reading holds for the boot window only) and [#518](https://github.com/hherb/kastellan/issues/518) (`channel.boot_failed` is written per attempt with no escalator gate, ~1440 rows/day/channel during a sustained outage). Spec + plan: `docs/superpowers/{specs/2026-08-04-channel-boot-supervision-design.md,plans/2026-08-04-channel-boot-supervision.md}`.
- [ ] ~~Telegram inbound adapter (`grammers`, Rust)~~ — **rejected as primary 2026-06-12** (no bot E2E, centralized, ban risk). Could return later as an additional `Channel` impl if a need arises.

## Phase 3 — Channels outbound + browser + web

- [x] Egress proxy (per-worker host allowlist, TLS pinning, audit logging) — **all 4 slices COMPLETE** (#1 boundary/SSRF, #2 force-routing, #3a MITM, #3b leak scanner, #4 TLS pinning).
  - [x] **Slice #1 — boundary host-allowlist enforcement + SSRF/IP defense** — new `workers/egress-proxy` crate (sandboxed per-worker CONNECT proxy over a UDS: reuses `web-common::HostAllowlist`, resolves DNS itself, rejects private/loopback/link-local/ULA/CGNAT/multicast resolved IPs with a literal-IP carve-out for an operator-allowlisted address, pins + dials the surviving IP, tunnels). `Net::ProxyEgress` sandbox variant across bwrap/seatbelt/container. `core/src/egress` (`spawn_sidecar`/`SidecarHandle` + pure `decision_to_audit`; proxy never touches PG — decisions flow proxy→core stdout→`audit_log`). Proven by an e2e test CONNECT client against the real sandboxed sidecar (allow/block/audit) + `#[ignore]` real-net + PG-gated audit-insert. **Does NOT route real workers yet** (mechanism only). Commits `df51c5c`..`29240eb`, branch `feat/egress-proxy-boundary`, 2026-06-10. Design+plan: `docs/superpowers/specs/2026-06-10-egress-proxy-boundary-enforcement-design.md`.
  - [x] **Slice #2 — unbypassable force-routing — COMPLETE, DGX-accepted, ON by default 2026-06-11** (branch `feat/egress-proxy-slice2-impl`). Built + Mac-verified (1521/0/7 workspace, clippy clean): the `web-common` CONNECT-over-UDS connector (`ProxyConnectGet` hyper+tokio-rustls + env-selected `make_get`; `web-fetch`/`web-search` swapped onto it); OS force-routing — Linux `bwrap` private netns + UDS bind, macOS Seatbelt deny-outbound-except-UDS (gating probe **confirms AF_INET denied** on the dev Mac, else `MacosContainer` fallback) + new additive `SandboxPolicy.proxy_uds`; **port-scoping the allowlist (closes [#241](https://github.com/hherb/kastellan/issues/241))** with a distinct audit flag for bare-host grants; the coupled host-side spawn (`core::egress::spawn_net_worker` — sidecar-first **fail-closed**, pure `rewrite_worker_policy`, 1:1 teardown via additive `SupervisedWorker.egress`, decision-ingest → `audit_log`). DGX kernel-barrier probe written (`sandbox/tests/linux_force_routing.rs`). **Task 4.4 — the live auto-flip — SHIPPED 2026-06-11** (branch `feat/egress-force-routing-autoflip`): `core/src/worker_lifecycle/force_route.rs` (pure `policy_net_is_force_routable`/`resolve_force_routing`/`spawn_worker_maybe_forced` + env-glue `from_env`) wires force-routing into both cold-spawn sites (`SingleUseLifecycle::acquire` + `idle_timeout::acquire_impl`) behind the opt-in **`KASTELLAN_EGRESS_FORCE_ROUTING` (default OFF ⇒ byte-identical legacy)**; `egress::net_worker::spawn_forced_net_worker` owns a per-worker scratch dir (RAII-cleaned via `EgressSidecar.scratch`); `main.rs` builds the config fail-closed (enabled-but-no-proxy-binary aborts startup). +16 Mac unit tests (1537/0/7, incl. a PR #250 review-hardening pass: UDS path-length guard, proxy-bin discovery tests, leak-not-remove on the unreachable no-bundle arm). **DGX acceptance + flip-on 2026-06-11** (native aarch64 over WireGuard SSH, branch `feat/egress-slice2-dgx-acceptance`): the kernel-barrier probe `force_routed_allowlist_worker_has_no_direct_route` passes with **real containment** (no `[SKIP]`); new `core/tests/egress_force_routing_e2e.rs` exercises the live coupling end to end (allow round-trip / off-allowlist 403 / on_decision ingest / 1:1 teardown / no-direct-route-from-the-jail / live `pg_decision_sink`→`audit_log`); **#243** confirmed (the `NetClient` seccomp profile allows AF_UNIX `bind`/`listen`/`accept`/`connect`); full workspace **1523/0/9**, clippy `-D warnings` clean. **Flipped ON by default**: `core_service_spec` injects `KASTELLAN_EGRESS_FORCE_ROUTING=1` (**fail-closed** — the daemon won't start without the `kastellan-worker-egress-proxy` binary beside it). Fixed a cross-platform default that would have broken macOS: `from_env`'s scratch root defaulted to `$TMPDIR`, too deep for macOS `sockaddr_un.sun_path` once nested — now `/tmp` on macOS. ~~Deferred: stale-scratch crash-sweep [#251](https://github.com/hherb/kastellan/issues/251)~~ **#251 SHIPPED 2026-07-05** (PR [#406](https://github.com/hherb/kastellan/pull/406) `720b5fd` — startup sweep of dead-owner `egress-<pid>-<seq>` + `matrix-<pid>-<seq>` scratch dirs, shared `kastellan_sandbox::pid::pid_is_alive` liveness; macOS `pyexec-*` residual = [#407](https://github.com/hherb/kastellan/issues/407)). Plan/spec: `docs/superpowers/{plans,specs}/2026-06-10-egress-proxy-slice2-force-routing*.md`.
  - [x] **Slice #3a — TLS interception (MITM) mechanism — COMPLETE 2026-06-12** (PR [#259](https://github.com/hherb/kastellan/pull/259), `e2a7b2b`). The per-worker proxy MITM-terminates each worker's TLS (in-proxy **ephemeral per-instance CA** via `rcgen`; private key never leaves the sandbox, public `ca.pem` exported beside the UDS) and re-originates a **webpki-validated** TLS session to the pinned origin — surfacing nothing new but an additive `tls_intercepted` audit flag (so slice #3b can scan the plaintext). New egress-proxy modules `ca.rs`/`leaf_cache.rs`/`mitm.rs` (`looks_like_tls` + async `intercept` — `tokio-rustls` `TlsAcceptor`/`TlsConnector` + `copy_bidirectional` on a per-connection current-thread runtime; accept-loop + `decide()` stay sync); `handle_conn` restructured connect→200→peek(`recv MSG_PEEK`)→branch (`0x16`→MITM, else transparent pass-through). Worker side: `web-common::ProxyConnectGet` trusts **only** the per-instance CA when `KASTELLAN_EGRESS_PROXY_CA` is set (fail-closed). Host side: `spawn_sidecar` gates readiness on `ca.pem`; `rewrite_worker_policy` binds it into the jail + sets the env. Verified: hermetic in-crate MITM round-trip (real two-leg TLS, Mac+DGX); live `real_mitm_fetch_through_sidecar` → **200 from example.com through the MITM** (macOS); DGX **under real bwrap** `forced_coupling`/`no_direct_route`/`pg_decision_sink` green (the rcgen-keygen + `MSG_PEEK` survive the `NetClient` seccomp profile). Mac `1555/0/8`, DGX `1538/0/10`, clippy `-D warnings` clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-11-egress-proxy-slice3-tls-intercept*`.
  - [x] **Slice #3b — credential-leak scanner — COMPLETE 2026-06-12** (branch `feat/egress-slice3b-leak-scanner`). New pure crate `kastellan-leak-scan` (single source of truth: `SecretFingerprint` + `fingerprint_value` [Rabin `fp64` + SHA-256], streaming `RollingMatcher` [rolling pre-filter + SHA-256 confirm + `(maxLen+1)`-byte carry-over so a secret split across reads still matches], `secret_hashes.json` wire). Host: `Vault::value_fingerprint` (one-way, never exposes plaintext), `egress::leak_provision` (atomic file writer + `egress.secret_hash.provisioned` audit row), spawn-wiring (`spawn_net_worker`/`spawn_forced_net_worker` take `secret_fingerprints`; today's callers pass `&[]`). Proxy: lazy per-connection read of `secret_hashes.json`, scanning bidirectional relay (`mitm/relay.rs`) replacing `copy_bidirectional` (scan-before-forward), `Verdict::BlockedCredentialLeak` → kill connection + redacted decision (hash+offset+direction, **never plaintext**); host audit maps `egress.blocked.credential_leak`. **Hashes-only** detection; **best-effort** streaming block; **fail-open** (defense-in-depth, not the containment boundary — missing/corrupt file ⇒ no scanning). Forward-looking: no current egress worker carries secrets; **dispatch-time live-append now SHIPPED ([#268](https://github.com/hherb/kastellan/issues/268), 2026-06-16)** — `tool_host::dispatch` writes each materialized secret's fingerprint into the worker's sidecar `secret_hashes.json` *before* egress (fail-closed if the write fails; union across worker reuse; one `egress.secret_hash.provisioned` audit row per newly-added fp, keyed by `ref_hash`); activates automatically with the first secret-bearing egress worker. PR #296 review pass (2026-06-17): collection + substitution unified onto one shared `for_each_ref` traversal (parity-tested so a redeemed ref can't be missed by the collector — silent fail-open closed); D3 dedup extracted to a pure unit-tested `select_provisioned_rows`; `merge_secret_hashes` concurrency contract documented (safe via the dispatch's `&mut SupervisedWorker` serialization). PR #269 review pass (2026-06-13): `scan_relay` rewritten as two independent per-direction `pump` futures (no cross-direction head-of-line stall on `write_all`); `MAX_SECRET_LEN` (16 KiB) bounds the fingerprintable range on both sides (no oversized-`len` allocation from a corrupt file); warn-not-skip on a parentless sidecar UDS. Mac `1644/0/8`, clippy `-D warnings` clean (+ cross-clippy Linux on the pure crate). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-egress-proxy-slice3b-credential-leak-scanner*` / `2026-06-12-egress-slice3b-credential-leak-scanner.md`.
  - [x] **Slice #4 — TLS pinning — COMPLETE 2026-06-13** (PR [#272](https://github.com/hherb/kastellan/pull/272), branch `feat/egress-slice4-tls-pinning`). SPKI-SHA256 pin set (RFC 7469) overlaid on the proxy's upstream webpki validation (`workers/egress-proxy/src/pins.rs`: `spki_sha256`/`PinSet`/`chain_has_pin`/`PinningVerifier` + `build_upstream_client_config`), so a compromised public CA can't silently MITM kastellan's own high-value frontier/LLM egress. Operator pins arrive via `KASTELLAN_EGRESS_PROXY_PINS` (unset/blank/`{}` ⇒ **byte-identical plain webpki**); a configured-pin mismatch **fails closed** as `Verdict::BlockedTlsPin` → host `egress.blocked.tls_pin`; a malformed pin set **aborts proxy startup** (fail loud). **Additive** defense — never weakens webpki and leaves the netns/allowlist/SSRF boundary untouched (`.dangerous()` installs a verifier strictly stronger than webpki). Host `proxy_policy`/`spawn_sidecar` thread `cert_pins_json`; spawn args bundled into `NetWorkerSpawn` (dropped both `#[allow(too_many_arguments)]`). New deps `x509-cert`/`sha2`/`base64` (all Apache-2.0/MIT). Mac `1668/0/8`, clippy `-D warnings` clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-13-egress-proxy-slice4-tls-pinning*`.
    - [x] **Operator pin-config host wiring — 2026-06-18** (branch `feat/egress-operator-cert-pins`, PR [#303](https://github.com/hherb/kastellan/pull/303)). Force-routed tool workers (web-fetch/web-search/browser-driver) now enforce operator pins: `KASTELLAN_EGRESS_CERT_PINS` (same `{host:["sha256/<b64>"]}` shape) parsed **fail-closed** on the daemon (`force_route::from_env`/`parse_cert_pins_env`, widened `ForceRoutingError`), stored in `ForceRoutingConfig.cert_pins`, and selected **per-worker** (least-privilege, by allowlist host) into `spawn_worker_maybe_forced`'s `cert_pins_json` — replacing the hard-coded `None`. New pure `core/src/egress/cert_pins.rs` (`CertPinMap`/`parse_cert_pins` [structural: shape + `sha256/` prefix; proxy's `PinSet::parse` stays the authoritative strict validator] + `host_of_endpoint` [IPv6-aware] + `select_pins_for_allowlist`). No new deps, OS-agnostic, byte-identical when unset. **Decoupled from the Phase-5 frontier path** (frontier LLM egress doesn't exist: `Router` denies all frontier calls + runs in-core via reqwest, not a sidecar). `cargo test --workspace` green + clippy `-D warnings` clean. **Still deferred to the first frontier worker:** routing frontier LLM egress through a pinned sidecar (needs the whole Phase-5 escalation path — Router-behind-a-sidecar + a real PolicyGate + frontier key from `db::secrets`); the pin plumbing is then ready to serve it. **Tracked in [#304](https://github.com/hherb/kastellan/issues/304):** a real-sandbox pin-enforcement e2e (pin-mismatch host → blocked `tls_pin`). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-17-operator-egress-cert-pins*`.
    - [x] **Upstream extra-CA seam (self-signed private origins) — capability + deployed operator config both SHIPPED** (capability: PR [#493](https://github.com/hherb/kastellan/pull/493) MERGED `c0ac4e62`, #491, 2026-07-27, DGX 2708/0/53; operator config: PR [#495](https://github.com/hherb/kastellan/pull/495) MERGED `0be03b30` 2026-07-27, #492, DGX 2748/0/53). The MITM re-origination leg can now trust an operator-provided extra CA in addition to webpki: `build_upstream_client_config(pins, extra_ca_path)` (pure `add_extra_ca_pem`) ← `KASTELLAN_EGRESS_PROXY_UPSTREAM_EXTRA_CA` (startup WARN) ← `proxy_policy`/`spawn_sidecar` (env + jail `fs_read`) ← `NetWorkerSpawn.upstream_extra_ca`. **Off ⇒ byte-identical; fail-closed** (bad PEM aborts startup); host-provisioned only; prod `force_route.rs` passes `None`. Proves out a force-routed self-signed localmail round-trip (see the mail-worker line), with a negative control asserting both `is_err` **and** a `mitm_failed: …` decision so the seam is provably load-bearing. **Two operator constraints, documented on `build_upstream_client_config`:** the cert must be a real CA that signed the origin leaf **or** a self-signed leaf with `basicConstraints CA:FALSE` — a `CA:TRUE` self-signed leaf is rejected at handshake time (rustls `CaUsedAsEndEntity`) even though `openssl verify` accepts it, and `openssl req -x509` commonly produces exactly that shape; and the anchor is trusted for **every host its sidecar can reach**, so it suits a single-origin worker only. **Deployed-origin operator config SHIPPED — [#492](https://github.com/hherb/kastellan/issues/492), branch `feat/492-upstream-extra-ca-wiring`, 2026-07-27.** `KASTELLAN_EGRESS_UPSTREAM_EXTRA_CA={"<private-ip-literal>":"/abs/ca.pem"}` → new pure `core/src/egress/upstream_ca.rs` (sibling of `cert_pins.rs`) → per-worker selection in `spawn_worker_maybe_forced` → `NetWorkerSpawn.upstream_extra_ca`. The review's ask was taken: the trust-scope constraint is **enforced, not documented** — an anchor is handed out only for a worker whose allowlist resolves to a **single private origin written as an IP literal** (`kastellan-net-classify::is_denied_range`, the proxy's own SSRF predicate, so the two cannot drift), and a refusal **fails the spawn**. The IP-literal rule is doubly motivated: trust widening is only defensible for an origin the operator controls, and the proxy's SSRF guard denies hostnames resolving into private ranges anyway (only allowlisted literals get the carve-out), so a name-keyed private origin would be unreachable regardless. Every configured PEM is **read at daemon startup** (fail-closed on unreadable/certificate-less) with a trust-widening WARN, so a typo aborts the daemon instead of surfacing on the first force-routed dispatch; `kastellan.env` carries both operator traps, pinned by a test that the block stays entirely commented. Unset ⇒ byte-identical. The `CA:TRUE` trap is real (it is why the DGX localmail cert was regenerated 2026-07-26) but is **not outstanding** — that cert is verified `CA:FALSE`, and localmail's source now emits non-CA leaves for fresh deployments. Spec `docs/superpowers/specs/2026-07-27-upstream-extra-ca-operator-config-design.md`. Signature-hygiene follow-up: [#494](https://github.com/hherb/kastellan/issues/494) (carries a `Mitm::Transparent | Mitm::Intercept { upstream_extra_ca }` enum suggestion, which would make the posture/anchor contradiction unrepresentable). **Sidecar bring-up hardening shipped with it (`a4d9af56`):** the proxy now builds the upstream TLS config **before** binding the UDS + exporting `ca.pem` — those two files are the readiness signal, so validating after them meant a fail-closed abort published "ready" first and `spawn_sidecar` returned a healthy handle for a dying proxy (**latent for the slice-#4 pin path too**, not just the extra CA). `spawn_sidecar` additionally drains the sidecar's stderr (previously piped and unread, so every fail-closed reason and the trust-widening WARN went nowhere), catches an early exit with `try_wait` instead of waiting out `READY_TIMEOUT`, rejects an `upstream_extra_ca` that is relative or paired with `disable_mitm`, and logs the trust widening host-side.
- [x] **Credential-leak scanner co-located in the egress proxy** (egress-proxy slice **#3b**) — **SHIPPED 2026-06-12** (see the `Slice #3b` line above). Outbound/inbound plaintext is scanned at the trust boundary for the verbatim bytes of every secret materialized for the calling worker (host provisions one-way value hashes into `<scratch>/secret_hashes.json`; the proxy holds no plaintext); hits kill the connection + audit hash+offset only. Mechanism + spawn-wiring + **dispatch-time live-append all complete ([#268](https://github.com/hherb/kastellan/issues/268) SHIPPED 2026-06-16)**; the scanner now activates automatically with the first secret-bearing egress worker. (Pattern: IronClaw `safety::leak_detector`, ZeroClaw `security/leak_detector.rs`.)
- [~] **Matrix outbound** (agent → user replies over the E2E `MatrixChannel`) — primary outbound path (decision 2026-06-12; slice #4). **Reply mapping shipped** (2026-06-12): `route::reply_body` surfaces the agent's real completion `plan.result` (`{"kind":"text","body":...}` → the body; `message` alias; compact fallback) and maps `error`/`blocked`/`refused` to safe sentences — fixing the slice-#1 stub that mis-handled the real shape. Live delivery rides slice #2 Phase D. (~~Telegram/Signal outbound~~ rejected as primary — see Phase 2 note.)
- [ ] SMTP outbound in mail worker (`lettre`) — fallback-channel outbound; low-trust notifications, never the primary command path (slice #5) — this is **email-channel slice 2** (slice 1, the gated inbound half, merged `bf8e850b`); until it lands `EmailChannel::send` fails unconditionally and email replies audit as `channel.reply_undelivered`
- [x] **`kastellan-worker-mail` — read-only personal mail-archive tool** (distinct from the IMAP inbound *channel* above; a read tool, not a channel): six read-only `mail.*` JSON-RPC methods (`search` / `get_message` / `list_messages` / `list_accounts` / `get_attachment_text` / `get_attachment` → delivers the original file to a durable per-task out-dir) over a localmail `/v1` REST endpoint (`KASTELLAN_MAIL_ENDPOINT`), `Net::Allowlist` + `Profile::WorkerNetClient`, force-routable. PR [#483](https://github.com/hherb/kastellan/pull/483) `ce144513`, 2026-07-22; first live-verified against the real 37k-message archive + two decode/mock-fidelity fixes in PR [#487](https://github.com/hherb/kastellan/pull/487) MERGED `87afd8b2`. Spec `docs/superpowers/specs/2026-07-22-localmail-mail-worker-integration-design.md`. **Live-test coverage MERGED [#490](https://github.com/hherb/kastellan/pull/490) `efc1001b`** (sandbox 1a/1c + egress-coupling 1b/1d + scripted/live-LLM daemon legs + Mac fidelity contract; shared `scripted_llm`/`mock_localmail`/`egress_forcing` lifts). **Force-routed FULL round-trip MERGED [#493](https://github.com/hherb/kastellan/pull/493) `c0ac4e62` (#491, 2026-07-27):** the new egress-proxy **upstream extra-CA seam** makes a self-signed localmail reachable force-routed; hermetic round-trip (asserts `tls_intercepted:true`) + negative control + live `#[ignore]` DGX tier; DGX 2708/0/53. Deployed-origin operator wiring [#492](https://github.com/hherb/kastellan/issues/492) shipped in PR [#495](https://github.com/hherb/kastellan/pull/495) MERGED `0be03b30` (see the egress line above). Still deferred: macOS Seatbelt-loopback verification of tier 1a.
- [x] `web-fetch` worker: HTTPS-only, host allowlist (self-enforced per redirect hop) + `Net::Allowlist` policy data for the egress proxy, 5 MiB body cap, 5-redirect cap, extracted readable text (HTML readability via `dom_smoothie`/`pdf-extract`/text+JSON), `Profile::WorkerNetClient` + `reqwest::blocking`+rustls — branch `feat/web-fetch-worker`, 2026-06-08. Deferred: egress-proxy enforcement (its consumer is now this worker); `web-search`; hermetic TLS happy-path e2e (waits on the proxy test-CA).
- [x] `web-search` worker (SearxNG default) — new crate `workers/web-search` exposing `web.search` (query → ranked `{title,url,snippet,engine}` hits from a SearxNG `/search?format=json` endpoint; web-search finds, web-fetch reads). Operator-configures `KASTELLAN_WEB_SEARCH_ENDPOINT`; the LLM supplies only the query (no URL-injection surface), so `http://` is allowed for loopback only, `https://` mandatory elsewhere. `Net::Allowlist` derived from the endpoint host:port; `cpu_ms=5_000`/`mem_mb=256`/`SingleUse`; fail-closed `from_env`. Carries the **shared `workers/web-common` crate** extracted from web-fetch (`HostAllowlist` + `HttpGet`/`ReqwestGet` transport + feature-gated `FakeGet`) — single source of truth for the security-critical allowlist matcher; web-fetch re-pointed, behaviour byte-preserved (its strict HTTPS-only rule unchanged). `scripts/web-search/setup-searxng.sh` stands up a local SearxNG with the JSON format enabled. — branch `feat/web-search-worker`, 2026-06-09. Deferred: category/language/engine params; pagination; hermetic SearxNG mock e2e (real round-trip stays `#[ignore]`); egress-proxy enforcement (shared with web-fetch).
- [x] **`web-research` composite worker** — new crate `workers/web-research` exposing `web.research {query, max_sources?, max_passages?}`: one call does search (SearxNG) → filter hits to the operator content allowlist → fetch top-N pages (drive+extract) → chunk → BM25-rank passages → returns `{sources[{url,title,snippet,passages[{text,score}]}], unfetched[{url,reason}], sources_fetched, passage_count}`; the LLM planner synthesizes. Ranking behind a `PassageRanker` trait seam (v1 = pure lexical `LexicalRanker`; hybrid/embedding-RRF deferred). No silent fallbacks (search-fail/empty-query → error; per-page failures recorded in `unfetched`). Reuse: consolidated `search`/`parse`/`fetch`/`extract` into `web-common` behind cargo features (`search`/`fetch`/`extract`; `extract` deps optional → web-search stays lean), web-fetch/web-search re-pointed behaviour-preserved. Manifest `core/src/workers/web_research.rs`: union `Net::Allowlist` (endpoint ∪ content), `WorkerNetClient`/`SingleUse`, force-routable like web-fetch, injection-guard `Relaxed`. Subagent-driven (9 tasks), final opus whole-branch review READY TO MERGE; Mac workspace build+clippy `-D warnings` clean, web-common 65 / web-research 25 / core lib 1123 green. Post-review (`/fixall`, 2026-07-08): non-2xx + zero-relevant-passage fetches now surfaced in `unfetched` (not silent error-HTML sources), BM25 df hoisted out of the doc loop, shared net-entry helper. — branch `feat/web-research-worker`, PR #419 (MERGED `b067b22`), 2026-07-07. Live `#[ignore]` SearxNG e2e (`core/tests/web_research_e2e.rs`, hermetic fail-closed + live composite round trip) added + verified 2026-07-08 (PR #420 `18c448b`). **EmbeddingRanker+HybridRanker(RRF) MERGED as `3c92b0f` (PR [#421](https://github.com/hherb/kastellan/pull/421), 2026-07-08):** opt-in `KASTELLAN_WEB_RESEARCH_EMBED_ENDPOINT` (+ `_EMBED_MODEL`, default `embeddinggemma`) → worker POSTs to an embedding-only endpoint over the shared egress transport; pure `bm25`/`cosine`/`rrf_fuse` (k=60) primitives behind an `Embedder` I/O seam; `PassageRanker` trait retired; hybrid default, **degrade-to-lexical-with-signal** on embed failure (`ranking`/`embed_note` in the result); manifest unions the embed host:port into `Net::Allowlist`. Unset endpoint → byte-identical lexical path. Spec `docs/superpowers/specs/2026-07-08-web-research-embedding-hybrid-ranker-design.md`. **Live `#[ignore]` embed e2e MERGED 2026-07-08** (`core/tests/web_research_e2e.rs::real_research_with_hybrid_ranking`, PR [#422](https://github.com/hherb/kastellan/pull/422) `174116c`): drives real `HttpEmbedder::post` bytes over the worker's egress transport against a live embedding backend (default local Ollama `embeddinggemma`), asserts `ranking == "hybrid"`; verified green on the Mac (real SearxNG + Ollama through the Seatbelt jail, 11.3s). **Cap passages-per-embed-POST MERGED 2026-07-08** (`1aa575d`, PR #423): `MAX_EMBED_PASSAGES = 128` bounds the per-page embed to the first 128 document-order chunks so one embed response can never exceed the 5 MiB record cap; BM25 still ranks all passages; capping sets an `embed_note`. **Parallel fetch MERGED 2026-07-08** (`46ba20c`, PR #425): `research()` now runs a two-phase pass — fetch every allowlisted candidate concurrently in bounded `std::thread::scope` waves (`fetch_candidates`, `MAX_CONCURRENT_FETCHES=6`, one shared `&transport`) then classify+rank sequentially in rank order, **output byte-identical to the old sequential pass** (only the fetch pattern changes; surplus over-fetched pages discarded, no extra embed POSTs). Enablers: `HttpGet: Send+Sync`, `ProxyConnectGet` multi-thread tokio runtime, `KeyedFakeGet` test double; `gather_source` split into `fetch_and_chunk`+`rank_fetched_page`+`hit_allowed`. Embed stays sequential. Final opus review READY TO MERGE, zero Critical/Important. **DGX gate DISCHARGED 2026-07-09:** native aarch64 `cargo test --workspace` = **2367/0/38** (0 `[SKIP]`, real bwrap + live PG; new baseline) + workspace clippy clean; the multi-thread-runtime + parallel-fetch tests ran green on Linux. Post-review `/fixall` (2026-07-09) filed the two non-trivial review follow-ups as issues: [#426](https://github.com/hherb/kastellan/issues/426) (`ProxyConnectGet` 4-thread runtime is spun even for single-request workers) and [#427](https://github.com/hherb/kastellan/issues/427) (no e2e for concurrent `ProxyConnectGet` fetch over real CONNECT+TLS through a live egress sidecar). **Firecracker VM entry DONE 2026-07-09** (branch `feat/web-research-microvm-entry`, PR [#428](https://github.com/hherb/kastellan/pull/428)): opt-in `KASTELLAN_WEB_RESEARCH_USE_MICROVM=1` (Linux) → `web_research_firecracker_entry` (mirror of web-fetch's VM entry: empty `fs_read`, in-rootfs binary, `FirecrackerVm` backend, `+KASTELLAN_MICROVM_*` env) via a `WebResearchManifest::resolve` short-circuit; keeps the union `Net::Allowlist` and forwards the SearxNG + embed endpoint config (loopback-embed caveat documented → degrade-to-lexical in VM mode). `build-web-research-rootfs.sh` → `web-research.ext4`; DGX-gated `web_research_firecracker_egress_e2e` (CONNECT-to-proxy gate). Shared pure `base_env()` helper; host path byte-identical when unset. **DGX gate DISCHARGED:** e2e GREEN (real VM boot → SearxNG search → CONNECT received) + `cargo test --workspace` = **2369/0/39** (0 `[SKIP]`, NEW baseline) + workspace clippy clean. systematic-debugging caught a test-only bug (endpoint host must be on the worker allowlist; production `tool_allowlists` carries it). Spec/plan `docs/superpowers/{specs,plans}/2026-07-09-web-research-firecracker-microvm-entry*`. **Web-research Slice-4 now functionally COMPLETE. Still deferred:** the `ProxyConnectGet` **embed** e2e over a live egress sidecar + proxy CA ([#427](https://github.com/hherb/kastellan/issues/427) family, DGX egress-sidecar); a resolve-time operator warning when VM mode is on with a loopback/private embed endpoint (silent hybrid→lexical downgrade — [#429](https://github.com/hherb/kastellan/issues/429), needs a manifest-resolve logging channel). Post-review `/fixall` (2026-07-09) found no code defects (all review items were minor/consistent-with-web-fetch); filed [#429](https://github.com/hherb/kastellan/issues/429).
  - [x] **…and supervised once it is up, not only while it starts** — closes [#517](https://github.com/hherb/kastellan/issues/517) (PR [#521](https://github.com/hherb/kastellan/pull/521), branch `fix/517-supervise-channel-liveness`). #516's `Started` arm parked on the shutdown oneshot and never re-examined the channel, but every `ChannelBus` pump has a terminal exit nothing watched — the completed-task pump returning when its listener gives up (replies stop going out), a per-channel task breaking on a closed `recv` (inbound stops coming in), or a panic in either — each reproducing #514's signature after boot instead of during it, and none writing a row. **The premise correction that shaped the fix:** `PgListener::recv()` already auto-reconnects in sqlx 0.9, so the `Err` arm is a *failed reconnect* (a sustained Postgres outage), not a dropped connection — the issue's smaller alternative ("make `next_completed` reconnect") would have fixed nothing sqlx does not already do, and could not cover a panic. New `channel::pump_liveness::{DeathBell,PumpLife}`: each pump holds a guard whose **`Drop`** rings, which is the only form that also covers a panicking or aborted task, and `notify_one` (not `notify_waiters`) so a death preceding the waiter is not lost. `StartedChannel` gains a liveness future (`new()` without one keeps park-until-shutdown exactly, so existing callers are unchanged); the loop selects shutdown-vs-death and re-enters itself, **stopping the channel on both paths** — a channel that lost one pump still holds the worker through its surviving per-channel task, whose drop is the RAII teardown, so abandoning it would leak what #502 fixed. Two guards keep the cure from being worse: a **flap guard** (`DowntimeEscalator::ran_long_enough` + `record_success`, 60 s = the backoff cap) so only a channel that stayed up resets the failure counter — without it a channel dying on arrival restarts at full speed forever, spawning a sandboxed worker per iteration — and a new `BootAudit::Died` → `channel.died` carrying `ran_ms`, kept distinct from `boot_failed` because "never came up" and "was up for six hours, then died" are different events. Both boot modules now open LISTEN/NOTIFY **before** spawning the worker: the dominant restart trigger is the Postgres outage that fails exactly that step, so the old order would spawn a worker and sit through a login on every retry of it. `boot_supervisor.rs` hit the 500-line cap → types lifted to `boot_supervisor/types.rs` (371 + 143). Mutation-checked: reversing the `biased` arms fails the ordering test and nothing else — and that test's first version passed either way, because `wait_or_shutdown` already declines the restart; what the bias actually prevents is a `channel.died` row written for a channel that was merely being shut down, and the test now asserts that instead. DGX **3025 / 0 / 53** (`--nocapture`, 4 `[SKIP]` all the `KASTELLAN_GLINER_RELEX_ENABLE` tier), clippy `--workspace --all-targets -D warnings` exit 0; Mac `core --lib` 1496/0 + `--bins` 87/0 + clippy clean. **Live-verified on the DGX** by stopping `kastellan-postgres` under the branch daemon: `outbound pump stopped` → `channel stopped working after running {ran_ms: 30557, retry_in_ms: 1000}` → retries failing on `PgCompletedTasks::connect` → `channel bus running {attempts: 6}` two seconds after Postgres returned, with `matrix worker logged in` appearing exactly twice in the run (so the five failed retries spawned no worker — the ordering fix). It also confirmed the audit limitation now documented in `pg_sink`: the outage that kills the pump also blocks the sink, so `channel.died` and the intervening `boot_failed` rows are absent and the daemon log is the record. **Review round 2026-08-05: one real defect, in the half nothing could observe.** `escalate_if_due` ran on *both* death arms, so a stable death did `record_success()` and then immediately `record_failure()` — opening an outage dated at the death that **nothing ever closed**, because the escalator learns about health only when a *stable* channel dies. A channel that came straight back and worked for hours still carried that instant, so its next flap reported every healthy hour as downtime, in the one line whose text asserts nothing sent to the channel has been received for that long — and past the 300 s threshold on the very first event, firing at once instead of after five minutes of real silence. The next outage is now opened by the first restart attempt that actually **fails**, the only version that stays correct when the restart *succeeds*: a real outage is dated one backoff delay late (≤60 s) where the eager version's error was unbounded. It survived the first round because escalation is a log line and nothing else, so the sequence had no test seam; the pure half is now `note_outage` over `Outage{Continues,Ends}` — the only place either half of the escalator's outage state is driven from — and two of its three tests fail before the fix (`Some(14400s)` where `None` is required). Two doc defects with it: `render_email_help`'s documented `audit_log` query never learned about `channel.died`, hiding the event this branch adds (the three action names are now substituted from `channel::actions`, and the test **iterates the set** so a new one is a failing test — #516's drift finding in its other direction, *adding* rather than renaming), and `record_success`'s docs claimed the supervisor calls it "on every successful start", which it does not and must not. Re-gated on the DGX at `3285a5e1`: **3028 / 0 / 53** (`TEST_EXIT=0`, +3 = exactly the new tests), clippy `--workspace --all-targets -D warnings` exit 0. Filed not fixed: [#522](https://github.com/hherb/kastellan/issues/522) — `STABLE_UPTIME` (60 s) sits below the escalation threshold (300 s), so a channel cycling "up 61 s → dead" is stable every time, resets the backoff and the escalator on every death, and never escalates at all; `RespawnRateAlarm` is built inside each `PersistentWorker`, so a channel restart discards its window and a death-rate alarm has to live in the supervisor. Spec: `docs/superpowers/specs/2026-08-05-channel-liveness-supervision-design.md`.
  - [x] **…and what it says about itself is rate-limited, with an alarm for a channel that keeps dying** — closes [#518](https://github.com/hherb/kastellan/issues/518) and [#522](https://github.com/hherb/kastellan/issues/522) (PR [#525](https://github.com/hherb/kastellan/pull/525), branch `fix/518-522-channel-event-reporting-gates`, **MERGED 2026-08-06 as `4e215481`**; not yet deployed — rides the next deploy). #518: the loud line was escalation-gated and the durable row was not, so a sustained outage wrote ~1440 identical `channel.boot_failed` rows/day/channel. #522: `STABLE_UPTIME` (60 s) sits below the escalation threshold (300 s), so a channel cycling "up 61 s → dead" was *stable* on every death — each one reset the failure counter **and** called `record_success()`, clearing the escalator — so the loud line never fired at any point while each cycle cost a sandboxed worker, its sidecar and a full login. One predicate answers both: `should_record(alarm_latched, alarm_spoke_now) = !latched || spoke`, driven by the alarm owning each stream (`DowntimeEscalator` for `boot_failed`, a new supervisor-owned `RespawnRateAlarm` for `died`). **This beat #518's own sketch** (`failures <= FIRST_N || escalated`) by needing no new constant — N falls out of the escalation threshold and backoff schedule — and by making the row and the line the *same* decision rather than two that agree today. New `boot_supervisor/reporting.rs` owns `ReportingPolicy` (both alarms, `Verdict{record, still_down, flapping}` with the two alarms as separate `Option`s so no precedence rule is invented) and replaces the `DowntimeEscalator` parameter of `ChannelSupervisor::spawn`; `RespawnRateAlarm` gains additive `with_repeat`/`in_storm`; `tests.rs` (719) split into `tests/{mod,bringup,liveness,reporting}.rs` as pure movement in its own commit. Row counts: 24 h bring-up outage 1440 → **~57**; 24 h flap ~2832 → **~1470**; a transient blip unchanged, because the gate only engages once an alarm has spoken. **Five things worth carrying.** (1) **Both latch reads happen AFTER the recording call** — reading `in_storm()` before `record()` reads better and is wrong, because `record()` is what re-arms on a cleared storm, so the first death of a *fresh* storm would be silently suppressed; mutation-pinned by `the_first_death_of_a_fresh_storm_is_recorded`. (2) **`channel.started` was gated and that was a design error caught mid-branch**: the latch only clears on a *later* death, so the start ENDING a storm was suppressed too and a channel that recovered for good never wrote another row — `channel.died` stayed the last durable event for a healthy channel. Now ungated; the extra ~1416 rows/day in a flap are the price of the answer to "is it up?", and the unbounded-forever case (a permanently-`Retry` bring-up outage) is fixed regardless. (3) **#522's own premise about `PersistentWorker` is wrong**, and so was this branch's first draft: `persistent.rs:167` builds the alarm before the job loop and `:220` swaps the *transport*, so it DOES accumulate across worker respawns — what it cannot see is a **channel** restart, which tears the whole thing down. Conclusion unchanged, attribution corrected; same lesson #521 learned about `PgListener`. (4) **#516's finding recurred inside the change that documents itself as immune to it** — `CHANNEL FLAPPING` shipped as a bare literal in `render_email_help` beside four const-interpolated neighbours, because the spec correctly predicted "no new *action* name, so the guard needs no change" and this was a *log phrase*: a guard protects the category it enumerates and nothing adjacent. Caught by the final whole-branch review. (5) `report()` now runs **before** the gated `emit().await`, so the alarm no longer sits behind a Postgres write whose reachable failure cause is Postgres going away. DGX **3042 / 0 / 53** at `22f8829d` (`TEST_EXIT=0`, `--nocapture`, 4 `[SKIP]` all the `KASTELLAN_GLINER_RELEX_ENABLE` tier), clippy `--workspace --all-targets -D warnings` exit 0; Mac `core --lib` 1513/0 + `--bins` 16+87 + clippy clean. **The +14 landed exactly on the prediction (3028 + 14)** and cross-checks against the raw diff (31 added `#[test]` attributes minus the 17 that merely moved in the split). Filed not fixed: [#523](https://github.com/hherb/kastellan/issues/523) (in the flap band a stable death clears the escalator latch, so a failed restart attempt writes an ungated `boot_failed` row every cycle — the naive fix is a bare stale-latch read *and* would suppress `cause`, the only forensic field) and [#524](https://github.com/hherb/kastellan/issues/524) (`CHANNEL STILL DOWN` is still a bare literal, third instance of #516's class). **Post-review `/fixall` wave (2026-08-06, `97488a34`):** the whole-PR review found no code defects but three gaps — the ungated-`channel.started` decision had no pinning test (new `a_start_during_a_latched_storm_is_still_recorded`, mutation run: gating the emit fails it and nothing else), `in_storm` narrowed `pub`→`pub(crate)` (the bare-latch-read footgun stays off the published API), and the plan doc got an as-shipped errata banner for the reversed `Started` gating (the spec had been corrected; the plan had not). Semver checked and dissolved: `ChannelSupervisor` postdates v0.2.0, so #525 touches nothing published. Gate: DGX **3043 / 0 / 53** at `97488a34` (`TEST_EXIT=0`, clippy exit 0, 4 `[SKIP]` same tier — landed on the +1 prediction); Mac `core --lib` 1514/0 + core clippy clean. Spec: `docs/superpowers/specs/2026-08-05-channel-event-reporting-gates-design.md`.
  - [x] **…and the flap band's `boot_failed` rows are rate-gated too, with the last operator phrase const-lifted** — closes [#523](https://github.com/hherb/kastellan/issues/523) + [#524](https://github.com/hherb/kastellan/issues/524) (PR [#526](https://github.com/hherb/kastellan/pull/526), branch `fix/523-524-boot-failed-rate-gate`, **MERGED 2026-08-06 as `5cbab01e`**; not yet deployed — rides the next deploy with #525). #523: each stable death clears the escalator (`Outage::Ends` → `record_success`), so a transiently-failing restart attempt was evaluated against a freshly-cleared latch — one ungated row per cycle, unbounded, in exactly #522's regime. Fix: a second, deliberately **silent** `RespawnRateAlarm` on the failed-attempt stream inside `ReportingPolicy` (same `FLAP_ALARM_*` constants), fed by `note_failed_attempt` itself so the latch read follows a `record()` on the same alarm **by construction** — the issue's bare-stale-latch-read trap is structurally unrepresentable — with its voice deferred while the escalator has escalated, so a sustained outage keeps #518's row schedule instead of double rows per repeat. Row arithmetic: 24 h outage ~57 → **~53**; flap-with-failing-restarts **unbounded → first 5 + one `cause`-bearing row per 30 min**; plain flap unchanged. Three mutation checks, each failing exactly its named test. #524: `CHANNEL_STILL_DOWN_LOG_PHRASE` interpolated into the `error!` line and the help block — the third and last #516-class literal there; every operator phrase in that block is now const-driven. Gates landed exactly on the +4 prediction: DGX **3047 / 0 / 53** at `200ce72e` (tip `ffb331db` comment-only, targeted suite green at tip both hosts), clippy exit 0; Mac `core --lib` **1518 / 0** + core clippy clean. Parked for the next release: workspace `Cargo.toml` still 0.2.0 while pub API accumulates since #516 — needs a minor bump at the next crates.io publish. Spec: `docs/superpowers/specs/2026-08-06-boot-failed-rate-gate-design.md`.

- [x] **embedding-broker sidecar** — a jailed / force-routed / micro-VM worker cannot reach the operator's embedding backend (loopback SSRF-blocked; MITM re-originates webpki-only), so a single-purpose **sidecar** bridges the worker's UDS to the backend -> the worker needs **zero embed egress** (supersedes the deferred embed-over-`ProxyConnectGet` e2e; the [#427](https://github.com/hherb/kastellan/issues/427) concurrent content-fetch e2e stays for Slice C). The user's fastembed idea was explored + rejected (per-worker model duplication). **Slice A DONE — MERGED as `b077629` (PR [#430](https://github.com/hherb/kastellan/pull/430)), 2026-07-09:** new crate `workers/embed-broker` (serves JSON-RPC `embed{model,input}` over a UDS via `protocol::server::serve`; OpenAI-compat forwarding behind an `HttpGet` seam — reorder+count-check; fail-closed caps `MAX_INPUTS=256`/`MAX_REQUEST_BYTES=1_000_000` enforced BEFORE the backend; `main.rs` binds the UDS before `lock_down()`) + `BrokeredEmbedder` behind web-research's `Embedder` seam + pure `choose_embedder` (broker UDS wins; unset byte-identical, `HttpEmbedder` untouched) wired into `from_env`. Fully hermetic; Mac-green (embed-broker 16/0, web-research 54/0, `build --workspace` + `clippy --workspace -D warnings` clean); DGX **2369/0/39** carries forward. Subagent-driven TDD (8 tasks); final opus review zero Critical/Important. Post-review `/fixall` (2026-07-09): hardened the trusted boundary — `forward_embed` now rejects **non-contiguous backend indices** (duplicate/gapped `index` passes the count check yet silently mispairs a vector with the wrong input position → fail-closed, +2 tests); remaining review minors tracked as [#431](https://github.com/hherb/kastellan/issues/431). **Slice B1 DONE — branch `feat/embed-broker-slice-b`, 2026-07-10:** `SandboxPolicy.embed_broker_uds: Option<PathBuf>` (dedicated field) + `linux_bwrap` `--bind` (orthogonal to the netns `match` — AF_UNIX is mount-ns-scoped) + `macos_seatbelt` `(allow network-outbound (remote unix-socket …))` emitted after the net match so it survives the force-routed deny-outbound posture; all 16 explicit `SandboxPolicy` literals updated (byte-identical when `None`). TDD (9 new tests: 4 bwrap Linux-gated, 4 seatbelt, 1 lib — the extra 4 from the post-review `/fixall`, 2026-07-10: seatbelt broker-allow under legacy-broad-allow + `Net::Deny`, bwrap `embed_broker_uds` rejection for relative + `..` paths; no code defects, rw-`--bind` rationale already documented); Mac-green (sandbox lib 84/0, workspace clippy `--all-targets -D warnings` clean, aarch64 cross-clippy clean). Did NOT touch the spawn chokepoint → safe solo merge. **Slice B2/B3 (core wiring) DONE — branch `feat/embed-broker-slice-b2b3`, PR [#433](https://github.com/hherb/kastellan/pull/433), 2026-07-10:** `core::embed_broker::{spawn_embed_broker,EmbedBrokerConfig,EmbedBrokerSidecar}` (mirrors `spawn_forced_net_worker`; `Net::Allowlist([backend])`, `WorkerNetClient`, lockdown-env derivation, sun_path-capped `embed-<pid>-<seq>/`, `embed-` joins the #251 sweep); `ToolEntry.embed_broker`/`SupervisedWorker.embed_broker`; `force_route::spawn_worker_with_optional_broker` chokepoint (fail-closed; `rewrite_policy_for_broker` sets `embed_broker_uds`+injects `KASTELLAN_EMBED_BROKER_UDS`; force-routing's clone-mutate preserves both — orthogonal per B1); web-research manifest broker-mode (`KASTELLAN_WEB_RESEARCH_USE_EMBED_BROKER=1`) drops the embed host from `Net::Allowlist` + omits the endpoint env. `EmbedBrokerConfig` threaded through the managers + `main.rs` (no daemon gate — manifest opts in). VM×broker deferred to Slice C. **Verified:** Mac (Seatbelt) core lib 1142/0, embed_broker 12/0, force_route 29/0, web_research 5/0, scratch_sweep 12/0, workspace build + `clippy --all-targets -D warnings` clean, real spawn e2e (`core/tests/embed_broker_spawn_e2e.rs`) GREEN; **DGX gate DISCHARGED** (native aarch64, real bwrap+Landlock+seccomp — the spawn e2e GREEN confirms `WorkerNetClient` permits AF_UNIX accept + AF_INET connect, no seccomp additions). **MERGED as `998143d` (PR #433). Post-merge `/review` follow-up — MERGED as `1917761` (PR [#434](https://github.com/hherb/kastellan/pull/434)), 2026-07-10** (three non-blocking spawn hardening items, off `main`@`998143d` since #433 merged before the follow-ups landed): (1) drain the broker's stdout too so a future stdout write can't fill the pipe + deadlock it; (2) `wait_for_socket`→`wait_for_broker_ready` interleaves `child.try_wait()` so an early broker exit fails fast with its real status instead of a 5s bind-timeout; (3) `spawn_embed_broker` rejects a hostless/unparseable endpoint up front (`InvalidInput`) before minting scratch or spawning. Mac-green (`embed_broker` lib 14/0 (+3), core `--all-targets` clippy `-D warnings` clean); no new DGX surface. **#431 worker-crate minors DONE — branch `fix/embed-broker-431-minors`, 2026-07-10:** shared `kastellan_worker_web_common::embed_rows::{reorder_embeddings,ReorderError}` (sort+count+contiguity) now backs `forward_embed`/`HttpEmbedder`/`BrokeredEmbedder` (the two client paths gain the contiguity guard); `EmbedError::Broker{code,message}`+`NonContiguous`; broker `READ_TIMEOUT=30s` + `WRITE_TIMEOUT=30s` + fail-closed `OnProtocolError::Close` (post-`/review` `/fixall`, 2026-07-11: an erratic worker that stalls a read, wedges a write once the send buffer fills, or sends an over-cap/malformed frame is dropped, not kept on a faulted connection — stdio workers keep the `Continue` default byte-for-byte); `protocol::server::serve_capped` made `pub` (+ new `on_error` policy param) + broker frames at `BROKER_MAX_RECORD_BYTES=16 MiB` (was 64 MiB); `web_common::http::ensure_crypto_provider()` installed when the endpoint is `https`. Pure-code/hermetic; Mac-green (web-common 77/0, embed-broker 20/0, web-research 56/0, protocol 12/0, workspace build + `clippy --all-targets -D warnings` clean); DGX 2416/0/40 carries forward. **Slice C host-mode e2e DONE — branch `test/embed-broker-slice-c-egress-e2e`, 2026-07-11:** new `core/tests/embed_broker_egress_e2e.rs` (test-only) — a hermetic policy-invariant pin (broker UDS bound + `KASTELLAN_EMBED_BROKER_UDS` injected + direct-embed env omitted + embed host absent from `Net::Allowlist`) + an `#[ignore]` live e2e (spawn real broker under the sandbox → jailed `web-research` worker → `web.research` → `ranking=="hybrid"` with the embed host absent from egress — strictly stronger than the direct `web_research_e2e` hybrid test). **Mac-green** (clippy `--all-targets -D warnings` clean, hermetic 1/0, `#[ignore]` skips cleanly); **DGX live gate DISCHARGED** (real bwrap + live SearxNG + live Ollama `embeddinggemma` → live e2e GREEN 14.34s; test-only + additive → DGX 2416/0/40 baseline unaffected). #427 (concurrent content-fetch e2e) already CLOSED. Post-review `/fixall` (2026-07-11): both tests now drive the **real** `rewrite_policy_for_broker` (widened `pub(crate)`→`#[doc(hidden)] pub` — the one non-test line) instead of a divergence-prone replica (which had already lost the env-dedup `retain`), so the hermetic pin genuinely guards the production manifest+rewrite pair; live re-assertion now fails closed on a non-`Allowlist` net variant. Mac clippy `--all-targets -D warnings` clean, hermetic 1/0. **VM×broker DONE — branch `feat/web-research-vm-embed-broker`, PR [#446](https://github.com/hherb/kastellan/pull/446) MERGED as `17d81cc3`, 2026-07-13:** a **second guest→host vsock channel (port 1026)** cloning the slice-4a egress channel (1025) so a `USE_MICROVM` web-research worker reaches the host-side broker → hybrid ranking against a local/loopback embed backend with zero embed egress (the Slice-C property, now for VMs). `sandbox/linux_firecracker` (`BROKER_VSOCK_PORT=1026`/`GUEST_BROKER_UDS`, `broker_vsock_port`/`broker_host_uds` plan fields from `policy.broker_uds`, kind-agnostic value-match env override → guest path, `kastellan.broker=1` token, launcher argv, VMM-jail bind; no net/NIC effect); `microvm-run` (2nd reverse-relay); `microvm-init` (`parse_broker_config` + `mount_run_tmpfs`-once + generic `setup_relay(uds,port)`); `core/workers/web_research.rs` (`resolve()` VM×broker branch + `web_research_firecracker_broker_entry` — replaces the prior `USE_EMBED_BROKER`+`USE_MICROVM` refusal). Worker binary + `rewrite_policy_for_broker` UNCHANGED. **DGX gate DISCHARGED** (native aarch64: full cfg(linux) compile clean, sandbox plan 45/0 + core resolve 9/0, hermetic broker e2e 1/0, live egress-VM boot 1/0 through the refactored relay, full workspace **2512/0/42** + clippy clean, 0 SKIP). Subagent-driven (6 tasks, all reviews Approved + final opus). Live full-hybrid e2e (embed bytes over 1026 + hybrid ranking) deferred → [#445](https://github.com/hherb/kastellan/issues/445) (blocker: web-research needs a MITM egress proxy; `spawn_net_transport` is transparent-only; MITM force-route spawn is `pub(crate)` → needs a test-seam). Spec/plan `docs/superpowers/{specs,plans}/2026-07-13-web-research-vm-broker-embed-channel*`; core-wiring plan `docs/superpowers/plans/2026-07-10-embed-broker-sidecar-slice-b-core-wiring.md`. **Post-review `/fixall` (2026-07-14, commit `703701dd`):** renamed the host-side relay `microvm-run::egress_relay`→`reverse_relay` (`spawn_reverse_relay`/`parse_reverse_relay_args`, genericized diagnostics — the module serves both channels now, matching the guest-side `setup_relay` generalization); added hermetic `vm_broker_real_rewrite_flows_through_plan_to_guest_path` chaining the REAL `rewrite_policy_for_broker` through the REAL `build_launch_plan` to pin the `KASTELLAN_EMBED_BROKER_UDS` host→guest value-match rewrite in one body; clarified the #445 deferral is a functionality (not containment) gap. DGX re-verified: full workspace **2513/0/42** (+1 test, 0 SKIP) + `clippy --workspace --all-targets -D warnings` clean; **new DGX baseline 2513/0/42.** **The embed-broker arc is COMPLETE (host + VM).** **[#445](https://github.com/hherb/kastellan/issues/445) live VM×broker hybrid e2e DONE — branch `feat/web-research-vm-broker-live-hybrid-e2e`, 2026-07-14:** the deferred live tier now runs. Unblocked by a minimal production seam — a host `sidecar_backend` on `NetWorkerSpawn`/`spawn_net_worker` (the egress-proxy sidecar runs on the host while the worker runs in the VM; mirrors `NetTransportSpawn`; all current callers pass the same backend → byte-identical). New live `#[ignore]` `brokered_vm_worker_ranks_hybrid_over_vsock_with_zero_embed_egress`: a web-research worker booted in a Firecracker VM ranks `hybrid` by embedding through the host broker over **vsock 1026**, SearxNG+content over the host MITM egress sidecar (vsock 1025), embed host **absent** from egress. **Root-caused + fixed a TEST bug** (the #446 broker channel was fine): the e2e passed the HOST `target/debug` binary path as the VM worker program → baked into `kastellan.worker=` → guest `execv` ENOENT → PID1 panic → boot loop → dispatch hangs to the wall-clock (looked like a channel hang); VM workers run from the rootfs-baked `/usr/local/bin` path (memory note filed). Also fixed the e2e proxy allowlist to mirror production (derive from `policy.net` → the `127.0.0.1:8888` literal the loopback carve-out needs). **DGX live GREEN** (clean, debug reverted): broker e2e **3/0 in 65.67s** (2 hermetic pins + live tier; `ranking==hybrid`, real SearxNG+Ollama, 0 SKIP); egress decisions confirm SearxNG loopback-carve-out + wikipedia MITM. Mac: `egress::net_worker` 12/0 (+1 recording-double pin) + clippy `--all-targets -D warnings` clean. Daemon-side VM force-routing (make it the supervised default) — **DONE, MERGED as `5760666c` (PR [#450](https://github.com/hherb/kastellan/pull/450), 2026-07-15, closes [#448](https://github.com/hherb/kastellan/issues/448)):** the host `sidecar_backend` is threaded through the daemon's own cold-spawn chokepoint (`spawn_worker_maybe_forced`/`spawn_worker_with_optional_broker`) + both lifecycle facades (`SingleUse`/`IdleTimeout`), so a force-routed VM `Net::Allowlist` worker runs its egress sidecar + embed broker on the host with no "is-VM" signal (`resolve(None,None)` is byte-identical for host workers). Core-only; 3 Mac recording-backend unit tests + DGX manager-level e2e (`SingleUseLifecycle::acquire` → `ranking==hybrid` + zero embed egress). DGX baseline **2516/0/44** (next full run 2517/0/44 with the Mac-only 3rd test). Spec/plan `docs/superpowers/{specs,plans}/2026-07-14-daemon-vm-force-routing*`. **The embed-broker/VM-force-routing arc is COMPLETE.** Spec/plan `docs/superpowers/{specs,plans}/2026-07-14-web-research-vm-broker-live-hybrid-e2e*`.
- [x] **search-broker sidecar (web-search × force-routing × loopback SearxNG)** — the search analog of the embed-broker: under `KASTELLAN_EGRESS_FORCE_ROUTING=1` a `Net::Allowlist` worker egresses only through the SSRF-guarded proxy (denies loopback + RFC1918), so the DGX's `127.0.0.1:8888` SearxNG was unreachable. **Generalizes** the merged embed-broker into a kind-parameterized `core/src/broker/` module (`BrokerKind{Embed,Search}` — every per-kind string off the enum; embed constants byte-identical → web-research unaffected; sandbox `embed_broker_uds`→`broker_uds`) + new `workers/search-broker` crate (JSON-RPC `search{query,count}` over a UDS → SearxNG; host-netns, `Net::Allowlist([searx])`) + web-search `SearchProvider` seam (`Hit: Deserialize`, `Direct`/`Brokered`, `choose_search_provider`) + manifest broker mode (`KASTELLAN_WEB_SEARCH_USE_BROKER=1` → **empty `Net::Allowlist`**, no endpoint env, `entry.broker=BrokerSpec::search`; worker keeps ZERO direct egress). 8 tasks TDD subagent-driven; final opus review READY TO MERGE (0 Critical/0 Important, Minors triaged). **Mac-green** (per-crate all green + `clippy --all-targets -D warnings` clean; hermetic zero-egress e2e passes). **DGX LIVE-PROVEN 2026-07-12:** `#[ignore]` zero-egress e2e GREEN under real bwrap + loopback SearxNG (worker on EMPTY allowlist returned real results via the broker); daemon cut over (force-routing kept, `USE_BROKER=1`), and a real Matrix `web.search` returned **10 real results (Reuters/BBC) in 1.7s through the broker** (audit id 1010). — branch `feat/search-broker-sidecar`, **PR [#440](https://github.com/hherb/kastellan/pull/440) MERGED to `main` as `3d92fa6` (2026-07-12).** **Deploy caveat:** a plain `cargo build --release --workspace` builds `kastellan-worker-matrix` WITHOUT `live-matrix` → a stub that silently kills the Matrix channel; deploy via `scripts/build-release.sh`. Spec/plan `docs/superpowers/{specs,plans}/2026-07-11-search-broker-sidecar*`.
- [x] **browser-driver Firecracker micro-VM entry (4th and last single-use net worker)** — **ARC COMPLETE. SLICE 1 (rootfs) MERGED as `8dbff298` (PR [#470](https://github.com/hherb/kastellan/pull/470)); SLICE 2 (the VM entry) MERGED as `ae1a9318` (PR [#472](https://github.com/hherb/kastellan/pull/472)); SLICE 3 (live render + budget measurement) MERGED as `61890c48` (PR [#474](https://github.com/hherb/kastellan/pull/474)), post-review hardening in-PR (`9ae425d3`, spec §8.5).**
  - **Slice 3 — the live render, 2026-07-19.** **A real page now renders inside the micro-VM through a real egress sidecar — the first time in this repo, in VM *or* host mode.** Slices 1+2 stopped at the `CONNECT` line (their stub proxy 503s, so the render deliberately failed), leaving end-to-end TLS, the response body, Playwright's post-JS extraction and the reply text untested. **A hermetic origin is impossible here:** browser-driver's sidecar runs no-MITM transparent-tunnel (`disable_mitm_for` names it), so Chromium does its own end-to-end TLS and must trust the ORIGIN's certificate — a self-signed loopback origin would need a CA in Chromium's NSS store (the deferred MITM-of-browser work), and weakening `DEFAULT_LAUNCH_ARGS` with `--ignore-certificate-errors-*` was rejected outright (production must not be loosened to make a test pass). This is exactly why the host-mode `forced_render_of_loopback_page_through_sidecar` navigates `https://` at a plain-HTTP server and settles for the decision row. **So slice 3 uses a real public HTTPS origin**, and both tiers drive the PRODUCTION `SingleUseLifecycle::with_force_routing(...).acquire(...)` manager rather than a hand-wired `NetWorkerSpawn` — strictly stronger, because the manager derives `disable_mitm` by calling `disable_mitm_for` **itself**, so the property is observed rather than restated. **Both slice-2 budgets are now MEASURED, not reasoned, and both confirmed:** `example.org` renders in **1.61 s = 1.8%** of the 90 s `wall_clock_ms`; a heavy Wikipedia article (38 408 B of text) peaks at **628 MiB VMM RSS = 30.7%** of the 2048 MiB `mem_mb`, closing slice-1 spec §10.4's worry that the `/tmp` tmpfs holding Chromium's shm could OOM the guest. Neither budget changed (trimming the wall clock was considered and rejected: it must cover a cold page cache, a slower origin and a heavier page). Memory is measured by sampling the Firecracker VMM's host RSS — guest RAM is faulted in lazily, so it tracks what the guest actually touched, tmpfs included — and **a missing sample fails loudly rather than skipping the assertion**. Two corrections found by running it: the egress action is `egress.allowed` not `allowed`, and a `tls_intercepted == false` assertion was added. **Negative-case verified:** tightening the memory threshold 85%→20% makes the test fail loudly with the intended message and the same reproducible reading. **No production code changed.** Debt lodged and since RESOLVED: [#473](https://github.com/hherb/kastellan/issues/473) (`real_web_fetch_through_sidecar` was a hollow `#[ignore]`d scaffold that passed trivially) — implemented 2026-07-20 on the same real-public-origin pattern, but with `tls_intercepted: **true**` as its load-bearing assertion, since web-fetch is MITM'd where browser-driver is transparently tunnelled. The hermetic origin the issue suggested does NOT work: under MITM the *proxy* validates the origin and trusts `webpki_roots` only. **DGX: full workspace 2590/0/50 + `clippy --workspace --all-targets -D warnings` clean, both exit 0, 4 `[SKIP]` (the known gliner-relex opt-ins only) — exactly +2 ignored over the 2590/0/48 slice-2 baseline.** Spec `docs/superpowers/specs/2026-07-19-browser-driver-microvm-live-render-design.md`.
  - **Slice 2 — the VM entry, 2026-07-19.** `KASTELLAN_BROWSER_DRIVER_USE_MICROVM=1` (atop the `ENABLE` gate) registers browser-driver as a Firecracker VM worker. `browser_driver_firecracker_entry` mirrors `web_fetch_firecracker_entry` with five load-bearing differences: empty `fs_read` (a VM shares no host paths in; no NIC ⇒ no local DNS), **no `lockdown_shim`/`LANDLOCK_RW`** (host-mode mechanisms — the shim seccomp+Landlocks a bwrap-spawned Python venv worker (#281) and is not staged in the rootfs, so requiring it would be a boot failure; in VM mode the boundary IS the VM), `mem_mb: 2048` vs 1024 (Firecracker enforces it as total guest RAM and it must cover Chromium **plus** the `/tmp` tmpfs `--disable-dev-shm-usage` redirects shm into), the in-rootfs `PLAYWRIGHT_BROWSERS_PATH`, and `wall_clock_ms: 90_000` (cold VM boot + Node driver + Chromium cold start). The `resolve()` branch short-circuits the **entire** host-side resolution rather than just binary discovery as in web-fetch — host mode resolves a venv/interpreter/lib-dirs then fail-closes `Misconfigured` on the missing lockdown shim, none of which exists on a VM-only deployment — and reads **both** flags via the unified `flag_enabled` dialect so `USE_MICROVM` is never an implicit opt-in. **The main win is test-side:** slice-1 spec §10.4 flagged the guest path as "an instruction, not a mechanism" (three unlinked copies), so the e2e's hand-rolled `browser_driver_vm_policy()` was deleted and **both tiers now drive the production entry through `BrowserDriverManifest::resolve`** — the pin's equality assertion is `MICROVM_WORKER_BIN` itself, carried through `resolve()` → `build_launch_plan` → the hex `kastellan.worker=` token; the live tier uses the entry's own `wall_clock_ms` so a too-tight manifest value cannot be masked. **Verified by the negative case:** repointing the const at `target/debug/…` makes both the hermetic pin and the new resolve test FAIL loudly, then restored green. **DGX: full workspace `cargo test` 2590/0/48 + `clippy --workspace --all-targets -D warnings` clean, 0 `[SKIP]` (+5 over the 2585 baseline = the 5 new tests); live VM tier GREEN on the production entry (CONNECT after 1.09 s).** Mac `cargo check -p kastellan-core --all-targets` clean. Spec/plan `docs/superpowers/{specs,plans}/2026-07-19-browser-driver-microvm-entry*`. **Post-review `/fixall` (2026-07-19):** three review minors fixed on-branch — hermetic `wall_clock_ms`/`cpu_ms` pins (the live tier uses the entry's own wall-clock but is `#[ignore]`d, so the budgets had no always-on guard); shared `ResolveCtx::microvm_image_dir()` deduping the five per-manifest `KASTELLAN_MICROVM_DIR` default copies; `MICROVM_BROWSERS_PATH` ↔ Dockerfile-`ENV` coupling documented (`docker export` drops env metadata — the const is the sole runtime source). Re-verified: DGX full workspace **2590/0/48** + clippy clean, 0 `[SKIP]` (counts unchanged), live VM tier green (CONNECT 1.83 s).
  - **Slice 1 — the rootfs, 2026-07-19.** Unlike every sibling rootfs (assembled from scratch, libraries discovered by `ldd`), Chromium `dlopen`s NSS/fontconfig/SwiftShader which `ldd` cannot see, so `browser-driver.ext4` is staged by a **build-time-only** `Dockerfile.browser-driver` running `playwright install --with-deps` + `docker export`, rejoining the identical `mkfs.ext4 -d` tail — docker never appears at runtime. Installs only `chromium-headless-shell` (the worker launches `headless=True`; saves ~620 MB), `ROOTFS_MIB=1280` measured with a fail-closed fit check, and a build-time `headless_shell --dump-dom` smoke fails the image build on an incomplete dlopen closure. `/dev` questions settled by measurement (`CONFIG_DEVTMPFS_MOUNT=y`; `/dev/shm` absent but `--disable-dev-shm-usage` suffices, proven by a two-arm experiment) ⇒ **option D, no `microvm-init` change**, now guarded by a test pin rather than a comment. Tests: a hermetic launch-plan pin (in-rootfs guest path, no KVM needed) + a live DGX tier asserting the in-VM Chromium's `CONNECT` reaches a stub proxy — positive proof of launch, since a browser that fails to start emits none. **DGX 2585/0/48 + clippy clean.** **Slice 2 shipped this** (see the slice-2 bullet above; its §10.4 worry that `MICROVM_WORKER_BIN` matching the baked path was "nothing mechanically enforces that" is now enforced by the production-entry-driven pin). **Remaining: slice 3** (live render through a real egress sidecar; §10.4 also carries the untested-under-real-render `/dev/shm`-into-`/tmp` memory question). Spec/plan `docs/superpowers/{specs,plans}/2026-07-19-browser-driver-microvm-rootfs*`. **Post-review (`/fixall`, 2026-07-19):** live-tier stub proxy drains connections until the navigation CONNECT (single accept was fragile against future background-connection drift), build-script fit check requires 10% ext4-metadata headroom, spec notes host/VM Playwright pin-skew; kernel-sha256 pinning for ALL `build-*-rootfs.sh` lodged as [#471](https://github.com/hherb/kastellan/issues/471). Re-verified: DGX core clippy clean, hermetic + live VM tiers green (CONNECT after ~1.1 s).
- [x] **web-search Firecracker micro-VM entry (3rd Mechanism-2 consumer)** — opt-in `KASTELLAN_WEB_SEARCH_USE_MICROVM=1` gives web-search a Linux-only Firecracker VM mode, generalizing the #445/#448 VM-worker + host-sidecar/broker mechanism (after web-fetch + web-research). `web_search_firecracker_entry` (direct: endpoint-derived `Net::Allowlist`, host MITM egress sidecar) + `web_search_firecracker_broker_entry` (VM×search-broker: empty allowlist, `BrokerSpec::search`, host broker over vsock 1026, zero direct worker egress) via a Linux-gated `resolve()` 2×2 (`USE_MICROVM`×`USE_BROKER`), mirroring web-research; host path byte-identical when unset; VM branch also threads the `web.search_batch` cap env. **Proves the #446 broker vsock channel is `BrokerKind`-agnostic** — `BrokerKind::Search` rides port 1026 with **zero new sandbox/microvm/broker code**. New `build-web-search-rootfs.sh` → `web-search.ext4`; DGX-gated `web_search_firecracker_egress_e2e.rs` (direct CONNECT gate + manager-level `SingleUseLifecycle::acquire` broker live test). Bundled `web_search.rs` test-lift → `web_search/tests.rs` (427 prod LOC). Containment fail-closed (a `Net::Allowlist` VM worker won't boot without a `proxy_uds` → never a direct NIC). Subagent-driven (6 tasks), final opus review READY TO MERGE (0 findings). **DGX (real KVM+vsock+PG+live SearxNG): first Linux compile clean, 15/0 unit, both e2es GREEN, full workspace `cargo test` 2520/0/46 + clippy clean, 0 [SKIP] — new baseline 2520/0/46.** — branch `feat/web-search-microvm-entry`, **PR [#451](https://github.com/hherb/kastellan/pull/451) MERGED as `02985295` (2026-07-15).** Spec/plan `docs/superpowers/{specs,plans}/2026-07-15-web-search-firecracker-microvm-entry*`.
- [x] **forced-endpoint resolve-time guard (#452 + #429) + the literal-IP carve-out correction** — resolve-time `Misconfigured` when a force-routed worker (micro-VM always; host iff `KASTELLAN_EGRESS_FORCE_ROUTING`) is configured with a **`localhost`/`*.localhost`-NAME** endpoint (web-search: unless `USE_BROKER=1`; web-research: any broker mode — its broker is embed-only), + a warn-only `tracing::warn!` for a `localhost`-name embed endpoint (#429, hybrid→lexical downgrade). Built on the NEW shared pure crate **`kastellan-net-classify`** (`is_denied_range` + 12 tests moved verbatim out of the bin-only egress-proxy — the leak-scan no-drift precedent; egress-proxy re-pointed) + `core/src/workers/endpoint_guard.rs` (`endpoint_is_localhost_name`, `egress_will_force_route` reusing `force_route::{ENV_ENABLE,env_flag_enabled}` widened `pub(crate)`, `embed_local_warning`). **The final opus review caught a spec-premise defect the whole team's lore carried:** the egress proxy's ORIGINAL slice-#1 **allowlisted-literal carve-out** (`decide()` dials an operator-allowlisted literal IP with the SSRF range check skipped; test-pinned; live-relied-upon by the #448 e2e) means a **literal** loopback endpoint (`http://127.0.0.1:8888`) WORKS force-routed — only `localhost` NAMES are statically dead. Operator decision: keep the carve-out, narrow the guard (literal expectations test-pinned as policy); corrected the false "loopback unreachable" claims in the #428/#451-era docstrings, both specs, and the `dgx-force-routing-deploy-facts` memory note; core's net-classify dep dropped after the narrowing (crate stands, egress-proxy consumes it). Subagent-driven (4 tasks + per-task reviews + opus whole-branch review→rework→re-review). **DGX gate: full workspace `cargo test` 2542/0/46 + clippy clean, 0 [SKIP] — new baseline 2542/0/46**; Mac build+clippy clean (one known parallel-run e2e flake, 6× green in isolation). Deferred: the **web-research × search-broker arc** (spec `docs/superpowers/specs/2026-07-16-web-research-search-broker-arc-design.md` — single-broker XOR slice + multi-broker risk analysis). — **MERGED to `main` as `77e016f6` (PR [#457](https://github.com/hherb/kastellan/pull/457)), 2026-07-16**; post-merge retro-review fix pass (remedy-text corrections, shared guard builder, content-allowlist warn) **MERGED as `675725df` (PR [#460](https://github.com/hherb/kastellan/pull/460)), 2026-07-16; post-merge DGX full-workspace gate 2545/0/46 + clippy clean (new baseline, 2026-07-17)** + follow-up [#459](https://github.com/hherb/kastellan/issues/459) (generalize the guard: matrix homeserver / web-fetch / browser-driver + broker-binary + truthiness residuals). Spec/plan `docs/superpowers/{specs,plans}/2026-07-16-{vm-loopback-endpoint-guard-design,forced-loopback-endpoint-guard}*`.
- [x] **generic forced-localhost guard (#459 slice 1)** — one chokepoint instead of per-manifest copies: pure `screen_net_allowlist` in `endpoint_guard.rs` (under force-routing: ALL `Net::Allowlist` hosts localhost-names ⇒ Refuse like `Misconfigured`; proper subset ⇒ warn + register; empty list / flag off ⇒ untouched; no DNS, literals never flagged — carve-out policy carried forward) wired into `assemble_registry`'s Register arm with cfg-gated `entry_is_vm` — newly covers web-fetch + browser-driver; matrix bring-up guard (fail-soft refusal instead of the silent respawn-loop); web-research `content_localhost_warnings` subsumed. — **MERGED to `main` as `6584b87a` (PR [#462](https://github.com/hherb/kastellan/pull/462)), 2026-07-17** (docs-PR #461 closed unmerged as superseded); **post-merge DGX full-workspace gate 2555/0/46 + clippy clean (new baseline, 2026-07-17)**. Spec/plan `docs/superpowers/{specs,plans}/2026-07-17-generic-forced-localhost-guard*`.
  - [x] **#459 residuals #1+#2** — resolve-time broker-binary discovery (`broker::config::broker_bin_present(kind, ctx)` reuses `discover_broker_bin_with` via the `ResolveCtx` probes; `assemble_registry` unconditionally refuses a broker-declaring worker whose broker binary is absent, instead of the per-dispatch spawn-chokepoint failure) + env-flag truthiness unification (every `USE_*`/`ENABLE`/`USE_CONTAINER` flag now parses via the one `env_flag_enabled` `1|true|yes|on` dialect through `ResolveCtx::flag_enabled`, killing the `…=true`-silently-off footgun). **DGX 2572/0/47 + clippy clean.** An on-branch `/review`→`/fixall` pass then fixed two macOS-only defects the DGX gate can't see: a stale macOS-gated test that red-lit `cargo test` on the Mac, and a 12th missed opt-in site (the matrix channel's `KASTELLAN_MATRIX_USE_MICROVM`) now unified too (Mac lib 1233/0 + clippy clean; DGX-count-neutral). — branch `feat/459-broker-discovery-truthiness`, **PR [#467](https://github.com/hherb/kastellan/pull/467) MERGED to `main` as `f5d9716c`**, 2026-07-18 (also folded the #465 reconciliation, superseding docs-PR #466). (Residual #3 landed separately as PR #469 — see the next entry; **#459 is now CLOSED**.) Spec/plan `docs/superpowers/{specs,plans}/2026-07-17-459-broker-discovery-truthiness-residuals*`.
  - [x] **#459 residual #3 — entry-kind-aware `tool_allowlists` validation (CLOSES #459)** — the port-bearing-row blind spot turned out to be a symptom of a deeper gap: `tool_allowlists` conflated argv0-paths (`shell-exec`) and domains (`web-fetch`/`web-research`/`browser-driver`) under ONE `/`-prefix validator + SQL CHECK, so **domain rows were uninsertable through any path** (the CLI validator AND the `0009` CHECK both refused them; e2es only worked by passing domain vecs straight to the entry builders) — meaning operator content allowlists could not be configured at all, and the `localhost:8888` row the residual described could not even exist. Four layers: (1) `db::tool_allowlists` gains `EntryKind{Argv0,Domain}` + a pure hand-rolled `validate_domain` (no new dep; IPv6 via `std::net::Ipv6Addr`) accepting bare/wildcard domains, IPv4, and **bracketed IPv6 literals** while rejecting embedded ports/schemes/paths/userinfo/whitespace — closing the footgun at the source — dispatched by `validate_entry`; (2) **migration `0021`** gives the row its own **`kind` column** (`'argv0'|'domain'`, `DEFAULT 'argv0'`) with a `CASE`-on-kind CHECK (argv0 ⇒ absolute path; domain ⇒ bare/wildcard host or IPv4 or bracketed IPv6), so `localhost:8888` is refused at the SQL layer too **while preserving `0009`'s argv0 absolute-path guarantee** — a first kind-blind "union-branch" CHECK was caught by the full DGX gate as a real regression (a bare `echo` matches the domain branch, so shell-exec lost that guarantee; structurally unfixable without seeing the row's kind). Chosen over a `CASE WHEN tool IN (…)` CHECK because the tool roster grows: with the kind in the row, SQL never knows a tool name, so adding a tool needs **zero migrations** and only a new *kind* needs schema work; (3) `WorkerManifest::allowlist_kind()` + pure `registry_build::allowlist_kind_for_tool` classify each tool, threaded through `add`/`remove` → `cli_audit` → the CLI (unknown tools fall back to `Argv0`, back-compat; `InvalidDomain` is an exit-2 user error); (4) `endpoint_guard::host_is_malformed` folds a non-bracketed embedded colon **or a `/`** into the `NetScreen` dead set as defense-in-depth (`localhost:8888:443` and the legacy `/example.org:443` ⇒ Refuse/Warn; bracketed `[::1]:443` and wildcard `.example.org:443` never flagged) — screened in **every** routing mode, since a malformed host is undialable regardless, unlike a `localhost` name. **Net functional win: domain allowlists are operator-configurable for the first time.** **A `/review`→`/fixall` pass (2026-07-19) then closed two containment gaps that domain rows becoming insertable had newly exposed:** browser-driver fed rows into `Net::Allowlist` **verbatim**, and a bare-host entry is an **all-port** grant at the proxy (`allowed:host-only-entry`) — so an allowlisted `example.org` also reached `example.org:22`; now mapped through the same `allowlist_to_net_entries` (`:443`, HTTPS-only), with the worker's own check still getting the verbatim rows. And `allowlist_to_net_entries` **flattened wildcards** (`.example.org` → `example.org:443`), so the worker allowed `docs.example.org` while the proxy blocked it; the dot is now preserved (`.example.org:443`, which the proxy suffix-matches), making both enforcement points agree. Same pass: `remove` no longer shape-validates (validating it left pre-split `/example.org` rows **undeletable**), the all-dead `Refuse` detail names only the cause that fired, the audit payload records `kind`, and a `WORKER_MANIFESTS` invariant test pins `allowlist_tool()` ⇒ `allowlist_kind()`. Two live-PG gotchas recorded: `pg_get_constraintdef` renders `LIKE` as `~~` (definition-matching the old constraint on `'argv0 LIKE'` finds nothing), and `sqlx::migrate!` embeds at COMPILE time so a new `.sql` silently does not apply until `kastellan-db` is rebuilt. — **DGX full workspace 2584/0/47 + clippy `-D warnings` clean (new baseline; +8 from the entry-kind work, +4 more from the /fixall pass); Mac compile-check clean.** — branch `feat/459-tool-allowlists-entry-kind-validation`, **PR [#469](https://github.com/hherb/kastellan/pull/469) MERGED to `main` as `58d3e38e`** (issue #459 CLOSED), 2026-07-19. Spec/plan `docs/superpowers/{specs,plans}/2026-07-18-tool-allowlists-entry-kind-validation*`.
- [x] **web-research × search-broker (single-broker XOR)** — gives web-research the search-broker option web-search already has (#440/#451): a force-routed / VM web-research worker reaches a **loopback/name-form** SearxNG through a core-spawned trusted search-broker over a bound UDS with **zero direct search egress**, as a **search XOR embed** choice (one broker socket per worker). Lifted web-search's `SearchProvider` seam (trait + `Direct`/`Brokered` providers + `choose_search_provider` + `search_err_to_rpc`) verbatim into `web-common::search_provider` (feature `search`, now pulling the internal `kastellan-protocol` for the brokered client); re-pointed web-search + web-research (behaviour byte-preserved). `research()` runs over a `&dyn SearchProvider`; `from_env` picks it like web-search (broker UDS wins; direct mode keeps the #428 endpoint-allowlist fail-closed). New manifest entries `web_research_search_broker_entry` (host) + `web_research_firecracker_search_broker_entry` (VM, Linux) carrying `BrokerSpec::search(endpoint)` — SearxNG host dropped from `Net::Allowlist`, no endpoint env, **content hosts (+ a direct embed host) KEPT** (web-research still fetches pages directly, unlike web-search whose only egress is SearxNG); `KASTELLAN_WEB_RESEARCH_USE_SEARCH_BROKER=1` gate; `resolve()` refuses `USE_SEARCH_BROKER`+`USE_EMBED_BROKER` (mutually exclusive), the #452 guard short-circuits under the search-broker (a `localhost`-name SearxNG now registers) and offers the broker as a fourth remedy; `net_entries`/`base_env` widened to `endpoint: Option<&str>` (byte-identical for existing callers). **Reuse-only** — no new sandbox/microvm/broker code (`BrokerKind::Search` rides the #446 vsock-1026 channel). New `core/tests/web_research_search_broker_e2e.rs` (hermetic policy pin + DGX VM live e2e); also test-lifted `core/src/workers/web_research.rs` → `web_research/tests.rs` (Item 9b, 573 prod LOC). **DGX gate DISCHARGED** (real KVM+vsock+PG+live SearxNG): `workers::web_research` lib **22/0** (incl. the Linux VM search-broker resolve test), the new VM search-broker e2e GREEN (real result, `en.wikipedia.org` content via MITM, **zero direct SearxNG egress**) + sibling embed-broker/direct VM e2es still GREEN, full workspace **2563/0/47** + `clippy --workspace --all-targets -D warnings` clean, 0 [SKIP]. The on-branch `/review`→`/fixall` follow-up (empty-endpoint search-broker guard + `port_or_known_default` e2e fix + `stub_broker` dedup) added +1 guard test; **post-merge DGX full-workspace gate 2564/0/47 + clippy clean (new baseline, 2026-07-17)**. Multi-broker follow-up deferred (spec § deferred). — branch `feat/web-research-search-broker`, **PR [#465](https://github.com/hherb/kastellan/pull/465) MERGED to `main` as `0e5ee0a4` (#464)**, 2026-07-17. Spec `docs/superpowers/specs/2026-07-16-web-research-search-broker-arc-design.md`, plan `docs/superpowers/plans/2026-07-17-web-research-search-broker-xor.md`.
  - [x] **Planner `<now>` context injection — date-relative `plan_iteration_cap_exceeded` loop FIXED** (branch `feat/planner-now-context`, **PR [#441](https://github.com/hherb/kastellan/pull/441) MERGED to `main` as `f02d04f`, 2026-07-12**). **Re-diagnosed live on the DGX** (audit tasks 88 FAIL / 89 completed-but-wrong-date / 90 clean): the earlier "planner not fed the tool output" framing was WRONG — the #338 feed-tool-output arc works (the planner summarises prior results) and non-date questions converge. The real cause: the planner has **no current date/time**, so date-relative Qs web-search to guess the date, get inconsistent SearxNG snippet dates, and loop to the 5-cap. Fix = a trusted, `jiff`-backed `<now>` block (weekday+date+minute+tz, local w/ auto-DST via `KASTELLAN_TIMEZONE`; unset→system tz, invalid→UTC) injected FIRST into the planner system prompt (pure `render_now_block`/`resolve_timezone`; `assemble_system_prompt` `now` param appended-last/rendered-first/byte-identical-when-None; `PgSystemPromptBuilder::with_timezone` opt-in per-formulation capture) + `agent_planner.md` "use `<now>`, never web-search for the date". 6-task TDD, Mac-green (core lib 1174/0/1, workspace build+clippy clean). **Live DGX acceptance DISCHARGED 2026-07-12** (rebuilt+swapped the daemon bin; DGX tz Australia/Sydney→`TzSource::System`): re-ran task 88's question → **task 91 completed in 3 plans** (vs 88's 5-plan fail) with plan-1 context "yesterday, which was July 11, 2026" (correct) — date-loop gone. **PR [#441](https://github.com/hherb/kastellan/pull/441) MERGED as `f02d04f`.** Spec/plan `docs/superpowers/{specs,plans}/2026-07-12-planner-now-context-injection*`.
  - [x] **batch summary cap — query-count-scaled planner-summary head for `web.search_batch`** (branch `feat/batch-summary-cap`, off `main`@`e7928e8`; **PR [#444](https://github.com/hherb/kastellan/pull/444) — MERGED as `fe07bded`**). Discharges the #443 flagged follow-up "does answer-synthesis read the FULL batch step-outcome?" — verified it did NOT: a whole batch is ONE `StepOutcome::Ok` capped at the flat 4 KiB `STEP_OK_SUMMARY_MAX` head by `render_step_outcome`, and a default 8-query batch (~21–35 KiB, UNDER the 64 KiB handoff-stash threshold → no `handoff_ref`, no recovery) surfaced only ~2/8 queries to the planner (measured via a faithful `extract_scannable_text` sim). Fix (user chose "scale by query count"): `render_step_outcome` gains the step `method`; pure `ok_summary_cap(method, value)` scales a `web.search_batch` head with its result-element count (`BATCH_PER_QUERY_SUMMARY_BYTES=3 KiB × N`, clamped `[4, STEP_OK_BATCH_SUMMARY_MAX=24] KiB`) so ~5–8 queries reach the planner; every other method keeps the flat 4 KiB; `PLANS_SUMMARY_BUDGET=32 KiB` unchanged (oldest-first `apply_summary_budget` still bounds the total). Batch method consolidated into `core::workers::web_search::WEB_SEARCH_BATCH_METHOD` — and, post-review `/fixall`, pinned cross-crate to the worker's dispatch (canonical string in `web-common`, worker matches on it, new `web_search_batch_method_matches_worker_contract` integration test) so a rename can't route batch calls to `METHOD_NOT_FOUND`. Front-loading caveat documented (global byte cap → verbose ~250 B snippets still truncate later queries ~5–6/8; true per-query sub-render is the deferred option). Pure + additive, no worker/handoff/dispatch/budget change, no dep. Mac-green: summary 13/0 (+5), core lib 1184/0/1, web_search 10/0, worker_manifest 9/0, registry 15/0, contract 2/0 (+1), web-common 63/0, web-search 23/0, `cargo build --workspace` + `cargo clippy --workspace --all-targets -D warnings` clean; no DGX gate (pure Rust). Brainstorm→spec→plan→inline TDD. Spec/plan `docs/superpowers/{specs,plans}/2026-07-13-batch-summary-cap*`.
  - [x] **batch web-search — `web.search_batch{queries:[…]}`** (user-requested perf feature; **PR [#443](https://github.com/hherb/kastellan/pull/443) MERGED as `e7928e8`**, off `main`@`f02d04f`). Planner runs several *independent* searches in ONE dispatch → collapses N ~46s plan iterations into 1. **Sequential loop above the existing `SearchProvider` seam → ZERO changes to web-common / search-broker / core `broker/`** (works identically in direct + broker mode). Per-query result-or-error elements (no silent drops); malformed/over-cap → `INVALID_PARAMS`. Separate method (`web.search` byte-identical). Advertised via a defaulted `WorkerManifest::tool_docs()` (both docs `name="web-search"`, drift guard holds). Operator-configurable cap `KASTELLAN_WEB_SEARCH_MAX_BATCH_QUERIES` (default 8, clamp [1,32], injected only when set). Brainstorm→spec→plan→subagent-driven TDD (4 tasks); per-task + final opus review READY TO MERGE (0 Critical/0 Important). **Post-review `/fixall` (2026-07-13):** wall **30→60 s** + new `batch::BATCH_SOFT_DEADLINE` (40 s = wall−transport; `run_batch` returns per-query "budget reached" errors past it rather than letting the watchdog SIGKILL the whole batch) closes the whole-batch-loss-on-timeout finding; injection-scan-tail finding VERIFIED SAFE (planner only sees the re-screened ≤4 KiB render head, no full-value→LLM path); duplicated env-var literal deduped into `web-common` + a cross-crate contract test. Mac-green: web-search 23/0, core lib 1179/0/1, contract test 1/0, workspace build+clippy clean; no DGX gate (pure Rust). Spec/plan `docs/superpowers/{specs,plans}/2026-07-12-batch-web-search*`.
- [x] **injection-guard per-tool profiles ([#142](https://github.com/hherb/kastellan/issues/142))** — chat-template tokens (`<|im_start|>`/`<|system|>`) no longer false-positive on fetched documentation. `GuardProfile { Strict (default, fail-closed) | Relaxed }` chosen per worker via `GuardProfile::for_tool` at the dispatch chokepoint; only `web-fetch`/`web-search` relax. Strict is byte-for-byte the Slice-1 algorithm; Relaxed collapses all chat-template matches to a single capped 0.40 sub-threshold contribution (handles the two-token tutorial case) so a lone token Allows but corroboration still Blocks. Committed benign/attack fixtures + full `extract_scannable_text`→`screen_with_profile` pipeline pin; `#[ignore]` live HuggingFace spot-check. — branch `feat/injection-guard-per-tool-profiles`, 2026-06-09. Deferred: Review tier; manifest-declared profiles; the catalogue-completeness evasion (Slice-1 limitation, documented).
- [x] `browser-driver` worker (Playwright headless, dedicated profile, scratch FS) — ROADMAP:147 — **DONE (egress-proxy-routed, 2026-06-14)**
  - [x] **Spike + slice #1 scaffold — 2026-06-12** (branch `feat/browser-driver-worker`). **Spike GREEN both platforms**: headless Chromium (`chromium-headless-shell`) renders inside the real jail on macOS Seatbelt **and** the DGX (aarch64, bwrap) with `--no-sandbox --disable-dev-shm-usage`; no `/dev/shm`, no `--single-process`, no Firefox fallback. Findings (design spec §3.1): Seatbelt needs `ipc-posix-shm*`+`iokit-open/get-properties`+`mach-lookup/register` (a deliberate mach-lookup re-grant to narrow later); DGX seccomp needs 9 additions over `net_client` (`fallocate ftruncate getresgid getresuid inotify_add_watch inotify_init1 memfd_create pidfd_open restart_syscall`) + an `io_uring` `Errno(EPERM)` carve-out. **Slice #1 scaffold (spike-independent, TDD):** `workers/browser-driver` Playwright-Python package (`browser.render` stdio JSON-RPC: post-JS readable text + final HTML; readability-lxml; https-only, timeout-clamp, wait_until validation; 14 py tests) + `core/src/workers/browser_driver.rs` host manifest (pure `resolve_env`+skip reasons, `WorkerNetClient`+legacy direct-net `Net::Allowlist`, `KASTELLAN_BROWSER_DRIVER_ENABLE` opt-in, registered) + `GuardProfile::Relaxed` join. Workspace 1562/0/8, clippy clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-12-browser-driver-worker*`.
  - [x] **Phase 2 — RENDERS on both platforms, 2026-06-14** (branch `feat/browser-driver-phase2`). Real `PlaywrightRenderer` in `render.py` (Chromium `--no-sandbox --disable-dev-shm-usage`, per-nav+per-subresource allowlist interception, explicit `start()/stop()`) + `Profile::BrowserClient` seccomp (9 adds + **io_uring→EPERM via a 2nd filter**, install-order-sensitive) + `Profile::WorkerBrowserClient` Seatbelt cluster (shm/iokit/mach + sysctl-write/system-socket, tool-gated) + `install.sh` (self-contained non-editable system-venv, chromium-in-venv) + `core/tests/browser_driver_e2e.rs` (`#[ignore]` real render + off-allowlist deny — **2/2 GREEN macOS Seatbelt + DGX bwrap** via the real dispatch chokepoint). #263 **collision resolved for dev** (force-route exemption + hard production lockout via `KASTELLAN_BROWSER_DRIVER_INSECURE_DIRECT_NET`); manifest `TasksMax=512` + `HOME=/tmp` + browsers-in-venv + interpreter-root binds (all DGX-found). Cross-cutting: `tool_host::spawn_worker` drains worker stderr (pipe-buffer deadlock fix). **Dev-only** until egress slice #2 ([#280](https://github.com/hherb/kastellan/issues/280)); pure-Python workers lack Linux seccomp ([#281](https://github.com/hherb/kastellan/issues/281)).
  - [x] **Slice #2 (production egress) — DONE 2026-06-14** (branch `feat/browser-driver-egress-slice2`; closes [#280](https://github.com/hherb/kastellan/issues/280) + [#263](https://github.com/hherb/kastellan/issues/263)). Browser is now egress-proxy-routed in the default force-routed deployment via **transparent tunnel (NOT MITM)** — preserves Chromium-grade origin cert validation + smaller blast radius; MITM-of-browser deferred (needs NSS cert-trust, once leak-scanning #3b is wired). In-jail pure-Python **`ProxyShim`** (loopback-TCP↔UDS byte-pipe) + Chromium `--proxy-server`; egress-proxy **no-MITM mode** (`KASTELLAN_EGRESS_PROXY_DISABLE_MITM`, `disable_mitm` threaded through `NetWorkerSpawn`); the force-route exemption + `DirectInsecureDevExempt`/`RefuseProductionUnconfined`/`ForceRouteUnconfined` + the `KASTELLAN_BROWSER_DRIVER_INSECURE_DIRECT_NET` escape hatch all removed; Seatbelt loopback-TCP for `WorkerBrowserClient`+proxy_uds (bwrap brings `lo` up in the netns). **Acceptance 2/2 GREEN on the DGX** (real bwrap + Chromium + egress sidecar): forced render reaches an `egress.allowed` sidecar decision (full Chromium→shim→loopback-in-netns→UDS→sidecar→origin path) + off-allowlist blocked at the sidecar (divergent-allowlist isolation). macOS render blocked by pre-existing [#284](https://github.com/hherb/kastellan/issues/284) (Chromium 148 SIGABRT under Seatbelt). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-browser-driver-egress-slice2*`.
  - [x] **#284 RESOLVED — 2026-06-15** (branch `fix/284-interpreter-lib-dep-autobind`). The "Chromium-148 Seatbelt SIGABRT" was a **misdiagnosis**: the browser-driver venv's external **pyenv** interpreter links a **Homebrew** dylib (`/opt/homebrew/opt/gettext/lib/libintl.8.dylib`) outside its bound prefix → dyld `open()` blocked under Seatbelt → **python SIGABRTs before Chromium launches**. New pure module `core/src/workers/interpreter_deps.rs` (transitive depth-first walk over the interpreter's deps via `otool -L`/`ldd`; binds the canonical parent dir of every out-of-prefix, non-system lib **read-only**; fail-safe) wired into the browser-driver manifest + e2e resolver. **`real_render_of_loopback_page` now renders under Seatbelt on macOS 26.5.1 / chromium-1223 with NO manual `EXTRA_FS_READ`** — no Chromium regression. No network/write/seccomp change; no-op on the DGX (system-python deps are all system libs). Follow-ups: the **forced** egress-sidecar macOS path still emits no decisions ([#287](https://github.com/hherb/kastellan/issues/287), distinct, DGX forced is green); adopt the helper in `python-exec`/`gliner-relex`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-interpreter-lib-dep-autobind*`.
  - [x] **`interpreter_deps` adopted in `python-exec` + `gliner-relex`** — PR [#289](https://github.com/hherb/kastellan/pull/289), `2d85ea1`, 2026-06-15. The #284 follow-up: both Python workers now bind their interpreter's out-of-prefix lib dirs via the shared `interpreter_deps.rs` helpers. Reads-only, fail-safe, no-op on Linux.
  - [x] **#287 RESOLVED — 2026-06-15** (branch `fix/287-browser-driver-stale-venv`). The macOS forced egress-sidecar "no decisions" was a **stale browser-driver venv**, NOT a code bug: `install.sh` did a non-editable `pip install "$WORKER_DIR"` of a static-`0.0.1` package, so a re-run after slice #2 reported "already satisfied" and kept the pre-slice-#2 worker (no `shim.py`, no `--proxy-server`). On macOS (no netns) that old Chromium connected directly to the host loopback → `ERR_SSL_PROTOCOL_ERROR` + zero sidecar decisions. Re-staging current source ⇒ **all 4 `browser_driver_e2e --ignored` pass on macOS, both forced tests included.** Fix: `install.sh` now `--force-reinstall`s the local package so a re-run always recopies current source. macOS-only; no Rust changed.
  - [x] **#281 (browser-driver portion) RESOLVED — 2026-06-15** (PR [#292](https://github.com/hherb/kastellan/pull/292), `80de534`). Pure-Python venv workers are spawned by bwrap directly and never ran the Rust prelude, so on Linux the built-and-tested `browser_client` seccomp filter was never applied. New **`kastellan-worker-lockdown-exec`** bin in the `prelude` crate (`rlimit` → `lock_down()` → `execve(target)`; the venv script inherits the seccomp filter under `NO_NEW_PRIVS`). browser-driver manifest discovers it (fail-closed on Linux ⇒ `Misconfigured`; binds it into `fs_read`) + sets `ToolEntry.lockdown_shim`; pure `tool_host::build_program_and_args` wraps the spawn at both cold-spawn sites (default `None` = byte-identical). **seccomp-only** (Landlock deferred behind the new additive `KASTELLAN_LANDLOCK_PROFILE=none`; bwrap mounts remain the FS layer). `capget`+`capset` added to `browser_client` (DGX-confirmed load-bearing: Playwright-Node startup + Chromium-zygote page-spawn). **DGX acceptance:** `browser_driver_e2e --ignored` 4/4 under the real filter + `lockdown_exec_smoke` (seccomp crosses `execve`); workspace `1829/0`. Mac `1837/0/13` + clippy clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-15-python-worker-linux-seccomp*`.
  - [x] **#281 (gliner-relex portion) RESOLVED — 2026-06-16** (PR [#293](https://github.com/hherb/kastellan/pull/293), branch `feat/281-gliner-relex-seccomp`, HEAD `0b38f4f`). The heavy torch/transformers worker also runs host-mode (pure-Python venv bwrap spawns directly), so it too was unfiltered on Linux. Routed its host-mode spawn through the same `kastellan-worker-lockdown-exec` shim (manifest fail-closed `discover_binary`; binds shim into `fs_read`; seccomp-only via `LANDLOCK_PROFILE=none`). New sandbox **`Profile::WorkerMlClient`** (byte-identical to `WorkerStrict` off Linux) + prelude **`ml_client`** seccomp profile = `net_client` (torch opens sockets even offline; `Net::Deny` still blocks the route) **+ `ML_CLIENT_ADDITIONS` {mbind, get_mempolicy, mlock, munlock, mknodat}** (DGX-enumerated). **Enumeration method:** `RET_LOG`/`dmesg` is printk-rate-limited on the DGX (`dmesg_restrict=1`, auditd off), so kill-mode is the deterministic enumerator — each run SIGSYS-dies on the first missing syscall, read via `journalctl -k | grep type=1326` (unprivileged, `adm` group). **DGX acceptance:** real `multi-v1.0` model load + `extract` passes under the **kill-mode** `ml_client` filter across all 3 real-model e2e suites; workspace `1839/0/15` + clippy clean. Mac `1841/0/13`. Landlock for gliner-relex landed 2026-06-16 (below). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-gliner-relex-linux-seccomp*`.
  - [x] **#281 (browser-driver Landlock) RESOLVED — 2026-06-16** (branch `feat/281-browser-driver-landlock`). Flipped Landlock **on** for browser-driver: `browser_driver_entry` no longer emits `KASTELLAN_LANDLOCK_PROFILE=none`, so the lockdown-exec shim installs the ruleset (RO set derived from `fs_read` — venv, interpreter libs, `/etc` resolver files, the shim, and the per-instance CA when force-routed; RW = the `/tmp` scratch, now load-bearing for Chromium's `--user-data-dir`). No `fs_read` iteration was needed — the render passed first try, because the Landlock RO set is `DEFAULT_RO_EXEC_ROOTS ∪ fs_read`, the same set bwrap binds (Landlock can only further-restrict). The **proxy UDS connect is not gated by Landlock `AccessFs`** (path-based AF_UNIX connect is unmediated; only the v6 abstract-UDS *scope* applies) — matching force-routed web-fetch, which already runs Landlock-on without the UDS in its RW set. **DGX acceptance:** all 4 `browser_driver_e2e --ignored` pass with Landlock on (direct render + force-routed-through-sidecar + both allowlist fail-closed), and a direct shim probe confirms `landlock: FullyEnforced` — out-of-RO-set reads (`/etc/shadow`, `/etc/hostname`) **denied**, RO set + `/tmp` RW readable/writable (real containment, not a silent `Disabled`). Workspace `1839/0/15` + clippy `-D warnings` clean; Mac `1841/0/13`. **#281 now fully closed for browser-driver (seccomp + Landlock); gliner-relex Landlock landed below.**
  - [x] **#281 (gliner-relex Landlock) RESOLVED — 2026-06-16 — #281 FULLY CLOSED** (branch `feat/281-gliner-relex-landlock`). Flipped Landlock **on** for the gliner-relex host-mode worker, the final piece of #281. `host_mode_entry` no longer emits `KASTELLAN_LANDLOCK_PROFILE=none`, so the lockdown-exec shim installs the ruleset alongside the already-DGX-validated `ml_client` seccomp filter. RO set derived from `fs_read` (weights, venv, src, interpreter root + lib dirs, the shim) by `derive_lockdown_env`; RW = `KASTELLAN_LANDLOCK_RW=["/tmp"]` (torch writes its inductor cache to `/tmp/torchinductor` on bwrap's per-spawn `/tmp` tmpfs) with `fs_write` empty (a `/tmp` entry there would bind the host `/tmp` over the tmpfs). **No `fs_read` iteration needed** — the 3 real-model e2e suites passed first try (RO set = `DEFAULT_RO_EXEC_ROOTS ∪ fs_read` = what bwrap binds). Mirrors browser-driver (PR #294) verbatim; FS-only (seccomp already validated). **DGX acceptance:** the 3 host-mode `gliner_relex_e2e` real-model suites (`happy_path_extract` / `warm_reuse` / `invalid_input`) load `multi-v1.0` + run `extract` under Landlock (~29.5s, real inference — not skipped), and a direct shim probe confirms `landlock: FullyEnforced` — `/tmp` RW write+readback OK, RO grant OK, a world-readable out-of-RO file (`/etc/hostname`) **denied** (real containment, not a silent `Disabled`). Workspace `1839/0/15` (test renamed, not added) + clippy `-D warnings` clean; Mac 52 gliner unit + clippy green. Neither browser-driver nor gliner-relex sets `KASTELLAN_LANDLOCK_PROFILE=none` any more.
- [ ] **MCP onboarding: discover → boot-spawn → validate → persist** — when kastellan grows third-party MCP-server support (any of the registries openhuman taps: Smithery, `modelcontextprotocol/registry`), naive "spawn it with the operator's intended policy" is a foothold attack: a malicious MCP server gets its production sandbox on first run. Adopt openhuman's pattern (`docs/MCP_SETUP_AGENT.md` — "boot-spawn for this one server... spawns the candidate subprocess in a scratch workspace"): every newly-discovered MCP server is first booted under a **maximally restrictive** `SandboxPolicy` (`Net::Deny`, `fs_read=[]`, `fs_write=[scratch]`, `Profile::Strict`, `cpu_ms=5000`, `mem_mb=128`), driven through `initialize` + `tools/list` over our existing `kastellan-protocol` stdio JSON-RPC, recording the declared tool surface to `db::mcp_servers` (new migration) only on success. Only then does the operator promote the server to its intended runtime policy via a separate explicit step that lands one `actor='cli' action='mcp.promoted'` audit row carrying the SHA-256 of the policy that was approved. Production runs refuse to spawn an MCP server whose policy hash has changed since promotion (mirror of the `tool_allowlists` SHA-256 drift detection from PR #51). Cross-platform "free" via `SandboxBackend` — same flow on bwrap, Seatbelt, and the new `MacosContainer` backend (Issue #55).

## Phase 4 — python-exec & agent-authored skills

- [ ] `python-exec` worker: scratch FS only, no net, hard CPU/mem/wallclock; curated stdlib bind
  - [x] **python-exec shipped (host bwrap/Seatbelt) — 2026-06-12 to 06-26** (per-slice detail in git + `docs/superpowers/` specs): `workers/python-exec` (`python.exec`, `<python> -I -S -B -`; a Python exception returns as `exit_code`+traceback, not an RPC error; 256 KiB caps), **opt-in `KASTELLAN_PYTHON_EXEC_ENABLE=1`**, strictest policy of any worker (`Net::Deny` / `Profile::WorkerStrict` / `fs_write=[]` per-spawn `/tmp` tmpfs, cpu 10 s / mem 512 MiB / wall 30 s, `SingleUse`); Mac (Seatbelt) + DGX (bwrap) `python_exec_e2e` green. Shipped follow-ups: per-spawn writable scratch (`ToolEntry.ephemeral_scratch` + `tool_host/scratch.rs`, Seatbelt subpath grant, Linux byte-identical; browser-driver adopted it -> #283 CLOSED, PR #308); >64 KiB param file-channel (`KASTELLAN_PYTHON_PARAMS_FILE`, operator ceiling `_FILE_MAX` default 1 MiB); macOS micro-VM mode (`KASTELLAN_PYTHON_EXEC_USE_CONTAINER=1` -> `MacosContainer`, enforces `mem_mb:512` Seatbelt can't; `Containerfile`+build-image.sh GLIBC base-pin; image-existence parity [#356](https://github.com/hherb/kastellan/issues/356)); warm/idle container (`KASTELLAN_PYTHON_EXEC_IDLE_SECONDS>0` reuses `IdleTimeout`, per-call scratch wipe, container-mode only).
  - [x] **Linux Firecracker micro-VM backend (`SandboxBackendKind::FirecrackerVm`) — SLICE 1 (2026-06-27, PR #364).** Generic
    separate-kernel backend any worker can opt into; first consumer `python-exec` (`KASTELLAN_PYTHON_EXEC_USE_MICROVM=1`, Net::Deny,
    KVM-enforced `mem_mb`). `FirecrackerVm` enum/registry + pure `build_launch_plan`/config + fail-closed `probe`; pure-std
    `kastellan-microvm-run` launcher-is-the-Child (host-initiated hybrid vsock: `CONNECT <port>` on the base UDS; per-chunk-flushed
    stdio↔vsock bridge; per-spawn unique UDS+CID); guest PID1 `kastellan-microvm-init` (unchanged `serve_stdio` worker, `/usr/bin/python3`,
    fd hygiene); `build-rootfs.sh` (lib closure + python stdlib) / `install-firecracker.sh` / privileged `install-firecracker-vsock.sh`.
    **DGX live e2e 4/4** (42 round-trip + KVM mem-cap + Net::Deny + >64 KiB file channel) in 0.21s, 0 orphaned VMs; both-platform
    `--all-targets` clippy green. Runbook `docs/devel/runbooks/2026-06-26-linux-microvm-setup.md`.
    **Slice-1 follow-ups (2026-06-27):** [x] #363 split `python_exec.rs` (594→283 + `entries.rs`, PR #365);
    [x] #360 **forward `policy.env` into the guest** via a hex `kastellan.env=` kernel-cmdline token (fail-closed cmdline cap;
    fail-safe baked fallback in the guest; fixed a latent PYTHON `/usr/local/bin`→`/usr/bin` mismatch) — DGX e2e **5/5** incl. the
    differential proving the `params_file_max` override is now live in-VM (PR #366).
    [x] #362 **per-spawn run-dir cleanup** — launcher self-cleans its run-dir on graceful exit (`--run-dir` + RAII
    teardown), KEEPS it on a panic so `fc.log` survives for post-mortem (#367 review); a launcher-pid-keyed orphan sweep
    (`<run_dir>/launcher.pid` + `/proc` liveness) at the top of
    `spawn_under_policy` backstops both the SIGKILL and panic-kept cases. No `SandboxBackend` trait change. DGX e2e **6/6**, 0 leftover run-dirs.
    Spec/plan: `docs/superpowers/{specs,plans}/2026-06-27-firecracker-rundir-cleanup*`.
    [x] **SLICE 2 — warm/idle reuse + re-armable watchdog (2026-06-27, MERGED `555f611`, PR #369).** The entry/resolver/in-guest-wipe wiring
    already existed from slice 1, so slice 2 = fix a latent watchdog bug + DGX-verify. The wall-clock watchdog was one-shot
    (armed at spawn) → a warm `IdleTimeout` worker was SIGKILLed `wall_clock_ms` after boot regardless of the idle window
    (latent on the macOS container warm path too). Replaced with a **re-armable `Watchdog`** owned by `SupervisedWorker`, armed
    for the span of each `SupervisedWorker::call` (RAII disarm on return) → a warm worker is never under a kill timer while idle;
    a single overrunning call is still killed; 2026-05-08 blackout guards intact. New DGX e2e `python_exec_firecracker_warm_idle_e2e.rs`
    **4/4 real** (warm reuse boots VM once; `/tmp` wiped between warm calls; idle teardown; warm survives gap past `wall_clock_ms`);
    slice-1 e2e **6/6** no-regression; clippy `--all-targets` clean both platforms; opus final review: concurrency primitive correct.
    Spec/plan: `docs/superpowers/{specs,plans}/2026-06-27-firecracker-microvm-slice2-warm-idle*`.
    [x] **SLICE 3 — host-dir sharing (2026-06-28, MERGED `b12f0dc`, PR #371).** Per-spawn **read-only ext4** drive built from `policy.fs_read`
    (`mkfs.ext4 -d`, non-root) exposed in-guest at original absolute paths via in-init bind-mounts (tmpfs-anchored so mkdir works
    on the RO root; `fs_read`/`fs_write` top-level must be an allowlisted share anchor `{opt,data,srv,mnt,work,tmp}` — any other,
    incl. system dirs `/usr|/etc` AND unanchored dirs like `/home|/var`, fails closed up front so a share never silently mis-mounts
    [PR #371 post-review fix]) + a disjoint **writable disk-backed
    scratch** drive from `policy.fs_write` (blank ext4, ephemeral, NO host write-back; default 64 MiB, `KASTELLAN_MICROVM_SCRATCH_MIB`).
    Mount plan rides a hex `kastellan.mounts=` cmdline token (sandbox `mounts.rs` encoder ↔ `microvm-init` decoder, no shared dep,
    roundtrip-fixture-pinned). New `sandbox/src/linux_firecracker/{mounts.rs,images.rs}`; `probe` gains `mkfs.ext4`. No overlayfs/no
    guest-kernel change. **DGX synthetic e2e 1/1 real** (host sentinel read at its abs path via RO bind + `/work` anchor scratch write);
    sandbox lib 80/0, microvm-init 10/0, slice-1 e2e 6/0 + slice-2 4/0 no-regression, 0 orphan run-dirs, clippy clean. opus final
    review: ready-to-merge, RO held at ext4 superblock. Follow-up [#370](https://github.com/hherb/kastellan/issues/370) (copy_tree
    dir-symlink policy). Spec/plan: `docs/superpowers/{specs/2026-06-27-firecracker-microvm-slice3-host-dir-sharing-design.md,plans/2026-06-28-firecracker-microvm-slice3-host-dir-sharing.md}`.
    Spec/plan: `docs/superpowers/{specs/2026-06-26-linux-firecracker-microvm-design.md,plans/2026-06-26-linux-firecracker-microvm-slice1.md}`,
    `specs/2026-06-27-firecracker-guest-env-forwarding-design.md`.
    [x] **SLICE 4a — egress vsock reverse-channel transport (2026-06-28, MERGED `bed1326`, PR [#373](https://github.com/hherb/kastellan/pull/373)).**
    A force-routed `Net::Allowlist` worker reaches the host egress proxy from inside a VM (NO virtio-net device) over a **second, guest-initiated
    vsock channel** — unchanged worker code, stronger isolation than the bwrap private-netns path. Transport only. Pure `build_launch_plan` detects
    force-routing (`Net::Allowlist` + `proxy_uds`) → `egress_proxy_vsock_port=Some(1025)` + `net_enabled=false` + overrides the guest
    `KASTELLAN_EGRESS_PROXY_UDS` env to `/run/kastellan-egress.sock` + ` kastellan.egress=1` token; bare `Net::Allowlist` w/o `proxy_uds` fail-closed
    rejected. `launcher_argv` emits `--egress-uds`/`--egress-vsock-port`; launcher (`egress_relay.rs`) pre-binds `<base>_1025` + relays to the host
    proxy UDS; guest init binds the in-guest UDS pre-exec + forks a relay to `AF_VSOCK(2,1025)` + a test-gated `PING/PONG` self-test; `/run` tmpfs
    mountpoint. Constants triple duplicated both crates (no shared dep). `SandboxPolicy`+bwrap byte-unchanged. **DGX e2e 1/1 real** (guest→host PING
    delivered over the reverse channel); sandbox lib 89/0, microvm-init 11/0, microvm-run 10/0, slice-1/2/3 e2e no-regression, 0 orphan run-dirs,
    clippy clean. opus final review: READY TO MERGE; 2 Important PID1-robustness findings fixed in-branch (`695a1d5`). Spec/plan:
    `docs/superpowers/{specs/2026-06-28-firecracker-microvm-slice4a-egress-transport-design.md,plans/2026-06-28-firecracker-microvm-slice4a-egress-transport.md}`.
    [x] **SLICE 4b — first real net worker in a VM: web-fetch (2026-06-28, branch `feat/firecracker-microvm-slice4b-web-fetch`, PR [#375](https://github.com/hherb/kastellan/pull/375)).**
    web-fetch runs in a VM reaching the egress proxy over the 4a channel, opt-in `KASTELLAN_WEB_FETCH_USE_MICROVM=1`, unchanged worker code.
    Per-worker rootfs filename (`KASTELLAN_MICROVM_ROOTFS`, default python-exec.ext4; web-fetch=`web-fetch.ext4`); file-aware guest RO bind delivers the
    per-instance proxy `ca.pem` in-guest (worker `make_get` fails closed without it); `kastellan.worker=<hex>` cmdline token forwards the exec path
    (root-cause fix for the baked python path — `build_launch_plan` had ignored `_program`); backend-only env keys filtered from the guest cmdline.
    `build-web-fetch-rootfs.sh` (no python, no system CA bundle — MITM-only). **DGX e2e 2/2** (hermetic CONNECT-stub gate proving boot+force-routing+vsock+CA
    delivery, + `#[ignore]` real-net origin scaffold); no-regression slice-1 6/0 + slice-3 1/0 + slice-4a 1/0, 0 orphan run-dirs; sandbox 94/0, microvm-init
    16/16, clippy clean. opus final review: READY TO MERGE; python-exec/slices 1–3 byte-identical (now boot via the forwarded token). MERGED to `main` as `a1db0a7`.
    Spec/plan: `docs/superpowers/{specs,plans}/2026-06-28-firecracker-microvm-slice4b-web-fetch*`.
    [x] **Follow-up [#374](https://github.com/hherb/kastellan/issues/374) — forward worker `args` into the guest (2026-06-29, branch `feat/374-microvm-forward-worker-args`).**
    Slice 4b forwarded only `program`; a *shimmed* VM worker (`lockdown_shim:Some`, none today) would exec the lockdown-exec shim with no target. `build_launch_plan` now
    forwards the argv via a sibling `kastellan.worker.args=<hex>,…` token (per-arg hex, `,`-joined; empty argv ⇒ no token ⇒ cmdline byte-identical to the pre-#374 baseline);
    guest `exec_worker` builds the full `[program, args…, NULL]` execv argv; all-or-nothing decode (bad component ⇒ run bare, never a shifted argv; interior-NUL never aborts PID1).
    Cross-crate roundtrip fixture pinned in both crates. **DGX: sandbox 98/0 (+4) + microvm-init 19/0 (+3) + clippy clean + slice-1 FC e2e 6/6 no-regression** (rebuilt rootfs bakes
    the refactored PID1). Mac: microvm-init 19/0 native + aarch64 cross-clippy clean. Pure-Rust, no migration.
    [x] **SLICE 5a — unprivileged VMM confinement (2026-06-29, branch `feat/microvm-slice5a-vmm-confinement`).** Firecracker's `jailer` needs root,
    which violates the non-root/never-self-escalate daemon invariant — so the VMM (launcher + `firecracker`) is confined the unprivileged way: `spawn_under_policy`
    wraps `kastellan-microvm-run` in the existing `systemd-run --user --scope` cgroup + a `bwrap` jail, and `firecracker` inherits the namespaces as the launcher's
    child. Closes the missing host-side cgroup gap for FC workers. New `sandbox/src/linux_firecracker/confine.rs`: `VmmConfinement{None,BwrapCgroup}` seam (true
    `Jailer` is a documented comment for a future privileged tier), pure `confinement_from_env` (default-ON, only a clear `KASTELLAN_MICROVM_CONFINE_VMM=0` opts out),
    `find_executable` ($PATH resolver to bind the two binaries into the jail), `build_vmm_jail_argv` (binds only `/dev/kvm`+`/dev/vhost-vsock`, kernel/rootfs/binaries RO,
    run-dir RW, egress UDS only when force-routed), `build_confined_spawn_argv`. Launcher gains optional `--firecracker-bin` (jail has no $PATH). **Fail-closed**: confinement
    on + binary/probe missing ⇒ refuse to spawn (no silent bare fallback); probe gated. **DGX merge gate GREEN**: confined-boot e2e + full slice 1-4b suite pass under
    default-ON confinement (python_exec 6/6 incl. mem-cap OOM + net-deny, warm-idle 4/4, hostdir 1/1, egress-4a 1/1, web-fetch-4b 2/2) ⇒ `/dev/kvm`+vsock survive the bwrap
    userns. sandbox lib 117/0, microvm-run 12/12, workspace clippy clean. opus final review: READY TO MERGE. **`/review` fix:** confined-mode run-dir leak (the run-dir is a bwrap bind-mount, so the launcher can't rmdir it on exit → empty pidfile-less husk the sweep never reclaimed); launcher now drops a `teardown.done` marker the sweep reclaims unconditionally (+3 sandbox / +1 launcher tests, e2e Phase 3, DGX re-green). Deferred [#378](https://github.com/hherb/kastellan/issues/378): MemoryMax==mem_mb VMM headroom for a future larger-RAM worker. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-29-firecracker-microvm-slice5a-vmm-confinement*`.
    [x] **SLICE 5b-1 + 5b-2 — long-lived persistent-VM worker lifecycle + persistent RW store (2026-06-30, branch `feat/microvm-slice5b1-persistent-vm-lifecycle`, PR [#379](https://github.com/hherb/kastellan/pull/379)).** A long-lived `Net::Deny` worker runs in a Firecracker VM (boot once, many calls, respawn-supervised) with a persistent ext4 RW store that survives a VM respawn. Additive `SandboxPolicy.persistent_store`; FC mkfs-once ext4 image + launcher `flock`; bwrap/Seatbelt persistent fs_write; reusable backend-agnostic `PersistentWorker` supervisor + `ClientTransport` (matrix untouched); new `kv-demo` worker + rootfs. DGX real-KVM green under default-ON confinement (`cargo test --workspace` 2230/0/33, clippy clean). The DGX gate caught a slice-5a gap — `build_vmm_jail_argv` now binds the persistent-store image RW into the VMM jail (stable path outside run_dir). opus final review READY TO MERGE. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-29-firecracker-microvm-slice5b1-*`.
    [x] **SLICE 5c — network egress in a VM (long-lived, transparent-tunnel) (2026-07-01/02, branch `feat/microvm-slice5c-net-egress-in-vm`, PR [#385](https://github.com/hherb/kastellan/pull/385) MERGED `3be3669`).** A long-lived `Net::Allowlist` worker runs in a Firecracker VM doing its own end-to-end TLS through a transparent-tunnel (no-MITM) egress sidecar that respawns 1:1 with the VM. New `net-demo` worker + rootfs; `web-common::make_transparent_get` (webpki + optional extra CA, fail-closed); `rewrite_worker_policy(mitm)` (no CA in transparent mode); `core/src/egress/persistent_net.rs` `NetClientTransport` (bundles `Client` + `EgressSidecar`, both reaped on respawn) + `spawn_net_transport`. Reuses `PersistentWorker` + slice-4a vsock reverse-channel + the egress sidecar. Hermetic cross-platform e2e RAN+PASSED on Seatbelt; **DGX real-KVM GREEN** (`net_demo_firecracker_egress_e2e`: E2E TLS in a VM over the vsock reverse-channel + host sidecar, genuine launcher-kill → fresh-VM respawn). 8-task subagent-driven TDD, all reviewed clean; opus final review READY TO MERGE. The DGX gate caught two Mac-masked bugs (sidecar-must-run-on-host `sidecar_backend`; vacuous `pkill` kill → `pkill -f` + fresh-VM assertion). Transparent-tunnel trade-off (no leak-scan of the worker's own-TLS payload; allowlist+SSRF stay the boundary) documented, per-worker. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-01-firecracker-microvm-slice5c-*`.
    [x] **SLICE 5b-4a — Matrix onto the shared `PersistentWorker` + transparent-tunnel sidecar egress (2026-07-03, PR [#394](https://github.com/hherb/kastellan/pull/394) MERGED `8f5ec97` — closes [#380](https://github.com/hherb/kastellan/issues/380)).** Bespoke `drive()`/`supervised_self_spawn` DELETED; new channel-generic `PolledWorkerDriver` (`core/src/channel/polled_driver.rs`: long-poll, login-identity, pending-outbound retention across respawns — IMAP/Telegram reuse it) over the untouched `PersistentWorker`. Matrix egress rides the global `KASTELLAN_EGRESS_FORCE_ROUTING` (1:1 sidecar+worker per respawn via `spawn_net_transport` + new `on_decision` audit sink; CLI probe stays direct). Mac + DGX workspace green (2270/0/34 + clippy), DGX live matrix e2e 2/2 genuine (incl. #321 restart recovery). **Live force-routed gate DISCHARGED 2026-07-03** — it exposed a PRE-EXISTING `spawn_sidecar` bug (raw policy, no `derive_lockdown_env` ⇒ sidecar Landlock blocked its own DNS on Linux + no seccomp; also the true cause of the old "DGX can't run real-net egress tests" caveat), fixed `e70174b` (+ [#395](https://github.com/hherb/kastellan/issues/395) scoped short-lived cpu cap follow-up — **RESOLVED 2026-07-05**: `spawn_sidecar`/`proxy_policy` take `long_lived: bool`; short-lived per-tool-call sidecars get a bounded `cpu_ms=10_000` back [the only CPU primitive on macOS], the long-lived matrix sidecar stays `cpu_ms=0`); both DGX real-net egress tests now pass; LIVE matrix logged in through the sidecar (`egress.allowed` audited) + operator DM round-tripped (task 73 `channel.replied`). Spec `docs/superpowers/specs/2026-07-02-firecracker-microvm-slice5b4-matrix-in-vm-design.md`, plan `docs/superpowers/plans/2026-07-02-microvm-slice5b4a-matrix-persistent-worker.md`.
    [x] **SLICE 5b-4b — matrix-in-a-VM (MERGED to `main` as `e3c675a`, PR [#397](https://github.com/hherb/kastellan/pull/397)).** Opt-in `KASTELLAN_MATRIX_USE_MICROVM=1` (Linux) runs the matrix worker in a Firecracker VM: `scripts/workers/microvm/build-matrix-rootfs.sh` (baked `--features live-matrix` worker + `ldd` closure + OS CA bundle + `/data`/`/run` anchors → `matrix.ext4`); `microvm-init` guest-loopback-up; pure `build_matrix_vm_policy` (`persistent_store` ext4 at `/data`, mkfs-once → survives respawn, preserves E2E identity + #321); `spawn_matrix_worker` `#[cfg(linux)]` VM branch (transient 0700/0600 bootstrap password RO-share, respawn-safe; bwrap else-arm byte-equivalent); daemon resolves the **worker** backend to `sandboxes.firecracker` while the egress **sidecar** stays host bwrap (5c invariant). Bootstrap-pw hardening residual [#396](https://github.com/hherb/kastellan/issues/396). Spec `docs/superpowers/specs/2026-07-02-firecracker-microvm-slice5b4-matrix-in-vm-design.md`, plan `docs/superpowers/plans/2026-07-03-microvm-slice5b4b-matrix-in-vm.md`. ✅ **DGX VM-mode live-e2e gate DISCHARGED 2026-07-04** (both `todo!()` bodies filled in + run on the real DGX Spark against live `matrix.kastellan.dev`): **`matrix_vm_send_recv_round_trip` + `matrix_vm_restart_recovers_downtime_message` BOTH GREEN** (VM login through the sidecar + E2E round-trip; fresh-VM `pkill -f kastellan-microvm-run` respawn + #321 downtime recovery from the persistent `/data` store), full DGX `cargo test --workspace` + clippy clean. **The gate found + fixed TWO real merged-code bugs the VM path had (never exercised until now):** (1) the ~1090-byte matrix boot_args overflowed the conservative 1024-byte kernel-cmdline cap ⇒ the VM couldn't boot at all — raised to 1920 (`plan.rs`); (2) `session.json` was written without fsync ⇒ a hard VM kill lost it (0-byte on the ext4 image), so the respawned worker hung in `restore_session` — now fsync'd (`sdk_live.rs`). Branch `test/matrix-fc-live-e2e` (PR [#444](https://github.com/hherb/kastellan/pull/444)). **True `jailer`** stays deferred to a privileged-tier `VmmConfinement::Jailer` sibling.
  - [ ] **Follow-ups:** curated-wheels RO dir if/when the skill catalog demands packages; planner-prompt surfacing
    (parity note: the net workers have none either).
- [ ] Skill catalog (named/persisted Python skills) with optional human-approve gate
  - [x] **Slice 1 — crystallise + approval + operator CLI — 2026-06-13** (branch `feat/python-exec-skill-catalog`,
    PR [#275](https://github.com/hherb/kastellan/pull/275)). Agent-authored Python skills mirror the L3 templated arc one payload over: a layer-3 `memories` row
    with `metadata.kind="python"` + `metadata.python={name,description,code}` (absent kind ⇒ templated, back-compat),
    deduped by canonical SHA-256, **verbatim code (no params)**. New `core/src/memory/l3py_crystallise.rs`
    (validate + canonical SHA + `crystallise_python_skill`; code allows only `\n`/`\t`, rejects every other ASCII control
    byte incl. NUL/CR/ESC + `secret://` + over-64-KiB, `<skills>` reserved-tag guard on the surfaced description) +
    `l3py_approval.rs` (`evaluate_python_approval` —
    structural + `secret://` scan, **NO registry/tool-existence check**: a Python skill dispatches no tools, the
    python-exec jail is the boundary; `RejectReason::CodeSecretRef` added). `Plan.python_skill` directive crystallised
    by `runner::drain_lane` under the same `dispatch_count>=1 && !invoke_used` grounding gate as `l3_skill`
    (`InnerLoopResult.terminal_python_skill`, `finish!` 4-arg form). CLI: `memory l3 show <id>` (read source before
    approving — the human read IS the gate) + kind-aware `list` + kind-aware `approve`/`pin` (Python branch gates via
    `evaluate_python_approval`, no registry). Post-review hardening: code validation + `show` escaping close a
    terminal-escape deception of the `show`-then-approve gate. Workspace 1725/0/8, clippy `-D warnings` clean; live-PG verified
    (writer dedup, scheduler crystallise capture, CLI approve-without-registry, `show` verbatim source). Spec/plan:
    `docs/superpowers/{specs,plans}/2026-06-13-python-exec-skill-catalog*`.
  - [x] **Slice 2 — invocation + surfacing — 2026-06-13** (branch `feat/python-exec-skill-catalog-slice2`, PR [#276](https://github.com/hherb/kastellan/pull/276)).
    Makes approved/pinned Python skills runnable, mirroring the L3 invocation arc. New `core/src/memory/l3py_invoke/`
    (facade + `pure`/`operator`/`agent`, mirroring `l3_invoke/`): `prepare_python_invocation` pure gate (trust →
    re-validate → **SHA-256 re-hash vs stored digest = the code TOCTOU close**, returns verbatim code);
    `invoke_python_skill` operator path (reuses `InvokeReport` + `run_steps` + the L3 audit builders with `kind:"python"`
    injected — one `python.exec` step expressed as a single `L3TemplateStep`, so the daemon→serialize→CLI→render
    pipeline is unchanged); `expand_python_for_agent` + `load_pinned_python_skill_by_name` (pinned-only, defensive
    trust re-check). Daemon `l3_run.rs` gained a `kind=="python"` branch (**fail-closed**: an unregistered python-exec
    surfaces a tool-not-found step error, never a silent no-op); `l3_surface.rs` is kind-aware (python surfaces
    name+description, **code never shown** — structurally enforced, no code field on `SurfacedSkill`); inner-loop
    `invoke_skill` resolves a pinned python skill by name (CASSANDRA review applies); CLI `run` resolves the python
    name for its header. `cli_memory_l3py_run_daemon_e2e` (2 scenarios, **live-green on Mac Seatbelt under PG 18**):
    real-jail round-trip via the daemon + `kind:"python"` audit row + fail-closed-when-disabled. Spec/plan:
    `docs/superpowers/{specs,plans}/2026-06-13-python-exec-skill-catalog*`.
  - [x] **Runtime params — 2026-06-14** (branch `feat/python-exec-runtime-params`, PR [#278](https://github.com/hherb/kastellan/pull/278)). A Python skill receives
    runtime params via the env var `KASTELLAN_PYTHON_PARAMS` (one JSON object the worker always sets, default `{}`) — the
    code stays verbatim/SHA-bound so params arrive on a side channel, not by mutating the source. **Slice A** (worker):
    `python.exec` accepts an optional `params` object, `serialize_params` validates object-ness + a **64 KiB serialized cap**
    (under the Linux `MAX_ARG_STRLEN` execve wall), `run_code` injects it as the env var (survives `-I`). **Slice B** (core):
    `validate_python_params` (object, snake_case top-level keys, 64 KiB; **null/empty ⇒ no params**, omitted from the step)
    threads through `l3py_invoke` operator + agent paths, `InvokeDirective.params`, the inner-loop `invoke_skill` arm, the
    daemon `l3_run` payload, and CLI `memory l3 run --param k=v` / `--params-json '<obj>'`. Values are **arbitrary JSON**;
    **secret:// refs in params materialise for free** through the existing `tool_host` dispatch chokepoint (recursive
    `substitute_refs_in_params` walker; `Net::Deny` contains them). **Free-form passthrough** — no declared schema, SHA +
    approval untouched; a missing key is a runtime `KeyError`. Live-green on Mac Seatbelt/PG 18 (`cli_memory_l3py_run_daemon_e2e`
    param round-trip `GOT:hi` + over-cap refusal). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-14-python-exec-runtime-params*`.
  - [x] **Output secret-scrub (params battle-test follow-up) — 2026-06-17** (branch `feat/python-exec-output-secret-scrub`).
    Closes the "battle-test free-form passthrough for risk slip-throughs" follow-up. A `secret://` param materialises to
    plaintext for the worker, and python-exec runs **agent-authored** code, so its returned stdout/stderr (its only channel —
    `Net::Deny`, the #3b egress scanner never runs on it) could surface the secret in `audit_log`/JSONL/InvokeReport. New pure
    `kastellan_leak_scan::redact` (bounded-buffer, all-hits, marker `[redacted:<8hex>]`; reuses the Rabin+SHA-256 detection,
    shared `pow_base`/`sha256_hex` extracted to `fingerprint.rs`) + `core/src/tool_host/secret_scrub.rs` (walks the result
    JSON's string leaves, scrubs against `Vault::value_fingerprint` — no plaintext copy; `secret.output_scrubbed` audit row,
    hash/offset/len only). Wired into `dispatch_with_sink` on the `Ok(v)` arm **before** the injection screen, gated to
    python-exec only (`shell_exec_e2e` proves the no-op for every other worker). Confirming e2e: python-exec child env is
    **exactly** `{HOME, KASTELLAN_PYTHON_PARAMS, TMPDIR}` (live-green PG18 + real jail — params can't become/clobber env vars).
    Accepted limit: secrets `<8` bytes unscannable (same as #3b). Mac `cargo test --workspace` **1877/0/13**, clippy `-D warnings`
    clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-17-python-exec-output-secret-scrub*`. **Deferred:** scratch-file
    channel for >64 KiB payloads (rides macOS writable-scratch); declared-param schema (only if free-form proves too loose).
    **Scrub e2e now proven in-process — 2026-06-17:** `python_exec_e2e::materialized_secret_param_is_scrubbed_from_output`
    materialises a real secret, passes it through the `params` channel as a `secret://` ref, runs the **real** python-exec
    worker in the **real** jail through the **real** `dispatch`, and asserts the plaintext returns `[redacted:…]` + one
    `secret.output_scrubbed` row (live-green PG18). The full **daemon** scrub e2e (CLI→scheduler→l3py routing) is now also
    **DONE — 2026-06-25, #298 (PR #352)**: `secret_param_round_trips_and_is_scrubbed_through_daemon` drives the live daemon via
    a `#[cfg(debug_assertions)]` `KASTELLAN_TEST_VAULT_SEED` seam (`Vault::seed_known_ref_for_test` + a `main.rs` reader, both
    physically absent from `--release` builds — proven via `strings`). macOS Seatbelt 6/0 + DGX bwrap 6/0.
- [ ] **Skill trust enum** — `Untrusted | UserApproved | Pinned`, each level mapping to an explicit capability ceiling (which workers it may invoke, which net allowlists, which fs paths). Authorship and approval recorded in `audit_log`; promotion requires re-approval. (Pattern: IronClaw skill trust model — user-placed vs registry-installed. The L3 templated-skill arc above is the first concrete implementation of this shape.)
- [ ] Optional micro-VM backend for `python-exec` (Firecracker on Linux, Apple `container` on macOS — discovery spike completed 2026-05-21, verdict COMMIT; see [`docs/superpowers/specs/2026-05-21-macos-container-spike-notes.md`](../superpowers/specs/2026-05-21-macos-container-spike-notes.md))
- [ ] **Tiered delegation policy with hard no-recursion ceiling** — when the scheduler grows subagent delegation (today everything is one inner loop), borrow openhuman's `docs/DELEGATION_POLICY.md` four-tier shape: Tier 1 reply-directly (no tools), Tier 2 direct tool, Tier 3 inline subagent (≤5 turns, no new thread), Tier 4 dedicated worker thread (>5 turns). **The structural constraint that matters: workers do not spawn workers.** Encode it in `tool_host` as a compile-time check (`SubagentContext: Sealed` newtype that can only be constructed from the root scheduler) so the spawn tree is provably finite and the audit log has bounded fan-out per task. Maps cleanly onto the existing `Lifecycle::{SingleUse, IdleTimeout}` shape: tier-3 inline subagents are `SingleUse`, tier-4 dedicated threads piggyback on `IdleTimeoutLifecycle`. Pre-req for any meaningful agent-authored-skills work; defines the budget per skill invocation.
- [ ] **Stability-scored preference learning** — when the agent starts inferring user preferences (style, vetoes, tooling, timezone, identity facts), a naive "remember whatever the latest message said" path is vulnerable: a single injected message in any channel permanently flips a long-standing preference. Adopt openhuman's `docs/AGENT_SELF_LEARNING.md` scoring shape: `stability(class, key, value) = base × cue × user_state`, evidence weighted by source (explicit user statement 1.0, structural data 0.9, behavioural heuristic 0.7, recurrence 0.6), only "Active" at stability ≥1.5 (requires corroboration). Storage: new `user_profile_facets` table behind `db::profile`, runtime-role can INSERT bounded candidate rows but only the explicit "operator pin" CLI surface (`kastellan-cli profile pin <class> <key>=<value>`) can promote a facet to Active or override automatic scoring. Keeps `memory access is core-only` invariant intact — workers never write profile state. Pre-req for any prompt-assembly surface that injects a `UserProfileSection` (current `assemble_system_prompt` ships an `L0`/`L1` block but no profile facets yet).

## Phase 5 — Frontier escalation, hardening, audit UI

- [ ] Policy gate: per-tool, per-task, per-data-classification routing decision
- [ ] Frontier escalation through egress proxy (Anthropic / OpenAI)
- [ ] **Model-based CASSANDRA guard tier — IBM Granite Guardian 4.1 (defense-in-depth, advisory only)** —
  a local safety/judge model (`ibm-granite/granite-guardian-4.1-8b`, **Apache-2.0** ⇒ license-audit clean;
  hybrid Mamba-2/Transformer ⇒ low memory; runs on Ollama `:11434` macOS / vLLM `:8000` Linux, i.e. the
  **existing `kastellan-llm-router` local backend pointer** — no new egress, no vendor lock-in, no NVIDIA dep).
  Three hook points, all **advisory** — Guardian is a probabilistic classifier (~0.79 F1 OOD-safety; misses
  ~1 in 5), so it **never** becomes a gate: the OS sandbox (bwrap/Seatbelt) + the egress proxy's deterministic
  allowlist/SSRF/pinning remain the only things that actually contain a compromise. It lowers attempt volume
  and feeds the audit log; it does not change the threat model.
  1. **`GuardianReviewStage` implementing `ReviewStage`** (`core/src/cassandra`) — slots into the existing
     `ChainReviewStage(vec![ConstitutionalGuard, DeterministicPolicy, …])` after the deterministic rules.
     `async fn review(&self, plan, ctx)` formats the agent's `Plan` (esp. tool-call steps) into Guardian's
     `<guardian>` criteria block, calls `Router::send` in **no-think mode** (low latency, direct `<score>`),
     maps `yes` → `Verdict::Advisory`/`Escalate` (NOT `Block` — advisory tier) carrying the jailbreak/harm
     reason tag. Fail-open on router error (defense-in-depth, not the boundary).
  2. **Function-calling-hallucination check before step dispatch** (`ToolHostStepDispatcher`) — Guardian's
     agentic detector validates that a tool-call step's args are consistent with the worker's manifest schema
     + the user intent *before* the worker is spawned. This is the strongest novel fit — semantic validation
     the argv allowlist structurally can't express. Advisory pre-flight; the allowlist + sandbox still gate.
  3. **RAG groundedness on `memory::recall`** — Guardian's context-relevance/groundedness/answer-relevance
     scores spot-check that a memory-backed answer is grounded in what was actually recalled (BYOC criteria
     can also encode kastellan-specific policy — "attempts secret exfiltration?", "targets a non-allowlisted
     host?" — as auditable yes/no rows). **Limitation: English-only.** `<think>` traces are NOT logged verbatim
     (IBM warns they may contain unsafe / unfaithful content). Cost: an 8B judge ~doubles inference load, so
     no-think inline + reserve think-mode for offline spot-checks. (Investigated 2026-06-15; sources in the
     handover.) Pairs naturally with the policy gate + frontier escalation above (screen what crosses the
     local↔frontier boundary).
- [ ] Read-only audit log viewer (CLI complete; web optional)
- [ ] 7-day adversarial soak test (prompt-injected channel content; no escapes in audit log)

---

## Cross-cutting / continuous

- [x] **Full-project security audit + remediation (pre-release)** — `docs/security-audit-2026-07-02.md`; 9 fixes landed (recall injection-screen + framing-escape of `<recalled>`/`<l1_insights>`, JSON-RPC 64 MiB record cap, `pdf-extract`→`lopdf 0.42`, SSRF IPv6-transition ranges, `MacosContainer` fail-closed egress, systemd path-field guard, migration 0020 `search_path`, script hardening) — PR [#392](https://github.com/hherb/kastellan/pull/392) MERGED `2d2aa70`, 2026-07-02. **Open follow-ups:** ~~[#386](https://github.com/hherb/kastellan/issues/386)~~ **DONE 2026-07-21**, ~~[#387](https://github.com/hherb/kastellan/issues/387)~~ **DONE 2026-07-22**, ~~[#388](https://github.com/hherb/kastellan/issues/388)~~ + ~~[#389](https://github.com/hherb/kastellan/issues/389)~~ **DONE 2026-07-23 (see below)**, and [#393](https://github.com/hherb/kastellan/issues/393) (SSRF site-specific NAT64 / Teredo / ISATAP residual) — CLOSED 2026-07-05. The audit-remediation family is now FULLY closed; no audit follow-up remains open.
- [x] **Guest-kernel integrity pin** — all 8 `build-*-rootfs.sh` fetched the micro-VM kernel unchecked and reused an existing copy unverified forever; now one shared `scripts/workers/microvm/lib/guest-kernel.sh` (sha256 per arch, quarantine-on-mismatch, verify-before-reuse). Recorded as an arch-invariant of the containment boundary, not a mere script fix: the kernel is what enforces the VM. — closes [#471](https://github.com/hherb/kastellan/issues/471), PR [#478](https://github.com/hherb/kastellan/pull/478), 2026-07-20. **Follow-up [#479](https://github.com/hherb/kastellan/issues/479) now DONE — see below.**
- [x] **Guest-kernel integrity at VM boot** — #471's pin was build-time only: `linux_firecracker.rs` booted `dir.join("vmlinux")` unconditionally, so a kernel verified during *some* build was then booted for months out of a directory the agent's own OS user could write. Two halves: the image dir becomes `root:<worker-group>` **1775** with `vmlinux` `root:root 0644` (removes the write primitive), plus a boot-time sha256 check in the deliberately dual-platform `sandbox/src/guest_kernel_pin.rs` (catches the case ownership misses), documented as TOCTOU-limited rather than sold as sufficient. **The first ownership attempt was a real hole that looked correct** — it chowned the *directory* to the worker user, and `unlink(2)` exempts the directory's owner as well as the file's, so the agent could still `rm` and replace the kernel; caught in review, fixed, and the structural test that missed it was strengthened and proved to fail against the old line. Verified live as the agent user: `rm`/`rename`/overwrite all refused, unprivileged rootfs builds still work. Hash cost 0.00–0.01 s per boot ⇒ no cache. **The final review then found three more routes around the ownership half** — builds could *re-create* an absent kernel agent-owned (group write suffices to create, so spec §3.3's `require_guest_kernel`, dropped during planning, was restored across all 8 build scripts); a pre-planted **symlink** survived remediation because `[ -f ]`/`chown`/`chmod` follow links; and the **parent** `/var/lib/kastellan` was never asserted. **Standing lesson: a permission-based control is not verified by reading its bits** — four bugs of that one shape here, all found only by attempting the attack as the untrusted user. **A post-review `/fixall` then fixed six more (PR #480):** a latent false-alarm in the run-dir assertion (it scanned the *shared* temp dir, so a concurrent live e2e would have reported a bogus "the pin check has moved"; now own-pid-scoped and proved both ways); the kernel's **mode** never being asserted (root-owned at `0666` is overwritable in place); the load-bearing absence of `-L` on `stat` now documented *and* test-enforced; a `realpath -m` normalisation of `fetch-guest-kernel.sh`'s bypassable default-dir guard; a `-e`-before-`-L` ordering that misreported dangling links; and — **the same lesson landing a fifth time** — my own first fix put the symlink re-check *after* `chown`/`chmod`, which both follow links, so root would still have followed one. Ordering the operations, not reading the bits. — closes [#479](https://github.com/hherb/kastellan/issues/479), 2026-07-21.
- [x] **Provisioning-binary checksum pins** — the last of the audit-#6 family after the guest kernel (#471/#479). `install-firecracker.sh` (VMM tarball, worker user) and `scripts/matrix/vps/phase2-homeserver.sh` (Continuwuity binary, installed root:root 0755 and run under systemd — it previously only *printed* the sha after install, verifying nothing) now verify against a pinned sha256 **before** the artefact is extracted/installed. The two pure hash helpers were lifted out of `guest-kernel.sh` into a shared `lib/verify.sh` that `install-firecracker.sh` sources; phase2 runs standalone on the VPS (no repo to source) so it carries its own inline copy. Firecracker uses upstream's published `.sha256.txt` sums (re-verified across hosts); Continuwuity has no upstream sums so it is honest 3-witness TOFU (live VPS binary + DGX + Mac). Hermetic tests assert a well-formed sum per artefact and verify-**before**-use *ordering* (anchored on command lines, not comments — the #479 lesson), each proved to fail against a weakened script. — closes [#386](https://github.com/hherb/kastellan/issues/386), 2026-07-21. **Remaining siblings:** [#388](https://github.com/hherb/kastellan/issues/388)–[#389](https://github.com/hherb/kastellan/issues/389).
- [x] **Linux bind-path symlink canonicalization** — the macOS Seatbelt backend canonicalizes every policy path (symlinks + `..`) before building a rule; the Linux backends only had the *lexical* half (`validate_linux_bind_path`, absolute + no-`..`). This closes the *symlink* half for cross-platform parity: `canonicalize_one` lifted into a shared `lib.rs` primitive (one source of the guarantee for all three backends); bwrap canonicalizes each host-source path and binds **`canonical-src → original-dest`** (TOCTOU-safe — the worker keeps its jail path, bwrap gets the resolved literal); Firecracker canonicalizes the `fs_read` root before the anchor check, so a symlinked root escaping the anchor set (e.g. `/opt/link → /etc/shadow`) is rejected on its resolved top-level. Resolve posture (parity, not reject); only host-source paths resolved (guest-side paths left lexical). Both security assertions proved to fail against un-hardened code (the #479 house rule). — closes [#387](https://github.com/hherb/kastellan/issues/387), PR [#482](https://github.com/hherb/kastellan/pull/482), 2026-07-22.
- [x] **Audit-#7 family tail — worker-discovery / lockdown-env / keyring / scrub** — the last two audit siblings, four Low-sev defence-in-depth fixes (each a pure fn + thin wiring, no boundary change): (#388.1) `assess_install_dir` flags an install dir writable by a principal other than root/self — warn-default, `KASTELLAN_REQUIRE_TRUSTED_INSTALL_DIR=1` fails closed; (#388.2) `detect_lockdown_overrides` WARNs when a manifest sets `SECCOMP/LANDLOCK_PROFILE=none` at both spawn paths (derive-then-warn, not reject — matrix's `--enforce-sandbox=false` dev opt-out stays valid); (#389.1) keyring first-init `resolve_or_init` over a `KeyringOps` seam does read-back-verify and converges on the winner's key (documented as defence-in-depth, not a full mutex); (#389.2) `redact` merges overlapping spans so no plaintext secret survives a coincidental overlap. Every assertion proved-to-fail against un-hardened code. DGX 2656/0/50, clippy clean, 0 `[SKIP]`. — closes [#388](https://github.com/hherb/kastellan/issues/388) + [#389](https://github.com/hherb/kastellan/issues/389), PR [#486](https://github.com/hherb/kastellan/pull/486), 2026-07-23. The audit-remediation family is now FULLY closed — [#393](https://github.com/hherb/kastellan/issues/393) was itself CLOSED 2026-07-05, so no audit issue remains open.
- [ ] Threat-model doc kept in sync with shipped backends
- [ ] Architecture doc kept in sync with shipped components
- [ ] License audit on every new dependency (AGPL-compatible only)
- [ ] CI: `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test --workspace` — both Linux and macOS. (Clippy `-D warnings` gate live on `linux-check`, #153; `cargo fmt` still TODO.)
- [x] **Public website `kastellan.dev`** — landing + roadmap + security + contributing pages, hand-rolled static `site/` (no build step) served by Cloudflare Pages free tier; kastellan-branded SVG diagrams; `scripts/site/check-site.sh` verification suite; curated content kept fresh via HANDOVER checklist item 7; CF-side wiring is an operator step in `site/README.md` — branch `claude/relaxed-davinci-ddf2ed`, 2026-06-11
- [x] **Early test + deletion-audit infra (2026-05-12)** — shared `kastellan-tests-common` dev-dep crate (`PgCluster`, RAII guards, skip helpers, sandbox factory, seeded embeddings; closes #15); plus `deleted_memories` AFTER DELETE trigger on `memories` (migration 0008), append-only by GRANT, preventive infra for a future GDPR-style forgetting path.
