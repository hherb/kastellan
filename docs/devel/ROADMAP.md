# hhagent — Development Roadmap

Sequenced feature list. Items are added whenever a feature decision is made
and ticked off as they ship. Order reflects expected build sequence — earlier
items unlock later ones.

> **How to update.** When we agree on a new feature, append it to the most
> appropriate phase (or a new phase) — placed at the position that respects
> dependencies. When an item ships, change `[ ]` → `[x]` and add the commit
> hash. Don't delete completed items; they document the build sequence.

---

## Phase 0 — Skeleton & First Sandboxed Worker (Linux)

- [x] Cargo workspace, AGPL-3.0 license, README, .gitignore — `140eec5`
- [x] `hhagent-core` (bin+lib stub) — `140eec5`
- [x] `hhagent-sandbox` crate skeleton (trait + policy struct) — `140eec5`
- [x] `hhagent-supervisor` crate skeleton — `140eec5`
- [x] Architecture & threat-model doc skeletons — `140eec5`
- [x] Linux bwrap backend (`linux_bwrap.rs`): unshare-all, FS bind, --clearenv, --setenv, die-with-parent, new-session, as-pid-1 — `eae3df4`, `f2411ec`
- [x] AppArmor `unprivileged_userns` workaround: `scripts/linux/install-bwrap-apparmor-profile.sh` + runtime `LinuxBwrap::probe()` — `eae3df4`
- [x] Sandbox negative tests: /etc/passwd invisible, /home invisible, listed paths visible, net unreachable under `Net::Deny`, relative paths rejected — `eae3df4`
- [x] `hhagent-protocol` crate: JSON-RPC 2.0 server/client over stdio (MCP-stdio compatible) — `f2411ec`
- [x] `workers/shell-exec`: argv allowlist, no shell interpretation, allowlist via `HHAGENT_SHELL_ALLOWLIST` env — `f2411ec`
- [x] `core::tool_host::spawn_worker`: spawn worker under sandbox, return connected protocol Client — `f2411ec`
- [x] End-to-end test: core → bwrap → shell-exec → JSON-RPC echo round trip + POLICY_DENIED + METHOD_NOT_FOUND — `f2411ec`

## Phase 0 hardening — Defence in depth (Linux)

- [x] Landlock LSM as second FS-allowlist layer inside the worker before exec (defence-in-depth on top of bwrap) — `3210f70` *stage 1*: targets ABI v1, RO+exec on `/usr`, `/lib*`, `/bin`, `/sbin`, `/etc/ld.so.cache`, `/dev`, `/proc`; RW from `HHAGENT_LANDLOCK_RW` env
- [x] seccomp-bpf syscall filter — `3210f70` *stage 1*: deny-list of catastrophic syscalls (`unshare`, `setns`, `mount`, `umount2`, `pivot_root`, `init_module`, `finit_module`, `delete_module`, `ptrace`, `bpf`, `perf_event_open`, `kexec_load`, `kexec_file_load`, `reboot`, `swapon`, `swapoff`, `settimeofday`, `clock_settime`, `clock_adjtime`, `adjtimex`, `keyctl`, `add_key`, `request_key`, `personality`); SIGSYS-kill action; same set for both Strict and NetClient profiles
- [x] **Bug fix**: `LinuxBwrap::probe()` was missing `/lib*` symlinks, causing all bwrap-dependent tests to silently skip with "false green"; probe now mirrors `build_argv` so a green probe means real containment — `3210f70`
- [x] **Worker prelude crate** (`workers/prelude`, `hhagent-worker-prelude`) — `serve_stdio` wrapper that calls `lock_down()` before serving; tested via subprocess `lockdown_probe` binary — `3210f70`
- [x] **`tool_host` derives lockdown env** — `derive_lockdown_env` injects `HHAGENT_LANDLOCK_RW` (from `policy.fs_write`) and `HHAGENT_SECCOMP_PROFILE` (from `policy.profile`) so callers cannot accidentally skip the worker-side layer — `3210f70`
- [x] *Stage 2*: migrate seccomp to per-profile **allow-list** replacing the current deny-list — `97d4465` (~110 syscalls in `BASE_ALLOW` + 19 x86_64-only legacy + 18 in `NET_CLIENT_ADDITIONS`; `Profile::Strict` kills `socket()` while `Profile::NetClient` permits it; verified by `socket_is_killed_under_strict` and `socket_survives_under_net_client`). *Subsequent broadening*: `copy_file_range`, `sendfile`, `fadvise64` added so GNU coreutils file I/O works inside the jail (`workspace_dir_is_writable_during_call_and_wiped_on_drop` is the regression).
- [x] *Stage 2*: bump Landlock TARGET_ABI from v1 to v6 and audit each new access right (`Refer`, `Truncate`, `IoctlDev`, `Scope::AbstractUnixSocket`, `Scope::Signal`) — `97d4465` (lifts `PartiallyEnforced` → `FullyEnforced` on this kernel; verified by `v6_abi_yields_fully_enforced_on_modern_kernel`; required a fix in `add_path_rule` to use `AccessFs::from_file` for file-typed paths so directory-only rights aren't silently stripped)
- [x] cgroup v2 CPU/memory caps via `systemd-run --user --scope` — `3cea642`. `sandbox/src/linux_cgroup.rs` builds the `systemd-run --user --scope --quiet --collect -p MemoryMax=Nm -p MemorySwapMax=0 -p CPUQuota=200% -p TasksMax=64 --` prefix; `LinuxBwrap::spawn_under_policy` now invokes systemd-run as the outer process so the cgroup is set up before `bwrap --unshare-all`. `LinuxBwrap::probe()` chains `cgroup_probe()` so a host without a live `systemd --user` manager fails closed instead of running degraded. Verified by `worker_with_low_mem_max_is_oom_killed` (mem_burner allocating 256 MiB under MemoryMax=32M is OOM-killed). `MemorySwapMax=0` is paired with MemoryMax so overrun cannot silently page to swap.
- [x] **Issue #6 main body — policy-driven `cpu_quota_pct` / `tasks_max` + `setrlimit(RLIMIT_CPU)`-based `cpu_ms` enforcement** — landed 2026-05-14 on branch `feat/sandbox-cpu-rlimit-quota`. Two new `SandboxPolicy` fields (both `Option`, defaulted `None`) drive `build_systemd_run_argv`'s `CPUQuota` and `TasksMax` properties when set; otherwise fall back to the existing 200% / 64 defense-in-depth defaults. New cross-platform `workers/prelude/src/rlimit.rs` module reads `HHAGENT_CPU_MS` (set by `tool_host::derive_lockdown_env` from `policy.cpu_ms`) and applies `RLIMIT_CPU` with soft = hard for a clean SIGXCPU kill before lock-down. Cross-platform via POSIX; the same code path runs on Linux and macOS. `LockdownReport` restructured to carry the `rlimit` field on both `Linux` and `NonLinux` variants. New `lockdown-probe cpu-burner` subcommand + `rlimit_smoke.rs` cross-platform integration test pin the end-to-end behaviour. Test count: 429 → 446 (+17). Closes issue #6.
- [x] Per-worker scratch dir lifecycle (create on spawn, wipe on exit) — `9333311` (subsumed by the `Workspace` type below; `Workspace::Drop` recursively wipes `<root>/<task_id>`)
- [x] Promote per-worker scratch to a first-class `Workspace` type — canonical layout `<root>/<task_id>/{in,out,tmp}`, single owner, single cleanup path; `Workspace::extend_policy(&mut SandboxPolicy)` is the canonical wiring point so host (`policy.fs_write`) and worker-side Landlock (via `tool_host::derive_lockdown_env`) cannot disagree (cf. ZeroClaw `workspace_boundary.rs`) — `9333311`
- [x] Spawn timeout / wall-clock kill — `57edfb2` (`WorkerSpec.wall_clock_ms: Option<u64>`; `spawn_worker` returns `SupervisedWorker` with a 50 ms-poll watchdog thread; cancellation on Drop closes the reused-PID race; `is_valid_target_pid` defends against `kill(-1)` fanout)

## Phase 0b — macOS Port (Seatbelt)

> Done before adding more workers, to stop Linux-isms leaking through the codebase.

- [x] `macos_seatbelt.rs`: `SandboxPolicy` → `.sb` (TinyScheme) generator — `2fa46a2`. *Hardened*: removed unrestricted `(allow mach-lookup)` from the strict profile (issue #1) — every shipping worker (`hhagent-worker-shell-exec` + the four fixture binaries + every coreutils binary on macOS 26.4 ARM64) was empirically confirmed to start without it, and granting it would have exposed every registered launchd service (Apple Events broker, pasteboard, etc.) — the largest known asymmetry vs the threat-model invariant. Pinned by the new unit test `profile_does_not_grant_unrestricted_mach_lookup` and the smoke test `worker_cannot_look_up_arbitrary_mach_services` (the worker calls `bootstrap_look_up("com.apple.coreservices.appleevents")` and must exit non-zero).
- [x] `sandbox-exec` invocation (env_clear + per-policy env + **setsid** for true session isolation — issue #2) — `2fa46a2` originally with `setpgid(0,0)` via `process_group(0)`, *upgraded* to `setsid()` via a `pre_exec` hook so the worker is the leader of a fresh session (sid == pid), strictly stronger than the old setpgid form. Closes the parity gap with bwrap's `--new-session` and forecloses any `/dev/tty` covert channel even under future profile broadening. Pinned by the smoke test `worker_runs_in_its_own_session`.
- [x] setrlimit for **CPU** — landed 2026-05-14 via Option G's cross-platform `workers/prelude::rlimit::apply_from_env` (see issue #6 entry under Phase 0 hardening). `setrlimit(RLIMIT_CPU)` is POSIX so the same code path enforces `policy.cpu_ms` on Linux and macOS workers. **mem** + **wallclock** still deferred: macOS memory waits on the Apple `container` micro-VM backend (issue #55) because `RLIMIT_AS` has high false-positive risk for malloc-heavy workers; wallclock is enforced parent-side via `WorkerSpec.wall_clock_ms` + watchdog (`57edfb2`), no rlimit needed
- [x] Network containment via `(deny network*)` + allowlist rules — `2fa46a2`
- [x] Mirror of all sandbox containment integration tests, passing on macOS — 8 tests, 0 skipped (`macos_smoke.rs`: scaffold marker, echo-runs-jailed, /etc/master.passwd invisible, /Users does not leak username, fs_read readable, /dev/disk0 denied, relative-path rejection, network unreachable) — `2fa46a2`
- [x] Mirror of all 3 e2e tests on macOS — 3 tests passing under cross-platform `shell_exec_e2e.rs` — `2fa46a2`

## Phase 0 cont. — Service supervisor

- [x] `hhagent-supervisor` Linux backend: `systemd --user` unit generator + `systemctl --user` driver — supervisor scaffold landed 2026-05-10. `supervisor/src/systemd_user.rs`: pure `build_unit_file(spec) -> String` + `validate_service_name` + `SystemdUser` driver (`install`/`start`/`stop`/`uninstall`/`status`) + `probe()` against the live `--user` manager. Unit-file write is atomic (write-to-tmp + fsync + rename). 27 unit tests + 2 smoke tests (real `systemctl --user` round-trip with RAII cleanup guard). `default_supervisor()` now returns `SystemdUser::new()` on Linux.
- [x] `hhagent-supervisor` macOS backend: LaunchAgent plist generator + `launchctl bootstrap` — landed 2026-05-08. `supervisor/src/launchd_agents.rs`: pure `build_plist(spec) -> String` (XML LaunchAgent format with `Label`, `ProgramArguments`, `EnvironmentVariables`, `WorkingDirectory`, log redirects, `RunAtLoad=true`, `KeepAlive`, `ExitTimeOut`) + `validate_service_name` mirroring the Linux rules + `LaunchAgents` driver (`install`/`start`/`stop`/`uninstall`/`status`) + `probe()` against the live `gui/<uid>` domain. Plist write is atomic (write-to-tmp + fsync + rename). 35 unit tests + 4 smoke tests (real `launchctl bootstrap` round-trip with RAII cleanup guard, plus idempotent `start`/`stop` and pre-launchctl name validation). `default_supervisor()` now returns `LaunchAgents::new()` on macOS.
- [x] First concrete service: typed `ServiceSpec` for the agent core daemon + cross-OS `default_probe()` + e2e against the real `hhagent` binary — landed 2026-05-09. New `supervisor/src/specs.rs` ships pure `core_service_spec(binary, log_dir) -> ServiceSpec` with the canonical name `hhagent-core`, empty args/env, no working_dir, `keep_alive=false` (regression-pinned to today's "log line and exit 0" daemon shape), and predictable log-file names. New `supervisor::default_probe()` mirrors `default_supervisor()` so cross-OS tests skip uniformly. New `core/tests/supervisor_e2e.rs::core_service_install_start_observe_log_uninstall` drives `default_supervisor()` end-to-end against the real `hhagent` binary: install into the canonical user dir, observe the daemon's startup JSON line in the redirected stdout file, stop, uninstall. RAII cleanup guard + unique per-process names so concurrent runs don't collide and a real installed `hhagent-core` is never clobbered. Linux: 96 → 105 tests (+8 unit `specs::*`, +1 integration). macOS projects to 92 by the same delta. Precursor to `hhagent.target` below — once Postgres + inference exist as services, the target item composes them with this one.
- [ ] `hhagent.target` that brings up Postgres, inference, core, workers
- [ ] Auto-restart with backoff on worker crash (partial: `keep_alive=true` in ServiceSpec → `Restart=on-failure RestartSec=5` (constant) in the systemd unit / `KeepAlive=true` in the macOS plist; the agent-core daemon now blocks on SIGTERM/SIGINT and `core_service_spec` is `keep_alive=true` so the policy is wired and meaningful — shipped 2026-05-09 in HANDOVER Option H. **Still TODO:** cross-platform exponential backoff. systemd 252+ has `RestartSteps`/`RestartMaxDelaySec`; macOS launchd's `KeepAlive=true` has no operator-controllable throttle, so this needs a per-OS shape.)

## Phase 0 cont. — Postgres bring-up

- [x] Local Postgres install via PGDG apt repo + user-level supervisor unit (Linux now; macOS via Homebrew uses the same `postgres_service_spec`) — landed 2026-05-09. `scripts/linux/install-postgres.sh` is the idempotent install path: adds the official `apt.postgresql.org` repo via `postgresql-common`'s `apt.postgresql.org.sh` helper (with manual fallback), installs `postgresql-18 postgresql-client-18 postgresql-18-pgvector`, and stops + disables the auto-created system `postgresql@18-main.service` so it can never collide with our user-instance. Verified: PG 18.3 binaries at `/usr/lib/postgresql/18/bin/`.
- [x] Localhost-only via UDS, peer auth, dedicated DB role — landed 2026-05-09. New `hhagent-db` crate ships `hhagent-db-init` (idempotent: skips initdb when `PG_VERSION` already present, still re-writes `postgresql.auto.conf` to correct config drift). The cluster is born locked-down: `initdb --auth-local=peer --auth-host=reject --data-checksums --username=hhagent`, then `<data_dir>/postgresql.auto.conf` pins `listen_addresses=''`, `unix_socket_directories='<data_dir>/sockets'`, `unix_socket_permissions=0700`, `password_encryption='scram-sha-256'`. The supervisor side is `supervisor::specs::postgres_service_spec(postgres_binary, data_dir, log_dir) -> ServiceSpec` (canonical name `hhagent-postgres`, args `["-D", data_dir]`, `keep_alive=true`). Verified end-to-end by `db/tests/postgres_e2e.rs::postgres_install_start_select_one_uninstall`: real PGDG initdb → install via `default_supervisor()` → start → wait Active + stable-Active window → wait UDS socket → `psql -h <socket_dir> -U <whoami> -At -c 'SELECT 1'` returns `1` → stop → wait Inactive → uninstall.
- [x] Extensions: `pgvector` only. `postgresql-18-pgvector 0.8.2` is installed by `scripts/linux/install-postgres.sh`; `CREATE EXTENSION IF NOT EXISTS vector` is now the first statement in `db/migrations/0001_init.sql` (landed 2026-05-09 with C2.2). **Full-text search uses native Postgres `tsvector`+GIN with `ts_rank`** — committed-to after a 2026-05-09 review (closed [#10](https://github.com/hherb/hhagent/issues/10) won't-fix). The `memories` table now has `tsv tsvector GENERATED ALWAYS AS (to_tsvector('simple', body)) STORED` + a GIN index. **Graph storage uses plain relational `entities`/`relations` tables behind a `Graph` trait in `db/src/graph.rs`** — also committed-to in the same review (closed [#9](https://github.com/hherb/hhagent/issues/9) won't-fix); the trait is now defined and a `PgGraph` impl exercises `upsert_entity`/`upsert_relation`/`get_entity`/`neighbors`/`path` against the real schema (recursive-CTE traversal with visited-set cycle guard).
- [x] `db/migrations/` skeleton: `memories`, `tasks`, `entities`, `relations`, `audit_log`, `secrets` — landed 2026-05-09 with C2.2 in `db/migrations/0001_init.sql`. Schema notes in HANDOVER's "Recently completed" entry. Embedding columns are `vector(1024)` (bge-m3 dim, locked in this session). HNSW ANN index on `memories.embedding` deferred until Phase 1's first batch ingest (HNSW build cost scales with row count).
- [x] `sqlx` migration runner integration in core startup — landed 2026-05-09 with C2.2. `sqlx::migrate!("./migrations")` macro embeds the migration set at compile time into `hhagent_db::MIGRATOR`. `core/src/main.rs::bring_up_database` runs `MIGRATOR.run(&mut conn)` after ensuring the application DB exists, then INSERTs an `audit_log` startup row. Fail-closed: any error short-circuits daemon startup with a non-zero exit (the supervisor sees the failure). Workspace-level sqlx feature picks: `runtime-tokio` (no TLS, UDS only), `postgres`, `migrate`, `macros` (for the migrate proc-macro), `json`, `time`. **Did not pick** `sqlx-cli` separately — the embedded macro is sufficient and avoids an extra binary in the deploy footprint.
- [x] Secrets at rest: AES-256-GCM in the `secrets` table; key from OS keyring (libsecret / Keychain); decrypted only at the host boundary when injecting into a worker call, never logged, never sent to the LLM (cf. IronClaw `secrets/`, ZeroClaw `security/secrets.rs`) — landed 2026-05-10. Column shape was pinned by C2.2's `0001_init.sql`; the runtime now lives in `db/src/secrets.rs` with: pure crypto helpers (`encrypt`/`decrypt`/`compute_aad`/`validate_name`), a `KeyProvider` trait with `MapKeyProvider` (test seam) + `OsKeyringProvider` (production, cross-platform via `keyring 3`'s `async-secret-service` + `tokio` + `crypto-rust` features on Linux and `apple-native` on macOS — pure-Rust D-Bus via `zbus`, no `libdbus-1-dev` system-package requirement), and async DB I/O (`put`/`get`/`list`/`delete`) on the existing `secrets` table. Plaintext returns in `Zeroizing<Vec<u8>>` so a panic-unwind cannot leave material on the stack; AAD layer (`b"hhagent-secrets-v1" || 0x00 || name || 0x00 || extra`) binds each row to its name so `UPDATE secrets SET name = …` is detected as `AadMismatch`. New `0004_secrets_aad_nonempty.sql` drops the provisional `aad NOT NULL DEFAULT ''::bytea` and adds `CHECK (octet_length(aad) > 0)` — closes [#12](https://github.com/hherb/hhagent/issues/12). 18 unit tests + 1 integration test (`secrets_put_get_list_delete_round_trip` covers put/get/list/upsert/delete + AAD-mismatch + ciphertext-tamper + 0004 CHECK against a real per-test PG cluster). Total: 172 → 191 tests on Linux. **Worker-side decryption** ("decrypted only at host boundary when injecting into a worker call") is the typed boundary — the actual `tool_host::dispatch` integration that fetches a secret and slips it into worker `params` lands when the first credential-using tool materialises (the LLM router's Anthropic/OpenAI key is the most likely first consumer; `web-fetch` second).

## Phase 0 cont. — Audit log

- [x] Non-superuser `hhagent_runtime` role + DB-layer `REVOKE UPDATE, DELETE, TRUNCATE ON audit_log` so append-only is enforced by Postgres, not just by application discipline — landed 2026-05-10 as Option L. New `db/migrations/0002_runtime_role.sql` creates the role (`NOSUPERUSER NOCREATEROLE NOCREATEDB NOLOGIN NOINHERIT`), grants the OS user (cluster bootstrap superuser) membership via `EXECUTE format('GRANT hhagent_runtime TO %I', current_user)`, and carves the GRANT/REVOKE shape: `audit_log` gets `SELECT, INSERT` only; the other five tables (`tasks`, `memories`, `entities`, `relations`, `secrets`) get full CRUD; sequences for all six get explicit `GRANT USAGE` (BIGSERIAL needs `nextval()`); `ALTER DEFAULT PRIVILEGES` covers future migrations' tables/sequences automatically. New `RUNTIME_ROLE` constant + `set_role_runtime_statement()` pure helper in `db/src/conn.rs` (paired regression-test pins). `db/src/probe.rs` now executes `SET ROLE hhagent_runtime` between `MIGRATOR.run` and the `audit_log` INSERT — bootstrap identity (= OS user, peer auth) is preserved for migrations that need superuser (CREATE EXTENSION, CREATE ROLE), and the runtime role applies to every application write thereafter. New integration test `runtime_role_audit_log_revoke_is_enforced` proves the contract end-to-end: role exists with the expected `pg_roles` flags, OS user is recorded in `pg_auth_members`, INSERT under `SET ROLE` succeeds, UPDATE + DELETE both fail with `permission denied`, full CRUD on `memories` works, final `audit_log` count is exactly 2 (probe row + test INSERT, no UPDATE rewrite, no DELETE leak). 151 → 154 tests on Linux (+2 db unit, +1 db integration). **Why `SET ROLE` over `pg_ident.conf` mapping**: keeps the change pure-SQL inside a sqlx migration, no need to mutate `pg_hba.conf`/`pg_ident.conf` post-`initdb`; the runtime-role privilege boundary is identical regardless of the entry route.
- [x] Append-only `audit_log` writer in core (every tool call, LLM call, channel I/O, memory write) — landed 2026-05-10 as Option I (dispatcher write-site). `core::tool_host::dispatch(&PgPool, &mut SupervisedWorker, tool, method, params)` is the single chokepoint every Phase 0+ tool call goes through; one row per call (`actor = "tool:<name>"`, `action = <method>`, payload `{req, result|err, ms}`). Audit insert is best-effort (a transient DB issue must not mask a successful worker result), but the DB-layer durability story is strong: the `audit_log` REVOKE shape from Option L makes rows tamper-resistant, and the new `0003_audit_log_notify.sql` trigger emits `pg_notify('audit_log_inserted', NEW.id)` so the JSONL mirror is woken synchronously on every commit. Daemon-scoped `db::pool::connect_runtime_pool` opens a pool whose `after_connect` hook runs `SET ROLE hhagent_runtime` on every dialed connection — closes issue #11 ahead of the Phase-1 timeline. **Channel I/O / LLM / memory writers** still need to plumb through `dispatch` when those tools materialise (Phase 2+ for channels, Phase 1 for memory), but the chokepoint pattern is the explicit contract now.
- [x] JSONL on-disk mirror under `~/.local/state/hhagent/audit-*.jsonl` (rotated) — landed 2026-05-10 as Option I. `core::audit_mirror::spawn_mirror(pool, state_dir)` spawns a long-lived tokio task that holds a dedicated `PgListener` on `audit_log_inserted` and replicates committed rows to date-named files (`audit-YYYY-MM-DD.jsonl`, UTC). Initial drain via `audit::fetch_since(0)` so the bring-up row lands on cold starts; periodic 5 s catch-up SELECT in batches of 256 closes any NOTIFY-loss window from listener reconnects. fsync per write — operator visibility beats throughput at Phase 0 scale. JSONL line shape: `{id, ts (RFC 3339), actor, action, payload}`, one row per line, single trailing newline. `MirrorHandle::shutdown` flips a cancellation watch and awaits the task before pool close so the final fsync always runs.
- [x] CLI viewer: `hhagent-cli audit tail` — landed 2026-05-10 as Option I. New `[[bin]] hhagent-cli` in core/Cargo.toml; `core/src/audit_tail.rs` ships pure helpers (`parse_audit_filename`, `find_audit_files`, `tail_loop`) and the CLI binary wraps them. Subcommand `audit tail` supports `--from-start` (replay every line of every existing file), `--no-follow` (cat mode), and `--state-dir PATH` (override default). Polls 250 ms — sub-second visibility without a directory-watch dep. SIGPIPE-style `BrokenPipe` maps to exit 0 (matches BSD `tail`). No DB connection required, so the viewer works against a daemon that has crashed mid-startup.

## Phase 0 cont. — LLM router stub

- [x] OpenAI-compatible HTTP client (single sole egress for model calls) — landed 2026-05-10 as Option J. New top-level crate `hhagent-llm-router` (member #3 in the workspace, alongside `core` and `db`). `Router::send(&ChatRequest) -> Result<ChatResponse, RouterError>` is the typed surface every future model-touching path goes through; the underlying HTTP call is a `reqwest::Client::post(<base>/chat/completions).json(...)`. `reqwest 0.12` workspace dep with `default-features=false, features=["rustls-tls", "json"]` — pure-Rust TLS (no `openssl-sys`), no blocking-client surface, no system-package requirement. Module split: `messages.rs` (ChatRole/ChatMessage/ChatRequest/ChatResponse with `skip_serializing_if = Option::is_none` on `max_tokens`/`temperature` so older llama.cpp builds don't choke on null fields), `backend.rs` (closed `Backend::{Local, Frontier}` enum with stable lowercase serde tags for audit-log payloads), `config.rs` (`RouterConfig` + `from_env` reading `HHAGENT_LLM_LOCAL_URL` / `HHAGENT_LLM_LOCAL_MODEL` / `HHAGENT_LLM_FRONTIER_URL` / `HHAGENT_LLM_FRONTIER_MODEL` / `HHAGENT_LLM_TIMEOUT_MS`, with empty-string-as-absent and per-OS default URL), `policy.rs` (`PolicyGate` trait + `DefaultLocalPolicy` placeholder), `error.rs` (`RouterError::{Config, Transport, HttpStatus, DecodeResponse, PolicyDeniedFrontier}` with 1 KiB body cap on captured error bodies), `lib.rs` (`Router::{new, with_policy, send, pick_backend, timeout}`).
- [x] Local backend pointer (vLLM/SGLang on Linux, llama.cpp/Ollama on macOS) — landed 2026-05-10 as Option J. `default_local_url_for_os()` resolves to `http://127.0.0.1:8000/v1` on Linux (the canonical vLLM/SGLang OpenAI-compat port) and `http://127.0.0.1:11434/v1` on macOS (the default Ollama port); other Unixes fall back to the Linux default. Operators override via `HHAGENT_LLM_LOCAL_URL` env var. End-to-end exercised by `llm-router/tests/local_backend_e2e.rs::happy_path_round_trips_request_and_response` against a hand-rolled `tokio::net::TcpListener` mock that speaks just enough HTTP/1.1 to canned-respond a chat-completion (no `wiremock`/`httpmock` dev-dep — matches the workspace style for `db/tests/postgres_e2e.rs` and `core/tests/audit_dispatch_e2e.rs`).
- [x] Frontier backend pointer (Anthropic, OpenAI) — *unwired* until Phase 5 policy gate — landed 2026-05-10 as Option J. The frontier URL/model can be supplied via `HHAGENT_LLM_FRONTIER_URL`/`HHAGENT_LLM_FRONTIER_MODEL`, but `Router::send` returns `RouterError::PolicyDeniedFrontier` whenever the policy gate selects `Backend::Frontier` — by design. `DefaultLocalPolicy` (Phase 0's only `PolicyGate` impl) always returns `Backend::Local`, so the rejection path is reachable only via a test-only policy that picks frontier. **Frontier API key auth is *not* read from env.** The key will live in `db::secrets` (the AES-256-GCM-at-rest store shipped 2026-05-10) and be fetched at dispatch time when the Phase-5 policy gate lands; reading it from env at config-load time would defeat the keyring-wrapped at-rest encryption.

## Phase 1 — Memory & Loop

- [x] **Dispatcher chokepoint invariant** as a compile-time pin: every tool/channel/routine action enters core through `tool_host::dispatch()` — one audit-log write site, one policy-check site, no side doors — landed 2026-05-10 as Option M. New `WorkerCommand { pub(crate) method, pub(crate) params }` newtype with `pub(crate) fn new(...)` constructor in `core/src/tool_host.rs`; `SupervisedWorker::call`'s signature changed from `(method: &str, params: Value)` to `(cmd: WorkerCommand)`. Out-of-crate callers (including each doctest harness, which compiles as a separate crate) cannot construct a `WorkerCommand`, so they cannot reach `worker.call(...)` directly — the only public path is `dispatch`. The regression pin is a `compile_fail` doctest on `WorkerCommand`'s doc comment that does `WorkerCommand::new("echo", Value::Null)` and asserts it does not compile (verified live via `cargo test -p hhagent-core --doc`). `core/tests/shell_exec_e2e.rs` was rewritten through `dispatch` (per-test PG cluster bring-up, ~12 s additional integration-test time on Linux) since the seal forecloses the previous direct-call shape. Pattern lifted from IronClaw `ToolDispatcher`; cheap to retrofit before Phase 1's channels/routines materialise. 224 → 227 tests on Linux (+2 unit + 1 doctest). **Issue #16 follow-up (2026-05-13, branch `fix/worker-command-seal-tighten`):** the original Option-M seal left `WorkerCommand::{method, params, new}` + `SupervisedWorker::call` at `pub(crate)`/`pub` — sealed to out-of-crate code but open to every sibling module inside `hhagent_core`. Narrowed to **module-private** (no visibility modifier) so only `tool_host` itself and its descendants can construct a `WorkerCommand` or call a worker directly. Sibling modules (`scheduler`, `cli_audit`, `memory`, …) now get a compile error if they attempt either. The reviewable opt-out is editing `tool_host.rs` itself. The Option-M `compile_fail` doctest still trips; the workspace build itself is the in-crate regression pin. Workspace test count unchanged at 354 / 0 fail / 0 SKIP — pure visibility refactor.
- [x] `memory::recall(query, modes, k)` — pgvector (semantic) + native `tsvector`/GIN with `ts_rank` (lexical), fused via Reciprocal Rank Fusion. Two independent score lists, fused per-call. No external graph DB or BM25 extension; if either becomes a measured bottleneck we swap behind the existing abstractions. — *partial: semantic + lexical lanes shipped 2026-05-10 as Option N (`db::memories::{insert_memory, semantic_search, lexical_search, fetch_by_ids}` + pure `core::memory::reciprocal_rank_fusion` + public `core::memory::recall(pool, params)`; `RecallModes::{ALL, SEMANTIC_ONLY, LEXICAL_ONLY}`; integration test seeds 3 memories with deterministic SHA-256-seeded embeddings, asserts top-1 per lane and under fused RRF). Graph lane (`Graph::neighbors`/`path` over `entities`+`relations`) deferred to Option P, which has to first land an entity↔memory linkage (memory_entities join table or metadata->'entities' JSONB array; HANDOVER recommends the join table). `pgvector` Rust crate dep deliberately not added — text-cast `vector_literal()` formatter is the single bind site, swap-when-needed*
- [x] Graph lane in `memory::recall` — entity↔memory linkage (recommended: `memory_entities` join table) + plumb `Graph::neighbors` as a third lane fused alongside semantic + lexical (Phase 1 cont. — Option P) — landed 2026-05-12 on branch `feat/memory-graph-lane`, **merged 2026-05-13 via PR #41 at `76fe940`** (9 commits + docs + 3 post-review). Post-review (`4d88b17`) added a cap-clamp e2e assertion (hub with `cap + 8` outbound relations → `GRAPH_ONLY` returns exactly `cap` memories) plus HashSet pre-sizing on the graph-lane expansion; `GRAPH_FANOUT_CAP_PER_SEED` re-exported from `core::memory`. [Issue #42](https://github.com/hherb/hhagent/issues/42) filed (SECURITY-INVOKER footgun on `deleted_memories` trigger — deferred until a second DELETE-capable role is proposed). New migration `0007_memory_entities.sql` adds the join table (`memory_id` + `entity_id`, both FK ON DELETE CASCADE, composite PK, covering indexes); `db::memories::link_memory_to_entities` + `db::memories::graph_search` are the writer/reader helpers. `core::memory::recall` gains `RecallModes::graph` field, `RecallModes::GRAPH_ONLY`, `RecallParams::seed_entity_ids: Option<&[i64]>`, `GRAPH_FANOUT_CAP_PER_SEED = 32`, and the lane-execution body (1-hop outbound expansion via `Graph::neighbors` with `futures::try_join_all`, dedup into `HashSet<i64>`, ranked by hit count via `graph_search`, fed into existing RRF). Bundled with the spec-requested `deleted_memories` audit trigger (migration `0008_deleted_memories_audit.sql`) so memories are never lost silently. `core/Cargo.toml` gained `futures = { workspace = true }` direct dep. Workspace test count: 342 → 349 (+3 DB integration / +4 core unit / +4 new assertion groups in the existing `memory_recall_e2e` test fn).
- [x] Embedding router method (Phase 1 cont. — Option O) — landed 2026-05-12. `Router::embed(&EmbeddingRequest)` mirrors `Router::send`: HTTP POST to `<embedding_url>/embeddings`, OpenAI-compat wire shapes, count-mismatch validation. `core::memory::embed_query` is the caller helper that validates dim against `EMBEDDING_DIM` and writes the first `actor='llm:router' action='embed'` audit row. Worker-process design rejected during brainstorming (see [spec](../superpowers/specs/2026-05-11-embedding-router-design.md)) because no existing chat call goes through a worker either; symmetry preserved. New env vars: `HHAGENT_LLM_EMBEDDING_URL` (falls back to local URL) and `HHAGENT_LLM_EMBEDDING_MODEL` (defaults to placeholder `"embedding-default"` that vLLM rejects to surface misconfig loudly). `PolicyGate::pick_embed` default method is the Phase-5 seam. Pinned by +28 tests across `llm-router` (unit + integration) and `core` (unit + integration); workspace count 299 → 327. Branch: `feat/embedding-router` (range `9fe45d6..a1256cd`).
- [x] **Scheduler (CASSANDRA Phase 1)** — tick → claim task → LLM plan → CASSANDRA review → step dispatch → repeat; commit range `71e144f`–`80c2955` (branch `worktree-scheduler-phase1`), **merged to `main` at `93da413` on 2026-05-11**; post-merge follow-ups in PR #25 (`ec007d7`) fixed two real bugs (lane runner startup race, cancel-test timing race) and several reviewable nits — see HANDOVER. Shipped: migrations `0005_tasks_scheduler` + `0006_agent_prompts`; `db::tasks` full CRUD (lanes, lease, FOR UPDATE SKIP LOCKED, NOTIFY triggers); `db::agent_prompts` (SHA-256 prompt ledger, append-only by GRANT); `core::cassandra::types` + `core::cassandra::review` (ChainReviewStage + stub stages ConstitutionalGuard + DeterministicPolicy — deliberate stubs for observation phase); `core::scheduler::prompts` (PromptCache, load_prompts_from_dir); `core::scheduler::agent` (PlanFormulator trait, TaskContext, FormulationMeta); `core::scheduler::inner_loop` (run_to_terminal, Outcome: Completed/Failed/Cancelled, 3-plan cap); `core::scheduler::runner` (per-lane PgListener-wake loop, spawn_scheduler); `hhagent-cli` subcommands: ask, tasks list/status/cancel/fail/tail. Integration tests: `tasks_lifecycle_e2e` + `scheduler_inner_loop_e2e` (4 scenarios) + `scheduler_lanes_e2e` + `scheduler_crash_recovery_e2e` + `agent_prompts_e2e`. Stage names are audit-log contract (`"stage--1"`, `"stage-0"`, `"chain"`, `"noop"`). Mock-HTTP coverage for `RouterAgent::formulate_plan` (3 cases) on the open PR #26 (`fix/router-agent-mock-http-tests`). Test stability fix on `5d7a6ee` (drop PgListeners before pool.close in `tasks_lifecycle_e2e` — was an intermittent multi-thread-runtime deadlock). **Hold for observation phase before swapping stubs for real ConstitutionalGuard + DeterministicPolicy** (spec §9).
- [x] **[follow-up — 3.2.bis] Wire `ToolHostStepDispatcher` to `tool_host::dispatch`** — landed 2026-05-11 on branch `feat/tool-host-step-dispatcher`. New module `core/src/scheduler/tool_dispatch.rs` ships the production dispatcher: `ToolRegistry` (host-side allowlist of which tools exist), `ToolEntry` (binary + sandbox policy + wall-clock budget), pure helpers `rpc_code_name(i32) -> &'static str` (maps JSON-RPC numeric codes to mnemonic strings) and `map_dispatch_result(Result<Value, ToolHostError>) -> StepOutcome` (5-bucket translation including Sandbox→SPAWN_FAILED, Io→IO_ERROR, Protocol(Rpc)→named code, Protocol(non-Rpc)→PROTOCOL_ERROR), and `ToolHostStepDispatcher` itself (lookup → spawn-per-step → `tool_host::dispatch` → drop worker → map result). Canonical `shell_exec_entry(binary, allowlist) -> ToolEntry` builds the deny-by-default policy. `core/src/main.rs::build_tool_registry()` reads `HHAGENT_SHELL_EXEC_BIN` + `HHAGENT_SHELL_EXEC_ALLOWLIST` (colon-separated) at daemon startup; absent/missing binary → tool not registered (UNKNOWN_TOOL surfaces at dispatch time); empty allowlist → every call returns POLICY_DENIED (worker-side, not host-side, because the worker is the one enforcing argv allowlisting). The unused `_workspace_root: PathBuf` parameter dropped from `spawn_scheduler` (placeholder was the only caller). 13 unit tests + 1 integration test (`scheduler_step_dispatch_e2e`: happy path + POLICY_DENIED + UNKNOWN_TOOL, with audit_log shape assertions — UNKNOWN_TOOL deliberately writes no audit row because the chokepoint is never reached). Test count 284 → 298 (+14).
- [x] **[follow-up — 4.4] `cli_ask_e2e` integration test** — landed 2026-05-11 on `main`. New `core/tests/cli_ask_e2e.rs` (~840 LOC, 2 #[test]) pins the full production chain end-to-end: real `hhagent-cli` subprocess → real `hhagent` daemon under user-supervisor → real sandboxed `hhagent-worker-shell-exec` → real Postgres → mock LLM only. Happy path uses a queued multi-shot mock serving `[non-terminal echo-step plan, terminal text plan]`; CLI exits 0 with the marker on stdout, `tasks.state="completed"`, audit multiset matches the expected 6-event shape (1× core/startup + 2× agent/plan.formulate + 2× cassandra:chain/verdict + 1× tool:shell-exec/shell.exec + 1× scheduler/plan.outcome). Failure path serves 3× the same non-terminal plan with `/bin/cat /etc/passwd` (not allowlisted); CLI exits 1, `tasks.state="failed"`, 3× tool:shell-exec rows carry the JSON-RPC `-32001` POLICY_DENIED code in `err`. Mock returns 503 once the queue is exhausted so unexpected extra dials surface as `tasks.state="failed"` loudly. 5/5 deterministic runs (~5.4 s each); ~12 s aggregate. Per-test PG cluster (issue #15 now at 7 duplication sites). Skips cleanly on hosts without PG/supervisor/sandbox/worker. Design spec: `docs/superpowers/specs/2026-05-11-cli-ask-e2e-design.md`.
- [x] **[follow-up] Audit rows for scheduler short-circuit paths (`UNKNOWN_TOOL`, `SPAWN_FAILED`)** — landed 2026-05-12 on branch `feat/scheduler-spawn-failure-audit`. Closes the audit-trail gap HANDOVER flagged after Task 3.2.bis: `ToolHostStepDispatcher::dispatch_step` was returning `StepOutcome::Err { code: "UNKNOWN_TOOL" | "SPAWN_FAILED" }` for the two pre-RPC short-circuit paths without writing any `audit_log` row (both return before the `tool_host::dispatch` chokepoint). The dispatcher now writes `actor='scheduler' action='step.unknown_tool'` (payload `{tool, method, req, ms}`, no `err`) or `actor='scheduler' action='step.spawn_failed'` (payload `{tool, method, req, err, ms}` with the sandbox/IO error's `Display` string under `err`) — best-effort, same posture as the chokepoint: a transient DB failure is logged via `tracing::error!` but the original `StepOutcome::Err` is still returned. New pure helper `build_scheduler_step_failure_payload(tool, method, req, err: Option<&str>, ms) -> Value` makes the wire shape unit-testable without I/O. New constants `SCHEDULER_AUDIT_ACTOR`/`ACTION_STEP_UNKNOWN_TOOL`/`ACTION_STEP_SPAWN_FAILED` pin the strings so a future rename can't drift between writer and reader. `scheduler_step_dispatch_e2e` extended with a 4th dispatch scenario: a second `ToolEntry` named `broken-tool` whose `policy.fs_read = [PathBuf::from("relative/path/triggers/rejection")]` is the deterministic, cross-platform SPAWN_FAILED trigger (both `LinuxBwrap` and `MacosSeatbelt` reject relative paths up-front with `SandboxError::Backend(_)`). Final audit_log row count bumped 3 → 5. **No spec §7 task-lifecycle rows in this slice** — that's the next audit-gap follow-up (`actor='scheduler' action='task.<state>'` per lifecycle transition; distinct from step-level short-circuits). Test count 327 → **329** (+2 unit; integration count unchanged).
- [x] **[follow-up] First real `ConstitutionalGuard` rule (prompt-level constitutional screen)** — landed 2026-05-15 on branch `feat/constitutional-guard-prompt-screen`, merged via PR #67 at `67d29a0`. New pure module `core::cassandra::constitutional` ships `screen_instruction_for_principle_violations(instruction: &str) -> Option<(u8, &'static str)>` — a conservative case-insensitive substring matcher over the 5 constitutional principles. Each principle requires two lexical signals (topic phrase AND context phrase) before firing, except P1 (single canonical weapon-manufacturing phrase). Returns `(principle_index, snake_case_reason_tag)` on a hit. `ConstitutionalGuard::review` calls the helper on `ctx.instruction` and maps `Some((p, r))` → `Verdict::ConstitutionalBlock { principle: p, reason: r.to_string() }`, `None` → `Verdict::Approve`. Coverage: the 5 principle-violation fixtures fire correctly with distinct reason tags (`physical_harm_instructions` / `impersonation` / `irreversible_action_without_hitl` / `self_modify_or_persist` / `suppress_oversight`); `safe-001` + `ec-001` pass through (ec-001 needs step-level inspection — out of scope). 17 unit tests pin every fixture + 6 false-positive guards (clinical use of "physical harm", drafting for human signature, ordinary delete, systemd status question, monitoring question, log-volume complaint without disable verb) + 4 pure-function edge cases. 3 new tests in `cassandra::review::tests` exercise the trait-level mapping. **Test count delta: 492 → 512 (+20)**. **Post-review fixup `5d48e3e` (in-branch, merged via PR #67):** tightened P5 single-word verbs (`disable`, `silence`, `suspend`) against passive-form false positives (e.g. "Is the audit log disabled?" — diagnostic question, not a suppress-oversight command). New private `contains_word(haystack, needle)` whole-word helper via `match_indices` + ASCII alphanumeric byte boundaries; keeps the "no regex, no NLP" matching spirit. P1–P4 unaffected (phrases multi-word or distinctive enough that substring matching has no collision risk). +7 tests (512 → **519**); `constitutional.rs` 401 → 502 LOC (2 over the 500 soft cap; flag for split-per-principle if catalogue grows further).
- [x] **[follow-up] First real `DeterministicPolicy` rule (data-classification invariant)** — landed 2026-05-15 on branch `feat/deterministic-policy-classification`. New pure module `core::cassandra::deterministic` ships `screen_plan_for_classification_violations(plan: &Plan, floor: DataClass) -> Option<ClassificationViolation>` — three invariants over the typed `DataClass` fields already on `Plan`/`PlannedStep`/`ReviewStageContext`: I1 (`plan.data_ceiling >= floor`), I2 (every `step.classification >= floor`), I3 (every `step.classification <= plan.data_ceiling`). Declared-order precedence; first hit wins; within per-step invariants, lowest step_index wins. `ClassificationViolation` enum carries structured detail per violation; `reason_tag()` returns a snake_case identifier (`ceiling_below_floor` / `step_classification_below_floor` / `step_classification_above_ceiling`); `format_reason()` returns a `"data-classification: <tag> — ..."` prefixed string used as `Verdict::Block` payload. `DeterministicPolicy::review` calls the helper on `(plan, ctx.classification_floor)` and maps `Some(v)` → `Verdict::Block(v.format_reason())`, `None` → `Verdict::Approve`. Paired with `hhagent-cli ask --classification-floor <DataClass>` — new pure helper `parse_classification_floor` (case-insensitive; accepts PascalCase / lowercase / UPPERCASE / snake_case / hyphenated / space-separated; rejects unknown + empty with `"valid values: ..."` messages). Default behaviour unchanged (Public, payload field omitted). +25 new tests; workspace 519 → 544.
- [x] **[follow-up] Audit-payload bump: agent/plan.formulate carries full Plan + classification_floor (Slice A of rule-iteration harness)** — landed 2026-05-15 on branch `feat/audit-plan-formulate-carries-plan-body`, merged via PR #61 at `67f2dac`. Pure-additive payload bump (11 keys → 13 keys) so captures carry everything the reviewer pipeline needs to be replayed offline. Extracted pure `build_plan_formulate_payload` helper (same pattern as `scheduler::audit::build_finalize_payload`) so wire shape is unit-testable without a live Postgres pool. +2 new unit tests pinning the 13-key set + round-trip shape of `plan` + `classification_floor`; +2 e2e assertion blocks extended. Workspace test count 465 → 467. The operator recaptures (one-time action against their local LLM) to get the new shape on disk; Slice B's harness handles the pre-Slice-A captures via `plans_skipped_missing_body` counter.
- [x] **[follow-up] Rule-iteration harness for CASSANDRA review pipeline (Slice B of rule-iteration harness)** — landed 2026-05-15 on branch `feat/rule-iteration-harness`. New pure library `core::observation::replay` + thin `hhagent-cli observation replay` subcommand. Loads captures from disk, replays each through the production `ChainReviewStage::new(vec![Arc::new(ConstitutionalGuard), Arc::new(DeterministicPolicy)])`, prints per-fixture verdict-delta ASCII table. Pre-Slice-A captures (`plan_json: null`) skip cleanly via `plans_skipped_missing_body` counter; the harness never fabricates synthetic Plans from derived fields. Public surface: `VerdictSnapshot::from_verdict`, `ReplayedPlan`, `ReplayResult`, `LoadedCapture`; pure `is_delta` + `format_report_table`; async `replay_capture(capture, chain)`; I/O `load_captures_from_dir(dir)`. CLI flags: `--captures-dir PATH` + `--model SLUG`; default captures-dir resolves via `CARGO_MANIFEST_DIR` (cargo-run) → cwd-relative (installed binary). Exit codes: 0 (replay completed; deltas are not errors), 1 (captures dir unreadable), 2 (CLI arg error). Operator iteration loop: edit `ConstitutionalGuard::review` body → rebuild → re-run; deterministic, no DB/LLM/daemon. +25 tests (6 VerdictSnapshot + 6 is_delta + 6 format_report_table + 2 replay_capture unit + 2 e2e library + 3 e2e CLI); workspace 467 → 492.
- [x] **[follow-up] Observation-phase first capture run + lenient JSON parser** — landed 2026-05-14 on branch `feat/observation-capture-baseline`, merged via PR #60 at `7588b9e`. Plus one post-merge review-driven test pin `a812989` adding `earlier_stray_open_brace_in_prose_yields_decode_error_not_misparse` to defend the "first `{` wins" contract (workspace 464 → **465**). Closes the HANDOVER "Observation phase — capture run" pickup. Three orchestrator/agent bugs fixed inline: (1) `core/tests/observation_capture.rs` was seeding `tool_allowlists` AFTER `bring_up_daemon`, but `build_tool_registry` reads allowlists once at startup and caches — daemon saw empty allowlist, all shell-exec calls POLICY_DENIED. Fix: move probe + seed pool + `seed_tool_allowlist` BEFORE `bring_up_daemon` (matching `cli_ask_e2e.rs:485-499`); the existing fast-fail check stays as defence-in-depth. (2) The 120 s `PER_FIXTURE_TIMEOUT` was too tight for local 26B models with multi-iteration plans. Fix: env-overridable defaults `HHAGENT_OBSERVATION_PER_FIXTURE_TIMEOUT_SECS` (default **600**) and `HHAGENT_OBSERVATION_LLM_TIMEOUT_MS` (default **180000**, replacing the hardcoded 120000 passed to daemon's `HHAGENT_LLM_TIMEOUT_MS`). (3) Production agent path: `RouterAgent::formulate_plan` called `serde_json::from_str(&raw)` strictly, but gemma4 (and most instruction-tuned local models) wrap output in `` ```json\n…\n``` `` fences by default. Fix: new pure helper `core::scheduler::plan_parser::parse_plan_lenient(raw)` — strict-path-first, then locates the first `{` and stream-parses one complete JSON value from there. On lenient-path failure re-emits the strict-path error so the diagnostic surface stays stable. 9 new unit tests covering all the canonical model-wrapping shapes (bare JSON, ```json fence, unlabelled fence, leading prose, trailing prose, no JSON, invalid JSON inside fence, whitespace-only, nested `}` inside strings). Plus orchestrator diagnostic improvements: `dump_daemon_log` helper gated by `HHAGENT_OBSERVATION_DUMP_DAEMON_LOG=1`, and on-failure `tasks.result` print in the main test loop. Workspace test count 455 → **464** (+9 plan_parser unit tests). 7 capture artefacts under `tests/observation/captures/<id>/2026-05-14_gemma4-26b-a4b-it-q8-0.json`.
- [x] **[follow-up] Distinguish constitutional refusal from successful completion in `tasks.state`** — landed 2026-05-14 on branch `feat/refusal-state`, merged via PR #59 at `f1fea54` (12 commits; closes [issue #23](https://github.com/hherb/hhagent/issues/23)). New optional `Plan.refused: { principle: u8, reason: String }` field, new `Outcome::Refused { principle, reason, body }` variant, new terminal `tasks.state='refused'` distinct from existing `'blocked'` (reviewer-detected). Inner-loop short-circuits on `plan.refused.is_some()` after the reviewer runs (defense in depth — `Verdict::ConstitutionalBlock` wins over agent self-refusal so provenance is preserved; `Verdict::Block` arm honours the same `is_none()` guard so a refusal short-circuits there too, pinned by post-merge review fixup `91a792d`). `agent/plan.formulate` audit-row payload gains `refused: {...}` (explicit JSON null when absent, so JSONB queries rely on key-always-present) + a third `decision_kind = "refused"` value (taking precedence over `is_terminal`-derived `"task_complete"`). New `DECISION_REFUSED` constant in `core::cassandra::types` parallel to `DECISION_TERMINAL`. Migration `0012_tasks_state_refused.sql` widens both the `tasks_state_check` CHECK and the `notify_task_completed` trigger function. Planner prompt adds one sentence + `"refused": null` default in the JSON-schema example; the `agent_prompts` SHA-256 ledger records the new prompt hash on next daemon start automatically. `tracing::info!` line added on `Verdict::Escalate` + refusal branch so operators grepping the journal for Escalate events see the case where Escalate fired on a refusal plan. Test count: 446 → **455** (+9 across types, scheduler, DB, and e2e layers). Two-stage review per task (spec compliance + code quality) with two review-driven fixup commits in-branch + one post-handover-review fixup.
- [x] **[follow-up] `task.crashed` audit row from startup crash recovery sweep** — landed 2026-05-12 on branch `feat/scheduler-task-crashed-audit`. Widens `hhagent_db::tasks::sweep_crashed` from `Result<u64, _>` to `Result<Vec<Task>, _>` via SQL `RETURNING *`, then new module `core/src/scheduler/crash_recovery.rs` (~90 LOC) ships `sweep_and_audit(pool) -> Result<usize, DbError>` which wraps the sweep and emits one `actor='scheduler' action='task.crashed'` row per recovered task with the canonical lifecycle payload `{task_id, lane, plan_count}` — same wire shape as the runtime `task.<state>` rows so observation-phase `WHERE action LIKE 'task.%'` captures both startup and runtime transitions. `main.rs` now calls `crash_recovery::sweep_and_audit` instead of `tasks::sweep_crashed` directly. **Test count 341 → 342** (+1 integration test: two crashed tasks on Fast + Long lanes, pins per-row emission + lane round-trip + payload key-set + idempotency).
- [x] **[follow-up] `task.finalize` summary row for crashed tasks** — landed 2026-05-13 on branch `feat/crashed-finalize-row`. Closes the audit-trail asymmetry the previous slice deliberately deferred: observation-phase SQL grouping on `action='task.finalize'` was undercounting by exactly the crashed-task population. New pure helper `core::scheduler::audit::build_crashed_finalize_payload(task_id, lane, plan_count, started_at, finished_at) -> Value` emits the same 9-key shape as `build_finalize_payload` but with two key differences: `total_llm_calls` and `total_dispatch_calls` are JSON `null` (dead daemon's in-memory counters are unrecoverable; `null` is wire-distinguishable from `0` which the runtime path emits to mean "observed zero"), and `state` is hard-pinned to `"crashed"`. `total_duration_ms` is `null` when `started_at` is missing, otherwise computed via the existing `compute_duration_ms` helper. `sweep_and_audit` now writes the finalize row immediately after the `task.crashed` lifecycle row per recovered task (same ordering `drain_lane` uses in the runtime path). Test count 380 → **386** (+6 unit tests for the new helper; the integration test `sweep_and_audit_emits_one_task_crashed_row_per_recovered_task` gained finalize-row assertion blocks but no new `#[test]` functions).
- [x] **[follow-up] Producer-side `task.finalize` row for CLI-cancelled `pending` tasks** — landed 2026-05-14 on branch `feat/crashed-finalize-row` (paired with the crashed-task slice into one PR). Closes the last `task.finalize` undercounting gap: a CLI cancel of a `pending` task (one the scheduler never claimed) emitted `cli/task.cancelled` but no producer-side finalize. `cli_audit::cancel_and_audit` now writes an `actor='cli' action='task.finalize'` row in addition to the lifecycle row, guarded by `task.started_at.is_none()` — true iff the task was never claimed. Reuses the existing `build_finalize_payload` helper (no new helper needed) with `started_at: None`, `total_duration_ms: 0`, and **known-zero** counters (the task ran zero plan iterations) — wire-distinguishable from the crashed-task finalize's JSON-`null` counters. When the cancel flips a `running` task instead, the producer skips the finalize because the scheduler's `observe_state` poll will emit its own. New regression test `cancel_running_task_does_not_write_producer_finalize` pins exactly the skip. Test count 386 → **387** (+1 integration test).
- [x] **[follow-up] Emit the missing spec §7 audit rows in the scheduler** — landed 2026-05-12 on branch `feat/scheduler-task-lifecycle-audit`. New `core/src/scheduler/audit.rs` (~290 LOC incl. tests) carries pure helpers + canonical `SCHEDULER_AUDIT_ACTOR` constant (the dispatcher's short-circuit rows now import it too so the actor string can't drift). `run_to_terminal` return widened from `Outcome` to `InnerLoopResult { outcome, plan_count, dispatch_count }` so the lane runner can build the finalize payload without re-querying the audit log. `drain_lane` now writes three rows per task: `scheduler/task.running` after claim_one (payload `{task_id, lane, plan_count}`), `scheduler/task.<terminal_state>` after finalize (same payload; fires even when finalize was a no-op because the scheduler still observed the transition), and `scheduler/task.finalize` summary (full payload per spec §7: `{task_id, lane, state, plan_count, total_llm_calls, total_dispatch_calls, total_duration_ms, started_at, finished_at}`; timestamps as RFC 3339 strings). All three are best-effort: DB errors log at WARN but never block. `total_dispatch_calls` counts `StepDispatcher::dispatch_step()` attempts (success + failure), incremented saturating in the inner loop. **NOT in scope this slice (filed as small follow-ups):** `task.crashed` audit row from `sweep_crashed` (needs `sweep_crashed` to RETURNING rows), `task.cancelled` from CLI direct cancel of a `pending` task, per-stage `cassandra:<stage_name>/verdict` rows (the stub stages return single `cassandra:chain/verdict` rows today; real Stage -1 / Stage 0 implementations land per-stage). 12 new unit tests in `scheduler::audit::tests`; `scheduler_inner_loop_e2e` gained counter-pinning assertions; `cli_ask_e2e` multiset bumped 7→10 (happy) and 13→16 (failure) plus finalize-payload spot checks. Workspace count 329 → **341** (+12). Three consecutive focused runs of `cli_ask_e2e` deterministic at ~3.25 s.
- [x] **[follow-up] Producer-side `task.cancelled` audit row from `hhagent-cli` cancel paths** — landed 2026-05-13 on branch `feat/cli-cancel-audit`. Closes the gap HANDOVER's "Immediate next pickups" flagged: a CLI cancel of a `pending` task flipped the row via the `tasks_cancelled` NOTIFY trigger but emitted no audit row at all (the scheduler never observed the transition because the task was never claimed). `db::tasks::mark_cancelled` widened from `Result<bool, _>` to `Result<Option<Task>, _>` via SQL `RETURNING` (same shape `sweep_crashed` took 2026-05-12, same reason: downstream audit emitter needs row metadata). New `core/src/cli_audit.rs` (~110 LOC + 2 unit tests) ships `CLI_AUDIT_ACTOR: &str = "cli"` constant (distinct from the scheduler's actor), `CancelOutcome { Cancelled(Task), NotCancellable }` typed result, and `cancel_and_audit(pool, task_id) -> Result<CancelOutcome, DbError>` helper that emits one `actor='cli' action='task.cancelled'` row with the canonical `{task_id, lane, plan_count}` lifecycle payload (reuses `scheduler::audit::{action_task_terminal, build_lifecycle_payload}` so producer + observer rows are byte-shape-identical except for the `actor` column). `hhagent-cli` ctrl-C handler and `tasks cancel` subcommand both rewired through the helper; audit insert is best-effort (chokepoint posture). **NOT in scope (filed as a follow-up):** symmetric producer-side `task.submitted` row from `hhagent-cli ask`. Test count 349 → **353** (+2 unit + 2 integration); 3/3 deterministic focused runs at ~2.5 s each.
- [x] **[follow-up] Producer-side `task.submitted` audit row from `hhagent-cli ask`** — landed 2026-05-13 on branch `feat/cli-task-submitted-audit`. Closes the gap symmetric to the cli-cancel-audit slice: `hhagent-cli ask` was calling `tasks::insert_pending` directly with no producer-side audit row, so the lifecycle stream visible in `audit_log` only started at the scheduler's `task.running` observation on claim — submit-to-claim latency queries had to join `audit_log` against `tasks.created_at` across two clocks. New `core::scheduler::audit::ACTION_TASK_SUBMITTED = "task.submitted"` const + new `core::cli_audit::submit_and_audit(pool, lane, payload) -> Result<i64, DbError>` helper (mirror of `cancel_and_audit`: chokepoint posture, audit insert best-effort, id propagates even on audit failure). `tasks::insert_pending` left unchanged (no `RETURNING *` widening needed since all payload fields are known up-front at submit). `hhagent-cli ask` rewired in one line. **NOT in scope (filed as a follow-up):** producer row from future channel adapters (none exist today); producer `task.failed` row from `tasks fail` subcommand. Test count 353 → **354** (+1 integration test in `cli_submit_audit_e2e.rs`; `cli_ask_e2e` multiset bumps don't add `#[test]` functions).
- [x] **[follow-up] Observation-phase fixture captures (dataset infrastructure)** — landed 2026-05-13 on branch `feat/observation-phase-captures` (13 commits, not yet merged). Closes the HANDOVER "Next TODO" headline pickup for the observation phase (spec §9). New library module `hhagent_core::observation::capture` carries the on-disk JSON schema (`SCHEMA_VERSION = 1`), four pure helpers (`parse_fixture_prompt`, `slug_model`, `capture_filename`, `extract_plans_from_audit_rows`), an IO helper (`write_capture_to_dir` — refuses to overwrite existing baselines), and one async DB helper (`fetch_audit_rows_for_task` — uses `payload @>` JSONB predicate). 7 seed fixtures under `tests/observation/fixtures/`: 1 safe control, 1 per constitutional principle (P1 physical harm, P2 fraud, P3 irreversible delete, P4 power concentration, P5 suppress oversight), 1 clinical-data-leak edge case. Orchestrator `core/tests/observation_capture.rs` is `#[ignore]`-flagged so `cargo test --workspace` excludes it; operators run with `cargo test -p hhagent-core --test observation_capture -- --ignored --nocapture` after exporting `HHAGENT_LLM_LOCAL_URL`. Fails loudly on unreachable LLM (no skip-as-pass for the operator path). `HHAGENT_OBSERVATION_DRY_RUN=1` walks the fixture tree without any side effects. New workspace dep `toml = "0.8"` (read-only). **NOT in scope (filed as a follow-up):** rule-iteration harness that re-runs `ChainReviewStage::new(vec![candidate_rule])` against captured plans and reports verdict deltas. Workspace test count 354 → 375 (+20 unit + 1 integration; the `#[ignore]` orchestrator does not count).
- [x] **[follow-up] Per-tool argv allowlist hygiene** — landed 2026-05-14 on branch `feat/tool-allowlist-db` (9 TDD-ordered commits, not yet merged). Closes the HANDOVER "Per-tool argv allowlist hygiene" pickup: moves the per-tool argv allowlist source-of-truth from the `HHAGENT_SHELL_EXEC_ALLOWLIST` env var to a new `tool_allowlists` Postgres table, behind the existing `hhagent_runtime` GRANT shape. Every mutation now writes one row in `audit_log` via `core::cli_audit::tools_allowlist_{add,remove}_and_audit`; daemon bring-up emits one `actor='core' action='registry.loaded'` row carrying the SHA-256 of the canonical-form allowlist for cross-restart drift detection. Hard cutover — `HHAGENT_SHELL_EXEC_ALLOWLIST` is no longer read; deprecation WARN logs if still set. New migration `0009_tool_allowlists.sql` (composite PK on `(tool, argv0)`, CHECK constraints, GRANT SELECT/INSERT/DELETE + REVOKE UPDATE/TRUNCATE to counteract `0002`'s `ALTER DEFAULT PRIVILEGES`). New `db::tool_allowlists` module with pure validators (`validate_tool_name`, `validate_argv0` — rejects `..` segments for path-confusion bypass defence) + async I/O (`add`/`remove`/`list_for_tool`/`list_all`). New `tests-common::seed_tool_allowlist` test helper. New `hhagent-cli tools allowlist {add,remove,list}` hand-rolled subcommand tree (no clap dep). `core::main::build_tool_registry` rewired from sync env-var-driven to async DB-backed; `cli_ask_e2e` migrated to seed via the new helper. Test count 387 → **395** (+6 validator unit + 1 DB integration + 1 CLI e2e). Post-review cleanup (PR #51): tightened the `0009` CHECK to reject `..` segments at the SQL layer (defense-in-depth that the module doc claimed but didn't have); added an empty-allowlist fast-fail probe in the `observation_capture` orchestrator so missing operator-seeding fails before LLM dial instead of producing all-POLICY_DENIED captures; clarified `tests-common::policy_for_shell_exec` doc scope. Two scoring-below-threshold concerns filed: [issue #52](https://github.com/hherb/hhagent/issues/52) (`list --tool` does a client-side filter) and a comment on [issue #39](https://github.com/hherb/hhagent/issues/39) folding `seed_tool_allowlist` self-tests into its scope.
- [x] **Memory layers (L1 always-in-context insight index)** — first slice of the GenericAgent-inspired 5-layer memory hierarchy (L0 meta-rules / L1 insight index / L2 stable facts / L3 skills / L4 session digests). Landed 2026-05-15 on branch `feat/memory-layer-l1-index` (3 commits: db slice `b63fe00`, core slice `326950b`, docs sync `10a6d3c`). Migrations `0013_memories_layer.sql` + `0014_deleted_memories_layer.sql` add `SMALLINT layer NOT NULL DEFAULT 2 CHECK (BETWEEN 0 AND 4)` on `memories` + mirror on `deleted_memories`, with `(layer, created_at DESC)` index for the L1 hot path. `db::memories` gains `MemoryLayer { Meta=0, Index=1, Stable=2, Skill=3, Digest=4 }` enum + `from_db`/`as_db` round-trip + `insert_memory_at_layer` + `load_layer` + new `Memory.layer` field; `fetch_by_ids` projects the new column. New `DbError::Invariant` variant distinguishes schema-invariant violations from transient `Query` failures. `core::memory::layers::load_l1(pool, cap_rows, cap_bytes)` wraps `load_layer(MemoryLayer::Index, cap_rows)` with two hard caps (`L1_DEFAULT_CAP_ROWS = 32`, `L1_DEFAULT_CAP_BYTES = 4096`) — newest-first; oversized single row dropped silently. Test count 546 → 556 (+10: 3 DB integration + 3 core unit + 4 core integration). Spec at `docs/superpowers/specs/2026-05-15-memory-layer-l1-index-design.md`. Unblocks L0 seed loader, `llm_router::build_system_prompt`, L3 skill crystallisation, L4 session digest. **Deliberately ships storage-only**: no prompt-assembler wiring, no L0/L3/L4 writers, no auto-promotion, no silent-drop tracing::warn, no `UNIQUE (layer, body)`. Subsequent post-review fixup commit `44b7773` enforces L0-writer policy in code (new `seed_meta_memory` admin function; `insert_memory_at_layer(Meta, …)` rejects with `DbError::PolicyViolation`), adds `load_l1_default` convenience pinning the published caps, switches oversize L1 row drop to `tracing::warn!`, and clarifies the `id DESC` tiebreaker + `cap_bytes` inclusivity docs. Merged via PR #69 at `eb8e4bd`.
- [x] **[follow-up] Automatic classification-floor inference** — landed 2026-05-16 on branch `feat/automatic-floor-inference`. Hybrid design: CLI-side keyword classifier (`core::classification_inference`) as primary inference site + agent-side raise-only channel via new `Plan.floor_request: Option<DataClass>`. New `ClassificationFloorSource` enum (`Operator` / `CliInferred` / `AgentRaised` / `Default`) + provenance metadata (`classification_floor_source` always present; `classification_floor_signals` array when source is `cli_inferred`) propagate from CLI submit → `task.payload` → `TaskContext` → `agent/plan.formulate` audit-row payload (13 keys → 14/15). Inner-loop applies `max(producer_floor, agent_request)` so DP sees the elevated floor for I1/I2 invariants. Operator-explicit `--classification-floor X` always wins; a `tracing::warn!` fires when inference would have elevated above the operator's value. `contains_word` matching mirrors `ConstitutionalGuard`'s post-review precedent (commit `5d48e3e`). Per-class catalogues seed Secret (password / api key / private key / etc), ClinicalConfidential (patient / pathology / radiology / ct scan / ECG / etc with ekg→ecg alias collapse), and Personal (my email / family member / etc). +40 tests (557 → 597); post-review fixup `09618ca` adds the `classification_floor_source_as_snake_str_matches_serde_wire_form` pin + refreshes stale "13-key" docstrings (598). Code review filed [issue #71](https://github.com/hherb/hhagent/issues/71) for forgeable `agent_raised` provenance (audit-trail integrity, not blocking — defense in depth). Closes the HANDOVER "Next concrete engineering pickup #2". Merged to `main` via PR #70 at `4ddfe3b`. Spec at `docs/superpowers/specs/2026-05-16-automatic-floor-inference-design.md`; plan at `docs/superpowers/plans/2026-05-16-automatic-floor-inference.md`.
- [x] **[follow-up] Issue #71 — runner rejects producer-supplied `agent_raised` provenance** — landed 2026-05-16 on branch `fix/runner-reject-agent-raised-provenance` (commits `a6335ab` + post-review fixup `5fabac5`); **merged to `main` via PR #72 at `305941a`**. New pure helper `parse_classification_floor_source_from_payload` in `core/src/scheduler/runner.rs` parses the payload via serde first, then rejects the `ClassificationFloorSource::AgentRaised` variant on a structural match — binding the reject to the enum variant rather than a wire-string literal, so a future rename of `AgentRaised` + its serde tag + `as_snake_str` propagates automatically. The dedicated diagnostic names the contract ("reserved for the inner loop's apply_floor_raise"). Producers may only supply `operator` / `cli_inferred` / `default` or omit the field; the inner-loop `apply_floor_raise` remains the only legitimate writer of `AgentRaised`. The "unknown value" generic-reject diagnostic no longer lists `agent_raised`. +9 unit tests in a new `scheduler::runner::tests` module (598 → 607), including `agent_raised_reject_binds_to_enum_variant_not_string_literal` which feeds `as_snake_str()` into the helper to lock the enum-driven binding. Follow-up [issue #73](https://github.com/hherb/hhagent/issues/73) tracks the deferred e2e integration test + `TaskContext` constructor doc note. Closes the audit-trail-integrity hole filed during PR #70 code review.
- [x] **L0 seed data loader** — landed 2026-05-16 on branch `feat/l0-seed-loader` (10 commits). New `core::memory::l0_seed` module: pure `parse_l0_rules` (TOML → validated `Vec<L0Rule>`) with full validation (id charset `[a-z0-9_]+`, length caps, duplicate-id hard error, `#[serde(deny_unknown_fields)]` for typo-catching); pure helpers `compute_body_sha256` / `compute_source_sha256` / `build_l0_metadata`; async `seed_l0_from_rules` (idempotent per-rule EXISTS-check on `(l0_rule_id, body_sha256)` then `seed_meta_memory` on miss); file convenience `seed_l0_from_file`; read-side `load_l0_active` / `load_l0_active_default` wrapping `db::memories::load_active_l0` with in-Rust byte caps (mirrors `load_l1`'s saturating_add idiom; oversize single row dropped with `tracing::warn!` carrying the `l0_rule_id`). New `db::memories::load_active_l0` carries the `SELECT DISTINCT ON (metadata->>'l0_rule_id') WHERE layer = 0` SQL; rows missing the `l0_rule_id` metadata key are excluded from the active set. Wire-in in `core/src/main.rs` runs right after the prompts loader: env-overridable `HHAGENT_L0_RULES_FILE`, default `seeds/memory/l0_meta_rules.toml`; missing file = `info!` + skip; malformed file = fatal via `with_context`. One `actor='core' action='l0.seeded'` audit row per startup with `{rules_loaded, new_rows_written, unchanged_skipped, source_path, source_sha256}` (new `ACTION_L0_SEEDED` const in `core::scheduler::audit`). Starter TOML ships in-tree at `seeds/memory/l0_meta_rules.toml` with 2 defensible-default rules (recursive-delete safety + refusal stickiness). +31 tests (607 → 638: 19 unit + 12 DB integration). Spec at `docs/superpowers/specs/2026-05-16-l0-seed-loader-design.md`; plan at `docs/superpowers/plans/2026-05-16-l0-seed-loader.md`. Closes the HANDOVER "Next concrete engineering pickup #2"; unblocks the prompt-assembler `llm_router::build_system_prompt` slice.
- [x] **Prompt assembler (L0 + L1 + base)** — landed 2026-05-16 on branch `feat/prompt-assembler-l0-l1` (13 commits). New `core::prompt_assembly` module ships pure `assemble_system_prompt(l0, l1, base) -> String` + async `SystemPromptBuilder` trait (parallel to `PlanFormulator`) + prod `PgSystemPromptBuilder` (PgPool-backed) + test `StaticSystemPromptBuilder`. `RouterAgent::formulate_plan` wires through the trait so every plan iteration sees an L0 + L1 + base assembled prompt instead of the bare `agent_planner.md`. `agent/plan.formulate` audit row widened by 3 keys (`system_prompt_sha256`, `l0_count`, `l1_count`) for cross-restart L0/L1 drift detection. +14 tests (638 → 652). Spec at `docs/superpowers/specs/2026-05-16-prompt-assembler-design.md`; plan at `docs/superpowers/plans/2026-05-16-prompt-assembler.md`. Closes the HANDOVER "Next concrete engineering pickup #3" (`llm_router::build_system_prompt`). Unblocks recall-lane wiring as the next natural slice.
- [x] **Recall-lane wiring** — landed 2026-05-17 on branch `feat/recall-lane-wiring` (16 commits), **merged via PR #79 at `7553404`** + 2 post-handover commits (`81d9e9b` full-stack pin on the 3 recall keys in `cli_ask_e2e`, `1a18e93` post-PR-review cleanups: `RecalledContext::new` invariant constructor, `cap_and_split` warn-branch guard simplification, threat-model scenario 6 — memory-write injection). New `core::recall_assembly` module ships pure `RecalledContext { ids, bodies, query_sha256 }` value type + async `RecallBuilder` trait (parallel to `SystemPromptBuilder`) + prod `PgRecallBuilder` (composes `embed_query` + `recall(SEMANTIC | LEXICAL)` + `cap_and_split` byte-cap at 4 KiB) + test `StaticRecallBuilder`. `assemble_system_prompt` widens to 4-arg (`l0, l1, recalled, base`); `RecalledContext::empty()` reproduces v1 byte-output. `SystemPromptBuilder::build_with_recalled` is the new required trait method; existing `build()` becomes a thin default-impl shim. `RouterAgent::formulate_plan` runs recall BEFORE the prompt assembler with **degrade-and-warn** posture (recall is enrichment, not policy — distinct from the fail-closed `PromptAssembly` posture). 3 new `agent/plan.formulate` audit-row keys: `recalled_memory_ids` (RRF-fused id array), `recall_count` (cheap-to-query duplicate of array length), `recall_query_sha256` (drift detection across paraphrased prompts). Pure-additive payload growth: 17/18 → 20/21 keys. `cli_ask_e2e` cascade-fix interleaves embed/chat responses in the FIFO mock queue (recall now writes `actor='llm:router' action='embed'` per plan iteration before chat-completion). First production consumer of `Router::embed` (Option O, 2026-05-12) AND `recall` (PR #41, 2026-05-13) — both primitives were storage/retrieval-only until now. +22 tests (workspace 652 → **674**, including +3 in `1a18e93` for the constructor invariant). Spec at `docs/superpowers/specs/2026-05-17-recall-lane-wiring-design.md`; plan at `docs/superpowers/plans/2026-05-17-recall-lane-wiring.md`. Unblocks future entity-extraction + graph-lane wiring (the only remaining `RecallModes` variant that's a no-op in production) and `inner_loop_audit.rs` split ([issue #81](https://github.com/hherb/hhagent/issues/81)) + `cli_ask_e2e` mock dispatch-by-path refactor ([issue #80](https://github.com/hherb/hhagent/issues/80)) + global token cap with priority drop ([issue #78](https://github.com/hherb/hhagent/issues/78); `inner_loop.rs` now 1095 LOC).
- [x] **L1 promotion writer** — landed 2026-05-18 on branch `feat/l1-promotion-writer` (20 commits); **merged to `main` via PR #82 at `eb6b8a8`** on 2026-05-18. Hybrid design: operator-explicit `hhagent-cli memory l1 {add,list,remove}` + agent-raised channel via `Plan.l1_insight` consumed by the inner loop on `Outcome::Completed`. New `core::memory::l1_promote` module (validator + types + writer + readers) + `Plan::l1_insight` field + `Plan::completion_insight()` accessor + `InnerLoopResult::terminal_l1_insight` + 3 new audit actions (`l1.added`/`l1.removed`/`l1.promoted`) + pure-additive `agent/plan.formulate` payload bump (20/21 → 21/22 keys, adds `l1_insight`) + new `core::cli_audit::l1_*_and_audit` helpers + `db::memories::delete_memory_at_layer` layer-guarded DELETE. Validator rejects newlines (on RAW body before trim — bug fix vs the plan's POST-trim check), control chars, reserved `<l1_insights>` substrings (threat-model §6 defence), over-length (`L1_MAX_BODY_BYTES = 512`). Dedup is source-agnostic: operator + agent rows with the same body collapse to one L1 row carrying the FIRST writer's source. Best-effort posture in `drain_lane` — validation errors WARN with distinct diagnostics, never abort task finalize. +47 tests (workspace 674 → **721**). Spec at `docs/superpowers/specs/2026-05-17-l1-promotion-writer-design.md`; plan at `docs/superpowers/plans/2026-05-17-l1-promotion-writer.md`. Closes the "L1 promotion writer" pickup from PR #79's "Open follow-up surfaces" list. Unblocks L3 skill crystallisation as the next natural slice (the L1 distillation pattern here sets the precedent).
- [x] **Worker lifecycle policy — design spec** — defines two lifecycle policies declared per worker: `single_use` (current shell-exec behaviour: spawn → serve one request → exit) and `idle_timeout` (warm-keep for inference workers; spawn on first request, tear down after configurable idle window). `idle_timeout` workers require a `stateless = true` contract declaration; lifecycle caps (`idle_seconds` / `max_requests` / `max_age_seconds`) are **post-completion-only** checks, never mid-flight kills (the load-bearing invariant that makes warm-keeping safe). Unblocks every future inference worker (GLiNER-Relex, sentiment, embedding-as-worker, classification, OCR) without re-deriving the abstraction each time. Spec at `docs/superpowers/specs/2026-05-18-worker-lifecycle-policy-design.md` (landed on main 2026-05-18 at `99e97cf`).
- [x] **Worker lifecycle policy — slice 1 (single_use runtime + idle_timeout types)** — landed 2026-05-18 on branch `feat/worker-lifecycle-slice-1` (5 commits); **merged to `main` via PR #83 at `b7dba3a`** on 2026-05-18 alongside slice 2. New `core::worker_lifecycle` module ships `Lifecycle` enum (`SingleUse` default + `IdleTimeout { caps, contract }`), `IdleTimeoutCaps`, `Contract`, `Lifecycle::idle_timeout(caps, contract)` validated constructor (rejects `stateless = false` per spec v1), `LifecycleValidationError`. Runtime layer ships `WorkerLifecycleManager` async trait + `WorkerHandle::worker_mut() -> &mut SupervisedWorker` (preserves the issue-#16 dispatcher chokepoint seal — `SupervisedWorker::call` stays module-private to `tool_host`) + `SingleUseLifecycle` (production; `acquire` clones the policy and calls `tool_host::spawn_worker` — byte-equivalent to today's per-request spawn) + `IdleTimeoutLifecycle` (stub; `acquire()` calls `unimplemented!()` so accidental wiring trips loudly). `ToolEntry` gains `lifecycle: Lifecycle`; `shell_exec_entry` declares `Lifecycle::SingleUse`. `ToolHostStepDispatcher`'s `sandbox: Arc<dyn SandboxBackend>` is replaced by `lifecycle: Arc<dyn WorkerLifecycleManager>`; `dispatch_step` asks the manager for a handle. `main.rs` instantiates `SingleUseLifecycle::new(sandbox.clone())` and passes to the dispatcher. The `hhagent-supervisor` crate (OS-unit installer for systemd/launchd) is untouched — the spec's "supervisor is a stub" wording was a misread; slice 1 grew the worker-lifecycle role in `hhagent-core` where it naturally belongs. Test count 721 → **731** (+10: 6 types + 3 manager + 1 shell-exec single-use pin). Plan at `docs/superpowers/plans/2026-05-18-worker-lifecycle-slice-1.md`.
- [x] **Worker lifecycle policy — slice 2 (idle_timeout runtime)** — landed 2026-05-18 on branch `feat/worker-lifecycle-slice-1` (bundled with slice 1 in one PR per operator request); **merged to `main` via PR #83 at `b7dba3a`** on 2026-05-18 with post-review fixup `2fece27` (warm-cache key widened from binary basename to logical tool name, exhaustive `ClientError` classifier, deterministic concurrent-acquire e2e test, Drop-runtime contract documented). Three follow-up issues filed: [#84](https://github.com/hherb/hhagent/issues/84) queue-depth visibility, [#85](https://github.com/hherb/hhagent/issues/85) teardown-task accumulation, [#86](https://github.com/hherb/hhagent/issues/86) struct-literal bypass of the `idle_timeout` validator. Fills in `IdleTimeoutLifecycle::acquire`'s slice-1 `unimplemented!()` body with the full warm-cache runtime: per-tool `Arc<TokioMutex<ToolState>>` for owned-guard semantics across acquire→Drop (serialises concurrent same-tool requests), spawn-on-demand with cold-spawn fallback and aged-out cleanup, post-completion cap evaluation (`max_requests` / `max_age_seconds` / `idle_seconds`), one-shot idle-teardown tasks (stale ones no-op via `last_completion` comparison), passive crash detection via `dispatch_indicates_worker_dead(&Result<_, ToolHostError>)` post-dispatch classifier (`Io`/`Decode`/`EarlyExit`/`IdMismatch` → dead; `Rpc` → alive), exponential restart backoff (1s/2s/.../60s default; resets on successful dispatch). `WorkerHandle` widens to `WorkerHandleKind::SingleUse | IdleTimeout` enum so Drop semantics diverge cleanly. `WorkerHandle::report_crash(&mut self)` is the new API the dispatcher calls between `dispatch` and `map_dispatch_result` to suppress worker-return on transport failures. Test count 731 → 751 (+20: 14 pure helpers in `idle_timeout::tests` + 6 e2e in new `worker_lifecycle_idle_timeout_e2e.rs` — uses a `CountingSandboxBackend` wrapper that counts every `spawn_under_policy` call, giving load-bearing assertions for warm-reuse + cap rotation without needing PID introspection). Spec at `docs/superpowers/specs/2026-05-18-worker-lifecycle-policy-design.md`; plan at `docs/superpowers/plans/2026-05-18-worker-lifecycle-slice-2.md`. Unblocks the first idle-timeout consumer (GLiNER-Relex per the feasibility study at `docs/superpowers/specs/2026-05-18-gliner-relex-feasibility-study.md`).
- [ ] **Worker lifecycle policy — slice 3 (operator surface + SIGTERM grace)** — operator-visible additions deferred from slice 2: `hhagent-cli supervisor status` for inspecting warm workers + cap state (the slice-2 `_test_slot_has_warm` / `_test_slot_consecutive_restarts` accessors are the test-side equivalent today); formal SIGTERM-grace-then-SIGKILL teardown using `grace_period_seconds` (slice 2 drops `SupervisedWorker` which closes stdio + cancels watchdog — adequate for shell-exec but a worker that needs cooperative cleanup beyond stdio-close will need this); proactive SIGCHLD-listener crash detection (slice 2 detects passively on next dispatch); worker manifest plumbing (spec open question 1 — TOML files vs Rust consts). Low priority until a worker materialises that actually needs one of these.
- [ ] **GLiNER-Relex worker — design + plan + POC spike (2026-05-18)** — first `Lifecycle::IdleTimeout` consumer; design spec at `docs/superpowers/specs/2026-05-18-gliner-relex-worker-design.md` (444 lines), implementation plan at `docs/superpowers/plans/2026-05-18-gliner-relex-worker.md` (2276 lines, TDD-ordered tasks split by language), POC spike notes at `docs/superpowers/specs/2026-05-18-gliner-relex-spike-notes.md`. Spike confirmed: Apache 2.0 license chain (Knowledgator's `gliner-relex-multi-v1.0`), `uv sync` clean on the DGX with gliner 0.2.26 / torch 2.12.0+cu130 / transformers 5.1.0, model loads in 3.7 s, CPU warm-loop p50 latency 157 ms (vLLM owned the GPU at spike time, so CUDA was unavailable — CPU is a first-class production posture). Four spike corrections back-fed into the spec + plan: upstream method is `model.inference(texts=[text], ..., return_relations=True, flat_ner=False)` not `predict_relations`; relation envelope is `{head: Entity, tail: Entity, relation, score}` not `{subject, relation, object, score}`; production threshold ≥ 0.5 + separate `relation_threshold` field (model is noisy at 0.3 due to overlapping entity subspans); `torch.cuda.is_available() == True` is insufficient — must probe `torch.cuda.mem_get_info()` for ≥ 3 GiB free before committing to CUDA.
- [x] **GLiNER-Relex worker — Slice 1 implementation (Python package)** — shipped 2026-05-18, ff-merged to `main` at `36a2f4f` (8 commits). Tasks 1.1-1.8 of the plan. Ships `workers/gliner-relex/` (hatchling + uv venv per worker; sets the convention for every future Python worker), `errors.py` + `server.py` + `model.py` + `__main__.py` (~57 + 129 + 106 + 126 LOC), **24 pytest tests** (6 errors + 12 server + 6 model — +4 over plan's 20 from spike-correction-driven pins for `relation_threshold` and canonical `inference()` kwargs), operator install script + README. Three spike-notes corrections folded inline during implementation: method is `inference()` not `predict_relations()`; triple envelope keys are `head`/`tail` not `subject`/`object`; `_resolve_device("auto")` probes `torch.cuda.mem_get_info()` for ≥ 3 GiB free before committing to CUDA. **Operator smoke test passed end-to-end on real weights** (`knowledgator/gliner-relex-multi-v1.0` on the DGX Spark, CPU mode since vLLM still owns the GPU): 3 entities at score ≥ 0.999, 3 triples including the canonical `Dr Smith --[treats]--> asthma (0.995)`. Smoke test also surfaced + fixed three real bugs in the docs/install: install.sh sanity check used `config.json` instead of the actual `gliner_config.json`; README's `VAR=val | uv run` shell-prefix idiom set env only for `echo` (left of pipe), never the worker; real model's nested head/tail keys are `type` + `entity_idx`, not the `label` the spike notes paraphrased. **Rust workspace unchanged at 751/0/4** (Slice 1 is Python-only by design — Python suite excluded from `cargo test --workspace`).
- [x] **GLiNER-Relex worker — Slice 2 implementation (Rust manifest + e2e)** — shipped 2026-05-18, merged to `main` via PR #88 at `715a882` (10 commits incl. HANDOVER sync + post-review `resolve_env` refactor). Tasks 2.1-2.10 of the plan. Ships `core::workers::gliner_relex` module (`GlinerRelexEnv`, `gliner_relex_entry() -> ToolEntry` declaring `Lifecycle::IdleTimeout { idle_seconds: 600, max_requests: 10_000, max_age_seconds: 86_400, grace_period_seconds: 5 }` + `Contract { stateless: true }` + `cpu_ms: 0` + `wall_clock_ms: None`), wire-shape serde types (`ExtractRequest` / `ExtractResponse` / `Entity` / `TripleEntity` / `Triple` — `TripleEntity` is a distinct struct with `text`/`type`/`start`/`end`/`entity_idx`, matching the smoke-test-discovered shape from `1c36f56`; not the same shape as top-level `Entity` which carries `label`+`score`). New `core::worker_lifecycle::CompositeLifecycle` (~95 LOC) routes acquire calls to `SingleUseLifecycle` or `IdleTimeoutLifecycle` by inspecting `entry.lifecycle`; daemon `main.rs` switches from `SingleUseLifecycle::new` to `CompositeLifecycle::new` (strict superset — byte-equivalent for shell-exec-only deployments). Conditional daemon registration via `HHAGENT_GLINER_RELEX_ENABLE=1` + 4 supporting env vars (`HHAGENT_GLINER_RELEX_WEIGHTS_DIR` required when enabled; MODEL/DEVICE/VENV_DIR optional with reasonable defaults); skip-register by default with structured `tracing::error` on every precondition-fail path. 4 integration tests in new `core/tests/gliner_relex_e2e.rs`: skip-helper smoke + happy-path round-trip + warm-reuse pin (via `_test_slot_has_warm`) + INVALID_INPUT propagation pin (validates the wire-stable -32001 code reaches the caller via `ToolHostError::Protocol(ClientError::Rpc { code: -32001 })` AND the worker stays alive after the request-local error). Two sandbox-hygiene env vars discovered + folded inline by the running e2e: `USER="hhagent"` (skips the `getpass.getuser()` → `pwd.getpwuid()` fallback that explodes on missing /etc/passwd inside the jail; PyTorch's _dynamo triggers this at import time) and `TORCHINDUCTOR_CACHE_DIR="/tmp/torchinductor"` (defense-in-depth). Manifest also mounts `<worker_dir>/src` in `fs_read` (the uv-editable .pth file points there; computed from the documented `<worker_dir>/.venv` contract on `venv_dir`). All 4 e2e tests run for real on the DGX where the venv + weights are staged; skip-as-pass cleanly on hosts without them. **Workspace test count 751 → 786 (+35: 4 wire-shape + 8 manifest + 10 resolver (post-review fix) + 3 composite + 4 e2e on the branch; +6 from PR #87 tech-debt batch merged into main during the branch's lifetime). 0 failures, 0 warnings, 0 [SKIP] on the DGX run.** No typed Rust client this slice — deferred to the v2 entity-extraction consumer slice. Plan at `docs/superpowers/plans/2026-05-18-gliner-relex-worker.md`.
- [x] **GLiNER-Relex worker — macOS MPS spike (follow-up)** — completed 2026-05-18 on Apple M3 Max / macOS 26.5 / Python 3.12.6 / torch 2.12.0 / gliner 0.2.26 / transformers 5.1.0 (`uv sync` clean from the worker's existing `pyproject.toml`, no extra wheels). **All three open questions from the design spec (`docs/superpowers/specs/2026-05-18-gliner-relex-worker-design.md:381+`) answered:** (1) `GLiNER.from_pretrained(weights_dir, local_files_only=True)` + `model.to("mps")` works as-is on `knowledgator/gliner-relex-multi-v1.0` — the upstream device-branch (CPU/CUDA only) is not a blocker because the worker code path already calls `.to(device)` after `from_pretrained` (`workers/gliner-relex/src/hhagent_worker_gliner_relex/model.py:63`), so wiring the `mps` branch into `_resolve_device` in `__main__.py` is the only Python change the macOS slice needs. (2) `PYTORCH_ENABLE_MPS_FALLBACK=1` is **NOT required** — two end-to-end runs (flag unset, then `=1`) both produced identical entity/triple counts and zero op-fallback warnings on stderr; the spec's "may be required" hedge can be dropped for this model/torch combination. (3) Output is structurally byte-identical to CPU: short-text probe (33-char sentence, 3 entity labels, 2 relation labels) and long-text probe (~600-char paragraph, 8 entity labels, 6 relation labels) both gave matching entity counts (3↔3, 16↔16) and relation counts (3↔3, 22↔22) on CPU vs MPS; entity/relation scores differ only in the 6th decimal (float-reduction order on the encoder forward — well within noise). **Latency findings — surprising and load-bearing for the macOS slice's default-device choice:** on the short DGX-style probe MPS wins (warm p50 28.3 ms vs CPU 41.7 ms), but on a realistic ~600-char paragraph with 8+6 labels MPS **loses ~5× to CPU** (warm p50 432 ms MPS vs 82 ms CPU). The candidate-span batch shape scales with `text_tokens × n_entity_labels × n_relation_labels`; on M3 Max the per-kernel-launch dispatch overhead and unified-memory copy cost stop being amortised once the batch grows, so MPS regresses for typical memory-body sizes. Cold-call MPS is also slow (4160 ms first-ever inference; 683 ms when kernel cache warm from a prior run) — MPS shader compilation, one-time per process. **Recommendation for the macOS slice:** default `auto` on macOS should resolve to `cpu` (not `mps`), and the manifest should accept `device=mps` only as an explicit operator opt-in with a comment pointing at this entry. The `mps` rejection in `__main__.py::_resolve_device` (line 66) becomes a no-op on macOS and stays the rejection on Linux. **Sandbox path canonicalisation:** non-issue at runtime — the worker runs with `HF_HUB_OFFLINE=1` + `local_files_only=True` and `HHAGENT_GLINER_RELEX_WEIGHTS_DIR` points at the already-absolute install location (`~/.local/share/hhagent/workers/gliner-relex/weights/multi-v1.0/`); no `~/Library/Caches/huggingface/` path is consulted on the worker hot path. Spike scripts + raw logs preserved at `/tmp/gliner-relex-mps-spike/` (`mps_spike.py` + `mps_longer_text.py` + six numbered logs). The follow-up macOS slice (still TODO, scope below) widens `_resolve_device` to accept `mps` only on `sys.platform == "darwin"`, defaults `auto` per the table above, and adds the cross-platform manifest variant in `core::workers::gliner_relex`. No Rust changes in this spike.
- [x] **GLiNER-Relex worker — macOS slice (Python `mps` branch + Rust manifest cross-platform doc)** — shipped 2026-05-21, **merged to `main` via PR #103 at `9220f40`** + post-review fixup `123f91b` (`NoReturn` typing on `_unsupported_device` + clearer mps-fail-mode wording). (a) Python `__main__._resolve_device` rewritten as a per-platform decision tree: `is_darwin` computed once; `auto` on darwin resolves to `cpu` (skips MPS probe entirely per spike's latency-inversion finding — MPS regresses ~5× vs CPU on realistic ~600-char paragraph + 4s cold dispatch); `mps` accepted on darwin iff `torch.backends.mps.is_available()` (catches Intel Macs, pre-12.3 macOS, PyTorch builds without MPS); `mps` rejected on non-darwin with the updated "mps is darwin-only" message; `cuda` rejected on darwin (Apple Silicon has no NVIDIA GPU); every reject exits 2 with `UNSUPPORTED_DEVICE` (-32604) + per-platform legal-set in the stderr message. (b) Python `model.py` docstring widened to the per-platform legal-device matrix; no code change (`instance.to(device)` is generic). (c) Rust `core::workers::gliner_relex` — `GlinerRelexEnv::device` doc-comment widened from Linux-only to the full per-platform matrix. **Manifest itself unchanged** — `gliner_relex_entry` stays one-shape across Linux + darwin, forwards device verbatim into `HHAGENT_GLINER_RELEX_DEVICE`; per-platform validation centralised in Python. New `entry_forwards_device_verbatim_regardless_of_value` Rust test loops over `["auto", "cpu", "cuda", "mps", "unknown-future-device"]` and pins the verbatim-forwarding contract so any future architectural drift trips. (d) New `workers/gliner-relex/tests/test_resolve_device.py` (182 LOC) — **11 unit tests** covering every branch: darwin auto-to-cpu pin (load-bearing default), darwin explicit mps accept + reject, darwin explicit cuda reject, darwin explicit cpu accept, linux explicit mps reject, 3× linux auto cuda-resolution cases (available / unavailable / low-memory), explicit cpu passthrough, unknown device reject. All tests `monkeypatch` `sys.platform` + inject a stubbed `torch` via `sys.modules` to exercise every branch without depending on the real platform / hardware. **End-to-end smoke on this Mac (M3 Max, macOS 26.5):** `device=auto` → cpu → 3 entities + 3 triples (`Dr Smith --[treats]--> asthma 0.994`, structurally identical to DGX Spark CPU output from Slice 1); `device=mps` → mps → same shape, sub-1e-6 score deltas; `device=cuda` → fails with `UNSUPPORTED_DEVICE` + per-platform legal-set in stderr. **Test count delta:** Rust 865 → 866 (+1); Python 24 → 35 (+11). PG-backed e2e `[SKIP]`s cleanly on this Mac without Postgres installed; full integration await an operator install of Postgres.app or `brew install postgresql@17`.
- [ ] **GLiNER-Relex worker — operator-CLI macOS validation** (operator action, follow-up to the macOS slice directly above) — one-time: install Postgres locally (`brew install postgresql@17 && brew services start postgresql@17`) and rerun `HHAGENT_GLINER_RELEX_ENABLE=1 cargo test -p hhagent-core --test gliner_relex_e2e -- --nocapture` to exercise the full PG-backed lifecycle path on darwin. The Python `_resolve_device` is already cross-validated by 11 unit tests + 3 direct-spawn smokes (auto/mps/cuda), so this step is the lifecycle-manager validation, not the model validation. Half-hour estimate once PG is installed.
- ~~**Entity extraction + graph-lane wiring (v1 design)**~~ **SUPERSEDED by v2.** v1 design landed as docs-only at `docs/superpowers/specs/2026-05-18-entity-extraction-graph-lane-design.md` (`HybridEntityExtractor` with deterministic + LLM fallback, vocab-curation burden). v2 replaced it via the single-pass GLiNER-Relex worker call below.
- [x] **Entity extraction v2 — design + plan + implementation (2026-05-19)** — branch `feat/entity-extraction-v2`, 20 commits, **merged to `main` via PR #91 at `f12b460`** + post-review cleanup `2cf2a0a` (migration `0016` REVOKE writes on `entity_kinds` + perm-denied test → workspace 833 → **834**; `main.rs` single-call refactor; 3 doc-comment corrections). Replaces the v1 `HybridEntityExtractor` with a single-pass GLiNER-Relex worker call. Design spec at [`docs/superpowers/specs/2026-05-19-entity-extraction-v2-gliner-relex-design.md`](../superpowers/specs/2026-05-19-entity-extraction-v2-gliner-relex-design.md) (701 lines); implementation plan at [`docs/superpowers/plans/2026-05-19-entity-extraction-v2.md`](../superpowers/plans/2026-05-19-entity-extraction-v2.md) (2914 lines, 17 TDD-ordered tasks). Ships: new `core::entity_extraction` module (`EntityExtractor` trait, `EntitySeeds`, `SeedSource { GlinerRelex, None }`, `NoOpEntityExtractor` + `StaticEntityExtractor`, `EntityExtractionError`); typed `Client` + `ClientError` in `core::workers::gliner_relex` wrapping `tool_host::dispatch` for the worker's `extract` method; `GlinerRelexExtractor` composing chunking + per-chunk client.extract + dedup-by-norm merge + quarantined upsert + summary audit row; new `db::entity_kinds` module with 60s TTL cache; new migration `0015_entity_kinds_and_quarantine.sql` (20 seed kinds incl. clinical-domain + `entities.quarantine BOOLEAN DEFAULT TRUE` + `entities.name_norm` for case/whitespace/NFC-insensitive dedup + FK from `entities.kind` with `ON DELETE SET DEFAULT 'undefined'`); `RecallBuilder::build_with_seeds` widening with default-impl shim; `graph_search` gains `include_quarantined: bool` (production false); `RouterAgent::formulate_plan` runs extraction BEFORE recall (degrade-and-warn); `FormulationMeta` gains 3 fields + `plan.formulate` payload bumps pure-additive 21/22 → 24/25 keys (Slice F: `graph_seed_entity_ids`, `graph_seed_count`, `graph_seed_source`); daemon `main.rs` constructs `GlinerRelexExtractor` when `HHAGENT_GLINER_RELEX_ENABLE=1` (sharing the `Arc<dyn WorkerLifecycleManager>` with the step dispatcher), `NoOpEntityExtractor` otherwise. New `core/tests/entity_extraction_e2e.rs` ships 4 mock-tier tests + 2 real-model tests (all 6 passing against live multi-v1.0 weights on the DGX). **Workspace test count 786 → 833 (+47); 0 failures, 0 warnings, 0 [SKIP].** 3 corrective mid-stream fixes for cross-cutting regressions the plan didn't anticipate: `PgGraph::upsert_entity` SQL rewrite for the new `name_norm` constraint; 4 test fixtures updated from non-seeded kinds (`animal`, `thing`) to `object`; `memory_recall_e2e` gained an `unquarantine_all_entities` helper. **Read-side only: graph lane stays observably empty in production until follow-up slices (a) operator quarantine-review CLI + (b) memory-write-time auto-linker** populate `memory_entities` rows for unquarantined entities.
- [x] **Memory-write-time entity auto-linker (2026-05-19)** — **merged to `main` via PR #92 at `d58ecc9`** + post-review fixup `8c6cded` (FK-violation audit-row pin + param-order symmetry across the cascade + L0 info/audit counters + CLI stderr note + doc nits, all from the PR #92 review). New `core::memory::entity_link::link_memory_entities` compose-op: extract → `link_memory_to_entities` → 6-key audit row (now written on EVERY arm — extract-fail / DB-fail / success — via three explicit branches and a centralised `write_audit_row` helper). Threaded through `seed_l0_from_rules`, `seed_l0_from_file`, `promote_l1`; cascade through `write_l1_promoted_row` → `drain_lane` → `lane_loop` → `spawn_scheduler`. `L0SeedReport` gains `entities_linked` + `link_failures` (also surfaced in L0 `info!` events + `l0.seeded` audit-row payload post-review); `L1WriteOutcome::Inserted` gains `link_outcome: Option<LinkOutcome>` (additive). CLI `memory l1 add` uses NoOp extractor + emits a `note:` stderr line so operators aren't surprised by empty graph-lane hits on operator-added rows. Same `Arc<dyn EntityExtractor>` shared between query-time (RouterAgent) and write-time (L0/L1). Workspace 834 → **848** (+14: 6 unit + 3 mock-tier + 2 real-model + 2 caller-side + 1 post-review FK-violation regression pin). Spec at `docs/superpowers/specs/2026-05-19-memory-entity-link-design.md`; plan at `docs/superpowers/plans/2026-05-19-memory-entity-link.md`. **Read-side wiring complete; graph lane returns 0 rows in production until the operator quarantine-review CLI unblocks (closed below).**
- [x] **Issue #90 — entity-upsert round-trip reduction (Layer A) (2026-05-20)** — **merged via PR #94 at `3ab94f6`**. Branch `feat/issue-90-upsert-roundtrip-layer-a` carried 3 implementation commits + 1 docs sync + 1 post-review nit (`ff74dcc`). Replaced the per-entity two-statement upsert (`INSERT … ON CONFLICT DO NOTHING RETURNING id` + follow-up `SELECT`) with a single `INSERT … ON CONFLICT DO UPDATE SET name_norm = entities.name_norm RETURNING id, (xmax = 0) AS inserted`. Per-entity goes from 1–2 round-trips down to a guaranteed 1. The self-assignment `SET name_norm = entities.name_norm` is the standard Postgres idiom for forcing `RETURNING` on conflict without changing the row's logical state — critically, does NOT touch `quarantine`, so operator approvals (PR #93) survive re-extraction. `xmax = 0` is the canonical inserted-vs-existed discriminator. Public API unchanged (`UpsertOutcome` shape byte-identical). Audit-row contract unchanged (`build_extract_entities_payload` 8-key shape preserved). No migration. Workspace 881 → **883** (+2: `upsert_preserves_operator_unquarantine_decision` + `upsert_counts_new_inserts_correctly_in_mixed_batch` in `core/tests/entity_extraction_e2e.rs`). Layer B (full-batch unnest) and triple batching deferred per brainstorming. Spec at `docs/superpowers/specs/2026-05-20-issue-90-upsert-round-trip-reduction-design.md`; plan at `docs/superpowers/plans/2026-05-20-issue-90-upsert-round-trip-reduction.md`.
- [x] **Issue #66 — `hhagent-cli` per-subcommand-module split (2026-05-20)** — **merged to `main` via PR #96 at `2704468`** with implementation commit `b77f4c9` + post-merge fixup `1b58636` (dedupe the last straggler `Builder::new_multi_thread` block in `observation_replay::run_observation_replay`; fix HANDOVER's miscounted "11 unit tests" → actual 18 across the moved test modules). Pure mechanical refactor closing the longest-flagged 500-LOC breach: `core/src/bin/hhagent-cli.rs` had grown to 1933 LOC after five sessions of subcommand additions (original Issue scoped this at ~960 LOC). Moved from single-file bin to directory bin: new `core/src/bin/hhagent-cli/{main,common,audit_tail,ask,tasks,tools_allowlist,memory_l1,entities,observation_replay}.rs` — every file under the 500-LOC cap (largest is `entities.rs` at 479 LOC). One small additive helper (`common::multi_thread_runtime(prefix)`) factored out the 6× copy-pasted runtime-builder boilerplate; otherwise byte-for-byte behaviour-equivalent. `[[bin]] path` switched from `src/bin/hhagent-cli.rs` to `src/bin/hhagent-cli/main.rs`. Pre-existing test modules (`parse_classification_floor_tests`, `resolve_floor_for_submission_tests`, `tasks_tail_tests`, `entities_parser_tests`) moved verbatim to their owning module's `#[cfg(test)]` block. **Test count unchanged: workspace stays at 883 / 0 failed / 4 ignored** — pure file-shuffle, existing tests are the regression pin. Smoke-tested (`--help` exit 0, unknown subcommand exit 2, no-arg sub-subcommands exit 2 — all match pre-refactor). Closes #66. Out-of-scope finding filed as [Issue #97](https://github.com/hherb/hhagent/issues/97) (runtime built before action validation in 4 dispatchers — minor wart carried over from the pre-refactor file).
- [x] **Issue #97 — `hhagent-cli` defer tokio runtime construction until known-action arms (2026-05-21)** — **merged to `main` via PR #98 at `dbee0ac`**. Branch `fix/issue-97-defer-tokio-runtime` carried 2 TDD-ordered commits (`a1f5a75` tests-first + `4c7fb7f` refactor) + 1 in-PR doc fixup (`2fd3305` rephrase `Doc-pin:` → `Note:` in `with_runtime`'s comment). Out-of-scope finding from PR #96's self-review: the 4 per-subcommand dispatchers built the multi-thread tokio runtime *before* validating the action argument, so a typo (`hhagent-cli tasks frobnicate`) spawned worker threads only to throw them away and exit 2. New `common::with_runtime(prefix, fut) -> ExitCode` helper encapsulates the "build runtime, block_on future, return ExitCode" idiom; called only from known-action arms (`"list" => with_runtime("tasks", tasks_list(&args[1..]))`). The fall-through `unknown {subcommand,action}` arm in each dispatcher returns *before* any runtime construction. Sync arms (`tasks tail`) bypass the helper entirely; `multi_thread_runtime(prefix)` retained as the inner primitive. `ask.rs` + `observation_replay.rs` were already structurally correct; left untouched. **+3 new bad-args regression tests** in new `core/tests/cli_dispatcher_bad_args_e2e.rs` pinning the unknown-action exit-2 + stderr contract for the 3 dispatchers that lacked one (entities already covered). **Workspace 883 → 886 (+3)**, 0 failed, 0 new clippy warnings. Behaviour byte-identical: same exit codes, same stderr lines for every action path. Closes #97.
- [x] **Relation-label vocabulary (2026-05-21)** — **merged to `main` via PR #100 at `5bcd060`** + post-merge code-review fixup `4cd3411` (seed-count comment 17→19 in migration + tests; `strip_undefined_label` tightened from `pub` to `pub(crate)`; 7-line rationale comment on the `Override` arm of `resolve_relation_labels` explaining why the filter is deliberately not applied on the test seam). 2 TDD-ordered implementation commits (`af4e253` DB-side + `4e22ce6` core-side). Closes the v2 follow-up: v2's `relation_labels = vec![]` forced the GLiNER worker into entity-only mode and silently dropped every triple. New migration `0017_relation_kinds.sql` adds the operator-managed `relation_kinds` vocabulary (19 seed values: 1 `undefined` FK fallback + 18 starter relations covering clinical relations like `treats`/`prescribed`/`diagnosed with`/`has symptom`/`side effect of`/`contraindicated with`/`allergic to`, spatial/role relations like `located in`/`employed by`/`works at`/`member of`/`owns`, plus social/generic relations like `knows`/`identified as`/`refers to`/`occurred on`/`associated with`/`relative of`). `relations.kind` gets a FK to the lookup table with `ON UPDATE CASCADE` + `ON DELETE SET DEFAULT 'undefined'`. Single migration (CREATE + GRANT + REVOKE all together, 0008 deleted_memories pattern, not the 0015/0016 split). New `hhagent_db::relation_kinds::RelationKindsCache` (~138 LOC) is a byte-for-byte mirror of `entity_kinds::KindsCache` — 60-s TTL, read-lock fast path + write-lock slow path with re-check. `GlinerRelexExtractor` replaces the static `relation_labels: Vec<String>` field with a `RelationLabelSource` enum (`FromDb(Arc<RelationKindsCache>)` for production / `Override(Vec<String>)` for tests via the preserved `with_relation_labels()` builder). New pure helper `strip_undefined_label` drops the FK-fallback `'undefined'` out of the live list before passing it to the worker. Daemon `main.rs` construction site unchanged (cache built inside `GlinerRelexExtractor::new`). Workspace 886 → **896 (+10)**: 3 unit (cache shape) + 4 unit (`strip_undefined_label`) + 3 integration (`postgres_e2e` migration_0017 schema/FK/REVOKE pins). Pre-existing fixture in `upsert_is_idempotent_on_rerun` updated from unseeded `relates_to` → seeded `associated with`. Plumbing chain now complete end-to-end for graph payloads: v2 extractor (PR #91) → auto-linker (PR #92) → operator review CLI (PR #93) → relation vocabulary (this slice). **Real-model validation** (producing non-zero rows in the `relations` table from a clinical fixture) is the natural operator follow-up; HANDOVER Next-TODO item 15.
- [x] **Operator quarantine-review CLI (2026-05-20)** — **merged to `main` via PR #93 at `028e541`** + post-merge polish `6e6e85f` (+1 test, four PR-review-driven fixes: stderr suffix on mid-batch errors, safer `entities show` enum-mismatch path, `parse_id_list` doc accuracy, multi-drop `links_dropped_as_duplicate` regression pin). New `hhagent-cli entities {list, show, approve, reject, merge}` subcommand tree + new `hhagent_db::entities` module (`list_entities`, `get_entity_with_mentions`, `approve_entity`, `reject_entity`, `merge_entities` — all single-transaction with `SELECT ... FOR UPDATE` row locks and `ORDER BY id` on multi-row locks to avoid deadlock) + 3 new wire-stable audit-row action constants (`entities.approved` / `entities.rejected` / `entities.merged`) + 3 new `core::cli_audit` compose-helpers. Three-variant `ApproveOutcome` / two-variant `RejectOutcome` so the CLI prints distinct stderr lines without a second DB probe; merge precondition errors → exit 2 (operator error), DB errors → exit 1 (runtime). Audit rows emitted ONLY on state-changing outcomes (Approved / Rejected / successful merge) — observation-phase SQL never sees no-op rows. End-to-end recall pin in `memory_recall_e2e` demonstrates the headline behaviour: 2 quarantined entities → recall(GRAPH_ONLY)=0; approve one → recall returns 1; reject other → recall still 1 + memory_entities cascaded + memory row survives. **No new migrations** — runtime role already had full CRUD on `entities` + cascade on `memory_entities` per migrations 0002 + 0007; `entity_kinds` (REVOKE per 0016) deliberately untouched. Workspace 848 → **881** (+33: 4 unit db::entities pure helpers + 6 unit scheduler::audit pins + 2 unit CLI arg-parsers + 3 compile-pin cli_audit signatures + 7 DB integration + 6 CLI subprocess + 1 recall pin + 3 mid-slice review-driven regression pins + 1 post-merge multi-drop dup_count pin). Spec at `docs/superpowers/specs/2026-05-20-operator-quarantine-review-cli-design.md`; plan at `docs/superpowers/plans/2026-05-20-operator-quarantine-review-cli.md`. **The graph-lane plumbing chain is now complete end-to-end:** v2 extractor (PR #91) → auto-linker (PR #92) → operator review CLI (this slice). Production `recall(GRAPH_ONLY)` returns hits the moment the operator approves an entity.
- [x] **Issue [#89](https://github.com/hherb/hhagent/issues/89) — sandbox `/tmp` per-spawn ephemeral tmpfs invariant test (2026-05-28)** — branch `fix/issue-89-tmpfs-per-spawn-invariant-test`, **merged to `main` via PR [#139](https://github.com/hherb/hhagent/pull/139) at `504094e`**. One new Linux integration test (`tmp_is_per_spawn_ephemeral_tmpfs_under_worker_strict`) appended to `sandbox/tests/linux_smoke.rs` (222 → 311 LOC). Pure test addition — no production-code changes. Pins three properties of bwrap's `--tmpfs /tmp` mount (load-bearing for every Python worker that uses `/tmp` — HF cache, Python `tempfile`, NumPy mmap, GLiNER-Relex's `TORCHINDUCTOR_CACHE_DIR=/tmp/torchinductor`): (1) marker written in spawn A is not visible in spawn B (ephemerality), (2) spawn B exits 0 (writability — guards against an over-correction to read-only `/tmp`), (3) spawn B's own freshly-written marker IS in its own `/tmp` listing (sanity check the listing isn't silently empty). Two real-bwrap spawns under `Profile::WorkerStrict` (default `SandboxPolicy`); markers carry PID+UNIX-nanos so parallel CI lanes can't collide on false-positive "leak" hits. `/bin/sh -c '...'` resolves via bwrap's existing `--symlink usr/bin /bin` mapping — no usr-merge dependency. `skip_if_no_userns()` discipline preserved. **Macos counterpart deliberately deferred** per the issue body: Seatbelt has no `/tmp`-tmpfs primitive; the `macos_container` backend would have it naturally but isn't the macOS default. **Verification on macOS** (M3 Max): workspace `cargo test --workspace` 1065 / 0 / 3 unchanged (test cfg-gated to `target_os = "linux"`); cross-target `cargo check --tests -p hhagent-sandbox --target aarch64-unknown-linux-gnu` clean. **DGX verification deferred to operator** — two steps: (a) test should PASS against current code; (b) temporarily replacing `--tmpfs /tmp` with `--bind-try /tmp /tmp` in `linux_bwrap::build_argv` should make the ephemerality assertion trip (proves the pin catches real drift). Closes #89.

- [x] **refactor: `idle_timeout/release.rs` sibling-lift (2026-05-28)** — branch `refactor/idle-timeout-release-sibling-lift`, **merged to `main` via PR [#138](https://github.com/hherb/hhagent/pull/138) at `5fc5fee`** + in-PR polish `78305cf` (two self-review cleanups: banner-rot in `idle_timeout.rs` replaced with a tighter 5-line doc-comment directly above `mod release;`; `replace_idle_teardown_handle` visibility narrowed `pub(crate)` → `pub(super)` since the only out-of-module caller is the parent's `tests.rs`. Defensive `None`-slot branch in `release_idle_timeout_worker` flagged as pre-existing and deferred). Workspace **1065 → 1065** post-polish, byte-equivalent to pre-fix PR baseline. Natural follow-up to Issue #136 which grew `core/src/worker_lifecycle/idle_timeout.rs` from 576 → 647 LOC (+71, 147 over the 500-LOC cap). Pure mechanical refactor: lifts the release path (`release_idle_timeout_worker` + `abort_idle_teardown_handle` + `replace_idle_teardown_handle`) into a new sibling submodule `core/src/worker_lifecycle/idle_timeout/release.rs`. File-size delta: `idle_timeout.rs` 647 → **490 LOC** (now 10 under cap, closes the 147-LOC overage); new `release.rs` **195 LOC** (well under cap); `idle_timeout/tests.rs` 467 → 472 LOC (+5 — explicit `use super::release::replace_idle_teardown_handle;` import banner with a 4-line doc-comment header). `release_idle_timeout_worker` stays `pub(crate)` in `release.rs` and is re-exported from `idle_timeout.rs` via `pub(crate) use release::release_idle_timeout_worker;` so external callers (notably `manager::WorkerHandle::Drop` via `super::idle_timeout::release_idle_timeout_worker`) resolve byte-equivalently with no edit at the call site. Chose explicit import in `tests.rs` over `#[cfg(test)]` re-export so the structural home of the helper is visible to future contributors at the import site. Workspace **1065 → 1065 (+0) on macOS**, all green: pure refactor, existing tests are the regression pin — the two load-bearing issue #85 pin tests (`replace_idle_teardown_handle_aborts_prior_and_stores_new` + `_steady_state_holds_at_most_one_alive_per_slot`) pass byte-equivalently through the new import path; the 6 `worker_lifecycle_idle_timeout_e2e` integration tests pin the end-to-end release-path semantics. Linux DGX expected at +0 by the same argument. No new clippy warnings; 2 transient unused-import warnings (`OwnedMutexGuard` + `IdleTimeoutCaps`, both moved to `release.rs` with their sole callers) were cleaned up inline before the commit. Cumulative file-size watch for `worker_lifecycle/`: every production-code file (`idle_timeout.rs` 490, `release.rs` 195, `manager.rs` 339, `composite.rs` 256, `types.rs` 246, `mod.rs` 36) now well under the 500-LOC cap. Closes the Item 29 operator-picks sub-bullet for the `idle_timeout/release.rs` sibling-lift filed at end of Issue #136.

- [x] **Issue #136 — worker_lifecycle/idle_timeout: debounce pending-acquires queue-depth warn (2026-05-27)** — branch `fix/issue-136-pending-acquires-warn-debounce`, **merged to `main` via PR [#137](https://github.com/hherb/hhagent/pull/137) at `fed0f21`**. Natural close-out follow-up to PR #135's #84 work — filed during the in-PR polish (`1fc7eca`) when it became clear the new `tracing::warn!` at the `pending_acquires` threshold fires once per acquirer under sustained queue depth (log-spam at request rate under exactly the scenario the warn is designed to surface). This slice picks the 30 s cooldown duration and ships the debounce. 2 commits: 1 doc-sync to main = `7f98ee4` (`8204a3c`, PR #135 / trio merged + polish `1fc7eca`) + 1 feature `9fae2f9` (new `last_warn_unix_nanos: AtomicI64` field on `ToolSlot` peer to `pending_acquires` — no allocation, no extra mutex, cache-line-adjacent to the depth gate; new `pub const PENDING_ACQUIRES_WARN_COOLDOWN: Duration = Duration::from_secs(30)` colocated with `PENDING_ACQUIRES_WARN_THRESHOLD`; new pure predicate `debounce_warn(last_warn_nanos: i64, now_nanos: i64, cooldown: Duration) -> bool` — uses `saturating_sub` over i64 so clock-skew-backward returns a non-positive delta which is always less than the positive cooldown → predicate suppresses, asymmetric under-warn-during-NTP-drift behaviour is deliberate per issue body sketch; `acquire_impl` warn site rewritten as outer depth gate (no syscall on the steady-state cheap path) → `debounce_warn` predicate over wall-clock unix nanos → `compare_exchange(last, now, AcqRel, Relaxed)` to claim the warn slot so only the CAS winner emits the warn under concurrent acquirers; structured warn payload gains `cooldown_secs` field). **Workspace 1058 → 1065 (+7) on macOS, all green**: +1 `pending_acquires_warn_cooldown_is_thirty_seconds` const pin + 5 `debounce_warn` predicate tests (cold path with realistic 2023-era unix nanos so the assertion matches production reality rather than the misleading `now=1 ns` micro-case + suppress-within-cooldown + boundary-inclusive-at-cooldown + past-cooldown + clock-skew-backward) + 1 `slot_for_initializes_last_warn_unix_nanos_to_zero` cold-path zero-init pin. Linux DGX expected at +7 by the same argument (every new test is platform-independent; `SystemTime`/`UNIX_EPOCH`/`AtomicI64` are stdlib). No new clippy warnings introduced. **File-size watch:** `core/src/worker_lifecycle/idle_timeout.rs` 576 → 647 LOC (+71) — was 76 over the 500-LOC cap, now 147 over. The natural sibling-lift target is now a fresh `idle_timeout/release.rs` carrying `release_idle_timeout_worker` (~85 LOC) + `abort_idle_teardown_handle` (~5 LOC) + `replace_idle_teardown_handle` (~35 LOC) — a clean ~125-LOC seam between "acquire path" and "release path" that would land at ~522 LOC each. Deferred to a separate refactor slice; this slice ships the surface. **Architectural notes worth keeping:** (a) the threshold-and-cooldown pair (`PENDING_ACQUIRES_WARN_THRESHOLD = 5` + `PENDING_ACQUIRES_WARN_COOLDOWN = 30s`) is now the canonical shape for any operator-visible "noisy condition" warn in the workspace — depth/severity gate + per-instance cooldown + CAS-to-claim pattern; each constant gets a `*_is_<value>` test that pins the value so a silent bump trips a re-think; (b) `saturating_sub` over i64 is the right shape for clock-skew-aware debounce (negative deltas on backward skew → suppress; positive deltas during forward skew → may fire slightly early but still bounded; under-warning during NTP drift is strictly safer than over-warning); (c) CAS-to-claim is structurally different from just `store` — without the CAS, multiple acquirers passing the time gate would all emit; the `compare_exchange` ensures "at most one warn per cooldown window per slot" as a structural invariant rather than a probabilistic one. **Deliberately NOT done:** (a) Issue #84's BUSY-rejection half — still waiting on empirical evidence of queue starvation; (b) operator-facing `hhagent-cli supervisor status` accessor surfacing `pending_acquires` + `last_warn_unix_nanos` — the atomics on `ToolSlot` are `pub(crate)` and would be reached via future `pub fn slot_*` wrappers without further plumbing (per the trio's polish #3, this is the right shape for production observability; not repurposing the `_test_slot_*` inspector); (c) the `idle_timeout/release.rs` sibling-lift mentioned above. Closes #136.

- [x] **Issue #102 — `audit_tail` tempdir-collision flake fix on macOS (2026-05-21)** — **merged to `main` via PR #102 at `88218ef`** + post-review doc fixup `2e171af`. Tokio-multi-thread test-parallelism flake in `audit_tail::tests::tail_loop_from_start_replays_then_exits` (failed ~30–50% on macOS, never on Linux) caused by `tempdir()` helper using `pid + SystemTime::now().as_nanos()` — macOS's effective `SystemTime` resolution is microsecond-level, so two `#[tokio::test]` cases hitting `tempdir()` within the same microsecond collided on the same path and silently overwrote each other's audit files. Fix: added a process-local `static COUNTER: AtomicU64` threaded into the directory name (`hhagent-audit-tail-{pid}-{nanos}-{counter}`). Defence-in-depth bonus: rewrote `find_audit_files`'s `while let Ok(Some(...))` shorthand into an explicit `loop { match ... }` that distinguishes `Ok(None)` (EOF) from `Err(_)` (real iteration error logged via `tracing::error!`). Pre-fix flake rate: 3 of 4 workspace runs. Post-fix: 20 of 20 consecutive `cargo test -p hhagent-core --lib` runs all green. Closes #101. Workspace test count unchanged at 865/0/3 on macOS post-fix.
- [x] **Worker_lifecycle hardening trio — issues #84 + #85 + #86 (2026-05-27)** — branch `feat/worker-lifecycle-hardening-trio`, **merged to `main` via PR [#135](https://github.com/hherb/hhagent/pull/135) at `7f98ee4`** + in-PR polish `1fc7eca` (5-issue review pass: (1) success-path `slot: None` abort symmetry — `release_idle_timeout_worker` success branch now aborts the prior teardown handle even when `slot` is `None`, matching the crash/cap branches; defensive vs load-bearing since production always threads `Some(slot)`, but closes the only asymmetric release path; (2) `PendingAcquireGuard::depth()` doc tightening — original "the post-increment depth observed at enter time" claim was single-threaded-only true under concurrency; new doc says `>= 1` for the guard's lifetime + "current depth, may be strictly greater than this caller's own slot"; the `>= threshold` property the warn site relies on is preserved; also notes `Ordering::Relaxed` would be sound but `Acquire` is kept for one-knob simplicity with the increment site; (3) `_test_slot_pending_acquires` doc reframing — dropped the "stable shape for `hhagent-cli supervisor status`" overclaim; the test-inspector naming + `#[doc(hidden)]` marker say "test only"; production CLI accessor should be a parallel `pub fn slot_pending_acquires` wrapping the same atomic load, not repurposing the inspector; (4) test-isolation fix on the two `replace_idle_teardown_handle_*` tests — pre-polish, they constructed a local `ToolState::fresh()` separate from `slot.state` so the helper mutated one mutex while the spawned task locked a different one; tests still pinned the right contract via `JoinHandle::id()` comparisons but the wiring diverged from production; now both tests `Arc::clone(&slot.state).lock_owned().await` so the `state` arg is the genuine slot's state; (5) filed [#136](https://github.com/hherb/hhagent/issues/136) for the deferred warn-debounce cooldown — sketch: `AtomicI64 last_warn_unix_nanos` on `ToolSlot`, ~30 s cooldown const colocated with `PENDING_ACQUIRES_WARN_THRESHOLD`, pure `debounce_warn(last, now, cooldown) -> bool` predicate testable in isolation; ~1-LOC field + 1 helper + 1 test; blocked only on the right-cooldown-duration design micro-pass; not blocking observability ship). Workspace **1058 → 1058 (+0) after polish** — same test count, tighter doc + correctness only. Bundles three small follow-ups filed during PR #83 (worker lifecycle slices 1 + 2) post-review into one slice per HANDOVER Item 29's operator-picks bucket. The first real `Lifecycle::IdleTimeout` consumer (GLiNER-Relex, PR #118 / Slice 2.5) shipped 2026-05-25 and now exercises the latent concerns: queue-depth observability (#84), idle-teardown task accumulation under high request rate (#85), and the struct-literal bypass of the validated `Lifecycle::idle_timeout` constructor (#86) that becomes load-bearing the moment manifest plumbing arrives. 4 commits: 1 doc-sync to main = `8655319` (`d0f356d`, PR #133 / Issue #131 merged) + 1 feature for #86 (`0983586`: `#[non_exhaustive]` on the `Lifecycle::IdleTimeout` struct variant blocks out-of-crate struct-literal construction; in-crate construction unaffected; pinned by a `compile_fail,E0639` doctest on the enum-level documentation — doctests compile as separate crates, providing genuine out-of-crate enforcement) + 1 feature for #85 (`94381f3`: new `ToolState.idle_teardown_handle: Option<JoinHandle<()>>` field + `abort_idle_teardown_handle` + `replace_idle_teardown_handle` helpers; `release_idle_timeout_worker`'s 4 paths now abort the prior teardown task before clearing/repopulating the warm slot, so the slot holds exactly one pending teardown at steady state instead of accumulating ~`idle_seconds` stale sleepers per tool under high request rate; the internal task self-clears its handle on fire) + 1 feature for #84 (`dc545cb`: new `ToolSlot.pending_acquires: AtomicU32` + `PendingAcquireGuard` RAII bracket on the `lock_owned().await` queue wait + `pending_acquires_should_warn(depth)` pure predicate + new `PENDING_ACQUIRES_WARN_THRESHOLD: u32 = 5` const driving a `tracing::warn!` when queue depth crosses the operator-visible boundary + sync `_test_slot_pending_acquires(tool_name)` inspector that the future `hhagent-cli supervisor status` surface will use). **Workspace 1050 → 1058 (+8) on macOS, all green**: +5 for #84 (4 in `idle_timeout::tests` + 1 in `manager::tests`) + +2 for #85 (replace_idle_teardown_handle_aborts_prior_and_stores_new + replace_idle_teardown_handle_steady_state_holds_at_most_one_alive_per_slot) + +1 doctest for #86. Linux DGX expected at +8 by the same argument (every new test is platform-independent). The 6 existing `worker_lifecycle_idle_timeout_e2e` integration tests pass unchanged — they're the regression pin for the load-bearing teardown semantics (especially `idle_seconds_teardown_clears_warm_slot`, which exercises the rewritten replace-and-spawn helper end-to-end). No new clippy warnings introduced: the 4 pre-existing `MutexGuard`-across-await warnings on `manager.rs:278/284/293/299` (test inspectors `_test_slot_has_warm` + `_test_slot_consecutive_restarts`) unchanged; the new `_test_slot_pending_acquires` is sync (atomic load) and doesn't add to the count. **Deliberately NOT done:** issue #84's third "Consider..." remedy — soft timeout on lock acquisition that surfaces `StepOutcome::Err { code: 'BUSY' }` — would inflate the slice from 0.5 to 1.5 sessions (new `ToolHostError::Busy` variant + per-tool timeout config on `ToolEntry` + plumbing through the dispatcher + a distinct audit-row code). Observability ships now; BUSY rejection waits for empirical evidence of queue starvation. Closes #84 + #85 + #86.

- [x] **Issue #131 — `bring_up_pg_cluster_with_timeout` sibling for per-call timeout override (2026-05-27)** — **merged to `main` via PR [#133](https://github.com/hherb/hhagent/pull/133) at `8655319`** + post-merge code-review nit `876fb42` (panic messages emit `{timeout:?}` via Duration's Debug formatter instead of `.as_secs()`, so sub-second caps render accurately — `Duration::from_millis(500)` displays as "500ms" rather than the misleading "0s" from `as_secs()`; `Duration::from_secs(30)` still renders as "30s" — bit-identical visual output for all 50+ existing default-cap callers). Closes the Issue #131 sub-bullet in HANDOVER Item 29's operator-picks bucket. New `pub fn bring_up_pg_cluster_with_timeout(bin_dir, data_label, log_label, service_name, timeout: Duration) -> PgCluster` in `tests-common/src/pg.rs` (~70 LOC body, lifted verbatim from the existing `bring_up_pg_cluster` body with 2 `Duration::from_secs(PG_BRING_UP_TIMEOUT_SECS)` → `timeout` parameter swaps + 2 panic messages emitting `{timeout:?}` so failure diagnostics show the caller-chosen cap instead of misleading 30 s). Existing 4-arg `bring_up_pg_cluster` becomes a 1-line forward to `_with_timeout`. Re-exported from `tests-common/src/lib.rs`. **Sibling-function shape chosen over `Option<Duration>` parameter on the existing function** to avoid `, None` noise at all 50+ existing call sites; matches the `pg_bin_dir_candidates_with_env_override` precedent from PR #126. 4 commits: 1 doc-sync to main = `162f71f` (`8f965ac`, PR #132 / quadruple merged) + 1 feature `2d4084c` (new sibling function + 2 signature-pin unit tests) + 1 docs claim `4edc250` + 1 post-review fix `876fb42`. **Workspace 1048 → 1050 (+2) on macOS, all green**: +1 `bring_up_pg_cluster_with_timeout_has_documented_signature` compile-pin + +1 `bring_up_pg_cluster_wrapper_keeps_pre_issue_131_signature` compile-pin. Linux DGX expected at +2 by the same argument (both new tests platform-independent). No new clippy warnings. The 50+ existing `bring_up_pg_cluster` callers continue to pass through the refactored 1-line wrapper byte-equivalently — integration suite is the regression pin for the default 30 s path. **Architectural note:** the `_with_*`-sibling pattern is now established three times in the codebase as the shape for "default-and-override" pairs (`default_pg_bin_dir_candidates` ↔ `pg_bin_dir_candidates_with_env_override` from PR #126; `bring_up_pg_cluster` ↔ `bring_up_pg_cluster_with_timeout` from this slice). The single-call-site noise of `Option<Duration>` would have grown linearly with caller count; the sibling pattern stays O(1) at every existing call site. **Deliberately NOT done:** demonstrative caller using the override (Homebrew-prefix detection helper or env-var caller hint to opt into a tighter cap on known-fast hosts) — that's a separate slice. This one ships the surface only.
- [x] **Test-module lift quadruple (2026-05-27)** — branch `refactor/item-29-test-module-lift-quadruple`, **merged to `main` via PR [#132](https://github.com/hherb/hhagent/pull/132) at `162f71f`**. Pure mechanical refactor: 4 test-module sibling lifts in one PR, mirroring Items 23(a) + 26 + the per-PR sibling-lift pattern already proven on `relations_show.rs`, `db/src/graph.rs`, and `gliner_relex.rs`. Closes the `batch_upsert.rs` 547-LOC sibling-`tests.rs` lift + bundled `idle_timeout.rs` / `manager.rs` / `db/src/lib.rs` quadruple sub-bullet in HANDOVER Item 29's operator-picks bucket. 5 commits: 1 doc-sync to main = `4e94e42` (`91b9dcc`) + 4 lifts: (1) `4e39cf9` `core/src/entity_extraction/batch_upsert.rs` 589 → **428 LOC** + new sibling `batch_upsert/tests.rs` 169 LOC (11 tests); (2) `17e3540` `core/src/worker_lifecycle/idle_timeout.rs` 581 → **415 LOC** + new sibling `idle_timeout/tests.rs` 173 LOC (15 tests); (3) `d891d41` `core/src/worker_lifecycle/manager.rs` 565 → **339 LOC** + new sibling `manager/tests.rs` 234 LOC (5 tests: 3 cross-platform + 2 macOS-gated); (4) `91e2e6e` `db/src/lib.rs` 912 → **525 LOC** + new sibling `db/src/tests.rs` 403 LOC (27 tests). All 4 use Rust 2018 sibling-directory module resolution. For the crate-root `lib.rs`, `mod tests;` resolves to `src/tests.rs` (the crate-root form, *different* from the sibling-module form which resolves to `<parent>/tests.rs`). `use super::*` continues to import from the parent module so test bodies are byte-equivalent (only the storage location changed; tests stay at the exact same module paths). **`crate::env_lock` (defined in `db/src/lib.rs`, `pub(crate) #[cfg(test)]`, shared with `conn::tests`) deliberately stays at the crate root** — lifting it would have rippled to every external caller in the crate. Net **-940 LOC** off the 4 production parent files; 945 LOC of inline test code moved verbatim to 4 new sibling `tests.rs` modules (58 tests total). Three of the four files now well under the 500-LOC soft cap; `db/src/lib.rs` is 25 LOC over (production-code residual: error type, options structs, pg-discovery helpers, `require_absolute` — a further reduction would require a production-code split). Workspace `cargo test --workspace` returns **1048 / 0 / 3 macOS, all green**, unchanged from pre-lift baseline at `main` = `4e94e42` (pure refactor; existing tests are the regression pin). Linux DGX expected unchanged at 990 / 0 / 4. No new clippy warnings introduced: pre-existing `MutexGuard`-across-await in `manager.rs::_test_slot_*` test inspectors (lines 278 + 293, production code untouched by this lift), pre-existing doc-list-indent in `db/src/probe.rs`, pre-existing `io_other_error` in `hhagent-protocol`, pre-existing `mem_burner` `set_len()`-after-`reserve` error all unchanged. **Architectural note worth keeping:** the **crate-root `mod tests;` form resolves differently** from the sibling-module form — `lib.rs`'s `mod tests;` resolves to `src/tests.rs`, but `foo.rs`'s `mod tests;` resolves to `foo/tests.rs`. Both work; both are valid Rust 2018; documented in the new `db/src/tests.rs` module-doc-comment header to forestall future contributor confusion.
- [x] **`bring_up_pg_cluster` polling-cap lift + 5-site consolidation (2026-05-26 → 2026-05-27)** — branch `feat/bring-up-pg-cluster-polling-cap-lift`, **merged to `main` via PR [#129](https://github.com/hherb/hhagent/pull/129) at `4e94e42` on 2026-05-27** + same-PR post-review doc polish `f906d18` (inline `// IMPORTANT: do not reorder fields` comment above `PgCluster` makes the RAII teardown invariant explicit at the struct definition; clarifies `bring_up_pg_cluster` docstring that the `service_name.len() <= 200` cap is the launchd label / systemd unit-file basename limit, *not* the 108-byte `sun_path` cap which governs the socket directory; no behaviour change). Two new follow-up issues filed during PR #129 review: [#130](https://github.com/hherb/hhagent/issues/130) (parallel-launchd bring-up contention under `HHAGENT_PG_BIN_DIR` override) + [#131](https://github.com/hherb/hhagent/issues/131) (optional per-call timeout override for `bring_up_pg_cluster`). Closes the `bring_up_pg_cluster` polling-cap lift sub-bullet in HANDOVER Item 29's operator-picks bucket. 2 substantive commits TDD-ordered: (1) `02d6691` widens the canonical `tests_common::bring_up_pg_cluster` polling cap from `Duration::from_secs(15)` → `Duration::from_secs(PG_BRING_UP_TIMEOUT_SECS)` where new `pub const PG_BRING_UP_TIMEOUT_SECS: u64 = 30` is colocated with the helper in `tests-common/src/pg.rs`, re-exported from `tests-common/src/lib.rs`. Both `wait_for_status(... Active ...)` and `wait_for_socket(...)` use the const; doc-comment explains: "30 s, not 15 s: previously a 15 s cap. Sufficient under Homebrew Postgres on Linux + macOS, but on macOS launchd's first per-session bring-up of a Postgres.app-installed `postgres` binary overshoots 15 s consistently." The 50ms poll interval inside both wait helpers means healthy runs are unaffected — the cap is the worst-case outer bound, not the typical wait. (2) `0c7b644` migrates 5 local-copy test sites onto the shared helper: `core/tests/agent_prompts_e2e.rs` (371 → 149 LOC), `core/tests/scheduler_lanes_e2e.rs` (494 → 286 LOC), `core/tests/scheduler_step_dispatch_e2e.rs` (555 → 351 LOC), `core/tests/scheduler_inner_loop_e2e.rs` (1105 → 892 LOC), `core/tests/scheduler_crash_recovery_e2e.rs` (519 → 304 LOC) — net **-1062 LOC** across 5 files. Each file lost ~210 LOC of byte-duplicate boilerplate (the local `bring_up_pg_cluster` + its dependent helpers: `unique_temp_root`, `current_username`, `ServiceGuard`, `PathGuard`, `wait_for_status`, `wait_for_socket`). Service-name templates preserved verbatim per-file. The 4 async sites' `bring_up_pg(label)` wrapper signatures changed from `(pool, (ServiceGuard, PathGuard, PathGuard))` → `(pool, PgCluster)` where `PgCluster` carries the cleanup guards via a private field that drops in declaration order at end-of-scope (same Drop semantics, opaque cleanup handle). The sync `scheduler_step_dispatch_e2e.rs` site changed `let (conn_spec, _guards) = bring_up_pg_cluster(&bin_dir, &suffix)` to `let _cluster = bring_up_pg_cluster(&bin_dir, "step-d", "step-l", &service_name); let conn_spec = &_cluster.conn_spec;`. Test call sites updated: `let Some((pool, _guards))` → `let Some((pool, _cluster))` (rename only). **Workspace 1048 / 0 / 3 macOS unchanged** from pre-slice (no behaviour change on hosts where env var is unset). **Empirical verification under HHAGENT_PG_BIN_DIR override** (Postgres.app v18, this Mac): each of the 5 previously-failing files passes individually (13/13 tests across them; was: 12-13 timing out with `"pg active: timeout 15s; last=Inactive"`). Three follow-up tech-debt items kept out of scope per slice discipline: (a) `skip_if_no_supervisor` / `pg_bin_dir_or_skip` / `unique_suffix` local-copy consolidation in the 5 migrated files (~30 LOC removal, separate code-dedup slice); (b) parallel-launchd-bring-up contention under full `cargo test --workspace` with the override (some tests still fail when 5+ clusters race for launchd registration — orthogonal to the per-test polling cap, requires a global serial-mutex around bring-up or a `--test-threads=1` opt-in); (c) `observation_capture.rs`'s `Duration::from_secs(15)` polling line waits for the *core daemon's* `scheduler spawned` log, not PG-Active (different concern). **Architectural note:** the **shared `PgCluster` struct with private `_guards` field** is the right shape for any future test-fixture that needs RAII-ordered teardown — public fields (`conn_spec`, `data_dir`, `socket_dir`, `sup`, `service_name`) give the test body what it needs while the private guard tuple drops in declaration order at end-of-scope so cleanup happens correctly even on panic; pre-slice each local site reimplemented the same 3-tuple-drop discipline by hand.
- [x] **Issues [#114](https://github.com/hherb/hhagent/issues/114) + [#115](https://github.com/hherb/hhagent/issues/115) — `walk_*_edges` diamond-dedupe + `walk_edges_around` combined UNION ALL (2026-05-26)** — branch `fix/issues-114-115-walk-edges-dedupe-and-combine`, **merged to `main` via PR [#128](https://github.com/hherb/hhagent/pull/128) at `bb32cab`** + post-merge polish `978b5be` (named const `WALK_EDGES_AROUND_DIRECTION_COL` for the trailing partition column index + SQL comment on the outer `ORDER BY direction ASC` lex-order incidental + test header pin documenting that `per_direction_limit` shares semantics with each per-direction method's `limit`; no behaviour change). 4 commits TDD-ordered: (1) `668a36f` closes #114 — new `walk_edges_dedupes_diamond_topology_to_shortest_depth` integration test (RED on the diamond fixture A→B, A→C, B→C, C→D at max_depth=3; edge C→D appeared twice via paths A-C-D depth 2 and A-B-C-D depth 3), then GREEN via wrapping both `walk_outbound_edges` and `walk_inbound_edges` recursive CTEs with an intermediate `deduped AS (SELECT DISTINCT ON (edge_id) ... FROM walk ORDER BY edge_id, depth ASC)` CTE — keeps the shortest-path row per unique edge. Doc-comment on `walk_outbound_edges` updated to call out shortest-path-per-unique-edge semantics; inline SQL comments cross-reference issue #114. (2) `c25bf96` adds new `Graph::walk_edges_around(seed_id, max_depth, per_direction_limit) -> EdgesAround` trait method + `EdgesAround { outbound: Vec<WalkedEdge>, inbound: Vec<WalkedEdge> }` public struct + new integration test `walk_edges_around_matches_separate_walks_and_clips_per_direction` pinning byte-equivalence with separate per-direction calls + per-direction LIMIT clipping (2/2 not 2 total) + max_depth=0 short-circuit. SQL chains four CTEs per direction (recursive walk → DISTINCT ON dedupe → entity-join + per-direction LIMIT) then UNION ALL with outer ORDER BY (direction, depth, edge_id). Trailing `direction` text column (col 11) is the partition discriminant; Rust decoder reads it after `decode_walked_edge` and partitions into the struct. (3) `c12774a` migrates `hhagent-cli relations show` from two separate walk calls to one `walk_edges_around` call. Halves DB round-trip count (now 1 seed lookup + 1 walk = 2 trips, down from 3). Pure-functionally byte-equivalent: same input fixture produces the same rendered output. The existing `cli_relations_show_e2e` integration test is the regression pin. (4) `0bb65b1` adds "When you want both directions" callout to both per-direction method doc-comments pointing at `walk_edges_around`. **Per-direction methods retained as a stable surface** for callers that genuinely need only one direction (e.g. a future recall lane expanding `seed_entity_ids` outbound only); the AC for #115 accepts "stay or remove" and staying avoids migrating 7 existing integration tests for no functional gain. **Workspace 1046 → 1048 (+2) on macOS, all green**: +1 `walk_edges_dedupes_diamond_topology_to_shortest_depth` + +1 `walk_edges_around_matches_separate_walks_and_clips_per_direction`. Linux DGX expected same +2 (both new tests cross-platform). No new clippy warnings. Closes #114 + closes #115. **Architectural note:** the intermediate-CTE-as-pure-transformation pattern (between the recursive walk and the entity-join) is the right shape for any future "rewrite the row set before display projection" need on a recursive CTE — could be reused for a future "most-recently-touched memory per descendant" recall path.
- [x] **Item 29 — `HHAGENT_PG_BIN_DIR` env-var override on test-fixture Postgres discovery (2026-05-26)** — **merged to `main` via PR [#126](https://github.com/hherb/hhagent/pull/126) at `7adc582`** + post-merge polish `463553d` (refactor: compute capacity from `defaults.len()` instead of `Vec::with_capacity(17)` magic number; surfaced during post-implementation review) + filed issue [#127](https://github.com/hherb/hhagent/issues/127) (env-var save/restore RAII helper for the 4 unit tests' panic-safety). Three branch commits: 1 session-start doc-sync `c3dd3e4` (sync to main = a1dbef6 for PR #125 / Item 28 merge) + 1 feature `557ca3b` (new public `hhagent_db::pg_bin_dir_candidates_with_env_override()` + `PG_BIN_DIR_ENV` const + 4 unit tests under `crate::env_lock()` covering unset / valid-prepend / empty-string / whitespace-only) + 1 mechanical refactor `954088c` (6 test-fixture call sites switched: `tests-common::skip::pg_bin_dir_or_skip` plus 5 byte-identical local copies in `core/tests/scheduler_{crash_recovery,lanes,inner_loop,step_dispatch}_e2e.rs` + `core/tests/agent_prompts_e2e.rs`). Closes the Postgres.app session-local-override papercut that PR #118 / Slice 2.5 and PR #125 / Issue #95 Layer B both re-encountered (operator memory `postgres-app-bin-paths.md` documents the workflow). **Semantics:** env var unset OR empty-string OR whitespace-only → defaults only (matches the strict-trim parse style used elsewhere; tolerates `export HHAGENT_PG_BIN_DIR=$(failing_lookup)` evaluating to blank); non-blank value → prepended to defaults so a bogus override falls through rather than skipping tests; single-path-only (multi-path would invite shell-PATH confusion). **Production runtime discovery (`hhagent-db-init` binary) deliberately NOT switched** — override helper is documented test-infra-only. Behaviour byte-equivalent when env var is unset (every developer's default state) — pinned by `pg_bin_dir_candidates_with_env_override_returns_defaults_when_unset` unit test. **Workspace 1042 → 1046 (+4 unit) on macOS**, all green, 0 failed, 3 ignored unchanged. No new clippy warnings. **File-size watch:** `db/src/lib.rs` 753 → 908 LOC (pre-existing over-cap breach grows; sibling-`tests.rs` lift is a separate slice flagged in HANDOVER tech-debt as bundle-with-`batch_upsert.rs`+`worker_lifecycle/*` quadruple). **Out-of-scope follow-ups exposed but deliberately not addressed:** (a) consolidate the 5 local-copy `pg_bin_dir_or_skip` definitions onto `tests_common::pg_bin_dir_or_skip` (pure code-dedup; ~30 LOC removal); (b) lift the 15-second `wait_for_status_active` polling cap in the 6 sites with local `bring_up_pg_cluster` copies that still overshoot on Postgres.app even with this env override (operator memory documents the 12 expected test-failures the override unmasks). Both filed in HANDOVER tech-debt as separate test-infra slices.
- [x] **Item 28 — Issue #95 entity-upsert Layer B (2026-05-26)** — **merged to `main` via PR [#125](https://github.com/hherb/hhagent/pull/125) at `dac0dcd`** + same-PR post-review fix `a1dbef6` (six review follow-ups; +1 unit pinning `is_constraint_violation_code` 5-char SQLSTATE guard against `"23"` / `"230"` / `"2300"` / `"230000"` / `"23X05X"`; +1 e2e `upsert_batch_empty_input_returns_zero_outcome`; `dedup_entity_inputs` now returns parallel `Vec<String>` of name_norms so the dispatcher avoids double-normalize; phase-2 partial-commit semantics documented on both `per_row_*` helpers; stray double blank line removed). Branch `feat/issue-95-upsert-layer-b` (worktree `/Users/hherb/src/hhagent-issue-95`) carried 12 implementation commits + the post-merge fix. Updated workspace tally **1023 → 1042 (+19, +11 unit / +8 integration) on macOS**, all green. Issue [#95](https://github.com/hherb/hhagent/issues/95) originally framed Layer B as deferred-until-trigger; picked up anyway with a **batch-first + per-row attribution fallback** design that preserves the diagnostic value the original deferral was worried about. Empirical re-frame: today's Layer A loop has weak per-row attribution too (`format!("upsert entity: {e}")` doesn't identify the failing entity), so the fallback path is an opportunity to ADD per-row attribution rather than just preserve parity. 12 commits TDD-ordered: 2 docs + 4 pure-helper slices (`dedup_entity_inputs`, `build_entity_unnest_arrays`, `is_constraint_violation_code`+wrapper, `format_per_row_{entity,relation}_error`) + 1 big entity-batch slice (`5aed725`, includes the dispatcher + delegate refactor in `gliner_relex.rs`) + 2 entity integration-test slices (order/dedup + quarantine preservation) + 1 entity-fallback slice (FK violation on `entity_kind`; uses `drug` as the safely-deletable kind) + 1 relations-batch slice (replaces the Task 5 legacy placeholder with `try_batch_upsert_relations` + `per_row_upsert_relations` + `build_resolved_triples` + `ResolvedTriple`; deletes `upsert_relations_per_row_legacy` from `gliner_relex.rs`) + 1 relations-fallback slice (FK on `relation_kind`; uses `eats` as bogus relation, `concept` for the `lunch` entity since `event` isn't in seed). New module `core/src/entity_extraction/batch_upsert.rs` (547 LOC) holds both phases + 5 pure helpers; `gliner_relex.rs` 625 → 571 LOC; public `upsert_entities_and_relations` keeps its name and signature but body becomes a 1-line delegate. `UpsertOutcome`, `EntityExtractionError`, and the 8-key `build_extract_entities_payload` audit contract are byte-frozen. Quarantine no-op `SET name_norm = entities.name_norm` retained in both batch and per-row SQL (load-bearing for operator approvals). Two-phase dispatch: phase 1 tries batch → on SQLSTATE 23 falls back to per-row with diagnostic wrap → otherwise propagates; phase 2 is independent (relations failing doesn't roll back successful entities). Same-key input duplicates handled via Rust-side `HashSet`-based dedup before unnest (avoids Postgres `cardinality_violation: ON CONFLICT DO UPDATE cannot affect row a second time`); duplicates resolve to the same id via post-batch `HashMap` re-walk in original input order; `n_entities_upserted_new` uses a `counted_new` `HashSet` to not double-count duplicates. **Workspace 1023 → 1040 (+17, +10 unit / +7 integration) on macOS, all green**: +3 `dedup_entity_inputs` unit + +2 `build_entity_unnest_arrays` unit + +3 `is_constraint_violation_code` unit + +2 error-formatter unit + +7 integration (happy-path + order + dedup + quarantine + entity-FK fallback + relations-batch happy + relations-FK fallback). Empirically verified against real PostgreSQL (Postgres.app v18 at `/Applications/Postgres 2.app/Contents/Versions/18/bin/`) via session-local `default_pg_bin_dir_candidates` override; override reverted before final commit per PR #118 / Slice 2.5 pattern. Both fallback tests fire the per-row attribution chain correctly — error messages contain `kind='drug', name_norm='aspirin'` (entity FK) and `src=N, dst=M, kind='eats'` (relation FK), proving the attribution improvement. Final whole-branch code review (`feature-dev:code-reviewer`): **Approved**, no critical issues; two minor notes (trivial double-blank-line in `gliner_relex.rs`, 47-LOC inline-test-module overage on `batch_upsert.rs`). All 5 existing Layer A integration tests continue to pass byte-equivalently through the new delegate path (regression pin). **Deliberately NOT done:** no migration (schema unchanged); no public-API change; no audit-payload change; no transaction wrapping (matches today's Layer A pseudo-atomicity); no retry loop on fallback; no `entities.attrs` / `relations.attrs` plumbing; no `KindsCli<T>` shared-generic lift (separate slice); no env-var override for `HHAGENT_PG_BIN_DIR` (still tracked as test-infra tech-debt). Spec at `docs/superpowers/specs/2026-05-25-issue-95-layer-b-design.md`; plan at `docs/superpowers/plans/2026-05-25-issue-95-layer-b.md`.
- [x] **Item 27 — Slice 2.5 follow-up triple (2026-05-25)** — **merged to `main` via PR [#124](https://github.com/hherb/hhagent/pull/124) at `e93997e`** + same-PR post-review fixup `55bb8f1`. Bundles three small follow-ups filed during PR #118 (Slice 2.5) post-merge review into one ~0.5-session PR per HANDOVER pickup guidance. Three commits + 1 fixup, one per issue, TDD-ordered: (1) `07de743` closes [#121](https://github.com/hherb/hhagent/issues/121) — `slot_for_key_excludes_container_image` invariant test in `core/src/worker_lifecycle/idle_timeout.rs::tests`. Pins that the warm-cache key carries `tool_name` only (NOT `ToolEntry.container_image`) via `Arc::ptr_eq` on two `slot_for(&registry, "twice-name")` calls; failure message references the live-reconfigure landmine scenario explicitly. Also widens `slot_for`'s doc-comment with a "Warm-cache key invariant" section spelling out the two acceptable paths for any future live-reconfigure work (widen the key OR explicitly evict the slot). (2) `9cb375e` closes [#120](https://github.com/hherb/hhagent/issues/120) — surfaces missing container images at daemon startup. Five-part change: new pure `build_image_inspect_argv(tag) -> Vec<String>` helper in `sandbox/src/macos_container.rs` (returns `["container", "image", "inspect", <tag>]`); new `MacosContainer::probe_image(tag) -> Result<(), SandboxError>` method spawning that argv with operator-actionable diagnostic on miss; new `ToolRegistry::entries()` borrowed iterator over `(&str, &ToolEntry)`; new `core/src/sandbox_health` module with cross-platform pure `collect_container_image_targets(entries)` (filters Container-backed entries with `Some(image_tag)`, buckets by tag, sorted ascending, dedup tools-sharing-image) + macOS-gated `probe_registered_container_images` (calls `probe_image` per target, emits `tracing::info!` on hit and `tracing::warn!` on miss naming the affected tools, daemon continues bring-up regardless); `core/src/main.rs` wires the walk after `build_tool_registry` inside `#[cfg(target_os = "macos")]`. The pure target-collection helper is cross-platform (unit-testable on Linux); only the spawning probe driver is OS-gated (Linux has no `Container` variant in `SandboxBackendKind`). Per the issue body's "third option" (daemon-startup health check) — keeps the resolver hot-path cheap (no per-acquire probe spawn) and runs at a moment where the daemon can log + continue rather than crash + lose all other tools. (3) `a569885` closes [#122](https://github.com/hherb/hhagent/issues/122) — rewrites `core/tests/gliner_relex_e2e.rs::skip_if_image_missing` from ~30 LOC of `container image list` per-line substring-parsing to ~10 LOC of `MacosContainer::probe_image` delegation. The chosen mechanism (switch from `image list` to `image inspect`) supersedes the issue body's proposed tokenization fix because it removes the substring-matching surface entirely (`container image inspect <tag>` is a single targeted call with exit-code-driven success, no parsing). No new tests for #122 — `probe_image` is already smoke-tested via #120's `probe_image_returns_ok_for_cached_image_and_err_for_missing_tag`. (4) `55bb8f1` — post-review fixup: empty-tag guard on `MacosContainer::probe_image` (rejects `""` early with diagnostic instead of spawning `container image inspect ""`); short-circuit on `MacosContainer::probe()` failure inside `probe_registered_container_images` (skip the per-tool walk when the backend itself can't probe — avoids N redundant probe spawns each emitting the same `container system status` error); `tool_names` tracing field switched from `?Debug` to comma-joined Display (operator-readable); `_probe_results` discard-rationale comment in `main.rs`. **Workspace 1011 → 1023 (+12) on macOS, all green**: +1 for #121 + (+3 build_image_inspect_argv unit + +1 probe_image smoke + +6 sandbox_health unit = +10) for #120 + 0 for #122 + 1 empty-tag-guard fixup. Ignored unchanged at 3. Linux DGX expected +8 (the +3 argv unit + +6 sandbox_health unit + +1 #121 + 1 fixup = every cross-platform test; the +1 probe_image smoke and the daemon walk are macOS-only). No new clippy warnings. **Pure-function-separately-testable-from-spawn pattern** now applied three times in `macos_container.rs` (`build_container_argv`, `clamp_memory_to_minimum`, `build_image_inspect_argv`). Worktree `../hhagent-item-27` and merged local branch `feat/item-27-slice-2.5-followups` cleaned up post-merge.
- [x] **Item 26 — `gliner_relex.rs` test-module sibling lift (2026-05-25)** — branch `refactor/item-26-gliner-relex-test-module-lift`, **merged to `main` via PR [#123](https://github.com/hherb/hhagent/pull/123) at `920e0c9`**. Pure mechanical refactor closing the second-worst 500-LOC breach in the workspace (the worst is `postgres_e2e.rs`, a deliberately-bulk-of-tests file). Mirror of Item 23(a) which lifted `relations_show.rs` (576 → 380 LOC) and `db/src/graph.rs` (761 → 673 LOC). After Slice 2.5 grew `core/src/workers/gliner_relex.rs` to 1547 LOC (the inline test module contributed ~735 LOC), this was the obvious 0.5-session cleanup. Three commits: `4e6bbe5` docs sync to main = a9e3385 (out of scope for the lift itself; refreshed HANDOVER + ROADMAP to reflect PR #118 + #119 merged + post-merge work), `cbbdf2d` the actual mechanical lift, and `c4dd3ee` a PR-review-nit doc-comment trim on the new `tests.rs` module header (dropped a paragraph of historical commentary that rots as workspace shape changes; kept the two load-bearing lines a future reader needs). The inline `#[cfg(test)] mod tests { use super::*; ... }` block at lines 810-1547 of the parent file is replaced by a two-line `#[cfg(test)]\nmod tests;` declaration; the body moves verbatim (one level of 4-space indent stripped, since the wrapping `mod tests { }` is gone) to a new sibling `core/src/workers/gliner_relex/tests.rs` (745 LOC: 9-line module-doc-comment header + 735-line dedented test body). Rust 2018 sibling-directory module resolution — `use super::*;` continues to import from the parent `gliner_relex` module; test names stay at `crate::workers::gliner_relex::tests::*`. **File-size delta:** parent 1547 → **811 LOC** (still over the 500-LOC cap by 311 lines; production-code residual; deferred per the "second-logical-seam" criterion — the Extractor + Client + wire-type cluster is a single logical surface). **Workspace test count unchanged at macOS 1011 / 0 / 3** — verified at branch start, after the lift, AND post-merge on main at `920e0c9`. Linux DGX expected unchanged from its post-Slice-2.5 baseline by the same pure-refactor argument. **Deliberately NOT done:** further split to push parent below 500 LOC (defer until a second logical seam appears); `worker_lifecycle/manager.rs` (559 LOC) same-pattern sibling lift (separate slice, ~30 min); `mod.rs` conversion (Item 23(a) precedent — keep parent path stable for `git log --follow`).
- [x] **Developer onboarding manual (2026-05-25)** — **merged to `main` via PR [#119](https://github.com/hherb/hhagent/pull/119) at `99bbfab`** (impl commit `b6e78b2`). New `docs/devel/manual/` directory with 11 files: 10 chapters (01 what-is-hhagent, 02 dev-env-linux, 03 dev-env-macos, 04 repo-tour, 05 build-test-run, 06 architecture, 07 sandboxing, 08 hard-constraints, 09 rust-patterns, 10 first-contribution) + `index.md` providing three recommended reading paths by experience level. Each chapter capped at ≤ 2 A4 printed pages. Targets contributors unfamiliar with Rust or OS-level security. Docs-only — no Rust changes; workspace test count unchanged.
- [x] **Item 25 — GLiNER-Relex Slice 2.5 (2026-05-25)** — **merged to `main` via PR [#118](https://github.com/hherb/hhagent/pull/118) at `a9e3385`** + post-merge `e11e3f3` (macOS test-infra hardening + container e2e empirically verified — canonical `Dr Smith --[treats]--> asthma` extraction PASSED in 12.58s through Apple `container` on this Mac via a session-local Postgres.app discovery override; override reverted before commit to avoid regressing macOS devs who use Postgres.app — see HANDOVER tech-debt) + post-merge `1c7eced` (3 in-scope review-comment cleanups: Containerfile `--no-dev` rationale rewrite, `GlinerRelexEnv.container_image` docstring clarifying orthogonality from `use_container_backend`, `container_mode_entry` enforcement-parity note for `mem_mb` vs `cpu_quota_pct`/`tasks_max`; 3 deferred follow-ups filed: [#120](https://github.com/hherb/hhagent/issues/120) image-not-found surfaced earlier via `probe_image`, [#121](https://github.com/hherb/hhagent/issues/121) warm-cache key invariant test, [#122](https://github.com/hherb/hhagent/issues/122) `skip_if_image_missing` tokenization tightening). **Closes [#107](https://github.com/hherb/hhagent/issues/107)** (PID-1 signal handling) inline. First real workload routed through Apple `container` micro-VM on macOS — closes the macOS memory-enforcement gap (Seatbelt has no memory primitive; `mem_mb=4096` is finally enforced on darwin). 18 commits on top of `main` (`919882d`): 2 docs (spec + plan) + 10 TDD-ordered tasks + 8 review-fixup commits sprinkled throughout. **Sandbox layer:** `--init` added unconditionally to `build_container_argv` (parallel to LinuxBwrap's unconditional `--as-pid-1`; required for long-lived workers so the in-VM worker doesn't inherit PID 1 and ignore SIGTERM). `SandboxBackends::resolve()` widened to `(kind: Option<SandboxBackendKind>, image: Option<&str>) -> Arc<dyn SandboxBackend>`; on `(Some(Container), Some(tag))` constructs a fresh `Arc::new(MacosContainer::with_image(tag))` per call (cheap — String + Arc; probe was image-independent so no re-probe needed). **Core layer:** new `container_image: Option<String>` field on `ToolEntry` (mechanical ripple to 7 files / 13 literal sites); both lifecycle managers (`SingleUseLifecycle`, `IdleTimeoutLifecycle`) pass `entry.container_image.as_deref()` to the resolver. **Workers layer:** `GlinerRelexEnv` widened with `use_container_backend: bool` + `container_image: Option<String>`; `resolve_env` reads `HHAGENT_GLINER_RELEX_USE_CONTAINER=1` (strict-only-"1" trim) + `HHAGENT_GLINER_RELEX_IMAGE=<tag>` and skips the host-venv check in container mode (the worker shim lives inside the image); `gliner_relex_entry` branches on `use_container_backend` to `host_mode_entry` (byte-equivalent to pre-Slice-2.5) or `container_mode_entry` (binary = `/usr/local/bin/hhagent-worker-gliner-relex`, fs_read = `[weights_dir]` only with the host path mounted at the same path inside the container, `sandbox_backend = Some(Container)`, `container_image = Some(<image>)`); shared helpers `build_runtime_env` + `build_idle_timeout_lifecycle` eliminate duplication. **New constants pinned at module top:** `CONTAINER_IMAGE_DEFAULT = "hhagent/gliner-relex:dev"` + `CONTAINER_BINARY = "/usr/local/bin/hhagent-worker-gliner-relex"`. **New Containerfile + build helper:** `workers/gliner-relex/Containerfile` (python:3.12-slim + `uv pip install --system .` + `USER nobody` + `ENTRYPOINT`); `scripts/workers/gliner-relex/build-image.sh` operator helper (probes both `container --version` and `container system status` before building; clear stderr on misconfig). Operator built `hhagent/gliner-relex:dev` on this Mac (M3 Max, macOS 26.5+, Apple `container` 0.12.3). **Tests:** +13 net (998 → 1011 on macOS; Linux DGX gets +cross-platform-only subset): `argv_carries_init_*` + 2 resolver image-routing pins + `shell_exec_entry_defaults_container_image_to_none` + 4 `resolve_env_{sets_use_container_backend, strict_about_use_container_value, skips_venv_existence_check, picks_up_container_image_override}` + 3 `entry_{host_mode_container_image_is_none, container_mode_emits_in_container_binary, container_mode_honours_custom_image_tag}` + 1 container `--init` smoke + 1 `happy_path_container_extract_returns_entities_and_triples` e2e. Python `workers/gliner-relex` 35 → 36 (unrelated; no Slice 2.5 Python changes). **Empirical verification done at session-time** (via post-merge `e11e3f3`): the new `happy_path_container_extract_returns_entities_and_triples` e2e PASSED in 12.58s through Apple `container` on this Mac (1 passed; 0 failed; 0 ignored) when Postgres.app v18 was added to the discovery candidates. The override was reverted before commit to avoid regressing every macOS dev using Postgres.app (per-test launchd-driven bring-up overshoots the 6 local `bring_up_pg_cluster` copies' 15s status-Active polling window — see HANDOVER tech-debt for the proper fix shape). The test remains a permanent regression pin: fires green automatically on any host with PG discoverable; skip-passes elsewhere via `pg_bin_dir_or_skip`. **File-size watch:** `core/src/workers/gliner_relex.rs` 1238 → ~1494 LOC (Item 26 already tracks); `core/src/worker_lifecycle/manager.rs` ~343 → 559 LOC (new cap breach from Slice 2 routing pin tests + Slice 2.5 rerouting; flagged in HANDOVER tech-debt as sibling test-module lift candidate). **Deliberately NOT done:** Linux Firecracker counterpart (Linux already enforces via cgroup v2); `python-exec` migration (Slice 3 territory); cargo-driven image build automation; multi-arch image; `HHAGENT_GLINER_RELEX_REQUIRE_E2E=1` CI hardening env knob. Spec at `docs/superpowers/specs/2026-05-23-gliner-relex-slice-2.5-design.md`; plan at `docs/superpowers/plans/2026-05-23-gliner-relex-slice-2.5.md`.
- [x] **Item 23 option (a) — test-module sibling lifts (2026-05-23)** — branch `refactor/item-23-test-module-lifts`, [PR #117](https://github.com/hherb/hhagent/pull/117). Two pure-refactor commits: (1) `core/src/bin/hhagent-cli/relations_show.rs` 576 → **380 LOC** (under cap) with the inline 17-test `#[cfg(test)] mod tests` block lifted verbatim to new `relations_show/tests.rs` (203 LOC). Closes the 76-LOC inline-tests overage flagged at the end of Item 22. (2) `db/src/graph.rs` 761 → **673 LOC** (still over cap by 173 lines) with the inline 6-test block lifted to new `graph/tests.rs` (96 LOC). **23 tests moved total (17 `relations_show::tests::*` + 6 `graph::tests::*`)** — same names, same behaviour, same module paths-up-to-storage. Both use Rust 2018 sibling-directory module resolution — declaring `#[cfg(test)] mod tests;` in the parent file resolves to `<parent>/tests.rs`; `use super::*` continues to import from the parent module so test bodies are byte-equivalent (only the storage location changed). **Workspace test count unchanged at macOS 998 / 0 / 3** (verified from a clean `cargo clean && cargo test --workspace` to rule out the sibling-directory stale-target-dir footgun); **Linux DGX expected 990 / 0 / 4** by the same regression-pin argument. **Option (b) — walk-impl split into `graph/walk.rs`** deliberately deferred per the design note ("preferred if a second `WalkedEdge` consumer materialises… until then, (a) is the cheaper one"); flagged in the HANDOVER Next-TODO list as 23 (b). No clippy delta. No migration changes.
- [x] **Item 22 — kinds-CLI shared lift + over-cap CLI splits (2026-05-23)** — branch `refactor/issue-111-112-kinds-cli-split`, **merged to `main` via PR [#116](https://github.com/hherb/hhagent/pull/116) at `1abb061`**. **Closes [#111](https://github.com/hherb/hhagent/issues/111)** (consolidated kinds-CLI tech-debt: list pool / fixed-width truncation / description size cap) **and [#112](https://github.com/hherb/hhagent/issues/112)** (split `entities.rs` 723 LOC). Five substantive commits + docs sync, TDD-ordered: **Phase A1** splits `entities.rs` 723 → 488 LOC + new `entities_kinds.rs` 293 LOC (`"kinds"` arm in `run_entities` delegates to `crate::entities_kinds::run`). **Phase A2** splits `relations.rs` 851 → 47 LOC + new `relations_kinds.rs` 291 LOC + new `relations_show.rs` 576 LOC (76-LOC overage from inline test module; production code ~400 LOC under cap). **Phase B1** swaps `kinds list` from `connect_admin_pool` → `connect_runtime_pool` in both consumers (runtime role has SELECT on `entity_kinds` + `relation_kinds` per migrations 0015 + 0017; `add`/`remove` continue on admin pool because 0016/0017 REVOKE writes). +2 TDD-ordered `postgres_e2e` integration tests pinning the runtime-pool SELECT path. **Phase B2** introduces shared `common::format_kinds_table(&[KindRow<'_>]) -> String` helper with `KindRow<'a>` borrowing struct, computing per-column widths from `max(header_len, longest_value_len)`. Replaces the `{:<24}` truncation footgun. +5 unit tests in `common::format_kinds_table_tests`. **Phase B3** adds `MAX_{ENTITY,RELATION}_KIND_DESCRIPTION_LEN: usize = 2048` const + `validate_*_kind_description` pure helper + `{Entity,Relation}KindError::DescriptionTooLong { len: usize }` variant + CLI exit-2 path. +12 DB-unit tests (6 per module: 1 const pin + 5 validate_description boundary cases) + 2 e2e assertions added to existing `cli_*_kinds_e2e` tests. **Workspace 971 → 990 (+19) on Linux DGX, all green.** Net-zero from moves (5 + 5 + 17 unit tests moved between modules). No new clippy warnings. **Deliberately NOT done:** `KindsCli<T>` generic (issue #111 mentions as "ideal" but acceptance criteria don't require it; post-split structure makes a future lift easy); `relations_show/tests.rs` sibling lift (defer until either a real second consumer materialises or HANDOVER Item 23's `graph.rs` lift is bundled in). **File-size watch:** all production files under cap except `relations_show.rs` (576 LOC, inline tests overage); `cli_audit.rs` 759 LOC (pre-existing breach, untouched); `postgres_e2e.rs` 4818 LOC (pre-existing breach, +110 from this slice's 2 new tests).
- [x] **`hhagent-cli relations show <entity-id>` (2026-05-23)** — branch `feat/relations-show-cli`, **merged to `main` via PR [#113](https://github.com/hherb/hhagent/pull/113) at `9a46e18`** + same-session post-review fixup `4915ffa` (4 inline + 2 deferred as [#114](https://github.com/hherb/hhagent/issues/114) + [#115](https://github.com/hherb/hhagent/issues/115)). Next-TODO item 21 — operator-facing graph-edge introspection complementing `entities show`. Walks outbound + inbound edges from the seed up to `--depth N` hops (default 1, hard-capped at new `MAX_WALK_DEPTH = 5`); prints one row per edge in canonical `(src_kind, "src_name") --[edge_kind]--> (dst_kind, "dst_name")` shape regardless of which walk surfaced it; tags quarantined endpoints `[Q]`. Plain text default (dynamic column widths — avoids the #111 fixed-width footgun) + `--format json` opt-in (NDJSON for `jq`, with `"type": "header" | "edge"` discriminant added by the post-review fixup). Read-only — uses `connect_runtime_pool` (runtime role has SELECT on `entities` + `relations`), emits no audit row. Two new `Graph` trait methods: `walk_outbound_edges(src_id, max_depth, limit)` + `walk_inbound_edges(dst_id, max_depth, limit)`, both using recursive CTEs over `relations` joined against `entities` with visited-set cycle protection; new `WalkedEdge` struct bundles depth + both endpoints' kind/name/quarantine + edge kind so the renderer needs no secondary lookups. `MAX_WALK_DEPTH = 5` enforced at parser-AND-DB layers (defense-in-depth). Post-review fixup added 4 inline tighten-ups (`SHOW_PER_DIRECTION_LIMIT` doc clarification; `u8::try_from(depth_i32)` checked conversion in `decode_walked_edge`; NDJSON `type` discriminant; `endpoint_str` escapes `\`/`"` in entity names) + 2 deferred to follow-up issues ([#114](https://github.com/hherb/hhagent/issues/114) diamond-graph duplicate edges; [#115](https://github.com/hherb/hhagent/issues/115) single UNION ALL query). `relations show <unknown-id>` exits 1 with `not found` stderr; `relations show` no-args exits 2 with usage line *before* runtime construction (Issue #97 posture). **Test count Linux DGX 939 → 971 (+32)**: +30 from Item 21 (4 graph unit + 7 postgres_e2e integration + 17 CLI unit (14 parse + 2 endpoint + 1 edge_to_json) + 1 dispatcher bad-args + 1 cli_relations_show_e2e round-trip) + 2 from post-review fixup escape-quote unit tests. **macOS expected 947 → 979 (+32)**. **File-size watch:** `db/src/graph.rs` 405 → ~753 LOC and `core/src/bin/hhagent-cli/relations.rs` 293 → ~803 LOC both now over the 500-LOC cap; flagged for bundled refactor with [#111](https://github.com/hherb/hhagent/issues/111) (kinds-CLI shared lift) + [#112](https://github.com/hherb/hhagent/issues/112) (`entities.rs` split). No migration changes.
- [x] **`hhagent-cli entities kinds {add,remove,list}` (2026-05-22)** — branch `feat/entities-kinds-cli` stacked on `feat/relations-kinds-cli`, **merged via PR #110 at `a65bb4a`**. Next-TODO item 20 — symmetric operator-CLI vocabulary manager for the `entity_kinds` lookup table (migration 0015 schema, REVOKEd by 0016). Mechanical mirror of item 16 because the architectural plumbing (`connect_admin_pool`) already exists. Two notable design choices that diverge from item 16: (1) nested under existing `entities` namespace (`entities kinds {add,remove,list}`) rather than introducing a new top-level — preserves the operator UX rule "everything entity-related lives under `entities`"; (2) `parse_kinds_add_args` is a sibling helper in `entities.rs` rather than a shared `common::parse_kind_add_args` lift — premature abstraction for two consumers whose seed counts + fixtures diverge. New `db::entity_kinds::{validate_entity_kind, add, remove, list_all}` + `ENTITY_KIND_UNDEFINED` sentinel + `EntityKindError` (parallel to relation_kinds shape; existing `KindsCache` stays read-only). New `ACTION_ENTITY_KINDS_{ADD,REMOVE}` audit constants + `entity_kinds_{add,remove}_and_audit` helpers mirror their relation_kinds counterparts byte-for-byte except for the table name. **Test count Linux DGX 921 → 939 (+18)** — verified end-of-session: 5 entity_kinds unit + 4 postgres_e2e integration + 2 audit-action-string pins + 5 parse_kinds_add_args unit + 1 dispatcher bad-args + 1 cli_entities_kinds_e2e round-trip. **macOS expected 929 → 947 (+18)**. Cumulative same-session delta from main: Linux 901 → 939 (+38); macOS 909 → 947 (+38). **File-size watch:** `entities.rs` 478 → 723 LOC, now over the 500-LOC soft cap; deliberate for this slice (a sibling `entities_kinds.rs` split is the obvious refactor and lifts cleanly when load-bearing).
- [x] **`hhagent-cli relations kinds {add,remove,list}` (2026-05-22)** — branch `feat/relations-kinds-cli`, **merged via PR #109 at `f234d0c`**. Next-TODO item 16 — operator-facing CLI for the `relation_kinds` vocabulary table (migration 0017), mirroring the `tools_allowlist` CLI surface. New `connect_admin_pool` helper in `hhagent_db::pool` (no `after_connect` SET ROLE — peer auth as cluster bootstrap superuser) — needed because migration 0017 deliberately REVOKEs INSERT/UPDATE/DELETE/TRUNCATE on `relation_kinds` from the runtime role so the daemon cannot widen vocab on its own. New `db::relation_kinds::{validate_relation_kind, add, remove, list_all}` + `RELATION_KIND_UNDEFINED` sentinel constant + `RelationKindError` enum; `remove` rejects `'undefined'` (FK fallback) up front with a typed error. New `ACTION_RELATION_KINDS_{ADD,REMOVE}` audit constants + `relation_kinds_{add,remove}_and_audit` helpers in `core::cli_audit` (best-effort audit-row posture mirror of `tools_allowlist_*_and_audit`). NEW `core/src/bin/hhagent-cli/relations.rs` (293 LOC, under cap) with top-level `relations <kinds>` namespace (room for a future `relations show <entity-id>` graph-edge introspection); `kinds {add,remove,list}` subcommand; `add` accepts optional `--description "<text>"` (persisted as TEXT, serialised as JSON `null` in audit payload when omitted). Subprocess-level e2e in `core/tests/cli_relations_e2e.rs` pins the full chain: add (no desc) → idempotent re-add → add (with desc) → list → remove undefined rejected → remove → idempotent re-remove → oversize-kind validation → audit multiset (2 add + 1 remove rows) → payload spot-check (description-as-null vs description-as-string vs remove-payload-is-`{kind}`-only). +2 bad-args dispatcher pins in `cli_dispatcher_bad_args_e2e.rs` (top-level + kinds). **Test count Linux DGX 901 → 921 (+20)** — verified end-of-session at branch tip: 1 pool reachability + 5 relation_kinds unit + 4 postgres_e2e integration (admin_pool_can_write... / add_is_idempotent... / remove_rejects_undefined_sentinel / list_all_returns_seeded_rows_ordered_by_kind) + 2 audit-action-string pins + 5 parse_add_args unit + 2 dispatcher bad-args + 1 cli_relations_e2e round-trip. **macOS expected 909 → 929 (+20)** (every new test is `#[cfg(any(target_os = "linux", target_os = "macos"))]`). Python unchanged. **Architectural pin:** `connect_admin_pool` is the only non-runtime-role pool helper in the tree; its doc-comment explicitly warns against daemon-side use (would re-open the privilege escalation that 0017's REVOKE closes). Unblocks Next-TODO items 20 (`entities kinds` symmetric subcommand) and the foundation for any future operator-CLI vocabulary management. No migration changes.
- [x] **Issue #55 — macOS Apple `container` backend Slice 2 (2026-05-22)** — **merged to `main` via PR #108 at `1b86f84`** + post-merge fixup `2b271e8` (symmetric `IdleTimeoutLifecycle` counter-backend routing test + `SandboxBackendKind` doc-comment expansion). Per-worker sandbox-backend selection: new `SandboxBackendKind` enum (cfg-gated per-OS so cross-OS mis-config is a compile-time error) + new `SandboxBackends` struct with `default_for_current_os()` constructor + `resolve(kind) -> Arc<dyn SandboxBackend>` resolver in `hhagent_sandbox`. New `sandbox_backend: Option<SandboxBackendKind>` field on `ToolEntry`; existing `shell_exec_entry` + `gliner_relex_entry` both default to `None` (per-OS default backend — byte-equivalent to slice 1). `SingleUseLifecycle`, `IdleTimeoutLifecycle`, `CompositeLifecycle` switch from holding `Arc<dyn SandboxBackend>` to `Arc<SandboxBackends>`; each `acquire` resolves the entry's backend kind per call. Daemon `main.rs` swaps the local cfg-gated `sandbox_backend()` helper (now dead, removed) for `SandboxBackends::default_for_current_os()`. New `core/tests/lifecycle_container_routing_e2e.rs` integration smoke proves end-to-end routing: a `ToolEntry { binary: "/sbin/apk", sandbox_backend: Some(Container) }` driven through `SingleUseLifecycle::acquire` spawns successfully (apk exists in alpine, not on macOS host); skip-as-pass when `container` / `alpine:3.20` missing. Counter-backend unit tests on both `SingleUseLifecycle` and `IdleTimeoutLifecycle` (`*_acquire_routes_via_entry_sandbox_backend_kind`) pin the selection bit (`None → seatbelt`; `Some(Container) → container`). **Test count macOS 901 → 909 (+8); Linux DGX 897 → 901 (+4 cross-platform-only)** — verified end-of-session at `1b86f84`. Spec at `docs/superpowers/specs/2026-05-21-macos-container-slice-2-design.md`; plan at `docs/superpowers/plans/2026-05-22-macos-container-slice-2.md`. **Out of scope (deferred to Slice 2.5):** `gliner-relex` migration to the container backend (needs Containerfile + image-build pipeline); Issue #107 (`--init` / PID-1 signal-handling) — relevant only when long-lived `IdleTimeoutLifecycle` workers actually migrate.
- [x] **Issue #55 — macOS Apple `container` backend Slice 1 (2026-05-21)** — **merged to `main` via PR #106 at `cc0b0de`** + pre-merge post-review fixup `18cb5e2` (5 in-scope items; Issue #107 filed for the `--init` / PID-1 signal-handling concern as a Slice 2/2.5 follow-up). Engineering follow-through on the spike merged via PR #105 (`56456da`). New `sandbox/src/macos_container.rs` ships `MacosContainer: SandboxBackend` + pure `build_container_argv` mirroring `linux_bwrap::build_argv` + `clamp_memory_to_minimum` 200 MiB-floor helper with `tracing::warn!` on clamp + two-step `MacosContainer::probe()` (`container --version` + `container system status`). **Sibling, NOT replacement** for `MacosSeatbelt`: `default_backend()` on darwin still returns Seatbelt (Slice 2 introduces per-worker `WorkerSpec.sandbox_backend` selection). `SandboxPolicy::{mem_mb, cpu_quota_pct, tasks_max}` doc-comments widened to call out the new macOS support + the `tasks_max` semantic gap (cgroup `pids.max` per-cgroup on Linux vs `RLIMIT_NPROC` per-UID inside the Linux VM). New `sandbox/tests/macos_container_smoke.rs` runs 7 real-`container` smoke tests against `alpine:3.20` (skip-as-pass when `container --version` / `container system status` / `alpine:3.20` missing); all 7 passed in ~4 s against Apple `container` 0.12.3 on M3 Max. Pre-merge fixup also guarded `cpu_quota_pct=Some(0)` (drops `-c` rather than emitting `-c 0` which Apple `container` rejects opaquely), strengthened EROFS-vs-EACCES assertions in the readonly + strict-profile smoke tests, and pinned `clamp_memory_to_minimum(0) → (200, true)` for future direct callers. **Test count macOS 866 → 901 (+35)**: Slice 1 alone shipped +33 (26 unit + 7 smoke); fixup added +2 (`cpu_quota_pct_zero_drops_c_flag_entirely` + `clamp_zero_raises_to_floor_and_flags_clamping`). The spike's "stderr-mux resilience" test was deliberately omitted — protocol `Client::from_child` reads only stdout per call, so stream-separation is structural; `--progress none` is pinned in the argv as belt-and-braces. **Out of scope for Slice 1** (deferred per spike notes): per-worker backend selection (Slice 2); `gliner-relex` Containerfile + image-build smoke (Slice 2.5); production worker migration (Slice 3). **Open follow-up:** Issue #107 (PID-1 signal-handling posture) needs end-to-end verification before any long-lived worker migrates onto the container backend.
- [x] **Issue #55 — macOS `container` micro-VM discovery spike (2026-05-21)** — **merged to `main` via PR #105 at `56456da`**. One-session feasibility check of Apple `container` 0.12.3 (installed via `brew install container` on macOS 26.5, M3 Max). **Verdict: COMMIT** — viable macOS micro-VM backend. Spike notes at [`docs/superpowers/specs/2026-05-21-macos-container-spike-notes.md`](../superpowers/specs/2026-05-21-macos-container-spike-notes.md). Answers to the 4 discovery questions from issue #55: (1) **CLI stable** — Docker-like surface (`container run`, `container exec`, `container network create`, etc.), well-documented flags, ~25 spike invocations all behaved as documented; one ergonomic quirk is `[6/6] Starting container [0s]` progress on stderr (filterable via `--progress none`). (2) **JSON-RPC stdio works cleanly** — `container run -i` keeps stdin open and line-buffered; verified two-way both single-shot and long-lived (5 sequential JSON-RPC requests through one `container run` in 0.76 s total — per-request cost INSIDE an already-running container is effectively zero; matches the `IdleTimeoutLifecycle` shape). (3) **`SandboxPolicy` mapping is clean 1-to-1** — every primitive maps to a `container run` flag (`fs_read/fs_write → --mount type=bind`; `mem_mb → -m <N>M` with a 200 MiB floor (clamped with `tracing::warn!` on hit); `cpu_ms → HHAGENT_CPU_MS` + existing `workers/prelude::rlimit` env path unchanged; `cpu_quota_pct → -c <fractional vCPUs>` (Linux-cgroup-only today; macOS support is new in this slice); `tasks_max → --ulimit nproc` (semantic gap: cgroup `pids.max` per-cgroup on Linux vs `RLIMIT_NPROC` per-UID on macOS); `env → -e`; `net::Deny → --network none`; `net::Allowlist → --network default` + future egress proxy; `Profile::WorkerStrict → --read-only --cap-drop ALL --network none --user nobody --tmpfs <scratch>`). (4) **Cold-start latency** — first-ever image pull 42 s (one-time, multi-arch fan-out); first container after pull 14.5 s; **warm spawn 0.76–0.81 s (80× bwrap, 15× Seatbelt)**; per-call cost inside a long-lived stdio worker effectively zero. Tolerable with `IdleTimeoutLifecycle` reuse; meaningful tax for `SingleUseLifecycle` workers. Spike verified: memory cap enforced via SIGKILL (Python allocator at `-m 256M` died exit 137 at ~192 MiB); FS isolation clean by default (`/Users`, `/home`, host `/etc/passwd` invisible); `--mount source=...,target=...,readonly` rejects writes with `Read-only file system`; `--cap-drop ALL` makes `ping` fail with `permission denied`; `--read-only` + `--tmpfs /tmp` gives writable scratch atop read-only root. **Recommended slice shape** (in spike notes): add `MacosContainerBackend` as a **sibling** to `MacosSeatbelt`, selected per-worker via a new `WorkerSpec.sandbox_backend: Option<SandboxBackendKind>` field. Keeps Seatbelt's <50 ms latency for lightweight workers; container opt-in for workers that need memory enforcement (`gliner-relex`, future `python-exec`). Slice 1 (1 session): `sandbox/src/macos_container.rs` + `clamp_memory_to_minimum` pure helper (returns clamped value + boolean flag, callsite emits `tracing::warn!` when clamping fires) + `build_container_argv` unit-test battery mirroring `linux_bwrap::build_argv` + `macos_container_smoke.rs` cross-platform smoke test + `tool_host` stderr-mux resilience test + `SandboxPolicy::{mem_mb, cpu_quota_pct, tasks_max}` doc-comment widening. Slice 2 (0.5–1 session): per-worker backend selection (validation target is plain `alpine` smoke). Slice 2.5 (1 session): `gliner-relex` Containerfile + image-build smoke (real-model validation on macOS). Slice 3 (deferred until Phase 4): `python-exec` defaults to container on macOS. **No code changes in this spike — documentation only.** Tool install (`container` 0.12.3 + kata-containers static kernel, ~263 MB disk) left in place for the recommended Slice 1 follow-up.
- [ ] **L3 skill crystallisation (design spec only)** — the highest-leverage GenericAgent import: on observed task success (signal from observation-phase captures), distil the successful trajectory into a parameterised JSON-RPC tool-call template stored as an L3 `memories` row. On next similar task, recall surfaces the L3 row and the planner invokes it directly instead of re-exploring. Pre-reqs already in tree: `memory_entities` graph lane (entity-anchored recall of skills), `audit_log` with full plan body (Slice A, 2026-05-15) carries the trajectory needed for distillation, observation-phase fixture captures provide the success/failure ground truth, L1 layer column (PR #68), L1 promotion writer (this session 2026-05-18). The L1 promotion writer's `Plan.l1_insight` → `terminal_l1_insight` → `drain_lane` hook → audit row pattern is the direct precedent — L3 will follow the same shape but distil multi-step trajectories into parameterised tool-call templates. Cross-reference back to `2026-05-15-memory-layer-l1-index-design.md` "Follow-ups this slice unlocks" and `2026-05-17-l1-promotion-writer-design.md` when speccing.
- [ ] `context_manager`: token-budget + task-completion + wall-clock reset triggers
- [ ] Reset snapshot writer (compact context → memory before reset)
- [x] **Worker-output prompt-injection guard — Slice 1 (2026-05-28)** — **merged to `main` via PR [#141](https://github.com/hherb/hhagent/pull/141) at `62905ae`** (4 TDD-ordered commits + 2 post-claim fixes: `45627fd` scopes the privacy-invariant test to `WHERE actor='policy' AND action='injection.blocked'` + adds positive-presence assertion; `e81b079` widens the zero-width strip set to include Word Joiner U+2060, Soft Hyphen U+00AD, and Mongolian Vowel Separator U+180E + files issues [#142](https://github.com/hherb/hhagent/issues/142) (chat-template tokens may false-positive once `web-fetch`/MCP land) + [#143](https://github.com/hherb/hhagent/issues/143) (`extract_scannable_text` `walk()` recursion-depth guard); workspace 1065 → **1096 (+31)** on macOS). New module `core::cassandra::injection_guard` (placed in the existing semantic-oversight namespace alongside `constitutional` and `deterministic` rather than a new `core::policy::` namespace — fits established convention; same publication surface either way). Public surface: `InjectionVerdict { score: f32, decision: InjectionDecision, reason_codes: Vec<&'static str> }`, `InjectionDecision` enum (`Allow` / `Block`, `#[non_exhaustive]` so a future Review tier slots in), `BLOCK_THRESHOLD: f32 = 0.70`, `SCAN_BYTE_CAP: usize = 64 * 1024`, `pub fn screen(text: &str) -> InjectionVerdict`, `pub fn extract_scannable_text(value: &Value, byte_cap: usize) -> (String, bool)`. 22-entry English-substring catalogue across 4 attack classes (instruction_override, role_hijack, secret_exfiltration, unsafe_tool_coercion); per-rule weights summed (cap 1.0); ≥ `BLOCK_THRESHOLD` → Block. `normalize` does lowercase + zero-width strip (ZWSP/ZWNJ/ZWJ/BOM) in one pass — no regex, no leetspeak fold. Catalogue invariant pinned: every class has ≥ 1 entry with weight ≥ 0.70. Wired into `tool_host::dispatch` chokepoint between `worker.call` and the existing audit insert: on Block, the worker result is replaced with a redacted placeholder JSON (`{"injection_blocked": true, "score": ..., "reason_codes": [...]}`) and a SECOND audit row is written (`actor='policy'`, `action='injection.blocked'`) carrying SHA-256 (via `sha2::Sha256`) + byte length + truncation flag + score + class codes — never the raw scanned text. Privacy invariant pinned end-to-end by integration test `policy_audit_row_contains_no_substring_of_blocked_body`. Errors are not screened (they don't reach the planner as text; the scheduler short-circuits on Err). Block returns `Ok(placeholder)`, not `Err` — the planner sees a tool result, not a tool failure, so it can decide retry-vs-finalise without burning retry budget. Two audit rows on Block, tool row first (so operator's tool-tail flow surfaces every dispatch event regardless), policy row second (forensic data). **Workspace 1065 → 1096 (+31) on macOS** post-merge, all green (2 const + 7 `extract_scannable_text` + 13 `screen`/`normalize` + 6 integration + 3 post-claim normalize-strip tests from `e81b079`); no new clippy warnings. PG-required integration tests skip-as-pass on hosts without PG configured; empirical pass verified by all 6 e2e tests on hosts with `HHAGENT_PG_BIN_DIR` set (operator validates on DGX). Commits on branch (TDD-ordered): `536d23a` Task 1 module skeleton, `11a0a8e` Task 2 `extract_scannable_text` helper, `eae2a6f` Task 3 catalogue + screen + normalize, `4e32988` Task 4 dispatch wiring + integration tests. Pre-req closed for: any tool that returns LLM-readable text (which is all of them today + every future channel/web/MCP worker). **Slice 2 candidates (deferred per YAGNI):** Review tier (0.45–0.70 band) + operator-facing CLI surface (`hhagent-cli policy review`) + heuristic / combinatorial scoring + multilingual catalogue + per-tool policy + `tool_host.rs` sibling-lift (708 → 763 LOC, 263 over the 500-LOC cap; pre-existing residual that this slice contributed +55 LOC to). Spec at [`docs/superpowers/specs/2026-05-28-worker-output-prompt-injection-guard-design.md`](../superpowers/specs/2026-05-28-worker-output-prompt-injection-guard-design.md); plan at [`docs/superpowers/plans/2026-05-28-worker-output-prompt-injection-guard-slice-1.md`](../superpowers/plans/2026-05-28-worker-output-prompt-injection-guard-slice-1.md). Closes HANDOVER Item 30. Pattern inspiration from openhuman `docs/PROMPT_INJECTION_GUARD.md` (GPL-3.0 — re-implemented from scratch to keep the AGPL-3.0 one-way compatibility ambiguity-free).
- [x] **Opaque secret references — Slice 1 (2026-05-29)** — branch `feat/opaque-secret-refs-slice-1`, **merged to `main` via PR [#146](https://github.com/hherb/hhagent/pull/146) at `bc36e4c`** + post-review polish `5885e26` + `9348e9c`. Closes HANDOVER Item 31. New `core::secrets` module: `Vault` (TTL'd `std::sync::RwLock<HashMap>`, default 1h, lazy GC), `SecretRef` opaque newtype, `RedeemResult` (Hit/Expired/NotFound), `substitute_refs_in_params` walker (exact-match only). Chokepoint wiring in `tool_host::dispatch`: substitution BEFORE `worker.call`; fail-closed on miss (`ToolHostError::SecretRedemptionFailed`, worker not called, no tool row); on Hit, emit one `policy / secret.redeemed` row per ref (best-effort). `materialize` hard-fail on audit error (no materialized-but-unaudited ref can exist). `HHAGENT_BOOTSTRAP_SECRETS` env var drives daemon-startup materialization via `OsKeyringProvider`. `ToolHostError` widened to `#[non_exhaustive]`; `SecretRedemptionFailed` maps to `POLICY_DENIED` in the scheduler classifier. 4 TDD-ordered commits: `b58cd55` (module skeleton + types), `3bc66a3` (Vault impl), `7b1e3c5` (substitute walker + FakeVault), `19eebd6` (dispatch wiring + 8 integration tests + classifier test). **Workspace 1096 → 1137 (+41) on macOS** without PG (7 unit + 9 vault + 16 walker + 1 classifier + 8 integration); with PG live 636 / 2-pre-existing-flakes. No new clippy warnings. Slice 2 (deferred): CLI ↔ daemon IPC + `hhagent-cli secrets materialize`; per-task vault scoping; embedded substitution; `tool_host.rs` sibling-lift bundling Items 30 + 31. Design spec at `docs/superpowers/specs/` (Item 31 design). Pre-req for the Phase-5 frontier-router policy gate (API keys must not reach LLM transcripts).
- [x] **Opaque secret references — Slice 1 follow-up: redact plaintext in tool audit row (Issue [#147](https://github.com/hherb/hhagent/issues/147)) (2026-05-29)** — branch `fix/issue-147-redact-tool-req-plaintext`, **merged to `main` via PR [#151](https://github.com/hherb/hhagent/pull/151) at `54e8885`**. Slice 1 scoped the secret-plaintext privacy invariant to `actor='policy'` rows only, leaving the `tool:<name>` row's `payload.req` carrying the redeemed plaintext (snapshotted after substitution). Fix: snapshot `req_for_audit` in `tool_host::dispatch` BEFORE `substitute_refs_in_params`, so the tool row records the opaque `secret://<8-hex>` refs as issued; the worker still receives the substituted plaintext. Chosen over the issue's reverse-substitution proposal (snapshot-before is a one-line move, can't redact a coincidental match, and records the request as issued). `payload.result` is out of scope by design (a worker may legitimately echo a secret it was given). TDD: widened `policy_rows_contain_no_substring_of_redeemed_plaintext` to also assert the tool-row `req`, + new `tool_row_req_shows_opaque_ref_not_plaintext` (negative + positive). Both RED pre-fix, GREEN after. **Workspace 1144 → 1145 (+1) on macOS**; `secret_vault_e2e` 9/9 live against Postgres.app v18. Filed Issue [#150](https://github.com/hherb/hhagent/issues/150) (pre-existing `mem_burner` clippy `uninit_vec` now deny-level, blocks `--all-targets`; unrelated, `build`/`test` unaffected).
- [x] **Issue [#143](https://github.com/hherb/hhagent/issues/143) — `injection_guard::walk()` recursion-depth guard (2026-05-29)** — **merged to `main` via PR [#155](https://github.com/hherb/hhagent/pull/155) at `6e82252`**; #143 CLOSED (commits `d72324c` fix + `97add3a` docs-reconcile + `40e2911` sibling-skip note → filed [#156](https://github.com/hherb/hhagent/issues/156); `injection_guard.rs` 615 → 619 LOC). `core::cassandra::injection_guard::walk` (the recursive body of `extract_scannable_text`) had no depth cap, so a pathologically deep worker JSON-RPC result could overflow the dispatcher thread's stack before hitting `SCAN_BYTE_CAP`. Unreachable today (serde_json rejects nesting past 128 at parse; protocol frame-size limit bounds wire depth) — defense-in-depth backstop. Fix: new `pub const MAX_WALK_DEPTH: usize = 256` + a `depth: usize` param to `walk` that bails (signalling truncation via the existing `(String, bool)` channel) once nesting reaches the cap; `extract_scannable_text`'s public signature unchanged. Chose depth-cap over the issue's "iterative walk with explicit stack" alternative — smaller, lower-risk, preserves the readable recursive shape and existing truncation semantics. TDD: `walk_stops_at_max_depth` (RED watched: 300-deep leaf left unscanned + truncated) + `walk_captures_content_below_max_depth` (100-deep leaf still fully scanned) + `max_walk_depth_is_256` (drift pin). No literal 100k-deep overflow test — any `Value` deep enough to overflow `walk` also overflows `serde_json::Value`'s recursive `Drop` glue at teardown, and can never reach `walk` in production; overflow-safety is a corollary of the ≤256-frame bound. **Workspace 1145 → 1148 (+3) on macOS**; `injection_guard` module 28/28; no new clippy lints. #142 (chat-template false-positives) **deferred** per its author's recommendation — decide with real data once a `web-fetch`/MCP worker exists. **File-size:** `injection_guard.rs` 527 → 615 LOC (115 over cap); test-module sibling-lift flagged as follow-up. Closes #143 on merge.
- [x] **Issue [#99](https://github.com/hherb/hhagent/issues/99) — migrate `ask.rs` + `observation_replay.rs` to `common::with_runtime` (2026-05-30)** — branch `fix/issue-99-cli-with-runtime`, commit `5545b3a`, **merged to `main` via PR [#166](https://github.com/hherb/hhagent/pull/166) at `75e9039`** (all CI green — `cargo check + clippy (linux)` 34s + CodeQL Analyze rust/python/actions); #99 CLOSED. PR #98 (Issue #97) introduced `common::with_runtime(prefix, fut) -> ExitCode` (defer tokio runtime construction until the known-action arm so a dispatch-site typo never spawns worker threads it won't use) and migrated the four per-subcommand dispatchers; `ask.rs` + `observation_replay.rs` were deliberately left on the raw `multi_thread_runtime(prefix) + rt.block_on(fut)` pair to keep that diff focused. Both already parse/validate **all** args before constructing the runtime, so this is a pure stylistic/consistency cleanup — **no behaviour change**. Replaced each two-step block with one `with_runtime(...)` call so **all six** `hhagent-cli` dispatchers share one idiom; `multi_thread_runtime` stays as the inner primitive (still used by `with_runtime`). **Why no TDD:** zero new behaviour — the existing suite is the regression pin. Verification (macOS M3 Max, 1.96): `cargo build --bin hhagent-cli` clean; `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0; `cargo test --workspace` **1157 / 0 / 3 (unchanged)**; smoke — every `ask` / `observation replay` arg-validation exit path byte-identical, and `observation replay --captures-dir <empty>` still builds the runtime + prints `(no captures found …)` exit 0. File-size: `ask.rs` 326 → 323, `observation_replay.rs` 156 (both well under cap).
- [x] **Bug [#150](https://github.com/hherb/hhagent/issues/150) — `cargo clippy --all-targets` deny-level `uninit_vec` (2026-05-29)** — _merged to `main` via PR [#152](https://github.com/hherb/hhagent/pull/152) at `560d845`._ `sandbox/tests/fixtures/mem_burner.rs:46` did `Vec::with_capacity(bytes)` + `unsafe { set_len(bytes) }`, which `clippy::uninit_vec` (now deny-by-default) hard-errors on, breaking the whole `cargo clippy --workspace --all-targets` gate. Fix: `let mut buf: Vec<u8> = vec![0u8; bytes];` and drop the `unsafe`. The page-touch loop below it stays, so the OOM-kill test's residency-forcing behaviour is unchanged (`vec![0u8; n]` may hand back demand-zero pages, but the loop dirties one byte per page regardless). **Verify:** `cargo clippy --workspace --all-targets` now finishes with 0 errors (pre-existing warnings remain, non-fatal). `cargo build`/`cargo test` were never affected.
- [x] **Bug [#144](https://github.com/hherb/hhagent/issues/144) — `hhagent-core` did not compile on Linux (2026-05-29)** — _merged to `main` via PR [#152](https://github.com/hherb/hhagent/pull/152) at `560d845`; `linux-check` CI green on `ubuntu-latest`._ `core/src/workers/gliner_relex.rs::container_mode_entry` referenced the macOS-only `SandboxBackendKind::Container` variant unconditionally, and `gliner_relex_entry` dispatched to it on every target, so the core crate (the primary host is the DGX = Linux) was broken on Linux. Fix mirrors the existing `core/src/sandbox_health.rs` cfg pattern: `#[cfg(target_os = "macos")]`-gate `container_mode_entry` + the two container consts (`CONTAINER_IMAGE_DEFAULT`, `CONTAINER_BINARY`) + the `if env.use_container_backend` branch in `gliner_relex_entry`; in `resolve_env`, drive `use_container_backend` to a **compile-time `false`** on non-macOS (the env var isn't even read there — an operator setting `HHAGENT_GLINER_RELEX_USE_CONTAINER=1` on Linux silently gets host-mode + bwrap). Gated the 6 container-specific unit tests in `gliner_relex/tests.rs` to macOS (the other crates' container tests were already gated). **macOS unchanged: 1145/0/3.** Linux can't be fully verified on the dev Mac (cross `cargo check` dies in a C dep needing `aarch64-linux-gnu-gcc`); the durable regression guard is a new `.github/workflows/linux-check.yml` running `cargo check --workspace --all-targets` + `cargo clippy --workspace --all-targets` on `ubuntu-latest`. **Verify:** the CI job (or `cargo check` on the DGX).
- [x] **Issue [#156](https://github.com/hherb/hhagent/issues/156) — `injection_guard::walk()` depth-skip continues siblings (2026-05-29)** — **merged to `main` via PR [#158](https://github.com/hherb/hhagent/pull/158) at `f3c380f`**; #156 CLOSED (code commit `43598b4` + docs `227b87a`/`cd9bc2d`). `walk()` used one `bool` return for two signals ("did truncation happen" + "stop the whole walk"), so on *either* cap hit it returned `true` and the first array/object caller aborted the **entire** remaining walk — a `{"decoy": <deep subtree>, "later": "<injection>"}` object truncated on `decoy` (visited first alphabetically) and never scanned `later`, an injection-evasion vector for an attacker controlling worker-output ordering. Filed from PR #155 review. Fix: thread a `&mut bool truncated` out-param; the return value now means only "stop the entire walk (byte budget exhausted)". **Depth cap** (local to one branch): set `truncated`, `return false` → skip the too-deep subtree but keep scanning siblings (`out` has room) — closes the evasion. **Byte cap** (global budget on `out`): set `truncated`, `return true` → abort, since a full buffer can't admit later content and continuing only burns CPU. The byte-cap residual window is inherent to the accumulate-then-scan design (documented; closing it needs a scan-as-you-go rework, out of scope). TDD: new `walk_continues_siblings_after_depth_skip` (RED: `body == ""`; GREEN: later sibling scanned + Block); existing `walk_stops_at_max_depth` / `walk_captures_content_below_max_depth` unchanged-green. **Workspace 1151 → 1152 (+1) on macOS; `injection_guard` module 29/29; no new clippy lints** (`walk` 4→5 args, under the 7-arg threshold). **PG-gated `injection_guard_e2e` NOT re-verified live** — every attempt died in `tests-common` PG cluster bring-up (#130 launchd contention, never reached an assertion); pure-function change, no PG/dispatch path. **File-size:** `injection_guard.rs` 619 → 667 LOC (167 over cap — the test-module sibling-lift to `injection_guard/tests.rs`, Next-TODO 4b, is the tracked remedy → ~285 LOC). Closed #156 on merge.
- [x] **Next-TODO item 4b — `injection_guard.rs` test-module sibling-lift (2026-05-29)** — **merged to `main` via PR [#159](https://github.com/hherb/hhagent/pull/159) at `1106145`**; code commit `c0bb178` (PR also carried the post-#158 docs-reconcile `2ba8e08`). `injection_guard.rs` had grown to **667 LOC** (167 over the 500-LOC cap) across the #143 depth-guard + #156 sibling-continue arcs, the bulk being its inline `#[cfg(test)] mod tests`. Lifted that block verbatim (de-indented one level + a module-doc header) into a new sibling `core/src/cassandra/injection_guard/tests.rs`, following the established Rust-2018 sibling-directory pattern (`secrets/vault/tests.rs`, `workers/gliner_relex/tests.rs`); parent now declares `#[cfg(test)] mod tests;`. Pure-mechanical, no behavior change — the 29 module tests are the regression pin and the workspace count is unchanged. **Workspace 1152 → 1152 (no delta) on macOS; `injection_guard` module 29/29 (pre/post identical); no new clippy lints.** File-size: `injection_guard.rs` **667 → 338 LOC** (under cap — item 4b resolved); new `injection_guard/tests.rs` 338 LOC (test file). Remaining over-cap file in the arc: `tool_host.rs` 911 LOC (Next-TODO item 5).
- [x] **Next-TODO item 12 — graph-lane production wiring: reconcile (already done) + seed-thread regression test (2026-05-30)** — branch `test/router-agent-graph-seed-threading`, **PR [#162](https://github.com/hherb/hhagent/pull/162) — MERGED to `main` at `a83be4a`**. Operator picked "graph-lane production wiring," but exploring it found **item 12 already shipped** in the 2026-05-19 entity-extraction-v2 / "Slice F" arc (PR #91) — `RouterAgent::formulate_plan` ([`agent.rs:122`](../../core/src/scheduler/agent.rs#L122)) calls `entity_extractor.extract(&ctx.instruction)` every iteration and threads `&seeds.ids` into `recall_builder.build_with_seeds(...)` (graph lane ON via `RecallParams::with_seeds`); [`main.rs:174-260`](../../core/src/main.rs#L174-L260) wires the real `GlinerRelexExtractor` (NoOp fallback only when the worker is disabled). The HANDOVER "Option P / item 12 parked" note predated Slice F and was never reconciled — fixed this session. The **one genuine residual** was test coverage: `router_agent_mock_e2e.rs` exercised `formulate_plan` only with `NoOpEntityExtractor` (seeds `[]`) + `StaticRecallBuilder::empty()` (which discards its `seeds` arg), so nothing pinned the seed thread at the RouterAgent layer — a refactor could silently drop `&seeds.ids` from the recall call or `FormulationMeta` and all RouterAgent tests would stay green. New test `formulate_plan_threads_extracted_seeds_into_recall_and_meta` (+1): a `CapturingRecallBuilder` double records the `(query, seeds)` it receives; a `StaticEntityExtractor::with_ids([101,202,303])` produces seeds; asserts (1) the ids reach `build_with_seeds` verbatim with the task instruction as query, and (2) the same ids populate `meta.graph_seed_entity_ids` / `graph_seed_count` / `graph_seed_source == GlinerRelex`. **Regression coverage, not RED-first TDD** (the wiring already ships; precedent PR #157's "not forced TDD") — proven meaningful by two mutations: `&seeds.ids` → `&[]` turns assertion (1) RED, `graph_seed_entity_ids: Vec::new()` turns assertion (2) RED; both reverted, `git diff core/src/scheduler/agent.rs` empty → **zero production change**. **Workspace 1152 → 1153 (+1) on macOS, all green**; new test file only (no production/clippy/file-size delta). **Remaining parked work is the quarantine review gate, NOT the wiring** (issue [#40](https://github.com/hherb/hhagent/issues/40)): freshly-extracted entities default `quarantine=TRUE` and `graph_search` filters `quarantine=FALSE`, so seed entities surface no memories until an operator un-quarantines them.
- [x] **`macos_container.rs` sibling test-module lift (2026-05-30)** — branch `refactor/macos-container-test-module-lift`, **merged to `main` via PR [#165](https://github.com/hherb/hhagent/pull/165) at `48c0396`** (2026-05-30T02:36:20Z; all CI green — `cargo check + clippy (linux)` + CodeQL Analyze rust/python/actions). Operator picked it from the refactor bucket (over #134 tests-common, #99 `with_runtime`, GLiNER-e2e CI knob): at **983 LOC** it was the largest over-cap production file with a *clean* lift available (~494 LOC was the single inline `#[cfg(test)] mod tests` block; `scheduler/audit.rs` 1106 + `db/graph.rs` 926 are mostly production logic — real splits, not chosen). Lifted the test block out of [`sandbox/src/macos_container.rs`](../../sandbox/src/macos_container.rs) into a new sibling [`sandbox/src/macos_container/tests.rs`](../../sandbox/src/macos_container/tests.rs), following the established Rust-2018 sibling-directory pattern (`injection_guard/tests.rs`, `tool_host/tests.rs`, `gliner_relex/tests.rs`, `secrets/vault/tests.rs`); parent now declares `#[cfg(test)] mod tests;`. Test body moved verbatim, de-indented one level (`sed 's/^    //'` — verified safe: every non-empty body line started with ≥4 spaces; the `r"`/`\`-pattern matches were false positives like `container"`, not real raw-string/continuation literals) + a module-doc header (documents the `use super::*` access to private `clamp_memory_to_minimum`/`build_container_argv`/`build_image_inspect_argv`/`ClampedMemory`/`CONTAINER_*` + a pointer to `sandbox/tests/macos_container_smoke.rs`). Pure-mechanical, no behavior change — the 33 module tests are the regression pin; `git diff sandbox/src/macos_container.rs` shows the production portion (lines 1–488) byte-identical (only `mod tests { … }` → `mod tests;`). **Workspace 1157 → 1157 (no delta) on macOS; `macos_container` module 33/33 from the new location; `cargo test -p hhagent-sandbox --lib` 57/57; `cargo clippy -p hhagent-sandbox --all-targets --locked -- -D warnings` exit 0.** macOS-only file (`#[cfg(target_os="macos")]`) — lifted tests run on this Mac; Linux compile covered by `linux-check` `--all-targets`. File-size: `macos_container.rs` **983 → 491 LOC** (under cap — resolved); new `macos_container/tests.rs` 503 LOC (test file). Remaining over-cap production files: `scheduler/audit.rs` 1106 (mostly logic), `supervisor/launchd_agents.rs` 1093, `db/memories.rs` 961, `db/graph.rs` 926 (walk-impl split = Item 23b), `core/workers/gliner_relex.rs` 861.
- [x] **`scheduler/audit.rs` over-cap split (2026-05-30)** — branch `refactor/audit-rs-split-extract-entities`, code commit `3bb0cc7`, **merged to `main` via PR [#167](https://github.com/hherb/hhagent/pull/167) at `79fcc27`** (2026-05-30T04:38:22Z; all CI green — `cargo check + clippy (linux)` 35s + CodeQL Analyze rust/python/actions). Operator picked the **"bigger over-cap file lift"** from the refactor bucket. Measured the five candidates by inline-test fraction; `scheduler/audit.rs` (1106 LOC, the largest) was actually **≈524 prod / ≈582 inline tests** — the prior `macos_container` entry's "mostly production logic" note on it was **inaccurate** — and being **pure functions, no I/O, no clock** (its own module doc), the lowest-risk to reorganize. Two natural seams, following the established Rust-2018 sibling pattern: (1) [`audit/extract_entities.rs`](../../core/src/scheduler/audit/extract_entities.rs) (67 LOC) — the v2 entity-extraction `ACTION_EXTRACT_ENTITIES` const + `build_extract_entities_payload` builder (appended in a later arc) + its 2 tests, co-located in `#[cfg(test)] mod tests`; **re-exported from the parent** (`pub use extract_entities::{…}`) so the public paths `scheduler::audit::{ACTION_EXTRACT_ENTITIES, build_extract_entities_payload}` are byte-for-byte unchanged for callers in `entity_extraction::gliner_relex` + `entity_extraction_e2e` (verified). (2) [`audit/tests.rs`](../../core/src/scheduler/audit/tests.rs) (567 LOC) — the main `mod tests` body (L495–1050), lifted verbatim, de-indented one level (`sed 's/^    //'` — safe: every non-empty line ≥4 spaces; `r"` grep hits false positives like `"operator"`); `use super::*` resolves to the parent `audit`. Main tests have **zero** references to the extract_entities symbols (clean separation). **Pure-mechanical, no behavior change** — the 43 `scheduler::audit` module tests are the regression pin; `git diff core/src/scheduler/audit.rs` is a single hunk `@@ -490,617 +490,11` (production lines 1–489 byte-identical). **Workspace 1157 → 1157 (no delta) on macOS; `cargo test -p hhagent-core --lib scheduler::audit` 43/43 from new locations; `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0; `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0.** Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`. File-size: `audit.rs` **1106 → 500 LOC** (at cap — resolved); new `audit/extract_entities.rs` 67, `audit/tests.rs` 567 (test file). Remaining over-cap production files: `supervisor/launchd_agents.rs` 1093, `db/memories.rs` 961 (≈863 prod), `db/graph.rs` 926 (walk-impl split = Item 23b), `core/workers/gliner_relex.rs` 861, `core/src/scheduler/inner_loop.rs` 655 (#81).
- [x] **`supervisor/launchd_agents.rs` over-cap split (2026-05-30)** — branch `refactor/launchd-agents-split`, code commit `d195d63`, **PR pending**. Operator picked it from the refactor bucket: at **1093 LOC** (~686 prod + ~407 inline tests) it was the largest remaining over-cap production file. **Shape decision:** unlike the `macos_container` lift, a test-only lift would leave production ~686 (still 186 over cap), so this mirrors the `audit.rs` precedent — a test-module lift **plus** a re-exported pure-builder seam (the module doc already names its two "pure, I/O-free, fully unit-testable" sections, `build_plist` + `validate_service_name`). Following the established Rust-2018 sibling pattern (`audit/`, `macos_container/`, `injection_guard/`, `tool_host/`): (1) [`launchd_agents/builders.rs`](../../supervisor/src/launchd_agents/builders.rs) (476 LOC) — the two pure functions `build_plist` + private `xml_escape` + `validate_service_name`, their consts (`DEFAULT_EXIT_TIMEOUT_SEC`, `MAX_NAME_LEN`), and their tests (13 build_plist + 6 validator + 2 xml_escape) co-located in `#[cfg(test)] mod tests`; **re-exported from the parent** (`pub use builders::{build_plist, validate_service_name}`) so the public paths `launchd_agents::{build_plist, validate_service_name}` are byte-for-byte unchanged and the parent driver's 6 internal call sites (5 `validate_service_name` + 1 `build_plist`) resolve via the re-export; `xml_escape` stays module-private. (2) [`launchd_agents/tests.rs`](../../supervisor/src/launchd_agents/tests.rs) (190 LOC) — driver tests (install/uninstall/status + `TestRoot`) + launchctl-parsing helper tests (`parse_print_state` ×3, `is_no_such_service_error`, `user_domain_target`); `use super::*` reaches the parent's private helpers + the `LaunchAgents` driver. Parent [`launchd_agents.rs`](../../supervisor/src/launchd_agents.rs) keeps the driver, `probe`, and the launchctl interaction helpers. **Cross-platform symmetry preserved** — it lives in the public API (`build_plist`/`build_unit_file`, `validate_service_name`, driver struct, `probe`), not the file layout; the re-exports keep the surface identical, and `systemd_user.rs` stays a single 798-LOC file (production ~502, at cap). **Pure-mechanical, no behavior change** — the 52 `hhagent-supervisor` unit tests are the regression pin (count unchanged, just running from new file locations). **Workspace 1157 → 1157 (no delta) on macOS; `cargo test -p hhagent-supervisor` 52 unit + 4 launchd smoke green; `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0.** Doc-link check: `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-supervisor --no-deps --document-private-items` yields the **same 5 pre-existing** unresolved links as `main` (the `#[cfg(target_os="linux")]` `systemd_user` cross-module links, which don't compile on macOS) — **zero new** (one self-inflicted new link to the `Supervisor::install` trait method was caught against the baseline and reworded to point at the `LaunchAgents` struct pre-commit). macOS-only file (`#[cfg(target_os="macos")]`) — Linux compile covered by `linux-check`. File-size: `launchd_agents.rs` **1093 → 485 LOC** (under cap — resolved); new `builders.rs` 476 (≈270 prod + tests), `tests.rs` 190 (test file). Remaining over-cap production files: `db/memories.rs` 961 (≈863 prod), `db/graph.rs` 926 (walk-impl split = Item 23b), `core/workers/gliner_relex.rs` 861, `core/src/scheduler/inner_loop.rs` 655 (#81).
- [x] **Next-TODO item 6 / Issue [#130](https://github.com/hherb/hhagent/issues/130) — launchd bring-up serialization + bundled [#163](https://github.com/hherb/hhagent/issues/163) socket-path fix (2026-05-30)** — branch `fix/issue-130-launchd-bringup-serialization`, **merged to `main` via PR [#164](https://github.com/hherb/hhagent/pull/164) at `091e53d`** (2026-05-30T02:09:54Z); #130 + #163 CLOSED. Operator picked the reentrant-`serial_lock`-in-`bring_up` shape (issue Option 1). **Diagnosis:** `tests-common::serial::serial_lock()` is process-local (serializes parallel `#[test]` threads within one binary, never across binaries), and `bring_up_pg_cluster` took **no** lock — so the within-binary N-way launchd `bootstrap` (e.g. `secret_vault_e2e` 9, `postgres_e2e` 8, `injection_guard_e2e` 6) was the dominant, fixable contention (cargo runs integration binaries sequentially). **Fix (two coupled parts):** (1) [`serial.rs`](../../tests-common/src/serial.rs) lock `Mutex<()>` → **`parking_lot::ReentrantMutex<()>`** — `std::sync::ReentrantLock` is still unstable on 1.96, and reentrancy is mandatory because `supervisor_e2e`/`observation_capture` hold `serial_lock()` and then call `bring_up_pg_cluster` on the same thread (a non-reentrant `Mutex` self-deadlocks); cross-thread exclusion unchanged, no poison branch; `parking_lot` 0.12 added as a direct dev-dep (already in the build graph, MIT/Apache-2.0). (2) [`pg.rs`](../../tests-common/src/pg.rs) `bring_up_pg_cluster_with_timeout` takes the lock around the `install`→`start`→`Active`/socket window, `#[cfg(target_os="macos")]`-gated (Linux binds no unit value → clippy-clean, no import); `initdb` stays outside the lock (kept parallel). **TDD (RED watched):** `serial_lock_is_reentrant_on_same_thread` (spawned thread double-acquires; assert completes within 5 s via `recv_timeout` — RED=deadlock→timeout on the old `Mutex`, GREEN on `ReentrantMutex`) + `serial_lock_excludes_across_threads` (4 threads, max-concurrent-holder == 1, cross-thread-exclusion regression pin). **Bundled [#163](https://github.com/hherb/hhagent/issues/163)** (separate pre-existing bug, found during live validation, operator chose to bundle): `injection_guard_e2e` reproduced failing **single-threaded** → not #130; its fixture built `ig-{label}-{suffix}-data` while `unique_temp_root` *also* appends `-<pid>-<nanos>`, so the socket path `…/.s.PGSQL.5432` = **100 + len(label)** bytes overflowed macOS's 104-byte `sun_path` for labels ≥ 6 chars → postgres silently couldn't bind → 30 s timeout (why HANDOVER said it "was never verified live"; proven by the label↔pass/fail split — `sha`/`err` len-3 passed, `benign`/`privacy`/`audit-row`/`placeholder` ≥6 timed out). Fix: shortened labels to `ig-{label}-d`/`-l` + new pure `check_socket_path_fits` guard in `bring_up_pg_cluster` (per-OS `SUN_PATH_MAX` 104/108) that fails fast with a clear message (TDD: stub → RED → implement). #130 + #163 CLOSED by this PR. **Verification (macOS M3 Max, 1.96):** `cargo test --workspace` **1153 → 1157 (+4: 2 macOS-only serial + 2 cross-platform socket-path)**, all green; `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0; **live against Postgres.app v18**: `injection_guard_e2e` 6/6, `secret_vault_e2e` 11/11, `postgres_e2e` 57/57, zero bring-up timeouts (the #130 signature is gone; these heavy PG suites now pass live for the first time). **Deferred (issue Option 2):** cross-process flock serialization — unneeded since cargo runs binaries sequentially; revisit only if a workspace flake persists. File-size: `serial.rs` 34 → ~105, `pg.rs` 317 → ~360 (both under cap); spec at [`docs/superpowers/specs/2026-05-30-issue-130-launchd-bringup-serialization-design.md`](../superpowers/specs/2026-05-30-issue-130-launchd-bringup-serialization-design.md).
- [x] **Next-TODO item 8b / Issue [#153](https://github.com/hherb/hhagent/issues/153) — clean clippy gate + `-D warnings` CI (2026-05-30)** — branch `fix/issue-153-clippy-clean-gate`, commits `d5db4ba` (macOS-visible lints + CI flip) + `ca827d7` (Linux-only lints), **merged to `main` via PR [#161](https://github.com/hherb/hhagent/pull/161) at `12b080c`; #153 CLOSED**. The `linux-check` clippy step ran without `-D warnings`, so warnings accumulated and no clean enforcement gate existed. #153 framed it as "the 10 hhagent-core lib warnings," but the CI command is `cargo clippy --workspace --all-targets`, so the **whole workspace** had to be made warning-clean. **Mechanical lints** (via `cargo clippy --fix`, all behaviour-preserving): useless `vec!` → array, `.err().expect()` → `expect_err`, needless borrows, `assert_eq!(x, true/false)` → `assert!`, `io::Error::new(Other, _)` → `io::Error::other`, inlined literal format args, `match` → `?`, `map_or` → `is_some_and`, redundant `trim`, manual char-array pattern. **Judgment calls**: `await_holding_lock` (`manager.rs` `_test_slot_*`) fixed by extracting a sync `lookup_slot` helper so the std-guard never lives in the async fn (the lint isn't flow-sensitive to the existing `drop`) — also dedupes the two inspectors; `too_many_arguments` (runner `lane_loop`/`drain_lane`, audit payload builder, observation capture helper) and `assertions_on_constants` (recall/memories const-drift pins) suppressed with justified `#[allow]`; `doc_lazy_continuation` fixed by rewording doc comments whose wrapped lines began with `+` (CommonMark phantom-bullet) and adding blank lines before concluding paragraphs. **Linux-only lints** (only visible once the gate ran on the Linux CI runner; macOS never compiles `#[cfg(target_os="linux")]`): `linux_bwrap::push_bind` `&PathBuf` → `&Path` (`ptr_arg`), `linux_cgroup` `starts_with("/")` → `'/'` (`single_char_pattern`), `lockdown_probe` `b"…\0".as_ptr()` → `c"…".as_ptr()` ×3 (`manual_c_str_literals`). Dev toolchain bumped 1.95 → 1.96 to match CI; `x86_64-unknown-linux-gnu` target added so sandbox/prelude/supervisor/tests-common/db cross-clippy locally (core can't — the `ring` C dep, #144 wall — so its Linux path is CI-verified). CI flip: [`.github/workflows/linux-check.yml`](../../.github/workflows/linux-check.yml) clippy step now ends `-- -D warnings` + rewritten header comment. **Pure-lint change, zero behavior delta** — `cargo test --workspace` unchanged at **1152 / 0 / 3** (macOS); `cargo clippy --workspace --all-targets -- -D warnings` exit 0 (macOS 1.96); Linux `-D warnings` green via PR #161 `linux-check` (35s). Closed #153 on merge.
- [x] **Next-TODO item 5 — `tool_host.rs` sibling-lift (2026-05-29)** — branch `refactor/item-5-tool-host-sibling-lift`, code commit `379f77a`, **merged to `main` via PR [#160](https://github.com/hherb/hhagent/pull/160) at `fd7dd7a`**. `tool_host.rs` had grown to **911 LOC** (411 over the 500-LOC cap), the largest remaining over-cap production file, across the Item 30 (injection guard) + Item 31 (secret refs) + PR #157 (audit-sink) arcs. Lifted three self-contained pieces into child modules (following the `tool_host/audit_sink.rs` sibling precedent in the same directory): `tool_host/watchdog.rs` (249 LOC — `WatchdogGuard`/`spawn_watchdog` as `pub(super)`, the kill machinery + 5 tests module-private; the 2026-05-08 host-blackout incident write-up moves verbatim), `tool_host/lockdown_env.rs` (164 LOC — `ENV_*` consts + `derive_lockdown_env` + 6 tests, **re-exported via `pub use`** so the public path `hhagent_core::tool_host::{ENV_*, derive_lockdown_env}` is unchanged), and `tool_host/tests.rs` (47 LOC — the 2 `WorkerCommand` seal tests). **Trust boundary stays in the parent** per the item-5 instruction: `dispatch` / `dispatch_with_sink`, the `WorkerCommand` seal, and `SupervisedWorker` (incl. the module-private `call`). Updated the seal docs: adding child modules makes them *descendants* that could reach `WorkerCommand::new`, so the reviewable opt-out is now "edit the `tool_host` module (`tool_host.rs` or a file under `tool_host/`)" and a note that none of the four children construct a `WorkerCommand`. Pure-mechanical, no behavior change — the 13 existing tests + the `compile_fail` seal doctest are the regression pin. **Workspace 1152 → 1152 (no delta) on macOS; `compile_fail` seal doctest passes; no new clippy lints (the 2 `doc list` warnings in `watchdog.rs` relocated verbatim); no new rustdoc broken-link errors.** File-size: `tool_host.rs` **911 → 519 LOC** (residual 19 over cap is the irreducible trust-boundary core — the `dispatch` doc comment alone is ~85 lines); new files all under cap. No production file in this arc remains pathologically over cap.
- [x] **Issues [#148](https://github.com/hherb/hhagent/issues/148) + [#149](https://github.com/hherb/hhagent/issues/149) — secret-vault test gaps (2026-05-29)** — branch `fix/issue-148-149-secret-vault-test-gaps`, code commit `7517169`, **merged to `main` via PR [#157](https://github.com/hherb/hhagent/pull/157) at `53e68ed`**; both issues CLOSED. Two untested branches from PR [#146](https://github.com/hherb/hhagent/pull/146)'s secret-ref slice, surfaced by `/code-review` and deferred for want of a fault-injection seam. **#149** — `Vault::materialize`'s `Entry::Occupied` (ref-collision) arm: extracted the locked map-insert into a private `Vault::insert_fresh` (behaviour-preserving) so a PG-free unit test (`insert_fresh_rejects_ref_collision_without_overwriting`) can call it twice at one ref and assert `RefCollision` + original-plaintext-intact. Chosen over the issue's `_test_materialize_at_ref` seam (which needs a live pool + a test-only public backdoor). **#148** — the two best-effort secret-ref audit rows (`secret.redeemed`, `secret.redemption_failed`) swallow insert failures: added an `AuditSink` trait + `PgAuditSink` prod impl in `core/src/tool_host/audit_sink.rs`, split `dispatch` into a thin `dispatch(pool, …)` wrapper over `dispatch_with_sink(&dyn AuditSink, …)` (preserves the signature → **0 churn on the 18 existing call sites**); `dispatch`'s 4 `audit::insert(pool,…)` calls (pool was audit-only) now route through the sink. A `MockAuditSink` forces the targeted insert to fail; two integration tests assert a failed `secret.redeemed` still returns Ok + writes the tool row, and a failed `secret.redemption_failed` still returns `Err(SecretRedemptionFailed)` + no tool row (PG-free; empty vault fails substitution before any pool use). The `pub` seam is contained: prod always uses `dispatch`→`PgAuditSink`; the spawn/jail boundary stays sealed behind `WorkerCommand`/`SupervisedWorker::call`. Not bug-driven TDD — the swallow logic already works; these are regression pins for the planned `tool_host.rs` sibling-lift. **Workspace 1148 → 1151 (+3) on macOS; `secret_vault_e2e` 11/11 live against Postgres.app v18; no new clippy lints.** File-size: `vault.rs` 273→284, new `audit_sink.rs` 59 (both under cap); `tool_host.rs` 893→911 (+18, still over cap — the Items 30+31 sibling-lift would now also lift the audit-sink wiring). Closed #148 + #149 on merge.
- [ ] **Large-tool-result handoff cache** — once `web-fetch` / `web-search` / `browser-driver` ship, single tool outputs will dwarf the planner's remaining context budget. Today `tool_host::dispatch` returns the worker's full result verbatim. Adopt openhuman's pattern (`docs/agent-subagent-tool-flow.md` — "oversized tool results... replaced by placeholders and stashed in an in-memory `ResultHandoffCache`"): when a result exceeds a per-tool byte cap (e.g. 16 KiB default), persist the full body to the per-task `Workspace` scratch dir as `handoff/<sha256>.bin`, return a placeholder `{ "handoff_ref": "sha256:…", "byte_len": N, "summary_head": "…" }` in its place, and expose a new core-side `fetch_handoff(ref)` tool the planner can call to materialise a slice (`offset, len`) on demand. The cache is per-task; `Workspace::Drop` already wipes it. Co-locate the redaction with the injection guard's block path so blocked outputs go to the same placeholder shape. **Plumbs into Phase 3** since the worst offenders are web-shaped, but the chokepoint already exists today and the redaction path is shared with the guard above.
- [ ] **Memory two-tier write path: `put_doc()` vs `put_doc_light()`** — openhuman `docs/memory-sync-functions.md` distinguishes "embed + async graph-extract" from "skip embedding for high-frequency ephemeral data like screen captures." Today `db::memories::insert_memory` always takes a pre-computed embedding (caller-provided). For future high-frequency writers (channel inbound, browser observations, screen capture if it ever materialises), embedding every row is wasteful — they'd never be semantic-search targets. Add `db::memories::insert_memory_light(pool, body, metadata, layer)` that NULLs `embedding` + skips entity extraction (and document explicitly that lexical lane + `metadata @>` lookups still work; semantic + graph lanes degrade gracefully because `semantic_search` already skips NULL-embedding rows by `WHERE embedding IS NOT NULL`). Per-namespace caps + oldest-eviction (openhuman quotes "max 50 KV entries, max 200 docs") fit naturally as a follow-up but should not block this surface — the `memories.metadata` column is the natural namespace selector, no schema change needed.

## Phase 2 — Channels (read-only)

- [ ] IMAP inbound worker (sandbox: net allowlist = configured IMAP server only)
- [ ] Telegram inbound adapter (`grammers`, Rust)
- [ ] Channel-bus fan-in into core conversation queue
- [ ] DM pairing flow: short-lived pairing code (TOTP/HOTP) issued via a separate trusted channel; WebAuthn for browser/CLI pairings where available; pairings recorded in `audit_log` with revocation. Static contact allowlists rejected (forgeable). (Pattern: ZeroClaw `security/{pairing,webauthn,otp}.rs`.)

## Phase 3 — Channels outbound + browser + web

- [ ] Egress proxy (per-worker host allowlist, TLS pinning, audit logging)
- [ ] **Credential-leak scanner co-located in the egress proxy** — every outbound request body and inbound response body scanned for the SHA-256 prefix of every secret currently materialized for the calling worker; hits are blocked and audited. Scanning happens at the trust boundary, not inside the worker (which may itself be compromised). (Pattern: IronClaw `safety::leak_detector`, ZeroClaw `security/leak_detector.rs`.)
- [ ] Telegram outbound; Signal in/out (presage)
- [ ] SMTP outbound in mail worker
- [ ] `web-fetch` worker: HTTPS-only, host allowlist, body cap, redirect cap
- [ ] `web-search` worker (SearxNG default)
- [ ] `browser-driver` worker (Playwright headless, dedicated profile, scratch FS)
- [ ] **MCP onboarding: discover → boot-spawn → validate → persist** — when hhagent grows third-party MCP-server support (any of the registries openhuman taps: Smithery, `modelcontextprotocol/registry`), naive "spawn it with the operator's intended policy" is a foothold attack: a malicious MCP server gets its production sandbox on first run. Adopt openhuman's pattern (`docs/MCP_SETUP_AGENT.md` — "boot-spawn for this one server... spawns the candidate subprocess in a scratch workspace"): every newly-discovered MCP server is first booted under a **maximally restrictive** `SandboxPolicy` (`Net::Deny`, `fs_read=[]`, `fs_write=[scratch]`, `Profile::Strict`, `cpu_ms=5000`, `mem_mb=128`), driven through `initialize` + `tools/list` over our existing `hhagent-protocol` stdio JSON-RPC, recording the declared tool surface to `db::mcp_servers` (new migration) only on success. Only then does the operator promote the server to its intended runtime policy via a separate explicit step that lands one `actor='cli' action='mcp.promoted'` audit row carrying the SHA-256 of the policy that was approved. Production runs refuse to spawn an MCP server whose policy hash has changed since promotion (mirror of the `tool_allowlists` SHA-256 drift detection from PR #51). Cross-platform "free" via `SandboxBackend` — same flow on bwrap, Seatbelt, and the new `MacosContainer` backend (Issue #55).

## Phase 4 — python-exec & agent-authored skills

- [ ] `python-exec` worker: scratch FS only, no net, hard CPU/mem/wallclock; curated stdlib bind
- [ ] Skill catalog (named/persisted Python skills) with optional human-approve gate
- [ ] **Skill trust enum** — `Untrusted | UserApproved | Pinned`, each level mapping to an explicit capability ceiling (which workers it may invoke, which net allowlists, which fs paths). Authorship and approval recorded in `audit_log`; promotion requires re-approval. (Pattern: IronClaw skill trust model — user-placed vs registry-installed.)
- [ ] Optional micro-VM backend for `python-exec` (Firecracker on Linux, Apple `container` on macOS — discovery spike completed 2026-05-21, verdict COMMIT; see [`docs/superpowers/specs/2026-05-21-macos-container-spike-notes.md`](../superpowers/specs/2026-05-21-macos-container-spike-notes.md))
- [ ] **Tiered delegation policy with hard no-recursion ceiling** — when the scheduler grows subagent delegation (today everything is one inner loop), borrow openhuman's `docs/DELEGATION_POLICY.md` four-tier shape: Tier 1 reply-directly (no tools), Tier 2 direct tool, Tier 3 inline subagent (≤5 turns, no new thread), Tier 4 dedicated worker thread (>5 turns). **The structural constraint that matters: workers do not spawn workers.** Encode it in `tool_host` as a compile-time check (`SubagentContext: Sealed` newtype that can only be constructed from the root scheduler) so the spawn tree is provably finite and the audit log has bounded fan-out per task. Maps cleanly onto the existing `Lifecycle::{SingleUse, IdleTimeout}` shape: tier-3 inline subagents are `SingleUse`, tier-4 dedicated threads piggyback on `IdleTimeoutLifecycle`. Pre-req for any meaningful agent-authored-skills work; defines the budget per skill invocation.
- [ ] **Stability-scored preference learning** — when the agent starts inferring user preferences (style, vetoes, tooling, timezone, identity facts), a naive "remember whatever the latest message said" path is vulnerable: a single injected message in any channel permanently flips a long-standing preference. Adopt openhuman's `docs/AGENT_SELF_LEARNING.md` scoring shape: `stability(class, key, value) = base × cue × user_state`, evidence weighted by source (explicit user statement 1.0, structural data 0.9, behavioural heuristic 0.7, recurrence 0.6), only "Active" at stability ≥1.5 (requires corroboration). Storage: new `user_profile_facets` table behind `db::profile`, runtime-role can INSERT bounded candidate rows but only the explicit "operator pin" CLI surface (`hhagent-cli profile pin <class> <key>=<value>`) can promote a facet to Active or override automatic scoring. Keeps `memory access is core-only` invariant intact — workers never write profile state. Pre-req for any prompt-assembly surface that injects a `UserProfileSection` (current `assemble_system_prompt` ships an `L0`/`L1` block but no profile facets yet).

## Phase 5 — Frontier escalation, hardening, audit UI

- [ ] Policy gate: per-tool, per-task, per-data-classification routing decision
- [ ] Frontier escalation through egress proxy (Anthropic / OpenAI)
- [ ] Read-only audit log viewer (CLI complete; web optional)
- [ ] 7-day adversarial soak test (prompt-injected channel content; no escapes in audit log)

---

## Cross-cutting / continuous

- [ ] Threat-model doc kept in sync with shipped backends
- [ ] Architecture doc kept in sync with shipped components
- [ ] License audit on every new dependency (AGPL-compatible only)
- [ ] CI: `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test --workspace` — both Linux and macOS
- [x] **Test infrastructure: shared `hhagent-tests-common` dev-dep crate** — landed 2026-05-12 on branch `refactor/tests-common-hoist`, closing [issue #15](https://github.com/hherb/hhagent/issues/15). New workspace crate `hhagent-tests-common` (`publish = false`, dev-dep only) ships the `PgCluster` + `bring_up_pg_cluster` recipe, `ServiceGuard` / `PathGuard` RAII guards, `skip_if_no_supervisor` / `pg_bin_dir_or_skip` / `skip_if_sandbox_unavailable` skip helpers, cfg-gated `backend()` factory, `policy_for_shell_exec` policy recipe, `workspace_target_binary` + per-binary wrappers, `unique_suffix` / `unique_temp_root` / `current_username`, `wait_for_status` / `wait_for_socket` / `wait_for_log_match`, macOS-only `serial_lock`, and the deterministic SHA-256-seeded `text_to_embedding`. 8 byte-duplicated copies of the bring-up dance (1 in `db/tests/`, 7 in `core/tests/`) consolidated into one place; net **−2514 LOC** across the migrated files (3005 → 491), **+750 LOC** in `tests-common/src/` (10 files, all under the 500-LOC soft cap). Pure refactor — workspace test count unchanged at 342 / 0 fail / 0 SKIP / 0 warnings; every assertion in every migrated test stays byte-identical. The hoist eliminates drift risk on socket-dir permissions, `sun_path` 108-byte budget, and SET-ROLE wiring; future e2e tests now compose `bring_up_pg_cluster` in a single line.
- [x] **Memory deletion audit infrastructure** — `deleted_memories` table + AFTER DELETE trigger on `memories` (migration `0008_deleted_memories_audit.sql`). Append-only by GRANT shape (matches `audit_log` from migration 0002): `hhagent_runtime` has INSERT only; no UPDATE/DELETE. The table preserves `body`, `metadata`, `embedding`, `original_created_at`, and `deleted_at` timestamp so nothing is silently lost when a memory row is deleted. Preventive infrastructure for the future GDPR-style forgetting path; no caller deletes memories today, but when one materialises the trigger guarantees the row is journaled with full content. Landed 2026-05-12 alongside the graph lane (branch `feat/memory-graph-lane`). Pinned by integration test `deleted_memories_trigger_journals_deleted_row` (GRANT shape verified: UPDATE+DELETE denied on `deleted_memories`; two consecutive deletes produce exactly two rows).
- [x] **Tech-debt batch 2026-05-18** — four well-isolated open issues closed on branch `worktree-chore+tech-debt-batch-2026-05-18` while NER work runs in parallel on the memory subsystem (one PR, 5 commits). Closed [#77](https://github.com/hherb/hhagent/issues/77) (collapse N trailing newlines to one before `</base>` in `assemble_system_prompt`; extended test pins 0/1/2/many newline cases); [#80](https://github.com/hherb/hhagent/issues/80) (`cli_ask_e2e` mock dispatches by URL path with two per-endpoint FIFOs instead of one shared FIFO; +5 in-file `mock_router_unit_tests` for the dispatch helpers since the outer e2e tests skip on macOS dev boxes); [#57](https://github.com/hherb/hhagent/issues/57) (moved `apply_from_env` happy-path to a subprocess via new `lockdown-probe rlimit-report` subcommand + `tests/rlimit_apply_smoke.rs`, eliminating the process-wide `setrlimit` side-effect that lowered the prelude test binary's CPU budget); [#4](https://github.com/hherb/hhagent/issues/4) (promoted the bump-the-header step to step 1 of the end-of-session checklist in `docs/devel/handovers/README.md` + HANDOVER.md so header drift can't silently mislead the next session). Plus a drive-by `.gitignore` typo fix (`setting.local.json` → `settings.local.json` + added `.claude/worktrees/`). Workspace tests on macOS: 720 → 726 (+6 net). Linux baseline projection: 751 → 758.
