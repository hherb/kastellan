# hhagent — Session Handover

> Rolling document. Updated at the end of every working session so the next
> session (likely a fresh Claude Code) can resume cold. See
> [`README.md`](README.md) for the convention. Older sessions are compressed
> into "Earlier history" below; full per-session detail lives in the
> [`archive/`](archive/) snapshots.

**Last updated:** 2026-05-31 (Next-TODO **item 6 / [#130](https://github.com/hherb/hhagent/issues/130)** — parallel-launchd bring-up contention. Operator picked the reentrant-`serial_lock`-in-`bring_up` shape (issue Option 1). Shipped: switched `tests-common::serial`'s lock `Mutex` → `parking_lot::ReentrantMutex` (std `ReentrantLock` still unstable on 1.96) so callers already holding it (`supervisor_e2e`/`observation_capture`) don't self-deadlock, and took it around the launchd window in `bring_up_pg_cluster` (macOS, cfg-gated). TDD: RED reentrancy test (deadlock→timeout on the old `Mutex`) + cross-thread-exclusion regression pin. **Live validation surfaced a *separate* pre-existing bug ([#163](https://github.com/hherb/hhagent/issues/163), filed + fixed here): `injection_guard_e2e`'s over-long data label overflowed macOS `sun_path`** → postgres couldn't bind its socket → silent 30 s timeout (this is why it "was never verified live"; reproduces single-threaded → NOT #130). Bundled fix: shortened the labels + added a `check_socket_path_fits` guard to `bring_up_pg_cluster`. Live after fix: `injection_guard_e2e` 6/6, `secret_vault_e2e` 11/11, `postgres_e2e` 57/57, zero timeouts. Branch `fix/issue-130-launchd-bringup-serialization`. See Recently-completed. **Prior session (now merged):** Next-TODO **item 12** — operator picked "graph-lane production wiring," but exploring it surfaced that **item 12 is already done** (shipped in the 2026-05-19 entity-extraction-v2 / "Slice F" arc, after the parked note was written and never reconciled). Operator chose: reconcile the stale note + close the one genuine residual — a RouterAgent-layer regression test pinning the seed thread. Added `formulate_plan_threads_extracted_seeds_into_recall_and_meta` to [`router_agent_mock_e2e.rs`](../../../core/tests/router_agent_mock_e2e.rs) (+1 test, **zero production change** — proven by two mutations + an empty `agent.rs` diff). Branch `test/router-agent-graph-seed-threading`, **PR [#162](https://github.com/hherb/hhagent/pull/162) — MERGED to `main` at `a83be4a`** (2026-05-29). Also reconciled the post-#161 header (the prior session left it describing #161 as open). See Recently-completed. **2026-05-30 reconcile (this session start):** the prior session left this header describing #162 as still open; #162 is in fact merged — header/Next-TODO/ROADMAP synced. **2026-05-30 reconcile #2 (this session start):** the prior session left this header describing PR [#164](https://github.com/hherb/hhagent/pull/164) as OPEN/awaiting-merge; #164 is in fact **MERGED to `main` at `091e53d`** (2026-05-30T02:09:54Z) and **#130 + #163 are CLOSED** — header/Last-commit/merged-branches synced. **Then this session's pick:** operator chose the **`macos_container.rs` sibling test-module lift** (refactor bucket — biggest *clean* over-cap file). Pure-mechanical: moved the inline `#[cfg(test)] mod tests` block (~494 LOC) into a new sibling `sandbox/src/macos_container/tests.rs`; production LOC **983 → 491** (under the 500 cap). Zero behaviour change — proven by the byte-identical production diff + workspace count unchanged at **1157**. Branch `refactor/macos-container-test-module-lift`. See Recently-completed.) **2026-05-30 reconcile #3 (this session start):** PR [#165](https://github.com/hherb/hhagent/pull/165) is now **MERGED to `main` at `48c0396`** (2026-05-30T02:36:20Z; all CI green) — the prior header left it as OPEN/awaiting-merge; header/Last-commit/Currently-on/merged-branches synced. No new code work yet — awaiting operator Next-TODO pick. **2026-05-30 reconcile #4 (this session start):** the prior session left this header describing PR [#166](https://github.com/hherb/hhagent/pull/166) (item 9 / #99) as OPEN/awaiting-merge; #166 is in fact **MERGED to `main` at `75e9039`** (all CI green) and **#99 is CLOSED** — header/Currently-on/Last-commit/merged-branches + ROADMAP synced. **Then this session's pick:** operator chose a **bigger over-cap file lift** (refactor bucket). Picked `scheduler/audit.rs` (largest over-cap at 1106 LOC; pure functions, no I/O → lowest-risk). Split into two natural seams: the v2 entity-extraction payload (const + builder + 2 tests) → sibling `audit/extract_entities.rs` (re-exported so public paths hold), and the main `mod tests` → sibling `audit/tests.rs`. Production **1106 → 500 LOC** (at cap). Zero behaviour change — region A (L1–489) byte-identical; workspace **1157/0/3** unchanged; clippy `--workspace -D warnings` clean. Branch `refactor/audit-rs-split-extract-entities`, code commit `3bb0cc7`. See Recently-completed.) **2026-05-30 reconcile #5 (this session start):** the prior session left this header (and ROADMAP) describing PR [#167](https://github.com/hherb/hhagent/pull/167) as OPEN/awaiting-merge; #167 is in fact **MERGED to `main` at `79fcc27`** (2026-05-30T04:38:22Z; all CI green) — header / Currently-on / Last-commit / merged-branches / ROADMAP synced (docs commit `eb955a8`). **Then this session's pick:** operator chose the **`launchd_agents.rs` over-cap lift** (refactor bucket — now the largest over-cap production file at 1093 LOC). Unlike the `macos_container` case, a test-only lift would leave production ~686 (still over cap), so this mirrors the `audit.rs` precedent: test lift **plus** a re-exported pure-builder seam. Split into siblings `launchd_agents/builders.rs` (the two pure I/O-free functions `build_plist` + `xml_escape` + `validate_service_name` + consts + their tests, re-exported so `launchd_agents::{build_plist, validate_service_name}` paths hold) and `launchd_agents/tests.rs` (driver + launchctl-helper tests). Production **1093 → 485 LOC** (under cap — resolved). Zero behaviour change — workspace **1157/0/3** unchanged, supervisor unit 52/52, clippy `--workspace -D warnings` exit 0, broken-doc-link set matches `main` exactly (5 pre-existing). Branch `refactor/launchd-agents-split`, code commit `d195d63`. See Recently-completed.) **2026-05-30 reconcile #6 (this session start):** the prior session left this header describing PR [#168](https://github.com/hherb/hhagent/pull/168) as OPEN/awaiting-merge; #168 is in fact **MERGED to `main` at `5bf010b`** (2026-05-30T09:15:07Z; all CI green) — header / Currently-on / Last-commit / merged-branches / ROADMAP synced. **Then this session's pick:** operator chose the **`db/memories.rs` over-cap split** (refactor bucket — at 961 LOC, ~863 prod, the largest remaining over-cap production file). Unlike the recent test-lifts, its inline tests are only ~98 LOC and test parent-resident items, so this is a **real production-logic split** along the read/write seam into re-exported siblings `memories/write.rs` (259 LOC) + `memories/search.rs` (400 LOC); the parent (360 LOC) keeps the shared vocabulary + tests + re-exports so every `db::memories::<name>` path holds byte-for-byte. Production **961 → 360 LOC** (under cap). Zero behaviour change — moved bodies byte-identical (diff-verified), workspace **1157/0/3** unchanged, clippy `--workspace -D warnings` exit 0, broken-doc-link set matches `main` (the 2 new `EMBEDDING_DIM` links repointed at `super::`). Live (Postgres.app v18): `postgres_e2e` 57/57, `memory_recall_e2e` 13/13, `memory_l1_promote_e2e` 9/9, `memory_l0_seed_e2e` 2/2, `embedding_recall_e2e` 4/4. Branch `refactor/memories-rs-split-write-search`, code commit `39e5cae`. See Recently-completed.) **2026-05-30 reconcile #7 (this session start):** the prior session left this header describing PR [#169](https://github.com/hherb/hhagent/pull/169) (the `db/memories.rs` split) as OPEN/not-yet-opened; #169 is in fact **MERGED to `main` at `e1be537`** (2026-05-30T11:57:51Z; all CI green; code commit `39e5cae` + style polish `7b41008`) — header / Currently-on / Last-commit / merged-branches / Next-TODO / ROADMAP synced. No new code work yet — awaiting operator Next-TODO pick. **2026-05-31 reconcile #8 (this session start):** the prior session left this header describing PR [#170](https://github.com/hherb/hhagent/pull/170) (the `tool_dispatch.rs` split) as OPEN/awaiting-merge; #170 is in fact **MERGED to `main` at `4e401cc`** (2026-05-30T22:16:20Z; all CI green) — header / Currently-on / Last-commit / merged-branches / Next-TODO / ROADMAP synced. **Then this session's pick:** operator chose **item 9b — the `core/src/observation/replay.rs` over-cap test-lift** (refactor bucket; the next CLEAN test-lift candidate). Pure-mechanical: moved the inline `#[cfg(test)] mod tests` block (L421–804, 384 LOC) into a new sibling [`observation/replay/tests.rs`](../../../core/src/observation/replay/tests.rs) (390 LOC incl. header doc); production **804 → 422 LOC** (under the 500 cap). Zero behaviour change — production region L1–420 byte-identical (diff-verified), de-indent byte-reversible, workspace **1157/0/3** unchanged, replay unit tests 20/20 from the new location, clippy `-p hhagent-core --all-targets -D warnings` exit 0, doc-link set matches `main` (25 pre-existing, zero replay/observation links; `#[cfg(test)]` modules aren't doc-built so the move can't affect it). Branch `refactor/replay-rs-test-module-lift`. See Recently-completed.) **2026-05-31 reconcile #9 (this session start):** the prior session left this header describing PR [#171](https://github.com/hherb/hhagent/pull/171) (the `replay.rs` test-lift) as OPEN/awaiting-merge; #171 is in fact **MERGED to `main` at `30aa52e`** (2026-05-30T22:56:00Z; all CI green) — header / Currently-on / Last-commit / merged-branches / Next-TODO / ROADMAP synced. **Then this session's pick:** operator chose the **next over-cap refactor**. A full census of inline-test fractions (the prior handover's "no clean test-lift-only candidate remains" was scoped only to the active arc) showed **`core/src/scheduler/inner_loop.rs` is a CLEAN test-lift that also closes tracked issue [#81](https://github.com/hherb/hhagent/issues/81)** — its first slice (PR #79 era → `inner_loop_audit.rs`) met #81's "under 700" but left it 155 over cap, exactly the "second slice" #81 anticipated. Pure-mechanical: lifted the remaining inline `#[cfg(test)] mod tests` block (state-machine / floor-raise pins) to a re-exported-by-declaration sibling [`inner_loop/tests.rs`](../../../core/src/scheduler/inner_loop/tests.rs) (230 LOC); production **655 → 438 LOC** (under cap — **#81 CLOSED**). Zero behaviour change — production region L1–437 byte-identical (diff-verified), de-indent re-indent byte-identical, workspace **1157/0/3** unchanged, `scheduler::inner_loop`/`inner_loop_audit` unit 22/22 from the new location, clippy `-p hhagent-core --all-targets -D warnings` exit 0, doc-link set matches `main` exactly (21 pre-existing; diff vs a fresh `main` worktree → empty). Branch `refactor/inner-loop-test-module-lift`, code commit `cb561fd`. See Recently-completed. **2026-05-31 reconcile #10 (this session start):** the prior session left this header describing PR `refactor/inner-loop-test-module-lift` (PR not yet opened); **PR [#172](https://github.com/hherb/hhagent/pull/172) is in fact MERGED to `main` at `98a5be0`** (2026-05-31T00:54:12Z; all CI green) — **issue [#81](https://github.com/hherb/hhagent/issues/81) is CLOSED** — header / Currently-on / Last-commit / merged-branches synced. **Then this session's pick:** the **engineering-pickup "L3 skill crystallisation — spec" item**: brainstormed → wrote spec → wrote plan → implemented via subagent-driven development (14 tasks, spec+quality reviewed = APPROVED each + final whole-branch review = MERGE-READY). **What shipped** (branch `feat/l3-skill-crystallisation`, off `main` at `98a5be0`, tip `0505986`; **PR [#173](https://github.com/hherb/hhagent/pull/173) OPEN**): the **first writer for `MemoryLayer::Skill` (L3)**, mirroring the L1 promotion writer one layer up. Writer-only / security-first slice: crystallised skills are **non-executable** (no invocation path); stored `trust:"untrusted"` (forward-compat placeholder for the future Skill trust enum); provenance `L3Source::AgentRaised` is writer-claimed only. On a successfully-completed multi-step task the agent emits a parameterised tool-call template (`Plan.l3_skill`); `runner::drain_lane` validates → dedups on a canonical SHA-256 → stores at `layer=3` with one `actor='scheduler' action='l3.crystallised'` audit row; operator CLI `hhagent-cli memory l3 {list,remove}`. New types `L3SkillCandidate`/`L3Param`/`L3TemplateStep` + `Plan.l3_skill` + `Plan::completion_skill()` (in `core/src/cassandra/types.rs`); new module `core/src/memory/l3_crystallise.rs` (`validate_l3_skill` with closed-world `{{placeholder}}` integrity + reserved-tag `<skills>` + caps; `canonical_json`/`compute_template_sha256` order-stable dedup SHA; `crystallise_l3` writer; `list_l3`/`remove_l3`); `InnerLoopResult.terminal_l3_skill` captured on `Outcome::Completed` under a **`dispatch_count >= 1` grounding gate** (no skill for pure-text tasks); `drain_lane` best-effort hook `write_l3_crystallised_row`; audit constants + `build_l3_write_payload`; `plan.formulate` gains a compact `l3_skill` key (key-count pins bumped 24→25 / 25→26); CLI `memory_l3.rs`; `prompts/agent_planner.md` teaches `l3_skill`; DB e2e 7/7 + CLI e2e 4/4. **Session-end verification (macOS M3 Max, rustc 1.96.0):** `cargo test --workspace` **1177 passed / 0 failed / 3 ignored** (1157 baseline + 20 new); live PG `memory_l3_crystallise_e2e` 7/7, `cli_memory_l3_e2e` 4/4; clippy `--workspace -D warnings` exit 0; doc-links 21 unresolved = `main`'s 21. **History note:** a `git add -A` in the first task swept two stray untracked files (`docs/essay-medium-draft.md`, `.claude/scheduled_tasks.lock`) into an early commit; they were stripped via `git filter-branch` (branch SHAs were rewritten — that's why the commit SHAs are post-rewrite); the essay draft was restored to disk as untracked. The branch's net diff vs `main` contains ONLY L3 files + the spec/plan docs. Spec: `docs/superpowers/specs/2026-05-31-l3-skill-crystallisation-design.md`; plan: `docs/superpowers/plans/2026-05-31-l3-skill-crystallisation.md`. Branch commits (post-rewrite SHAs): `a323e29` spec, `516124c` plan, `0d4aef8` types, `58be626` validator+canonical-SHA+metadata, `cb65c52` crystallise_l3 writer, `33691a5` audit consts+payload, `846e5cc` inner-loop gate, `88f6c85` formulate l3_skill key, `41db3fe` runner hook, `e4e1223` cli_audit, `79ed54f` CLI, `06b7564` prompt, `c5eede3` DB e2e, `3da3284` CLI e2e, `0505986` e2e pin (tip).

**Start-of-session reconcile #11 (2026-05-31):** the prior session left the header / Currently-on / Last-commit / merged-branches / ROADMAP describing PR [#173](https://github.com/hherb/hhagent/pull/173) (the L3 skill crystallisation writer) as OPEN/awaiting-merge; **#173 is in fact MERGED to `main` at `6eb966e`** (2026-05-31T11:46:35Z; all CI green). Synced. `docs/essay-medium-draft.md` stays untracked. **Then this session's pick:** operator chose the **`l3_crystallise.rs` test-lift** (Next-TODO item 10a — the direct follow-up to the just-merged L3 writer, and a prerequisite for further L3 slices). Pure-mechanical: moved the inline `#[cfg(test)] mod tests` block (208 body lines) into a new re-exported-by-declaration sibling [`core/src/memory/l3_crystallise/tests.rs`](../../../core/src/memory/l3_crystallise/tests.rs) (220 LOC incl. header doc); production **676 → 467 LOC** (under the 500 cap). Zero behaviour change — production region L1–464 byte-identical to `main` (diff-verified), de-indent exactly reversible (re-indent matches the original block), workspace **1177/0/3** unchanged, 20 lifted unit tests pass from `memory::l3_crystallise::tests::*`, clippy `-p hhagent-core --all-targets -D warnings` exit 0, doc-links 21 = `main`'s 21 (zero new). PG-free (unit tests only); no live PG run needed. Branch `refactor/l3-crystallise-test-module-lift`, code commit `34bc262`. See Recently-completed.

**Start-of-session reconcile #12 (2026-05-31):** the prior session left the header / Currently-on / Last-commit / merged-branches describing PR [#175](https://github.com/hherb/hhagent/pull/175) (the `l3_crystallise.rs` test-lift) as OPEN/awaiting-merge; **#175 is in fact MERGED to `main` at `55b212e`** (2026-05-31T12:06:50Z; all CI green). Synced. `docs/essay-medium-draft.md` stays untracked. **Then this session's pick:** operator chose **Next-TODO item 10(b) — L3 invocation + Skill trust enum**, scoped (operator decision) to the **gate, not the door**: the security control before any execution. Brainstormed → spec → plan → 8-task subagent-driven implementation (each spec+quality reviewed = APPROVED; final whole-branch review = MERGE-READY). See Recently-completed + Currently-on.

**Currently on (this session, 2026-06-01):** branch `feat/l3-skill-recall-surfacing` (off `main` at `bbcc7b3`, tip `ed226e1`) — **Next-TODO item 10(c): L3 skill recall surfacing**, the `<skills>` planner block. **PR [#177](https://github.com/hherb/hhagent/pull/177) OPEN** (pushed 2026-06-01; awaiting CI + merge). Brainstorm → spec → plan → 6-task subagent-driven development (each spec+quality reviewed = APPROVED) → final whole-branch review (opus) = **MERGE-READY** after one fix. **What shipped (surfacing-only — no invocation; the prompt explicitly forbids it):** new module [`core/src/memory/l3_surface.rs`](../../../core/src/memory/l3_surface.rs) (330 LOC) — `SurfacedSkill{name,description,params}` typed projection (steps deliberately omitted → compile-time fact), fail-safe `parse_surfaced_skill` (malformed `template`/`null` ⇒ `None`, skipped), `is_surfaceable(SkillTrust)` gate (`UserApproved|Pinned` only), pure `render_skill_entry` + `cap_surfaced` (row+byte caps, inclusive boundary, mirrors `load_l1`), query-independent loader `load_l3_skills_for_prompt`/`_default` (load_layer → trust filter via `SkillTrust::from_metadata_str` → parse → cap), consts `L3_SKILLS_CAP_ROWS=16`/`L3_SKILLS_CAP_BYTES=4096`. `assemble_system_prompt` gains a `skills` param + `<skills>` block placed **L0 → L1 → skills → recalled → base** (empty slice ⇒ byte-identical to before). `AssembledPrompt.skill_count` + `FormulationMeta.skill_count` threaded; `skill_count` emitted in the `agent/plan.formulate` audit payload alongside `l0/l1_count` (key-count pins bumped **25→26 / 26→27**). `prompts/agent_planner.md` documents the block (reference-only, explicit no-invoke). **Security invariant (load-bearing):** only `user_approved`/`pinned` surface; `untrusted`/corrupted/absent trust markers never reach the planner (fail-safe `from_metadata_str`). **Final-review security fix (commit `ed226e1`):** param *descriptions* are surfaced verbatim by `render_skill_entry` but `validate_l3_skill` previously guarded only the skill description + param *names* — a `</skills>` in a param description could break out of the block. Added the reserved-tag/newline/control-char guards to param descriptions; closes the gap at the writer AND the approval gate (which re-runs `validate_l3_skill`), so a breakout-carrying param description can never reach `user_approved` and never surfaces. **Session-end verification (macOS M3 Max, rustc 1.96.0):** `cargo test --workspace` **1243 / 0 / 3** (1220 baseline + 23 new); live PG (Postgres.app v18) `l3_surface_e2e` **4/4**, `prompt_assembly_e2e` **3/3**, regression `cli_memory_l3_e2e` **8/8** + `memory_l3_crystallise_e2e` **7/7** (zero `[SKIP]`); `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0; doc-links **21 = main's 21** (zero new). Spec: [`docs/superpowers/specs/2026-06-01-l3-skill-recall-surfacing-design.md`](../../superpowers/specs/2026-06-01-l3-skill-recall-surfacing-design.md); plan: [`docs/superpowers/plans/2026-06-01-l3-skill-recall-surfacing.md`](../../superpowers/plans/2026-06-01-l3-skill-recall-surfacing.md). Branch commits: `05c19a4` spec+reconcile#13, `f49981d` plan, `0174cf0`+`ff187cd` l3_surface projection/parse/gate, `eb04966` render+cap, `f6383b9` loader+e2e, `08fb4ce` `<skills>` block, `853a8b9` wiring+skill_count, `46d714f` prompt doc, `ed226e1` param-desc breakout guard (tip). `docs/essay-medium-draft.md` stays untracked. **Remaining L3 arc (the DOOR):** 10(b-next) invocation/execution (param substitution + live-`ToolRegistry` re-validation at dispatch + sandboxed step run; only `UserApproved`/`Pinned` runnable); the `pin` command lands with it. **Prior session (now merged):** branch `feat/l3-skill-approval-gate`, **PR [#176](https://github.com/hherb/hhagent/pull/176) MERGED to `main` at `bbcc7b3`** — the **L3 skill trust enum + operator approval gate** (item 10(b), gate-only). Shipped (writer-only/gate-only, **no execution**): a typed `SkillTrust` enum (`Untrusted|UserApproved|Pinned`) with a **total fail-safe** `from_metadata_str` (unknown/absent ⇒ `Untrusted`, never trusted); a **pure** `evaluate_approval` gate (new module [`core/src/memory/l3_approval.rs`](../../../core/src/memory/l3_approval.rs)) that re-runs `validate_l3_skill` (structural, short-circuits) + scans step params for baked-in `secret://` refs + checks every step tool against an injected `known_tools` set (empty ⇒ all-unknown, fail-closed); a layer-guarded db helper [`set_skill_trust`](../../../db/src/memories/write.rs) (`jsonb_set` on `{trust}`, `WHERE id AND layer=3`, preserves other keys); three audit constants + pure payload builders (`l3.approved`/`l3.approve_rejected`/`l3.revoked`); two `cli_audit` composers; and `hhagent-cli memory l3 {approve,revoke}` — `approve` sources `known_tools` from the **latest `registry.loaded` audit snapshot** (the daemon's own record) and **fails closed (`NoRegistrySnapshot`)** when none exists; `revoke` downgrades to `untrusted` (no gate). `list` now renders trust through the typed enum. **Security invariant (whole-branch-review-confirmed):** the only path that flips trust → `user_approved` is the `ApprovalDecision::Approve` arm; every reject/error/no-snapshot/wrong-layer/bad-template path leaves trust `untrusted`; rejected approvals **are** audited (security trail). `Pinned` is defined but command-less (forward-compat, like `trust:"untrusted"` was); **no skill execution, no recall surfacing** — those stay the next L3 slices (Next-TODO 10b-followups + 10c). **Session-end verification (macOS M3 Max, rustc 1.96.0):** `cargo test --workspace` **1220 / 0 / 3** (no-PG skip-as-pass); live PG (Postgres.app v18) `cli_memory_l3_e2e` **8/8**, `set_skill_trust` db e2e **1/1**, writer-regression `memory_l3_crystallise_e2e` **7/7** (zero `[SKIP]`); `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0; doc-links **21 = main's 21** (zero new). New tests directly added by this slice: l3_approval unit **15**, audit-builder unit **3**, cli e2e **4**, db e2e **1**. Spec: [`docs/superpowers/specs/2026-05-31-l3-skill-approval-gate-design.md`](../../superpowers/specs/2026-05-31-l3-skill-approval-gate-design.md); plan: [`docs/superpowers/plans/2026-05-31-l3-skill-approval-gate.md`](../../superpowers/plans/2026-05-31-l3-skill-approval-gate.md). Branch commits: `c615af5` spec, `434ab0b` reconcile-#12 docs, `076cb32` plan, `4e981d6`+`0314ace`+`ca5cc7d`+`afb9159` l3_approval module, `4f97503` db helper, `3d3adf1` audit, `e8bdd51` cli_audit, `cd99feb` CLI, `b7935e7` e2e, `ac9ec2e` final-review nit (tip). `docs/essay-medium-draft.md` stays untracked. **Prior session** (now merged) shipped the **L3 skill crystallisation writer** on branch `feat/l3-skill-crystallisation`, **PR [#173](https://github.com/hherb/hhagent/pull/173) MERGED to `main` at `6eb966e`** (2026-05-31T11:46:35Z). It implemented the first writer for `MemoryLayer::Skill` (L3): **writer-only, non-executable, stored `trust:"untrusted"`** (forward-compat placeholder for the future Skill trust enum). Key shipping decisions: agent emits a parameterised tool-call template (`Plan.l3_skill`) on task completion; `runner::drain_lane` validates → dedups on canonical SHA-256 → stores at `layer=3` with a `scheduler`/`l3.crystallised` audit row; operator CLI `hhagent-cli memory l3 {list,remove}`; `InnerLoopResult.terminal_l3_skill` captured only when `dispatch_count >= 1` (grounding gate preventing pure-text tasks from emitting skills). New module `core/src/memory/l3_crystallise.rs` + types in `cassandra/types.rs` + audit constants + `plan.formulate` key bump + prompt update + DB e2e (7/7) + CLI e2e (4/4). Start-of-session reconcile #10 applied: PR [#172](https://github.com/hherb/hhagent/pull/172) (the `inner_loop.rs` test-lift) was described as OPEN/PR-not-yet-opened; it is in fact **MERGED to `main` at `98a5be0`** (2026-05-31T00:54:12Z; all CI green); **issue [#81](https://github.com/hherb/hhagent/issues/81) CLOSED** — header/Currently-on/Last-commit/merged-branches synced. One untracked, unrelated file stays out: `docs/essay-medium-draft.md` (restored to disk after the filter-branch history rewrite). **Prior session** (now merged) was on branch `refactor/inner-loop-test-module-lift` (off `main` at `30aa52e`, code commit `cb561fd`) — the **`core/src/scheduler/inner_loop.rs` over-cap test-lift, closing issue [#81](https://github.com/hherb/hhagent/issues/81)**: lifted the remaining inline `#[cfg(test)] mod tests` block (state-machine / floor-raise orchestration pins) verbatim to a re-exported-by-declaration sibling; production **655 → 438 LOC** (under cap). **PR [#172](https://github.com/hherb/hhagent/pull/172) MERGED to `main` at `98a5be0`** (2026-05-31T00:54:12Z; all CI green).

**Start-of-session reconcile #13 (2026-06-01):** the prior session left the header / Currently-on / Last-commit / merged-branches / ROADMAP describing PR [#176](https://github.com/hherb/hhagent/pull/176) (the L3 skill trust enum + approval gate) as OPEN/awaiting-merge; **#176 is in fact MERGED to `main` at `bbcc7b3`** (2026-05-31T22:50:23Z; all CI green). Synced (header / Last-commit / merged-branches / ROADMAP). `docs/essay-medium-draft.md` stays untracked. No new code work yet — awaiting operator Next-TODO pick.

**Last commit on `main`:** `bbcc7b3` (Merge pull request #176). Confirm with `git log --oneline -1`. `origin/main` is at `bbcc7b3` (in sync). This session's work (item 10(c), L3 recall surfacing) shipped on branch `feat/l3-skill-recall-surfacing` (tip `ed226e1`), **PR [#177](https://github.com/hherb/hhagent/pull/177) OPEN** (pushed 2026-06-01; awaiting CI + merge) — see "Currently on" above.

**Recently merged (safe to delete locally, `git branch -d`):** `feat/l3-skill-approval-gate` (PR #176), `refactor/l3-crystallise-test-module-lift` (PR #175), `feat/l3-skill-crystallisation` (PR #173), `refactor/inner-loop-test-module-lift` (PR #172), `refactor/replay-rs-test-module-lift` (PR #171), `refactor/tool-dispatch-split-result-mapping` (PR #170), `refactor/memories-rs-split-write-search` (PR #169), `refactor/launchd-agents-split` (PR #168), `refactor/audit-rs-split-extract-entities` (PR #167), `fix/issue-99-cli-with-runtime` (PR #166), `refactor/macos-container-test-module-lift` (PR #165), `fix/issue-130-launchd-bringup-serialization` (PR #164), `test/router-agent-graph-seed-threading` (PR #162), `fix/issue-153-clippy-clean-gate` (PR #161).

**Toolchain note (standing, from the 2026-05-30 #153 session):** the dev box was bumped `rustup update stable` 1.95.0 → **1.96.0** to match CI (`dtolnay/rust-toolchain@stable`); the `x86_64-unknown-linux-gnu` target/std was added so Linux-only crates can be clippy-checked locally (`cargo clippy -p <crate> --target x86_64-unknown-linux-gnu`). This works for every crate except `core` (its `ring` dep's build script needs `x86_64-linux-gnu-gcc`, the #144 cross-compile wall) — `core`'s Linux path stays CI-verified.

**Session-end verification:** `cargo test --workspace` on macOS (M3 Max, rustc **1.96.0**), on branch `feat/l3-skill-crystallisation` at `0505986`: **1177 passed / 0 failed / 3 ignored** (1157 baseline on `main` at `98a5be0` + 20 new L3 tests). **Live PG (Postgres.app v18):** DB e2e `memory_l3_crystallise_e2e` **7/7**, CLI e2e `cli_memory_l3_e2e` **4/4**; only the known pre-existing `embedding_recall_e2e`/`gliner_relex_e2e` initdb/pg_notify race flaked (passes on retry — matches `main`, NOT a regression). `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-core --no-deps --document-private-items` → **21 unresolved = `main`'s 21** (zero new broken links). **Standing Linux note:** the `linux-check` CI is **compile + clippy only** (no `cargo test`); the last full `cargo test --workspace` on the DGX was `990` at `1abb061` and is still the stale Linux *test* baseline (operator action to refresh). The dev Mac cannot cross-`cargo test`/`check` the `core` crate (the `ring` C dep needs `x86_64-linux-gnu-gcc`), which is exactly why the CI job exists. **Prior `inner_loop.rs` test-lift for reference (reconcile #10):** 1157 at `cb561fd`/`98a5be0` (pure test-module sibling-lift; production region byte-identical; now merged via PR #172).
<!-- prior session-end verification (replay.rs, now on main) -->
**Prior — `cargo test --workspace` on branch `refactor/launchd-agents-split` at `d195d63`: 1157 / 0 / 3** — **unchanged** from the `79fcc27` baseline (pure sibling-module split; the same 52 `hhagent-supervisor` unit tests run, the build_plist/validator/xml_escape ones now from `launchd_agents/builders.rs` and the driver/launchctl-helper ones from `launchd_agents/tests.rs`). `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0; `cargo test -p hhagent-supervisor` 52 unit + 4 launchd smoke green. The split is behaviour-preserving — the two re-exported symbols (`build_plist`, `validate_service_name`) keep their `launchd_agents::…` public path for the parent driver's `install`/lifecycle calls; `xml_escape` stays module-private in `builders.rs`. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-supervisor --no-deps --document-private-items` yields the **same 5 pre-existing** unresolved links as `main` (all the `#[cfg(target_os="linux")]` `systemd_user` cross-module links, which don't compile on macOS) — **zero new**; one self-inflicted new link (`crate::launchd_agents::LaunchAgents::install`, a trait method) was caught and reworded to point at the struct before commit. **macOS-only file** (`#[cfg(target_os="macos")]` via `lib.rs`) — Linux compile covered by `linux-check`. **Prior `audit.rs` split for reference:** 1157 at `3bb0cc7`/`79fcc27` (test lift + re-exported `extract_entities.rs` seam). **Prior `macos_container` lift for reference:** 1157 at `48c0396` (sibling test-module lift; production lines 1–488 byte-identical). **#164 baseline for reference (still current `main`):** 1157 at `091e53d` (the #130/#163 verification: +4 over the 1153 `a83be4a` baseline — 2 macOS-only `serial` tests + 2 cross-platform socket-path tests). Live against Postgres.app v18 at PR #164 time: `injection_guard_e2e` 6/6, `secret_vault_e2e` 11/11, `postgres_e2e` 57/57, zero bring-up timeouts. **Prior #162 run for reference:** **1153 passed / 0 failed / 3 ignored** — +1 over the `12b080c` baseline of 1152 (the new graph-seed-thread regression test; skip-as-pass posture without `HHAGENT_PG_BIN_DIR`). New behaviour proven by two mutations (recall-thread + meta both go RED when broken); `git diff core/src/scheduler/agent.rs` empty → **zero production change**. `router_agent_mock_e2e` 4/4 in isolation. **Clippy `-D warnings` gate: `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0 (macOS 1.96).** Process note: the first push (`6625927`) skipped local clippy and PR [#162](https://github.com/hherb/hhagent/pull/162)'s `linux-check` caught a `clippy::type_complexity` deny on the test's `Arc<Mutex<Option<(String, Vec<i64>)>>>` field — fixed by extracting a `CapturedCall` type alias (`8d6...`), now clippy-clean. Lesson: **run the workspace clippy gate locally before pushing now that `-D warnings` is live**, even for test-only changes (the lint is platform-independent; a `cargo test`-only check misses it). **PG-gated tests not re-run** — the new test uses a hand-rolled mock backend + in-memory doubles (no PG); live PG suites (`secret_vault_e2e` 11/11, `injection_guard_e2e`) last confirmed in their own PRs (#157 / earlier). **Standing Linux note:** the `linux-check` CI is **compile + clippy only** (no `cargo test`); the last full `cargo test --workspace` on the DGX was `990` at `1abb061` and is still the stale Linux *test* baseline (operator action to refresh). The dev Mac cannot cross-`cargo test`/`check` the `core` crate (the `ring` C dep needs `x86_64-linux-gnu-gcc`), which is exactly why the CI job exists.

**Recently merged branches — safe to delete locally** (`git branch -d <name>`): `feat/l3-skill-approval-gate` (PR #176, merged at `bbcc7b3`), `refactor/l3-crystallise-test-module-lift` (PR #175, merged at `55b212e`), `feat/l3-skill-crystallisation` (PR #173, merged at `6eb966e`), `refactor/inner-loop-test-module-lift` (PR #172, merged at `98a5be0`), `refactor/replay-rs-test-module-lift` (PR #171, merged at `30aa52e`), `refactor/tool-dispatch-split-result-mapping` (PR #170, merged at `4e401cc`), `refactor/memories-rs-split-write-search` (PR #169, merged at `e1be537`), `refactor/launchd-agents-split` (PR #168, merged at `5bf010b`), `refactor/audit-rs-split-extract-entities` (PR #167, merged at `79fcc27`), `fix/issue-99-cli-with-runtime` (PR #166, merged at `75e9039`), `refactor/macos-container-test-module-lift` (PR #165, merged at `48c0396`), `fix/issue-130-launchd-bringup-serialization` (PR #164, merged at `091e53d`), `test/router-agent-graph-seed-threading` (PR #162, merged at `a83be4a`), `fix/issue-153-clippy-clean-gate` (PR #161, merged at `12b080c`), `refactor/item-5-tool-host-sibling-lift` (PR #160, merged at `fd7dd7a`), `refactor/injection-guard-test-module-lift` (PR #159, merged at `1106145`), `fix/issue-156-walk-sibling-continue` (PR #158, merged at `f3c380f`), `fix/issue-148-149-secret-vault-test-gaps` (PR #157), `fix/issue-143-walk-recursion-depth-guard` (PR #155), `fix/issue-144-150-linux-build-clippy-gate` (PR #152), `fix/issue-147-redact-tool-req-plaintext` (PR #151), `feat/opaque-secret-refs-slice-1` (PR #146), `feat/injection-guard-slice-1` (PR #141), `fix/issue-89-tmpfs-per-spawn-invariant-test` (PR #139), `refactor/idle-timeout-release-sibling-lift` (PR #138). Older merged branches are listed in the archive snapshots.

---

## Read these first

1. [`docs/architecture.md`](../../architecture.md) — high-level diagram, process model, cross-platform table
2. [`docs/threat-model.md`](../../threat-model.md) — invariant, scenarios in scope, defence-in-depth layers
3. [`docs/devel/ROADMAP.md`](../ROADMAP.md) — the master sequenced TODO list with commit hashes for shipped items
4. The design plan (outside the repo) — `~/.claude/plans/i-d-like-to-design-logical-starlight.md`
5. Memory notes (auto-loaded) — see `~/.claude/projects/-home-hherb-src-hhagent/memory/MEMORY.md`
6. Older handovers — `archive/handover_<timestamp>.md` (one snapshot per pruning event; full historical detail lives there). Most recent: [`archive/handover_20260529_pre-prune.md`](archive/handover_20260529_pre-prune.md).

## Working state (what's green right now)

```
hhagent (Rust workspace, 9 crates, AGPL-3.0)
├── core               hhagent-core: lib + 2 bins (`hhagent` daemon + `hhagent-cli` audit-tail viewer). Daemon blocks on SIGTERM/SIGINT via tokio::signal::unix; main.rs runs db::probe::run → connect_runtime_pool → spawn_mirror before wait_for_shutdown (fail-closed startup; mirror failures are logged but non-fatal). lib modules: tool_host (spawn_worker, dispatch chokepoint, lockdown-env derivation, wall-clock watchdog, sealed WorkerCommand, secret-ref substitution on input + injection-guard screen on output), secrets (Vault TTL'd RwLock<HashMap> + SecretRef opaque newtype + substitute_refs_in_params walker), cassandra/injection_guard (22-entry substring catalogue + screen + extract_scannable_text), workspace (per-task scratch with RAII cleanup), audit_mirror (PgListener-driven JSONL writer with daily rotation + fsync per write), audit_tail (`tail -f`-style follower used by `hhagent-cli audit tail`), scheduler/ (audit.rs pure helpers + canonical SCHEDULER_AUDIT_ACTOR; runner.rs spec §7 lifecycle rows; tool_dispatch.rs short-circuit rows; crash_recovery.rs sweep_and_audit), memory/ (mod.rs facade + recall.rs three-lane RRF-fused recall + embed.rs embed_query), worker_lifecycle/ (Lifecycle enum + SingleUse/IdleTimeout/Composite managers; idle_timeout.rs acquire path + idle_timeout/release.rs release path), entity_extraction/ (batch_upsert.rs two-phase unnest + per-row attribution), workers/ (gliner_relex.rs host+container entries)
├── db                 hhagent-db: pure helpers (build_initdb_argv, build_postgresql_auto_conf, find_pg_bin_dir, pg_bin_dir_candidates_with_env_override) + conn::ConnectSpec + RUNTIME_ROLE/set_role_runtime_statement + probe::run (ensure DB → migrate as superuser → SET ROLE → audit, fail-closed) + graph::{Graph trait, PgGraph; recursive-CTE path() + walk_outbound/inbound_edges + walk_edges_around with DISTINCT ON diamond-dedupe} + audit::{insert, fetch_by_id, fetch_since, truncate_payload} + memories::{insert, semantic/lexical/graph search, link_memory_to_entities} + entity_kinds + relation_kinds lookup caches + pool::{connect_runtime_pool, connect_admin_pool} + MIGRATOR (0001..0017) + memory_entities join table + deleted_memories audit table + secrets (AES-256-GCM at rest + OS keyring) + hhagent-db-init bin
├── llm-router         hhagent-llm-router: sole egress for LLM calls. Router::send + Router::embed over reqwest+rustls; Backend::{Local, Frontier} closed enum; PolicyGate trait (DefaultLocalPolicy always Local — Phase-5 seam). RouterConfig::from_env reads HHAGENT_LLM_* env. Per-OS default URL: vLLM/SGLang on Linux (:8000), Ollama on macOS (:11434). Frontier dispatch returns PolicyDeniedFrontier until Phase 5
├── sandbox            hhagent-sandbox: SandboxPolicy + SandboxBackend trait + SandboxBackendKind (cfg-gated per-OS) + SandboxBackends resolver + LinuxBwrap (wrapped in systemd-run --scope cgroup) + MacosSeatbelt + MacosContainer (Apple `container` micro-VM, macOS-only, opt-in per-worker)
├── supervisor         hhagent-supervisor: SystemdUser (Linux) + LaunchAgents (macOS) + specs::{core_service_spec, postgres_service_spec} + default_probe
├── protocol           hhagent-protocol: JSON-RPC 2.0 over stdio (working)
├── tests-common       hhagent-tests-common: shared dev-dep crate (publish = false) — PgCluster + bring_up_pg_cluster(+_with_timeout), RAII guards, skip helpers, sandbox factory, binary discovery, macOS launchd serial lock, deterministic SHA-256-seeded embedding seed. Consumed only from [dev-dependencies]; never linked into a runtime binary.
├── workers/prelude      hhagent-worker-prelude: Linux-only Landlock + seccomp lock_down (no-op on macOS) + cross-platform setrlimit(RLIMIT_CPU)
└── workers/shell-exec   hhagent-worker-shell-exec: uses prelude::serve_stdio
```

**`cargo test --workspace` on macOS (M3 Max): 1152 / 0 / 3** on `main` at `f3c380f` (Issue #156 `walk()` depth-skip-continues-siblings test now in the baseline; was 1151 at `53e68ed`). Count measured without `HHAGENT_PG_BIN_DIR` (skip-as-pass posture); with PG live ~640 passed / 2 pre-existing flakes (`embedding_recall_e2e` + `gliner_relex_e2e` PG initdb/pg_notify race, identical on `main`). `secret_vault_e2e` verified 11/11 live against Postgres.app v18 (last session, PR #157). Prior checkpoint: **1137** on `feat/opaque-secret-refs-slice-1` at `19eebd6` (Item 31; +41 over 1096 on `main` at `62905ae`/PR #141). **Linux DGX baseline (most recent known): 990 on `main` at `1abb061`** (Item 22 PR #116) — but see the Linux build gap ([#144](https://github.com/hherb/hhagent/issues/144)) in the header before trusting any Linux number. 3 ignored = explicit doctest markers in `hhagent-core`/`hhagent-sandbox`/`hhagent-worker-prelude`. 4 `[SKIP]` lines on `--nocapture` are GLiNER-Relex real-model tests gated on `HHAGENT_GLINER_RELEX_ENABLE=1`. (Full per-session test-count history is in the archive snapshots.)

| Suite | Tests | What's verified |
| ----- | ----- | --------------- |
| `protocol` unit | 3 | dispatch, parse-error fallback, method-not-found |
| `sandbox` unit (linux) | 16 | bwrap argv builder shape (6) + cgroup `systemd-run` argv builder shape: starts with `systemd-run`, uses `--user --scope --quiet --collect`, sets `MemoryMax`+`MemorySwapMax=0`, omits both when `mem_mb=0`, defense-in-depth `CPUQuota=200%` + `TasksMax=64` defaults, ends with `--`, no inner-program leakage, 4 `-p` flags total (10) |
| `sandbox` unit (macos) | 14 | sandbox-exec profile builder shape + path canonicalization + on-host probe + TinyScheme-injection rejection + canonicalize error propagation + strict profile does NOT contain unrestricted `(allow mach-lookup)` (issue #1) |
| `sandbox` integration (`linux_smoke`) | 7 | **real** bwrap+cgroup: echo runs jailed, /etc/passwd & /home invisible, listed paths visible, net unreachable under `Net::Deny`, relative-path policy rejected, mem_burner under `MemoryMax=32M` is OOM-killed, `/tmp` is per-spawn ephemeral tmpfs (#89) |
| `sandbox` integration (`macos_smoke`) | 10 | **real** sandbox-exec: scaffold marker, echo runs jailed, /etc/master.passwd invisible, /Users does not leak username, fs_read paths readable, /dev/disk0 denied, relative-path policy rejected, network unreachable under `Net::Deny`, worker is leader of a fresh session (issue #2), worker cannot `bootstrap_look_up` appleevents (issue #1) |
| `sandbox` integration (`macos_container_smoke`) | 7+ | **real** Apple `container`: argv builder shape, alpine smoke under `--init`, bind-mount-readonly EROFS vs EACCES, strict profile, probe + image-presence skip |
| `core` unit | 60+ | lockdown-env, watchdog loop, workspace RAII, audit_mirror/audit_tail parsers, Option M/N pins, dispatch-result mapping, ToolRegistry, scheduler short-circuit payload pins, Option O embed payload, graph-lane RecallModes pins, injection_guard catalogue + normalize + extract_scannable_text, secrets Vault + SecretRef + substitute_refs_in_params walker (see archive for the full per-area breakdown) |
| `core` integration (`shell_exec_e2e`) | 4 | **cross-platform real** core → bwrap+landlock+seccomp (Linux) / sandbox-exec (macOS) → shell-exec round-trip; every call routes through `tool_host::dispatch` (WorkerCommand seal). Echo; non-allowlisted argv → POLICY_DENIED; unknown method → METHOD_NOT_FOUND; workspace e2e |
| `core` integration (`memory_recall_e2e`) | 1 | **cross-platform real** Phase-1 entry: all three lanes (semantic + lexical + graph) + 1-hop entity expansion + fused RRF + empty-seed degrade |
| `core` integration (`audit_dispatch_e2e`) | 1 | **cross-platform real** dispatcher chokepoint: success + POLICY_DENIED rows; exactly 3 rows total |
| `core` integration (`supervisor_e2e`) | 1 | **cross-platform real** end-to-end smoke: install → start → wait Active → probe-succeeded log → audit_log startup row → JSONL mirror drained → stop → uninstall |
| `core` integration (`scheduler_step_dispatch_e2e`) | 1 | **cross-platform real**: dispatch_step four ways (ok / POLICY_DENIED / UNKNOWN_TOOL+audit row / SPAWN_FAILED+audit row); exactly 5 rows |
| `core` integration (`cli_ask_e2e`) | 2 | **cross-platform real**: full prod chain (CLI → PG → scheduler → LLM → CASSANDRA → dispatch → finalize → CLI exit) against a queued mock LLM; happy path + plan-cap failure |
| `core` integration (`injection_guard_e2e`) | 6 | **PG-required**: placeholder shape, exactly-one policy row, privacy invariant (no scanned body in policy-row payload), SHA shape, benign passthrough, error-path bypass |
| `core` integration (`secret_vault_e2e`) | 9 | **PG-required**: materialize/redeem audit rows, redemption_failed fail-closed, tool-row `req` shows opaque ref not plaintext (#147), policy rows contain no redeemed plaintext |
| `core` integration (`embedding_recall_e2e`) | 4 | **Option O**: embed_query round-trip, audit-row shape, dim-mismatch reject, embed→recall semantic lane |
| `db` unit | 71+ | initdb/auto_conf/bin-dir builders, ConnectSpec, graph field pins, probe SQL pin, RUNTIME_ROLE pins, audit truncate envelope, secrets AES-GCM round-trip + AAD pins, Option N memory pins, entity/relation-kinds validation + description-length cap |
| `db` integration (`postgres_e2e`) | 8+ | probe idempotency + PgGraph happy path; runtime-role REVOKE enforced; audit pool + NOTIFY round-trip; secrets round-trip; memory_entities link + cascade; deleted_memories trigger journalling; kinds runtime-pool list path; walk-edges dedupe |
| `llm-router` unit | 41 | error truncate, ChatRole/ChatResponse decode, Backend serde, config from_env, Option O embedding wire shapes + count-mismatch + pick_embed seam, compose_url, send/pick_backend incl. PolicyDeniedFrontier |
| `llm-router` integration | 8 | hand-rolled TCP mock (no wiremock dep): chat happy/err/decode/chokepoint (4) + embedding happy/err/count-mismatch/chokepoint (4) |
| `prelude` unit + smoke | 21 | env/profile parse, BPF builds, syscall presence; landlock_smoke (4); seccomp_smoke (6) |
| `supervisor` unit | 44 (linux) / 52 (macos) | build_unit_file / build_plist shape, validate_service_name, driver round-trips, specs::* |
| `supervisor` integration | 2 (linux) / 4 (macos) | systemctl --user / launchctl bootstrap round-trips with RAII guards; invalid-name rejection; macOS serialised via static Mutex |
| `core` integration (scheduler_*_e2e) | 8 | inner_loop (4), lanes (1), crash_recovery (2), agent_prompts (1) — cross-platform skip-as-pass without PG |

**Build & test:**
```sh
source "$HOME/.cargo/env"
cargo build --workspace          # produces ./target/debug/hhagent + workers (macOS; see #144 for Linux)
cargo test --workspace           # all green on macOS
./target/debug/hhagent           # runs the core daemon, emits one JSON log line
```

**Required one-time host setup (Ubuntu 24.04+ only):** the AppArmor profile that lets `bwrap` create unprivileged user namespaces is already installed on the user's DGX Spark. Other Linux hosts may need `sudo scripts/linux/install-bwrap-apparmor-profile.sh`. macOS uses `sandbox-exec` (no setup needed).

---

## Recently completed (this session, 2026-06-01 — L3 skill recall surfacing, branch `feat/l3-skill-recall-surfacing`, tip `ed226e1`, PR [#177](https://github.com/hherb/hhagent/pull/177) OPEN)

Start-of-session **reconcile #13**: prior header described PR [#176](https://github.com/hherb/hhagent/pull/176) (the L3 approval gate) as OPEN; it is **MERGED to `main` at `bbcc7b3`** — synced.

**Next-TODO item 10(c) — the `<skills>` planner block.** Full detail (files, decisions, verification, branch-commit map) is in the **"Currently on"** header block above; this section records the why-and-how for the next session.

- **Goal.** Surface operator-approved L3 skills to the planner so they can eventually be reused — the prerequisite for invocation (10b-next), which can't invoke a skill it can't see. **Surfacing-only**: no execution; the prompt explicitly forbids invocation.
- **Pattern.** Mirrors the `<l1_insights>` path one layer over: a query-independent loader (NOT a recall lane) → pure `assemble_system_prompt` block → `skill_count` audit field. New module `core/src/memory/l3_surface.rs` keeps the L3 concern isolated and under the 500-LOC cap (330).
- **Key decisions (why-not-X).** (1) Surface **name + description + params, not steps** — `SurfacedSkill` is the typed projection so "no steps" is a compile-time fact; steps are a dispatch-time concern. (2) Trust filter runs **in SQL** via `load_layer_by_trust` (`WHERE layer=3 AND metadata->>'trust' = ANY(...)`), the marker list derived from `SkillTrust::as_str` (`surfaceable_trust_markers` — single vocabulary source) plus a defensive Rust re-check via `is_surfaceable` over the bounded result. **(post-review perf fix, see below.)** (3) **SQL-filtered fetch capped at `cap_rows`** — NOT the original `load_layer(usize::MAX)` fetch-all: the L3 crystallisation writer appends a `trust:"untrusted"` row on every completed task, so the Skill layer grows with task history; fetching it all and discarding the untrusted majority on every plan formulation was a scaling cliff. The trust push-down bounds the fetch to the handful of approved rows; fetching `cap_rows` is sufficient because `cap_surfaced` never keeps more than `cap_rows` and stops at the row/byte cap. (4) Block placed with the curated layers (**L0 → L1 → skills → recalled → base**), before unverified recall. (5) Caps 16 rows / 4096 bytes (tighter rows than L1's 32 — approved skills are few). (6) Audit: `skill_count` only (no new audit *action* — read-only context assembly, like `l1_count`), but emitted in the `plan.formulate` payload for parity (key pins 25→26 / 26→27).
- **Security.** Load-bearing invariant: only `user_approved`/`pinned` surface; `untrusted`/corrupted/absent ⇒ never (fail-safe `from_metadata_str`). Malformed `template` ⇒ row skipped, never panics. **Final whole-branch review (opus) caught a real gap:** param *descriptions* are surfaced verbatim by `render_skill_entry` but `validate_l3_skill` only guarded the skill description + param *names* — a baked-in `</skills>` in a param description could break out of the block. Fixed (`ed226e1`) by adding the reserved-tag/newline/control-char guards to param descriptions; this closes it at the writer AND the approval gate (which re-runs the validator), so a breakout-carrying param desc can never reach `user_approved` and never surfaces.
- **Post-review perf fix (`/review` → `/fixall`).** The first reviewer flagged a scaling cliff: the loader fetched the **entire** L3 layer (`load_layer(usize::MAX)`) and trust-filtered in Rust, so every plan formulation fetched + JSON-decoded every skill ever crystallised (the writer appends one `untrusted` row per completed task) just to surface ≤16 approved ones. Fixed by adding `db::memories::load_layer_by_trust` (`WHERE metadata->>'trust' = ANY($2)` push-down, exact/case-sensitive match = same fail-safe "unknown ⇒ excluded" posture, capped at `cap_rows`) and deriving its marker list from `SkillTrust::as_str` via `surfaceable_trust_markers` (pinned in sync with `is_surfaceable` by `surfaceable_markers_match_is_surfaceable`). New live-PG e2e `large_untrusted_pile_surfaces_nothing` (40 untrusted rows ⇒ empty; +1 approved ⇒ exactly that one). Reviewer finding #2 (name/param-name breakout) was verified already-safe — `is_snake_ident` makes `<`/`>`/`/`/control chars structurally impossible in names — so no change there.
- **Process.** Brainstorm → spec → plan → 6-task subagent-driven development (fresh implementer + two-stage spec/quality review per task = APPROVED each) → final opus whole-branch review = MERGE-READY after the one security fix. Spec: [`2026-06-01-l3-skill-recall-surfacing-design.md`](../../superpowers/specs/2026-06-01-l3-skill-recall-surfacing-design.md); plan: [`2026-06-01-l3-skill-recall-surfacing.md`](../../superpowers/plans/2026-06-01-l3-skill-recall-surfacing.md).
- **Next.** The L3 arc's only remaining slice is **10(b-next), the DOOR**: invocation/execution (param substitution + live-`ToolRegistry` re-validation at dispatch + sandboxed step run; only `UserApproved`/`Pinned` runnable; the `pin` command lands with it).

---

## Recently completed (prior session, 2026-05-31 — L3 skill trust enum + approval gate, branch `feat/l3-skill-approval-gate`, tip `71188e3`, PR [#176](https://github.com/hherb/hhagent/pull/176) MERGED to `main` at `bbcc7b3`)

Start-of-session **reconcile #12**: prior header described PR [#175](https://github.com/hherb/hhagent/pull/175) (the `l3_crystallise.rs` test-lift) as OPEN; it is **MERGED to `main` at `55b212e`** — synced.

Operator chose **Next-TODO item 10(b) — L3 invocation + Skill trust enum**, scoped (operator decision in brainstorming) to the **gate, not the door**: the security control that must precede any skill-execution path, mirroring how the writer slice shipped storage before invocation. Flow: brainstorm → spec → plan → 8-task subagent-driven development (fresh implementer + two-stage spec/quality review per task = APPROVED each) → final whole-branch review = **MERGE-READY**.

**Why gate-only.** Actual skill invocation executes agent-authored tool-call sequences — the single largest new attack surface in the memory system — and also depends on item 10(c) recall surfacing (the planner can't invoke a skill it can't see). The security-first decomposition builds the approval control + trust enum first; execution is the next slice.

**Design decisions (the why-not-X record).**
- **Pure, registry-parameterised gate.** `evaluate_approval(template, known_tools)` is I/O-free and takes an injected tool-name set, so it's fully unit-testable both ways AND reusable at the future invocation site. Checks (collect ALL reasons): (1) re-run `validate_l3_skill` (structural; short-circuits — later checks assume well-formed); (2) scan step params for any string-leaf beginning with `secrets::REF_PREFIX` (`secret://`) — a baked-in vault ref is dead-on-reuse (refs are per-task/TTL'd) and a cross-task leak vector; (3) every step tool ∈ `known_tools` (distinct unknowns deduped).
- **Tool check sourced from the latest `registry.loaded` snapshot, fail-closed.** The `ToolRegistry` is a daemon-runtime artifact (built at startup from env + DB allowlists); the CLI can't reconstruct it. The daemon writes a `registry.loaded` audit row at every boot listing its registered tool *names* — the CLI reads the latest and feeds `extract_tool_names` into the gate. No snapshot ⇒ **reject** (`NoRegistrySnapshot`): "cannot verify ⇒ do not approve." This is defense-in-depth (invocation re-validates against the live registry — TOCTOU); a stale snapshot can only *false-reject*, never false-approve. Method existence is NOT checked (the registry has no method index — that's inherently dispatch-time).
- **`SkillTrust` hand-converted, not serde-derived.** `from_metadata_str` is total + fail-safe: any unknown/absent/wrong-case value ⇒ `Untrusted`. An unrecognised trust marker must never read as trusted. Single source of truth for the metadata literals via `as_str()` (every trust write + the `list` display routes through it).
- **Rejected approvals are audited** (`l3.approve_rejected`) — a deliberate departure from the writer's silent-validation-failure rule, because an operator attempting to approve a secret-carrying skill is a security event worth a trail.
- **`approve` + `revoke` only.** `Pinned` is defined for forward-compat but command-less (inert until invocation gives the tiers meaning, exactly as `trust:"untrusted"` was inert until this slice). No `pin`, no `add`, no execution.
- **No new migration / db table.** `set_skill_trust` is a layer-guarded `jsonb_set` UPDATE on the existing `metadata` column; db takes a `&str` (db sits below core, no `SkillTrust` dep leaks down).

**Audit-row contract.**

| Actor | Action | Payload |
|---|---|---|
| `cli` | `l3.approved` | `{memory_id, skill_name, body_sha256, tools:[…]}` (gate Approve; trust→user_approved) |
| `cli` | `l3.approve_rejected` | `{memory_id, skill_name?, body_sha256?, reasons:[…]}` (gate Reject; **trust unchanged**) |
| `cli` | `l3.revoked` | `{memory_id, updated}` (trust→untrusted, no gate) |

**Security invariant (whole-branch-review-confirmed).** The *only* code path that flips trust → `user_approved` is the `ApprovalDecision::Approve` arm of `memory_l3_approve`; every reject / error / no-snapshot / wrong-layer / unparseable-template path returns without a trust write. The layer guard (`fetch_by_ids` + `layer == MemoryLayer::Skill`) blocks approving an L0/L1/L2 row. Each invariant has a test that fails on regression (cli e2e asserts the DB trust value after reject + fail-closed; db e2e asserts the L1-id no-op).

**What shipped (files).**
- `core/src/memory/l3_approval.rs` (NEW, ~395 prod+test LOC) — `SkillTrust`, `RejectReason`(+Display), `ApprovalDecision`, `evaluate_approval`, pure `scan_secret_refs` + `extract_tool_names`; 15 unit tests.
- `core/src/memory/mod.rs` — `pub mod l3_approval;`.
- `db/src/memories/write.rs` + `db/src/memories.rs` — `set_skill_trust` (layer-guarded) + re-export; `db/tests/postgres_e2e.rs` +1 live test.
- `core/src/scheduler/audit.rs` (+ sibling `audit/tests.rs`) — 3 action consts + 3 pure payload builders + 3 unit tests.
- `core/src/cli_audit.rs` — `l3_approve_and_audit` / `l3_approve_rejected_audit` / `l3_revoke_and_audit`.
- `core/src/bin/hhagent-cli/memory_l3.rs` — `approve` + `revoke` handlers, `latest_registry_tools` snapshot fetch, typed-trust `list`.
- `core/tests/cli_memory_l3_e2e.rs` — +4 subprocess scenarios (approve happy, secret-ref reject, fail-closed-no-snapshot, revoke-after-approve).
- Spec + plan under `docs/superpowers/{specs,plans}/2026-05-31-l3-skill-approval-gate*`.

**Verification (macOS M3 Max, rustc 1.96.0).** `cargo test --workspace` **1220 / 0 / 3** (no-PG skip-as-pass); live PG (Postgres.app v18): `cli_memory_l3_e2e` **8/8**, `set_skill_trust` db e2e **1/1**, writer-regression `memory_l3_crystallise_e2e` **7/7** — zero `[SKIP]`; `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0; doc-links **21 = main's 21** (zero new). **Follow-ups (new Next-TODO candidates):** (b-next) L3 invocation/execution + live-registry re-check + param substitution; (c) `<skills>` recall surfacing so the planner sees `UserApproved`/`Pinned` skills; the `pin` command lands when the tiers gain behavioural meaning. `docs/essay-medium-draft.md` stays untracked. **PR [#176](https://github.com/hherb/hhagent/pull/176) OPEN** (pushed 2026-06-01).

---

## Recently completed (prior session, 2026-05-31 — `l3_crystallise.rs` test-lift, branch `refactor/l3-crystallise-test-module-lift`, code commit `34bc262`)

Start-of-session **reconcile #11**: the prior session left the header / Currently-on / Last-commit / merged-branches / ROADMAP describing PR [#173](https://github.com/hherb/hhagent/pull/173) (the L3 skill crystallisation writer) as OPEN/awaiting-merge; **#173 is in fact MERGED to `main` at `6eb966e`** (2026-05-31T11:46:35Z; all CI green) — synced before starting.

Operator then chose **Next-TODO item 10a — the `l3_crystallise.rs` test-lift**, the direct follow-up to the just-merged L3 writer and a stated prerequisite for the next L3 slices. `core/src/memory/l3_crystallise.rs` was 676 LOC, over the 500-LOC soft cap. Pure-mechanical sibling-module lift (Rust-2018 pattern; precedents `inner_loop/tests.rs`, `observation/replay/tests.rs`, `tool_dispatch/tests.rs`): moved the inline `#[cfg(test)] mod tests` block (208 body lines) verbatim — de-indented one level — into a new re-exported-by-declaration sibling [`core/src/memory/l3_crystallise/tests.rs`](../../../core/src/memory/l3_crystallise/tests.rs) (220 LOC incl. header doc); the parent keeps `#[cfg(test)] mod tests;` and `use super::*` resolves to the parent so every item the tests exercise stays reachable. Production **676 → 467 LOC** (under cap).

**Verification (macOS M3 Max, rustc 1.96.0).** Zero behaviour change, proven mechanically: production region L1–464 byte-identical to `main` (`diff` exit 0); the de-indent is exactly reversible (re-indenting the new body reproduces the original inline block byte-for-byte); `cargo test --workspace` **1177 / 0 / 3** (unchanged from the post-#173 baseline); the 20 lifted unit tests pass from `memory::l3_crystallise::tests::*`; `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0; `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-core --no-deps --document-private-items` → 21 unresolved = `main`'s 21 (zero new). PG-free (unit tests only) — no live PG run needed, matching the `inner_loop`/`replay` precedent. `docs/essay-medium-draft.md` stays untracked. **PR [#175](https://github.com/hherb/hhagent/pull/175) OPEN.**

---

## Recently completed (prior session, 2026-05-31 — L3 skill crystallisation writer, branch `feat/l3-skill-crystallisation`, PR [#173](https://github.com/hherb/hhagent/pull/173) MERGED at `6eb966e`)

Operator chose the **engineering-pickup "L3 skill crystallisation — spec" item** from the Next-TODO list. All pre-reqs were in-tree; the L1 promotion writer (PR #82, `Plan.l1_insight` → `drain_lane` hook → audit row) was the direct precedent one layer down. Start-of-session also applied reconcile #10: the prior header described PR #172 (the `inner_loop.rs` test-lift) as "PR not yet opened" on branch `refactor/inner-loop-test-module-lift`; in fact **PR [#172](https://github.com/hherb/hhagent/pull/172) is MERGED to `main` at `98a5be0`** (2026-05-31T00:54:12Z; all CI green) and **issue [#81](https://github.com/hherb/hhagent/issues/81) is CLOSED** — header/Currently-on/Last-commit/merged-branches synced before starting.

**Why this session's pick.** The L3 crystallisation item had been in the Next-TODO "engineering pickups — need a spec/design first" bucket since the L1 promotion writer landed (2026-05-18). All pre-reqs were now in-tree (L3 `MemoryLayer::Skill` column, canonical `insert_memory_at_layer`/`delete_memory_at_layer`/`load_layer` helpers, `drain_lane` best-effort hook pattern from L1, `audit_log` trajectory, observation-phase ground truth). The flow: brainstormed → spec ([`docs/superpowers/specs/2026-05-31-l3-skill-crystallisation-design.md`](../superpowers/specs/2026-05-31-l3-skill-crystallisation-design.md)) → plan ([`docs/superpowers/plans/2026-05-31-l3-skill-crystallisation.md`](../superpowers/plans/2026-05-31-l3-skill-crystallisation.md)) → 14-task subagent-driven development (each task spec+quality reviewed = APPROVED) → final whole-branch review = MERGE-READY.

**Design decisions (the why-not-X record).**

- **Writer-only, non-executable this slice.** The L1 precedent ships storage + recall surfacing in one PR; L3 is intentionally writer-only here because the invocation path requires a Skill trust enum (`Untrusted|UserApproved|Pinned`) and an approval gate that scans step params for secrets + validates tool/method existence against the `ToolRegistry` before any promotion. Shipping invocation without that gate would be a security regression. Writer first; invocation in the next L3 slice.
- **Agent-emitted structured candidate (`Plan.l3_skill`).** The agent includes a compact `l3_skill` JSON block in the plan that names the skill, declares template steps with `{{placeholder}}` parameters, and lists a param manifest. This mirrors `Plan.l1_insight` exactly; the planner is taught the schema in `prompts/agent_planner.md`.
- **`{{name}}` closed-world invariant.** Every `{{placeholder}}` in template steps must appear in the param manifest (and vice versa), validated by `validate_l3_skill`. This prevents a poisoned template from embedding live tool-call fragments that aren't parameterised — the guard runs before any storage.
- **Two emit gates: `Outcome::Completed` + `dispatch_count >= 1`.** The `terminal_l3_skill` field is only populated on `Outcome::Completed` AND when `dispatch_count >= 1` (the inner loop actually dispatched at least one tool). This prevents pure-text completions (e.g. a clarifying response with zero tool calls) from emitting skills. The spec described a "total_steps_executed" accumulator; `dispatch_count` already tracks this behaviourally — no separate accumulator was needed (NOTE: see Next-TODO).
- **`trust:"untrusted"` placeholder.** The stored row carries a `trust:"untrusted"` metadata field as a forward-compat seam for the future Skill trust enum. When invocation lands, it will check this field and reject any non-approved skill from running; the field is already wired in on write.
- **`reserved_tag <skills>` guard.** `validate_l3_skill` blocks any template step whose body contains the substring `<skills>` (analogous to `<l1_insights>` guard in L1) to prevent a poisoned skill from injecting into the recall-surface prompt block the future `<skills>` assembler will produce.
- **No new `db/` helper.** `crystallise_l3` reuses the existing `insert_memory_at_layer`/`delete_memory_at_layer`/`load_layer` — no migration, no new SQL, no new `db/` function. The layer column already carries `layer=3`; the type system guarantees no cross-layer confusion.

**Audit-row contract.**

| Actor | Action | Payload fields |
|---|---|---|
| `scheduler` | `l3.crystallised` | `source:"agent_raised"`, `task_id`, `skill_name`, `action:("inserted"|"skipped_duplicate")`, `memory_id`, `body_sha256` |
| `cli` | `l3.removed` | `memory_id`, `deleted` |
| `agent` | `plan.formulate` | gains `l3_skill:{name, step_count, param_count}` or `null` |

**What shipped (files).**
- `core/src/cassandra/types.rs` — new types `L3SkillCandidate`, `L3Param`, `L3TemplateStep`, `Plan.l3_skill` field, `Plan::completion_skill()` accessor.
- `core/src/memory/l3_crystallise.rs` (new module, ~458 prod LOC) — `validate_l3_skill` (closed-world placeholder integrity + `<skills>` reserved-tag + caps); `canonical_json`/`compute_template_sha256` (order-stable dedup SHA-256); `crystallise_l3` writer; `list_l3`/`remove_l3`.
- `core/src/scheduler/inner_loop.rs` — `InnerLoopResult.terminal_l3_skill` field + `dispatch_count >= 1` grounding gate on the `Outcome::Completed` arm.
- `core/src/scheduler/runner.rs` — `drain_lane` best-effort hook `write_l3_crystallised_row`.
- `core/src/audit.rs` — new constants `ACTION_L3_CRYSTALLISED`/`ACTION_L3_REMOVED` + `build_l3_write_payload`.
- `core/src/scheduler/audit.rs` — `plan.formulate` payload gains `l3_skill` key (key-count pins bumped 24→25 default-source / 25→26 cli-inferred).
- `core/src/cli_audit.rs` — `l3_remove_and_audit`.
- `core/src/bin/hhagent-cli/memory_l3.rs` (new) — `memory l3 {list,remove}` CLI subcommands.
- `core/prompts/agent_planner.md` — teaches the planner the `l3_skill` JSON schema.
- `core/tests/memory_l3_crystallise_e2e.rs` (new) — 7-scenario DB e2e (insert, dedup idempotency, grounding gate, validation rejection, list/remove, audit-row shape).
- `core/tests/cli_memory_l3_e2e.rs` (new) — 4-scenario CLI e2e (list empty, list after add, remove, audit trail).
- Spec: `docs/superpowers/specs/2026-05-31-l3-skill-crystallisation-design.md`.
- Plan: `docs/superpowers/plans/2026-05-31-l3-skill-crystallisation.md`.

**History note (filter-branch rewrite).** A `git add -A` in the first task swept two stray untracked files (`docs/essay-medium-draft.md`, `.claude/scheduled_tasks.lock`) into an early commit. They were stripped from the branch via `git filter-branch` (branch SHAs were rewritten — that's why the commit SHAs are post-rewrite values). The essay draft was restored to disk as an untracked file. The branch's net diff vs `main` contains ONLY L3 implementation files + the spec/plan docs; no stray files.

**Verification (macOS M3 Max, rustc 1.96.0).** `cargo test --workspace` (no-PG, skip-as-pass) = **1177 passed / 0 failed / 3 ignored** (1157 baseline + 20 new). Live PG (Postgres.app v18, `HHAGENT_PG_BIN_DIR='/Applications/Postgres 2.app/Contents/Versions/18/bin/'`): DB e2e `memory_l3_crystallise_e2e` **7/7**, CLI e2e `cli_memory_l3_e2e` **4/4**; only the known pre-existing `embedding_recall_e2e`/`gliner_relex_e2e` initdb/pg_notify race flaked (passes on retry — matches `main`, NOT a regression). `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-core --no-deps --document-private-items` → 21 unresolved = `main`'s 21 (zero new broken links). Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`.

**File-size note.** `core/src/memory/l3_crystallise.rs` is 670 LOC (~458 prod / ~212 inline tests) — **over the 500-LOC cap**. A clean test-lift to a sibling `l3_crystallise/tests.rs` mirrors the `inner_loop`/`replay` pattern and would land production under cap. Flagged as a Next-TODO follow-up (see below).

**Follow-ups (see Next-TODO section):**
- **`l3_crystallise.rs` test-lift** — 670 LOC (~458 prod / ~212 inline tests), over cap; clean test-lift available.
- **L3 invocation + Skill trust enum** (`Untrusted|UserApproved|Pinned`) — the next L3 step; requires approval gate scanning step params for secrets + validating tool/method against `ToolRegistry` before any promotion.
- **Recall surfacing** — a `<skills>` prompt block surfacing L3 rows to the planner at context-assembly time.

---

## Recently completed (2026-05-31 — `core/src/scheduler/inner_loop.rs` over-cap test-lift, **closes issue [#81](https://github.com/hherb/hhagent/issues/81)**, branch `refactor/inner-loop-test-module-lift`, **merged to `main` via PR [#172](https://github.com/hherb/hhagent/pull/172) at `98a5be0`**)

---

## Recently completed (this session, 2026-05-31 — `core/src/scheduler/inner_loop.rs` over-cap test-lift, **closes issue [#81](https://github.com/hherb/hhagent/issues/81)**, branch `refactor/inner-loop-test-module-lift`)

Operator picked the **next over-cap refactor**. Start-of-session also reconciled the stale header (#9) + ROADMAP: PR [#171](https://github.com/hherb/hhagent/pull/171) (the prior `observation/replay.rs` test-lift) arrived described as OPEN/awaiting-merge; it is in fact **merged to `main` at `30aa52e`** (2026-05-30T22:56:00Z, all CI green) — header / Currently-on / Last-commit / merged-branches / Next-TODO / ROADMAP synced before starting.

**Why `inner_loop.rs` (the file choice).** The prior handover's Next-TODO claimed "no clean test-lift-only candidate remains," but that census was scoped only to files in the active refactor arc. A **full `wc -l` + `mod tests` census of every over-cap production file** turned up five clean test-lift candidates the handover had never analyzed (`memory/l0_seed.rs` 730, `observation/capture.rs` 715, `memory/recall.rs` 622, `sandbox/macos_seatbelt.rs` 604, and `scheduler/inner_loop.rs` 655). `inner_loop.rs` was the standout: a clean test-lift (655 → 438) that **also closes a tracked issue ([#81](https://github.com/hherb/hhagent/issues/81))**. #81's first slice (PR #79 era) had already extracted the audit-payload builder + writers into `inner_loop_audit.rs`, meeting its "under 700" acceptance but leaving the file 155 over the 500-LOC cap — exactly the "second slice (complete restoration to under 500 may need a second slice)" the issue text anticipated.

**The change (pure-mechanical, no behaviour change).** Following the established Rust-2018 sibling pattern (`tool_dispatch/`, `audit/`, `launchd_agents/`, `macos_container/`, `injection_guard/`, and the file's own existing `inner_loop_audit.rs` sibling):

- [`core/src/scheduler/inner_loop/tests.rs`](../../../core/src/scheduler/inner_loop/tests.rs) (230 LOC incl. a 13-line module-doc header) — the inline `#[cfg(test)] mod tests` body (parent L438–654), lifted **verbatim** and de-indented one level (`sed 's/^    //'` — verified safe: zero non-empty body lines started with <4 spaces; re-indent-diff vs the original → IDENTICAL). `use super::*` (+ `use crate::cassandra::types::DataClass;`) move with the body; `super` resolves to the parent `inner_loop` module so every item the tests exercise (`TaskContext`, `ClassificationFloorSource`, `apply_floor_raise`, the `inner_loop_audit` writer re-exports, …) is reachable exactly as before. These pin the state-machine / floor-raise orchestration; the audit-payload-shape pins live with their builder in `inner_loop_audit.rs`.
- [`core/src/scheduler/inner_loop.rs`](../../../core/src/scheduler/inner_loop.rs) (438 LOC, **under cap**) — production region L1–437 kept **byte-identical** (the `git diff` is a single hunk removing only the trailing test block); the inline block replaced by the two-line declaration `#[cfg(test)]` / `mod tests;`.

**Why no TDD / no RED.** Refactor with zero new behaviour; the 22 `scheduler::inner_loop` + `scheduler::inner_loop_audit` unit tests ARE the regression pin (count unchanged before/after, just running from the new file location). Behaviour-preservation proven three ways: (1) the production region is byte-identical to the pre-move file; (2) the lifted body re-indents to byte-identical; (3) `#[cfg(test)]` modules aren't compiled by `cargo doc`, so the doc-link set provably can't shift.

**Verification (macOS M3 Max, rustc 1.96).** `cargo build -p hhagent-core` clean; `cargo test -p hhagent-core --lib scheduler::inner_loop` **22/22** from the new location; `cargo test --workspace` **1157 / 0 / 3** (identical to the `30aa52e` baseline); `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-core --no-deps --document-private-items` → the **same 21 pre-existing** unresolved links as `main` (diff against a fresh `main` worktree → empty). **No live PG run needed** — the lifted tests are PG-free unit tests (all ran in the workspace pass). Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`.

**File-size watch.** `inner_loop.rs` **655 → 438 LOC** (under cap — **#81 resolved**); new `tests.rs` 230 (test file; the cap targets production files). **No clean test-lift-only candidate remains** that lands a parent under cap in one move — the remaining over-cap production files each need a real prod split or a re-exported pure-helper seam: `db/graph.rs` (926, design-gated Item 23b walk-impl split), `core/workers/gliner_relex.rs` (861, tests already lifted), `db/secrets.rs` (848, ~645 after a test-lift → needs a seam), `core/src/cli_audit.rs` (771, ~670 after a test-lift), `core/src/main.rs` (729, ~690 prod), `core/src/memory/l0_seed.rs` (730), `core/src/observation/capture.rs` (715), `core/src/memory/recall.rs` (622), `sandbox/src/macos_seatbelt.rs` (604) — the last four have inline `mod tests` blocks but a re-census is warranted before picking; `supervisor/systemd_user.rs` (798, ~502 prod — at cap).

---

## Recently completed (2026-05-31 — `core/src/observation/replay.rs` over-cap test-lift, branch `refactor/replay-rs-test-module-lift`, **merged to `main` via PR [#171](https://github.com/hherb/hhagent/pull/171) at `30aa52e`**)

Operator picked **item 9b — the next CLEAN over-cap test-lift** from the refactor bucket. Start-of-session also reconciled the stale header (#8) + ROADMAP: PR [#170](https://github.com/hherb/hhagent/pull/170) (the prior `tool_dispatch.rs` split) arrived described as OPEN; it is in fact **merged to `main` at `4e401cc`** (2026-05-30T22:16:20Z, all CI green) — header / Currently-on / Last-commit / merged-branches / Next-TODO / ROADMAP synced before starting.

**Why `replay.rs` (the file choice).** Per the Next-TODO item-9b ranking, `replay.rs` (804 LOC, ~420 prod / ~384 inline tests) was flagged as the **next CLEAN test-lift-only**: unlike `db/secrets.rs` (848, ~645 prod after a lift) or `gliner_relex.rs` (861, tests already lifted → needs a real prod split), lifting its inline `#[cfg(test)] mod tests` block alone lands the parent under cap. No pure-helper seam needed (contrast `audit.rs`/`launchd_agents.rs`/`tool_dispatch.rs`, where a test-lift left production over cap).

**The change (pure-mechanical, no behaviour change).** Following the established Rust-2018 sibling pattern (`tool_dispatch/`, `audit/`, `launchd_agents/`, `macos_container/`, `injection_guard/`):

- [`observation/replay/tests.rs`](../../../core/src/observation/replay/tests.rs) (390 LOC incl. an 8-line module-doc header) — the inline `#[cfg(test)] mod tests` body (parent L423–803), lifted **verbatim** and de-indented one level (`sed 's/^    //'` — verified safe: zero non-empty body lines started with <4 spaces; re-indent-diff vs the original → IDENTICAL). The two `use` lines (`use super::*;` + `use crate::cassandra::types::Severity;`) move with the body; `super` still resolves to the parent `replay` module and `crate::` paths are absolute, so every item the 20 tests exercise (`VerdictSnapshot`, `ReplayBundle`, `replay_capture`, `format_report_table`, …) is reachable exactly as before.
- [`observation/replay.rs`](../../../core/src/observation/replay.rs) (422 LOC, **under cap**) — production region L1–420 kept **byte-identical** (diff-verified); the inline block L421–804 replaced by the two-line declaration `#[cfg(test)]` / `mod tests;`.

**Why no TDD / no RED.** Refactor with zero new behaviour; the 20 `observation::replay` unit tests ARE the regression pin (count unchanged before/after, just running from the new file location). Behaviour-preservation proven three ways: (1) the production region is byte-identical to the pre-move file; (2) the lifted test body re-indents to byte-identical; (3) `#[cfg(test)]` modules aren't compiled by `cargo doc`, so the doc-link set provably can't shift.

**Verification (macOS M3 Max, rustc 1.96).** `cargo build -p hhagent-core` clean; `cargo test -p hhagent-core --lib observation::replay` **20/20** from the new location; `cargo test --workspace` **1157 / 0 / 3** (identical to the `4e401cc` baseline); `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-core --no-deps --document-private-items` → the **same 25 pre-existing** unresolved-link errors as `main` (zero mention replay/observation; the move can't affect doc links). **No live PG run needed** — `replay.rs` operates on in-memory captured fixtures; its tests are PG-free unit tests (all 20 ran in the workspace pass). Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`.

**File-size watch.** `replay.rs` **804 → 422 LOC** (under cap — resolved); new `tests.rs` 390 (test file; the cap targets production files). Remaining over-cap production files for future slices: `db/graph.rs` (926, walk-impl split = design-gated Item 23b), `core/workers/gliner_relex.rs` (861, needs a real prod split), `db/secrets.rs` (848, ~645 prod after a test-lift), `supervisor/systemd_user.rs` (798, ~502 prod — at cap), `core/src/scheduler/inner_loop.rs` (655, #81).

---

## Recently completed (2026-05-30 — `core/src/scheduler/tool_dispatch.rs` over-cap split, branch `refactor/tool-dispatch-split-result-mapping`, code commit `4c1241e`, merged to `main` via PR [#170](https://github.com/hherb/hhagent/pull/170) at `4e401cc`)

Operator picked the **over-cap file split** option from the refactor bucket. Start-of-session also reconciled the stale header (#7) + ROADMAP: PR [#169](https://github.com/hherb/hhagent/pull/169) (the prior `db/memories.rs` write+search split) arrived described as OPEN/not-yet-opened; it is in fact **merged to `main` at `e1be537`** (2026-05-30T11:57:51Z, all CI green; code commit `39e5cae` + style polish `7b41008`) — header / Currently-on / Last-commit / merged-branches / Next-TODO / ROADMAP synced before starting.

**Why `tool_dispatch.rs` (the file choice).** Measured the remaining over-cap candidates by inline-test fraction: `db/graph.rs` (926, but the *real* walk-impl split is design-gated Item 23b — "until a 2nd `WalkedEdge` consumer materialises"), `core/workers/gliner_relex.rs` (861, tests already lifted → needs a real prod split), `db/secrets.rs` (848, a test-lift leaves ~645 → not clean), `tool_dispatch.rs` (828, ~502 prod / ~326 inline tests), `observation/replay.rs` (804, ~420 prod / ~384 inline tests). `tool_dispatch.rs` was the **biggest with a clean lift available**: a test-lift alone would leave it ~503 (still over cap), but it has a natural **pure-helper seam** — the two I/O-free result-mapping functions named in its own module doc — so it mirrors the `audit.rs` / `launchd_agents.rs` precedent exactly (test-lift **plus** a re-exported pure-builder seam) and fully resolves under cap.

**The change (pure-mechanical, no behaviour change).** Following the established Rust-2018 sibling pattern (`audit/`, `launchd_agents/`, `macos_container/`, `injection_guard/`, `tool_host/`), re-exported so every `scheduler::tool_dispatch::<name>` call site is byte-for-byte unchanged:

- [`tool_dispatch/result_mapping.rs`](../../../core/src/scheduler/tool_dispatch/result_mapping.rs) (88 LOC) — the two **pure** functions `rpc_code_name` (JSON-RPC code → mnemonic) + `map_dispatch_result` (`Result<Value, ToolHostError>` → `StepOutcome`). Moved **byte-identical** (diff-verified: `sed`-extracted vs `git show main:…` → IDENTICAL). Imports `codes`/`ClientError` from `hhagent_protocol`, `StepOutcome` from `crate::scheduler::inner_loop`, `ToolHostError` from `crate::tool_host` directly. **Re-exported from the parent** (`pub use result_mapping::{map_dispatch_result, rpc_code_name}`) so the public paths hold and the parent's own `dispatch_step` call to `map_dispatch_result` resolves unchanged. These two symbols have **zero external callers** (only a doc-comment mention in `gliner_relex`) — confirmed before the move.
- [`tool_dispatch/tests.rs`](../../../core/src/scheduler/tool_dispatch/tests.rs) (339 LOC) — the inline `#[cfg(test)] mod tests` body (L505–827), lifted verbatim and de-indented one level (`sed 's/^    //'` — verified safe: every non-empty body line started with ≥4 spaces; re-indent-diff vs the original → IDENTICAL). `use super::*` resolves to the parent (incl. the re-exported result-mapping symbols + parent-private `build_scheduler_step_failure_payload`/`ACTION_STEP_*`).
- [`tool_dispatch.rs`](../../../core/src/scheduler/tool_dispatch.rs) (442 LOC, **under cap**) — keeps `ToolEntry`, `ToolRegistry` + impl, `shell_exec_entry`, `ToolHostStepDispatcher` + `StepDispatcher` impl, and the audit-payload helpers (`build_scheduler_step_failure_payload` + the `ACTION_STEP_UNKNOWN_TOOL`/`ACTION_STEP_SPAWN_FAILED` consts) — those stay because they're tightly coupled to the dispatcher's two short-circuit audit rows, not pure result-mapping.

**Import fixups (the only non-mechanical part).** Moving the two functions out left `codes` + `ClientError` unused in the parent's `use hhagent_protocol::{client::ClientError, codes}` and `ToolHostError` unused in `use crate::tool_host::{dispatch, ToolHostError}` (the latter's remaining 4 references are doc-links/comments, not code) → both would fail clippy `-D warnings`. Removed both from the parent imports; added `use crate::tool_host::ToolHostError;` + `codes`/`ClientError` to `tests.rs` (the lifted tests reference them and previously reached them via the parent's `use super::*` glob). One parent doc-link `[`ToolHostError`]` (L54) that previously resolved via the now-removed import was qualified to `[`ToolHostError`](crate::tool_host::ToolHostError)` — displayed text preserved.

**Why no TDD / no RED.** Refactor with zero new behaviour; the 17 `scheduler::tool_dispatch` module tests + the live `scheduler_step_dispatch_e2e` ARE the regression pin. Behaviour-preservation proven three ways: (1) the moved function bodies are byte-identical to `main` (diff-verified); (2) the lifted test body re-indents to byte-identical; (3) live e2e green.

**Verification (macOS M3 Max, rustc 1.96).** `cargo build -p hhagent-core` clean; `cargo test -p hhagent-core --lib scheduler::tool_dispatch` **17/17** from the new locations; `cargo test --workspace` **1157 / 0 / 3** (identical to the `e1be537` baseline); `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-core --no-deps --document-private-items` → the **same 25 pre-existing** unresolved-link errors as `main` (diff against a fresh `main` worktree → IDENTICAL set; the `[`spawn_worker`]`/`[`tool_host::dispatch`]` ones were already broken). **Live against Postgres.app v18** (`HHAGENT_PG_BIN_DIR='/Applications/Postgres 2.app/Contents/Versions/18/bin/'`): `scheduler_step_dispatch_e2e` **1/1** (`dispatcher_routes_ok_denied_and_unknown_tool_paths` — exercises `map_dispatch_result` via the re-export + `build_scheduler_step_failure_payload` in the parent for the unknown-tool/spawn-failed rows). Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`.

**File-size watch.** `tool_dispatch.rs` **828 → 442 LOC** (under cap — resolved); new `result_mapping.rs` 88, `tests.rs` 339 (test file; the cap targets production files). Remaining over-cap production files for future slices: `db/graph.rs` (926, walk-impl split = design-gated Item 23b), `core/workers/gliner_relex.rs` (861, needs a real prod split), `db/secrets.rs` (848, ~645 prod after a test-lift), `core/src/observation/replay.rs` (804, **clean test-lift available** → ~420 prod), `supervisor/systemd_user.rs` (798, ~502 prod — at cap), `core/src/scheduler/inner_loop.rs` (655, #81).

---

## Recently completed (2026-05-30 — `db/memories.rs` over-cap split into write + search siblings, branch `refactor/memories-rs-split-write-search`, code commit `39e5cae`, merged to `main` via PR [#169](https://github.com/hherb/hhagent/pull/169) at `e1be537`)

Operator picked the **`db/memories.rs` over-cap split** from the refactor bucket — at **961 LOC** (~863 prod + ~98 inline tests) it was the largest remaining over-cap production file. Start-of-session also reconciled the stale header (#6) + ROADMAP: PR [#168](https://github.com/hherb/hhagent/pull/168) (the prior `launchd_agents.rs` split) arrived described as OPEN; it is in fact **merged to `main` at `5bf010b`** (2026-05-30T09:15:07Z, all CI green) — header / Currently-on / Last-commit / merged-branches / ROADMAP synced before starting.

**Why a real split, not a test-lift (the shape decision).** The recent over-cap remedies (`macos_container`, `audit`, `launchd_agents`) all leaned on lifting a large inline `#[cfg(test)] mod tests` block. `memories.rs` can't: its inline tests are only ~98 LOC **and they exercise parent-resident items** (`EMBEDDING_DIM`, `DEFAULT_RECALL_K`, `vector_literal`, `check_embedding_dim`, `limit_as_i64`) — so lifting them leaves production at ~863, still 363 over cap, and the tests would have to stay with the parent anyway. This is therefore a **real production-logic split** along the table's natural read/write seam.

**The change (pure-mechanical, no behaviour change).** Following the established Rust-2018 sibling pattern (`audit/`, `macos_container/`, `injection_guard/`, `tool_host/`), re-exported so every `db::memories::<name>` call site is byte-for-byte unchanged:

- [`db/src/memories/write.rs`](../../../db/src/memories/write.rs) (259 LOC) — the write path: `insert_memory`, `insert_memory_at_layer`, `delete_memory_at_layer`, `seed_meta_memory`, `link_memory_to_entities`, and the private `insert_row_at_layer_unchecked`. Imports the shared guards from the parent via `use super::{check_embedding_dim, vector_literal, MemoryLayer}`.
- [`db/src/memories/search.rs`](../../../db/src/memories/search.rs) (400 LOC) — the read path: `semantic_search`, `lexical_search`, `graph_search`, `fetch_by_ids`, `load_layer`, `load_active_l0`. Imports `use super::{check_embedding_dim, limit_as_i64, vector_literal, Memory, MemoryLayer}`.
- [`db/src/memories.rs`](../../../db/src/memories.rs) (360 LOC, **under cap**) — keeps the shared vocabulary (`MemoryLayer` enum, `Memory` row struct, `EMBEDDING_DIM`, `DEFAULT_RECALL_K`, the private `check_embedding_dim`/`limit_as_i64` guards, the pure `vector_literal` pgvector encoder), the unit tests for those, and `mod search; mod write;` + `pub use search::{…}; pub use write::{…};`. Child modules reach the parent-private guards via `super::` (Rust grants descendant modules access to parent-private items).

**Why the parent shrank to 360, not ~256.** The tests (~98 LOC) stay in the parent because they test parent-resident items — that's a feature, not a leftover: the read/write siblings carry zero test weight, keeping them purely production.

**Why no TDD / no RED.** Refactor with zero new behaviour. Proven byte-identical two ways: (1) `sed`-extracted the moved ranges and `diff`'d them against `git show HEAD:db/src/memories.rs` — **IDENTICAL** for both siblings' function bodies; (2) the SQL strings are unchanged, so live behaviour is guaranteed. The existing suite (unit + live PG) is the regression pin.

**Doc-link care.** Moving the two `[`EMBEDDING_DIM`]` shortcut doc-links into the siblings (where `EMBEDDING_DIM` isn't in scope) would break them under `-D rustdoc::broken_intra_doc_links`. Importing the const just for the doc would be an unused-import → clippy `-D warnings` failure. Resolved by repointing both at the explicit path `[`EMBEDDING_DIM`](super::EMBEDDING_DIM)` — displayed text preserved, zero import. The `cargo doc` broken-link set now matches `main` **exactly** (3 pre-existing errors — `MIGRATOR`, `db/migrations/`, `probe::ensure_database_exists` — + 1 pre-existing private-link warning on `insert_memory_at_layer`→`check_embedding_dim`; **zero new**).

**Verification (macOS M3 Max, rustc 1.96).** `cargo build -p hhagent-db` clean; `cargo test -p hhagent-db --lib memories` **9/9** from the parent; `cargo test --workspace` **1157 / 0 / 3** (identical to the `5bf010b` baseline); `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0. **Live against Postgres.app v18** (`HHAGENT_PG_BIN_DIR='/Applications/Postgres 2.app/Contents/Versions/18/bin/'`): `postgres_e2e` **57/57** (directly exercises the moved write/search helpers), and across the crate boundary via the re-exports `memory_recall_e2e` **13/13** (all three recall lanes), `memory_l1_promote_e2e` **9/9** (`insert_memory_at_layer`/`load_layer`/`delete_memory_at_layer`), `memory_l0_seed_e2e` **2/2** (`seed_meta_memory`/`load_active_l0`), `embedding_recall_e2e` **4/4**. The `embedding_recall_e2e` run hit the **pre-existing #104 flake** once under parallel threads (`initdb: … "data": File exists` + `could not open directory "pg_notify"` — a pid+nanos tempdir collision in `tests-common/src/pg.rs:249`, **before any memories SQL runs**, "identical on `main`"); `--test-threads=1` cleared it 4/4, confirming it's the flake and not this change.

**File-size watch.** `memories.rs` **961 → 360 LOC** (under the 500 cap — resolved); new `write.rs` 259, `search.rs` 400 (both under cap). Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`. Remaining over-cap production files for future slices: `db/graph.rs` (926, walk-impl split = Item 23b), `core/workers/gliner_relex.rs` (861), `db/secrets.rs` (848), `core/src/scheduler/tool_dispatch.rs` (828), `core/src/observation/replay.rs` (804), `supervisor/systemd_user.rs` (798, ~502 prod — at cap), `core/src/scheduler/inner_loop.rs` (655, #81).

---

## Recently completed (2026-05-30 — `supervisor/launchd_agents.rs` over-cap split, branch `refactor/launchd-agents-split`, code commit `d195d63`, merged to `main` via PR [#168](https://github.com/hherb/hhagent/pull/168) at `5bf010b`)

Operator picked the **`launchd_agents.rs` over-cap lift** from the refactor bucket — at **1093 LOC** it was the largest remaining over-cap production file. Start-of-session also reconciled the stale header (#5) + ROADMAP: PR [#167](https://github.com/hherb/hhagent/pull/167) (the prior `scheduler/audit.rs` split) arrived described as OPEN; it is in fact **merged to `main` at `79fcc27`** (2026-05-30T04:38:22Z, all CI green) — header / Currently-on / Last-commit / merged-branches / ROADMAP synced (docs commit `eb955a8`) before starting.

**Why a test-lift alone wasn't enough (the shape decision).** `launchd_agents.rs` was ~686 LOC production + ~407 LOC inline tests. Unlike the `macos_container` lift (where moving the inline `mod tests` got production under cap), a test-only lift here would leave production at ~686 — **still 186 over cap**. So this mirrors the **`audit.rs` precedent**: a test-module lift **plus** a re-exported pure-builder seam. The file's module doc already names its two "pure, I/O-free, fully unit-testable" sections (`build_plist`, `validate_service_name`) — the natural seam.

**The change (pure-mechanical, no behaviour change).** Following the established Rust-2018 sibling pattern (`audit/`, `macos_container/`, `injection_guard/`, `tool_host/`):

- [`supervisor/src/launchd_agents/builders.rs`](../../../supervisor/src/launchd_agents/builders.rs) (476 LOC) — the two pure functions `build_plist` + private `xml_escape` + `validate_service_name`, their consts (`DEFAULT_EXIT_TIMEOUT_SEC`, `MAX_NAME_LEN`), and their tests (13 build_plist + 6 validator + 2 xml_escape) co-located in a `#[cfg(test)] mod tests`. **Re-exported from the parent** (`pub use builders::{build_plist, validate_service_name}`) so the public paths `launchd_agents::{build_plist, validate_service_name}` are byte-for-byte unchanged and the parent driver's 6 internal call sites (5 `validate_service_name` + 1 `build_plist`) resolve via the re-export. `xml_escape` stays module-private (it's an implementation detail of `build_plist`).
- [`supervisor/src/launchd_agents/tests.rs`](../../../supervisor/src/launchd_agents/tests.rs) (190 LOC) — the driver tests (install/uninstall/status with a `TestRoot` tempdir) + the launchctl-parsing helper tests (`parse_print_state` ×3, `is_no_such_service_error`, `user_domain_target`). `use super::*` reaches the parent's private helpers + the `LaunchAgents` driver. `minimal_spec` is duplicated across the two test modules (a 12-line fixture; the file already duplicates `TestRoot` from `systemd_user::tests`).
- [`supervisor/src/launchd_agents.rs`](../../../supervisor/src/launchd_agents.rs) (485 LOC, **under cap**) — keeps the `LaunchAgents` driver, `probe`, and the launchctl interaction helpers (`write_atomic`, `user_domain_target`, `current_uid`, `run_launchctl`, `parse_print_state`, `is_loaded_in_domain`, `is_no_such_service_error`).

**Cross-platform symmetry preserved.** The module doc says it "mirrors `systemd_user.rs`"; that symmetry is in the **public API** (`build_plist`/`build_unit_file`, `validate_service_name`, the driver struct, `probe`), not the internal file layout — the re-exports keep the public surface identical. `systemd_user.rs` stays a single 798-LOC file (production ~502, basically at cap); splitting it the same way is a future option if it grows, but isn't required for symmetry.

**Why no TDD / no RED.** Refactor with zero new behaviour; the 52 `hhagent-supervisor` unit tests ARE the regression pin (count unchanged before/after, just running from the new file locations).

**Verification (macOS M3 Max, rustc 1.96).** `cargo test -p hhagent-supervisor` **52 unit + 4 launchd smoke** green; `cargo test --workspace` **1157 / 0 / 3** (identical to the `79fcc27` baseline); `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0. **Doc-link check:** `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc -p hhagent-supervisor --no-deps --document-private-items` yields the **same 5 pre-existing** unresolved links as `main` (the `#[cfg(target_os="linux")]` `systemd_user` cross-module links, which don't compile on macOS) — **zero new**. One self-inflicted new link (a fully-qualified path to the `Supervisor::install` trait method) was caught against the `main` baseline and reworded to point at the `LaunchAgents` struct before commit. macOS-only file — Linux compile is `linux-check`-covered.

**File-size watch.** `launchd_agents.rs` **1093 → 485 LOC** (under the 500 cap — resolved); new `builders.rs` 476 (≈270 prod + ≈206 tests), `tests.rs` 190 (test file; the cap targets production files). Remaining over-cap production files for future slices: `db/memories.rs` (961, ≈863 prod), `db/graph.rs` (926, walk-impl split = Item 23b), `core/workers/gliner_relex.rs` (861), `core/src/scheduler/inner_loop.rs` (655, #81). `systemd_user.rs` (798) is the Linux counterpart — production ~502, at cap, not yet over.

---

## Recently completed (2026-05-30 — `scheduler/audit.rs` over-cap split, branch `refactor/audit-rs-split-extract-entities`, code commit `3bb0cc7`, merged to `main` via PR [#167](https://github.com/hherb/hhagent/pull/167) at `79fcc27`)

Operator picked the **"bigger over-cap file lift"** option from the refactor bucket. Start-of-session also reconciled the stale header (#4): PR [#166](https://github.com/hherb/hhagent/pull/166) (item 9 / #99) arrived described as OPEN; it is in fact **merged to `main` at `75e9039`**, #99 CLOSED — header / Currently-on / Last-commit / merged-branches / ROADMAP synced before starting.

**Why `audit.rs` (the file choice).** Measured the five over-cap production candidates by inline-test fraction: `audit.rs` 1106 (≈524 prod / ≈582 inline tests), `supervisor/launchd_agents.rs` 1093 (≈686 prod), `db/memories.rs` 961 (≈863 prod), `db/graph.rs` 926 (Item 23b walk-split, deferred), `core/workers/gliner_relex.rs` 861. `audit.rs` was both the **largest** and — being **pure functions, no I/O, no clock** (its own module doc) — the **lowest-risk** to reorganize. (The prior handover's "mostly logic" note on `audit.rs` was inaccurate; it was ≈53% inline tests.)

**The change (pure-mechanical split, no behaviour change).** `audit.rs` was interleaved: prod region A (L1–491, lifecycle/finalize/l1/entities-approval payload builders), the main `mod tests` (L493–1051), prod region B (L1053–1082, the `ACTION_EXTRACT_ENTITIES` const + `build_extract_entities_payload`, appended in the v2 entity-extraction arc), then `mod tests_extract_entities` (L1084–1106). Two natural seams, following the established Rust-2018 sibling pattern (`macos_container/tests.rs`, `injection_guard/tests.rs`, `tool_host/*`):

- [`core/src/scheduler/audit/extract_entities.rs`](../../../core/src/scheduler/audit/extract_entities.rs) (67 LOC) — region B (const + builder) + its 2 tests, now co-located in a `#[cfg(test)] mod tests`. **Re-exported from the parent** (`pub use extract_entities::{build_extract_entities_payload, ACTION_EXTRACT_ENTITIES}`) so the public paths `scheduler::audit::{…}` are byte-for-byte unchanged for the external callers (verified: `entity_extraction::gliner_relex` lines 354/367, `entity_extraction_e2e.rs` lines 479/485). The builder is fully `serde_json::`-qualified → the sibling needs no imports.
- [`core/src/scheduler/audit/tests.rs`](../../../core/src/scheduler/audit/tests.rs) (567 LOC) — the main `mod tests` body (L495–1050), lifted verbatim and de-indented one level (`sed 's/^    //'` — verified safe: every non-empty line started with ≥4 spaces; the `r"` grep hits were false positives like `"operator"`). `use super::*` resolves to the parent `audit` module (incl. the re-exported extract_entities items). The main tests have **zero** references to the extract_entities symbols (confirmed) — clean separation.

**Why no TDD / no RED.** Refactor with zero new behaviour; the 43 existing `scheduler::audit` module tests ARE the regression pin. Behaviour-preservation proven two ways: (1) `git diff core/src/scheduler/audit.rs` is a single hunk `@@ -490,617 +490,11` — production lines 1–489 byte-identical; (2) workspace count **unchanged at 1157**.

**Verification (macOS M3 Max, rustc 1.96).** `cargo build -p hhagent-core` clean; `cargo test -p hhagent-core --lib scheduler::audit` **43/43** from the new locations; `cargo test --workspace` **1157 / 0 / 3** (identical to the `75e9039` baseline); `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0 and `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0. Cross-platform file (no `#[cfg(target_os)]`) — Linux compile covered by `linux-check`. **PR [#167](https://github.com/hherb/hhagent/pull/167): all CI green** — `cargo check + clippy (linux)` 35s + CodeQL Analyze rust/python/actions all pass (the linux-check confirms the split compiles on Linux, which the dev Mac can't cross-test for `core`).

**File-size watch.** `audit.rs` **1106 → 500 LOC** (at cap — resolved); new `audit/extract_entities.rs` 67, `audit/tests.rs` 567 (test file; the cap targets production files). Remaining over-cap production files for future slices: `supervisor/launchd_agents.rs` (1093), `db/memories.rs` (961, ≈863 prod), `db/graph.rs` (926, walk-impl split = Item 23b), `core/workers/gliner_relex.rs` (861), `core/src/scheduler/inner_loop.rs` (655, #81).

---

## Recently completed (2026-05-30 — Next-TODO item 9 / Issue [#99](https://github.com/hherb/hhagent/issues/99): migrate `ask.rs` + `observation_replay.rs` to `common::with_runtime`, branch `fix/issue-99-cli-with-runtime`, **merged to `main` via PR [#166](https://github.com/hherb/hhagent/pull/166) at `75e9039`**)

Operator picked the tech-debt bucket → Issue #99 (over the file-size refactors, test-infra polish, and the design slices). Start-of-session also reconciled the stale header: PR [#165](https://github.com/hherb/hhagent/pull/165) (last session's `macos_container.rs` lift) arrived described as OPEN; it is in fact **merged to `main` at `48c0396`** (2026-05-30T02:36:20Z, all CI green) — header / Last-commit / Currently-on / merged-branches / ROADMAP synced before starting.

**Background.** PR #98 (Issue #97) introduced `common::with_runtime(prefix, fut) -> ExitCode` — it defers tokio runtime construction until the **known-action match arm**, so a dispatch-site typo never pays for worker threads it won't use — and migrated the four per-subcommand dispatchers (`tasks`, `entities`, `memory_l1`, `tools_allowlist`). `ask.rs` + `observation_replay.rs` were **deliberately left** on the raw `multi_thread_runtime(prefix) + rt.block_on(fut)` pair to keep that diff focused. Both already parse/validate **all** args before building the runtime, so the migration is purely stylistic.

**The change (pure consistency cleanup, no behaviour change).** Replaced each two-step `match multi_thread_runtime(prefix) {…}; rt.block_on(fut)` block with one `with_runtime(prefix, fut)` call:
- [`ask.rs`](../../../core/src/bin/hhagent-cli/ask.rs): import `with_runtime` instead of `multi_thread_runtime`; the runtime block at the tail of `run_ask` is now `with_runtime("ask", ask_async(lane, instruction, floor))`. The "multi-thread so `block_in_place` is available" rationale comment was preserved (pointed at `with_runtime`, which builds the same multi-thread runtime) + the Issue #97 deferral note.
- [`observation_replay.rs`](../../../core/src/bin/hhagent-cli/observation_replay.rs): same swap; `run_observation_replay`'s tail is now `with_runtime("observation replay", observation_replay_async(&dir, model_filter.as_deref()))`.

Now **all six** `hhagent-cli` dispatchers use the same idiom. `multi_thread_runtime` stays as the inner primitive (still called by `with_runtime` in `common.rs:92`, so no dead-code warning) — kept for any future caller that needs the `Runtime` handle directly, per the issue's out-of-scope note. **Shipped: commit `5545b3a`, merged to `main` via PR [#166](https://github.com/hherb/hhagent/pull/166) at `75e9039`** (all CI green — `cargo check + clippy (linux)` 34s, CodeQL Analyze rust/python/actions); #99 CLOSED.

**Why no TDD / no RED.** Zero new behaviour — the existing suite is the regression pin. (Precedent: every prior `with_runtime`/sibling-lift refactor in this log.)

**Verification (macOS M3 Max, rustc 1.96).** `cargo build --bin hhagent-cli` clean; `cargo clippy -p hhagent-core --all-targets --locked -- -D warnings` exit 0; `cargo test --workspace` **1157 / 0 / 3** — **identical** to the `48c0396` baseline. **Smoke (no PG needed — these paths exit before/around the runtime):** every `ask` / `observation` / `observation replay` arg-validation error path byte-identical (usage lines + exit 2 unchanged), and `observation replay --captures-dir <empty>` still builds the runtime and prints `(no captures found …)` exit 0.

**File-size watch.** `ask.rs` 326 → 323 LOC, `observation_replay.rs` 156 LOC (net −3 + −1; both well under the 500 cap). No new files; no production crate logic touched.

---

## Recently completed (2026-05-30 — `macos_container.rs` sibling test-module lift, branch `refactor/macos-container-test-module-lift`, **merged to `main` via PR [#165](https://github.com/hherb/hhagent/pull/165) at `48c0396`**)

Operator picked the `macos_container.rs` sibling test-module lift from the refactor bucket (over #134 tests-common, #99 `with_runtime`, and the GLiNER-e2e CI knob). At **983 LOC** it was the largest over-cap production file with a *clean* lift available — ~494 of those lines were the single inline `#[cfg(test)] mod tests` block, so the established Rust-2018 sibling-directory pattern (precedents: `injection_guard/tests.rs`, `tool_host/tests.rs`, `gliner_relex/tests.rs`, `secrets/vault/tests.rs`) applies directly. (`scheduler/audit.rs` at 1106 LOC and `db/graph.rs` at 926 are mostly *production* logic — real splits, deliberately not chosen here.)

**The change (pure-mechanical move, no behaviour change).** Lifted the inline test block out of [`sandbox/src/macos_container.rs`](../../../sandbox/src/macos_container.rs) into a new sibling [`sandbox/src/macos_container/tests.rs`](../../../sandbox/src/macos_container/tests.rs). The parent now ends with `#[cfg(test)]` + `mod tests;`; the test body moved verbatim, de-indented one level (`sed 's/^    //'` — verified safe: every non-empty body line started with ≥4 spaces, and the only `r"`/`\`-pattern matches were false positives like `container"`, not real raw-string or line-continuation literals), and gained a module-doc header explaining the `use super::*` resolution (which preserves access to the private `clamp_memory_to_minimum` / `build_container_argv` / `build_image_inspect_argv` helpers, the `ClampedMemory` struct, and the `CONTAINER_*` consts) plus a pointer to the `sandbox/tests/macos_container_smoke.rs` integration tests.

**Why no TDD / no RED.** Refactor with zero new behaviour; the 33 existing `macos_container` module tests ARE the regression pin. Behaviour-preservation is proven two ways: (1) `git diff sandbox/src/macos_container.rs` shows the production portion (lines 1–488) byte-identical — only `mod tests { … }` collapsed to `mod tests;`; (2) the workspace count is **unchanged at 1157** before and after.

**Verification (macOS M3 Max, rustc 1.96).** `macos_container` module **33/33** from the new location; `cargo test -p hhagent-sandbox --lib` 57/57; `cargo test --workspace` **1157 / 0 / 3** (identical to the `091e53d` baseline); `cargo clippy -p hhagent-sandbox --all-targets --locked -- -D warnings` exit 0. macOS-only file (`#[cfg(target_os = "macos")]`), so the lifted tests are macOS-only — exercised on this Mac; Linux compile is CI-covered by `linux-check` (`--all-targets` type-checks the `#[cfg]`-gated tree).

**File-size watch.** `macos_container.rs` **983 → 491 LOC** (now under the 500 cap — resolved). New `macos_container/tests.rs` is 503 LOC (a test file; the cap targets production files). No production file changed. Remaining over-cap production files for future slices: `scheduler/audit.rs` (1106, mostly logic), `supervisor/launchd_agents.rs` (1093), `db/memories.rs` (961, ~863 prod), `db/graph.rs` (926, walk-impl split = Item 23b), `core/workers/gliner_relex.rs` (861).

**Also in this session (start-of-session, same commit): #164 docs reconcile.** The HANDOVER header arrived describing PR [#164](https://github.com/hherb/hhagent/pull/164) (the prior session's #130/#163 launchd-serialization fix) as still OPEN/awaiting-merge; it was in fact **merged to `main` at `091e53d`** (2026-05-30T02:09:54Z) with **#130 + #163 CLOSED**. Synced the header (`Last updated` note, `Currently on`, `Last commit on main` → `091e53d`, merged-branches list, `Session-end verification` baseline → 1157) and added the PR #164 merge reference to the matching ROADMAP entry.

---

## Recently completed (this session, 2026-05-30 — Next-TODO **item 6 / [#130](https://github.com/hherb/hhagent/issues/130)**: launchd bring-up serialization + bundled [#163](https://github.com/hherb/hhagent/issues/163) socket-path fix, branch `fix/issue-130-launchd-bringup-serialization`)

Operator picked item 6 (#130, "parallel-launchd bring-up contention under `HHAGENT_PG_BIN_DIR`") and the reentrant-`serial_lock`-in-`bring_up` shape (issue Option 1) over a cross-process flock or diagnose-first. Spec: [`docs/superpowers/specs/2026-05-30-issue-130-launchd-bringup-serialization-design.md`](../../superpowers/specs/2026-05-30-issue-130-launchd-bringup-serialization-design.md).

**Diagnosis that shaped the fix.** `tests-common::serial::serial_lock()` is a **process-local** `static` lock — it can serialize the parallel `#[test]` threads *within* one binary (e.g. `secret_vault_e2e` 9, `postgres_e2e` 8, `injection_guard_e2e` 6 each fire that many simultaneous launchd `bootstrap`s) but never across binaries. `bring_up_pg_cluster` took **no** lock at all. `cargo test` runs integration binaries sequentially, so the within-binary N-way registration is the dominant, fixable contention source.

**The #130 fix (two coupled parts).**
- [`tests-common/src/serial.rs`](../../../tests-common/src/serial.rs): lock `Mutex<()>` → **`parking_lot::ReentrantMutex<()>`**. `std::sync::ReentrantLock` is still **unstable** on 1.96 (feature `reentrant_lock`) — `parking_lot` 0.12 is already in the build graph (MIT/Apache-2.0, AGPL-OK) and added as a direct dev-dep. Reentrancy is **mandatory**: `supervisor_e2e.rs:106` and `observation_capture.rs:469` take `serial_lock()` and *then* call `bring_up_pg_cluster` on the same thread — a non-reentrant `Mutex` would self-deadlock. Cross-thread exclusion is unchanged; no poison branch.
- [`tests-common/src/pg.rs`](../../../tests-common/src/pg.rs): `bring_up_pg_cluster_with_timeout` takes `serial_lock()` around the `install`→`start`→`Active`/socket window, `#[cfg(target_os = "macos")]`-gated (Linux binds no unit value → clippy-clean under `-D warnings`, no import). `initdb` stays outside the lock (no launchd interaction → kept parallel).

**TDD (RED watched).** `serial_lock_is_reentrant_on_same_thread` — a spawned thread acquires twice; assert it completes within 5 s via `recv_timeout`. **RED on the old `Mutex`** (deadlock → timeout → fail), GREEN on `ReentrantMutex`. Plus `serial_lock_excludes_across_threads` (4 threads, max-concurrent-holder counter == 1) as the cross-thread-exclusion regression pin.

**Bundled [#163](https://github.com/hherb/hhagent/issues/163) (separate bug found during live validation; operator chose to bundle).** Validating live against Postgres.app v18 showed `injection_guard_e2e` still failing — but **not from #130**: it reproduces with `--test-threads=1`. Root cause: its fixture built `ig-{label}-{suffix}-data` where `unique_temp_root` *also* appends `-<pid>-<nanos>`, so the socket path `…/.s.PGSQL.5432` = **100 + len(label)** bytes, overflowing macOS's 104-byte `sun_path` for labels ≥ 6 chars; postgres silently couldn't bind the socket → 30 s timeout. (Exactly why HANDOVER said it "was never verified live.") Proven by the label↔pass/fail split: `sha`/`err` (len 3 → 103 B) passed, `benign`/`privacy`/`audit-row`/`placeholder` (≥6) timed out. Fix: shortened labels to `ig-{label}-d`/`-l`, **and** added a pure `check_socket_path_fits` guard to `bring_up_pg_cluster` (per-OS `SUN_PATH_MAX` 104/108) that fails fast with a clear message instead of a silent timeout (TDD: stub → RED → implement). `#163` filed + closed by this PR.

**Verification (macOS M3 Max, rustc 1.96).** `cargo test --workspace` **1157 / 0 / 3** (+4 over the 1153 baseline at `a83be4a`: +2 macOS-only `serial` tests, +2 cross-platform socket-path tests). `cargo clippy --workspace --all-targets --locked -- -D warnings` exit 0. **Live against Postgres.app v18** (`HHAGENT_PG_BIN_DIR='/Applications/Postgres 2.app/Contents/Versions/18/bin/'`): `injection_guard_e2e` **6/6**, `secret_vault_e2e` **11/11**, `postgres_e2e` **57/57**, **zero** bring-up timeouts — the #130 `pg active: timeout 30s` signature is gone and the multi-test PG suites now pass live (a strict improvement; these were never live-verifiable before). The pre-existing `embedding_recall_e2e` flake (#104 pid+nanos tempdir collision, "identical on `main`") was observed once during a full live run and is unaffected by this change.

**File-size watch.** `serial.rs` 34 → ~105 LOC (under cap); `pg.rs` 317 → ~360 LOC (under cap). No production (non-test) crate touched.

---

## Recently completed (2026-05-30 — Next-TODO **item 12** reconcile + graph-lane seed-thread regression test, branch `test/router-agent-graph-seed-threading`, **merged to `main` via PR [#162](https://github.com/hherb/hhagent/pull/162) at `a83be4a`**)

Operator picked item 12 ("Production caller wiring for the graph lane") from the Next-TODO bucket. **Exploring the code surfaced that item 12 was already done** — the wiring shipped in the 2026-05-19 entity-extraction-v2 / "Slice F" arc (PR #91), *after* the Option-P/item-12 "parked" note was written, and the note was never reconciled. Verified directly in source:

- [`agent.rs:122`](../../../core/src/scheduler/agent.rs#L122) — `RouterAgent::formulate_plan` calls `entity_extractor.extract(&ctx.instruction)` every iteration (degrade-and-warn on failure).
- [`agent.rs:140-141`](../../../core/src/scheduler/agent.rs#L140-L141) — threads `&seeds.ids` into `recall_builder.build_with_seeds(...)`, which flips `RecallParams::new` → `RecallParams::with_seeds` (graph lane ON) in [`pg_builder.rs`](../../../core/src/recall_assembly/pg_builder.rs).
- [`main.rs:174-260`](../../../core/src/main.rs#L174-L260) — production wires the **real** `GlinerRelexExtractor` into `RouterAgent::new`; `NoOpEntityExtractor` only when the worker is disabled.

Operator chose (over "reconcile-only / re-pick" and "quarantine-gate policy work"): **reconcile the stale note + close the one genuine residual** — a missing RouterAgent-layer regression test.

**The residual (what this session shipped).** [`router_agent_mock_e2e.rs`](../../../core/tests/router_agent_mock_e2e.rs) exercised `formulate_plan` *only* with `NoOpEntityExtractor` (seeds `[]`) + `StaticRecallBuilder::empty()` (which **discards** its `seeds` arg). So **nothing pinned the seed thread at the RouterAgent layer** — a refactor could silently drop `&seeds.ids` from the recall call or from `FormulationMeta`, and every RouterAgent test would stay green. New test `formulate_plan_threads_extracted_seeds_into_recall_and_meta` (+1, ~110 LOC incl. a `CapturingRecallBuilder` double that records the `(query, seeds)` it receives + a `StaticEntityExtractor::with_ids([101,202,303])`). Asserts (1) the extractor's ids reach `build_with_seeds` verbatim with the task instruction as query, and (2) the same ids populate `meta.graph_seed_entity_ids` / `graph_seed_count` / `graph_seed_source == GlinerRelex`.

**Regression coverage, not RED-first TDD** (precedent: PR #157's "not forced TDD" note) — the wiring already ships, so the test is GREEN on current code. Its value was proven by **two mutations**: (A) `&seeds.ids` → `&[]` in `agent.rs:141` turns assertion (1) RED (`left: []`, `right: [101,202,303]`); (B) `graph_seed_entity_ids: Vec::new()` turns assertion (2) RED. Both reverted; `git diff core/src/scheduler/agent.rs` is empty — **zero production change**.

**Reconciled the stale docs:** former Next-TODO item 12 struck-through as already-done; the "Option P" design note rewritten (wiring done as of Slice F; the *remaining* parked work is the quarantine review gate / issue [#40](https://github.com/hherb/hhagent/issues/40), not the caller wiring).

**Verification (macOS M3 Max):** `router_agent_mock_e2e` 4/4; `cargo test --workspace` **1153 passed / 0 failed / 3 ignored** (skip-as-pass posture without `HHAGENT_PG_BIN_DIR`; +1 over the 1152 baseline at `12b080c`). New test file only — no production file changed, so no file-size or clippy delta.

---

## Recently completed (2026-05-30 — Next-TODO **item 8b / [#153](https://github.com/hherb/hhagent/issues/153)**: clean clippy gate, branch `fix/issue-153-clippy-clean-gate`, commits `d5db4ba` + `ca827d7`, **merged to `main` via PR [#161](https://github.com/hherb/hhagent/pull/161) at `12b080c`**, #153 CLOSED)

Operator picked item 8b from the Next-TODO bucket (over #130 launchd flake, #81 inner_loop split, item 12 graph-lane wiring). The repo's `linux-check` CI clippy step ran *without* `-D warnings`, so the long-standing pile of clippy warnings silently accumulated and no clean enforcement gate existed. #153 frames it as "the 10 hhagent-core lib warnings," but the actual CI command is `cargo clippy --workspace --all-targets`, so flipping it to `-D warnings` required the **whole workspace** to be warning-clean (protocol, db, and every test target too).

**The change (two commits).** Commit `d5db4ba` = the macOS-visible cleanup + the CI flip; commit `ca827d7` = the Linux-only lints that only surfaced once the gate was live (the first PR push failed CI on them).

**Mechanical lints** (machine-applied via `cargo clippy --fix`, all behaviour-preserving, diff reviewed): useless `vec!` → array ([`sandbox_health.rs`](../../../core/src/sandbox_health.rs)); `.err().expect()` → `expect_err` ([`plan_parser.rs`](../../../core/src/scheduler/plan_parser.rs)); needless borrows (`&[…]`, `&request()`, `&conn_spec`); `assert_eq!(x, true/false)` → `assert!(x)`/`assert!(!x)`; `io::Error::new(ErrorKind::Other, _)` → `io::Error::other(_)` ([`protocol/client.rs`](../../../protocol/src/client.rs)); inlined literal format args; `match {Some=>…,None=>return None}` → `?`; `map_or` → `is_some_and`; `trim` before `split_whitespace`; manual char-array pattern.

**Judgment calls** (kept as deliberate decisions, not papered over):
- **`await_holding_lock`** ([`worker_lifecycle/manager.rs`](../../../core/src/worker_lifecycle/manager.rs) `_test_slot_*`): the inline `drop(map)` before the `.await` was already correct, but `clippy::await_holding_lock` is not flow-sensitive to an explicit drop. Extracted a sync `lookup_slot(&self, tool_name) -> Option<Arc<ToolSlot>>` helper so the std-guard never lives in the async fn — silences the lint *legitimately* and dedupes the two `_test_slot_*` inspectors. (NB: the tail expr must stay `let state = slot.state.lock().await; state.field` — the chained `.lock().await.field` form trips a borrow-lifetime error.)
- **`too_many_arguments`** (`runner::lane_loop`/`drain_lane`, `audit::build_extract_entities_payload`, `observation_capture::capture_one_fixture`): justified `#[allow]` with a comment — flat payload builders / dependency-threading loops where a bundle struct would only relocate the list to the call site.
- **`assertions_on_constants`** (`recall` ALL-lanes pin, `memories` `DEFAULT_RECALL_K` pin): justified `#[allow]` — deliberate const-drift pins that must stay runtime tests.
- **`doc_lazy_continuation`**: the subtle one. Several doc comments had wrapped lines beginning with `+` (a method-chain separator at line start) which CommonMark parses as a **phantom bullet list**, making the following lines look like under-indented children. Reworded so no continuation line starts with `+`/`/`; added blank lines before concluding paragraphs that were misparsed as list-item continuations (`watchdog.rs`, `capture.rs`, `probe.rs`, `entity_extraction_e2e.rs`, `cli_relations_e2e.rs`).

**Linux-only lints (commit `ca827d7`).** macOS clippy never compiles `#[cfg(target_os="linux")]` code, so these only appeared once the `-D warnings` gate ran on the Linux CI runner: `linux_bwrap::push_bind` `&PathBuf` → `&Path` (`clippy::ptr_arg`; moved the now-test-only `PathBuf` import into the test module); `linux_cgroup` test `starts_with("/")` → `starts_with('/')` (`single_char_pattern`); `lockdown_probe` `b"…\0".as_ptr() as *const _` → `c"…".as_ptr()` ×3 (`manual_c_str_literals`). Found by running `cargo clippy --target x86_64-unknown-linux-gnu` locally on the crates that cross-build (sandbox, prelude); `core` can't (the `ring` C dep), so its Linux path was CI-verified.

**The CI flip.** [`.github/workflows/linux-check.yml`](../../../.github/workflows/linux-check.yml) clippy step now ends `-- -D warnings`; the header comment was rewritten (it previously said "warnings do NOT fail the job"). Any new lint now hard-fails CI; an unavoidable one must be suppressed at the site with a reviewable `#[allow]`.

**Why no TDD.** Pure-lint change, zero behavior delta — the existing 1152-test suite is the regression pin (unchanged before/after). Verification: macOS `cargo clippy --workspace --all-targets -- -D warnings` exit 0 (clippy 1.96); `cargo test --workspace` **1152 / 0 / 3**; Linux `-D warnings` green via the PR #161 `linux-check` job (35s).

**File-size watch.** No production file grew materially (net cleanup). `manager.rs` shed ~the inline duplication via `lookup_slot`. No new over-cap files.

---

## Recently completed (2026-05-29 — Next-TODO **item 5**: `tool_host.rs` sibling-lift, branch `refactor/item-5-tool-host-sibling-lift`, code commit `379f77a`, **merged to `main` via PR [#160](https://github.com/hherb/hhagent/pull/160) at `fd7dd7a`**)

Operator picked item 5 from the Next-TODO bucket over #153 (clippy gate), item 12 (graph-lane wiring), and #130 (launchd flake). It was the largest remaining over-cap production file: `tool_host.rs` had grown to **911 LOC** (411 over the 500-LOC cap) across the Item 30 (injection guard) + Item 31 (secret refs) + PR #157 (audit-sink) arcs.

**The change (pure-mechanical lift, no behavior change).** Lifted three self-contained pieces out of [`core/src/tool_host.rs`](../../../core/src/tool_host.rs) into child modules, following the established Rust-2018 sibling pattern (`tool_host/audit_sink.rs` was already a precedent in the same directory):

- [`core/src/tool_host/watchdog.rs`](../../../core/src/tool_host/watchdog.rs) (249 LOC) — the wall-clock watchdog subsystem: `WatchdogGuard`, `spawn_watchdog`, `watchdog_loop`, `is_valid_target_pid`, `send_sigkill` + their 5 co-located tests. `WatchdogGuard`/`spawn_watchdog` are **`pub(super)`** (visible to the parent so `SupervisedWorker` can hold the guard and `spawn_worker` can start it); the kill machinery stays module-private. The 2026-05-08 host-blackout incident write-up and its two-layer guard (`is_valid_target_pid` + injected killer) moved verbatim.
- [`core/src/tool_host/lockdown_env.rs`](../../../core/src/tool_host/lockdown_env.rs) (164 LOC) — `ENV_LANDLOCK_RW` / `ENV_SECCOMP_PROFILE` / `ENV_CPU_MS` consts + `derive_lockdown_env` + its 6 co-located tests. **Re-exported via `pub use lockdown_env::{…}`** so the public path `hhagent_core::tool_host::{ENV_*, derive_lockdown_env}` is byte-for-byte unchanged (verified against the external references in `workspace.rs`, `sandbox/`, `prelude/`).
- [`core/src/tool_host/tests.rs`](../../../core/src/tool_host/tests.rs) (47 LOC) — the 2 `WorkerCommand` seal tests (`use super::*` reaches the module-private `WorkerCommand::new`).

**What deliberately stayed in the parent (the trust boundary).** Per the Next-TODO item-5 instruction, the dispatch chokepoint (`dispatch` / `dispatch_with_sink`), the `WorkerCommand` seal, and `SupervisedWorker` (including the module-private `call`) all stay in `tool_host.rs`. These are the irreducible trust-boundary core — the `dispatch` doc comment alone is ~85 lines — which is why the parent lands at **519 LOC**, still 19 over the cap. Lifting them would require either trimming load-bearing docs or widening `SupervisedWorker`'s private fields to a sibling, both rejected.

**Seal-doc correctness (the "more care than 4b" part).** Adding child modules makes them *descendants* of `tool_host`, which can reach the module-private `WorkerCommand::new`. The seal doc previously said the reviewable opt-out was "editing `tool_host.rs` itself"; that wording predated even `audit_sink` and was already slightly imprecise. Updated both the `WorkerCommand` and `WorkerCommand::new` docs to: the opt-out is "edit the `tool_host` module (`tool_host.rs` or a file under `tool_host/`)", and to note none of the four child modules (`audit_sink`, `lockdown_env`, `watchdog`, `tests`) actually constructs a `WorkerCommand`. The out-of-crate `compile_fail` doctest and the in-crate sibling-module build remain the regression pins — both verified green.

**Why no TDD / no RED.** Pure-mechanical refactor with zero new behavior; the 13 existing tests (6 derive + 5 watchdog + 2 worker_command) ARE the regression pin. Proof of behavior-preservation: workspace count unchanged at **1152** and the `compile_fail` seal doctest still passes.

**Verification (macOS M3 Max).** `cargo test --workspace` **1152 / 0 / 3** (identical pre/post); `WorkerCommand` `compile_fail` doctest passes; `cargo clippy -p hhagent-core --all-targets` no new lints; `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc … --document-private-items` no new broken-link errors.

**File-size watch.** `tool_host.rs` **911 → 519 LOC** (the −392 is the watchdog + lockdown_env + tests blocks). Residual 19 over cap is the trust-boundary core and is the feasible floor given the item-5 constraint. New files all under cap: `watchdog.rs` 249, `lockdown_env.rs` 164, `tests.rs` 47 (+ the pre-existing `audit_sink.rs` 59). No production file in this arc remains pathologically over cap.

---

## Recently completed (2026-05-29 — Next-TODO **item 4b**: `injection_guard.rs` test-module sibling-lift, **merged to `main` via PR [#159](https://github.com/hherb/hhagent/pull/159) at `1106145`** — code commit `c0bb178`)

Operator picked 4b from the Next-TODO bucket over the `tool_host.rs` lift (item 5), graph-lane production wiring (item 12), and the #130 launchd flake (item 6). It was the most pressing file-size remedy: `injection_guard.rs` had grown to **667 LOC** (167 over the 500-LOC cap) across the #143 depth-guard and #156 sibling-continue arcs.

**The change (pure-mechanical move, no behavior change).** Lifted the inline `#[cfg(test)] mod tests` block out of [`core/src/cassandra/injection_guard.rs`](../../../core/src/cassandra/injection_guard.rs) into a new sibling [`core/src/cassandra/injection_guard/tests.rs`](../../../core/src/cassandra/injection_guard/tests.rs), following the **established Rust-2018 sibling-directory pattern** already used by `secrets/vault/tests.rs` and `workers/gliner_relex/tests.rs` (the 2026-05-18 / 05-27 lifts). The parent now ends with `#[cfg(test)]` + `mod tests;`; the test body moved verbatim (de-indented one level) and gained a module-doc header explaining the `use super::*` resolution — which preserves access to the private `normalize` / `walk` helpers and the `CATALOGUE` const — plus a pointer to the `injection_guard_e2e.rs` integration tests.

**Why no TDD / no RED.** This is a refactor with zero new behavior; the 29 existing module tests ARE the regression pin. The proof of behavior-preservation is that the module still reports **29/29** and the full workspace count is **unchanged at 1152** before and after the move.

**Verification (macOS M3 Max).** `injection_guard` module **29/29** (identical pre/post); `cargo test --workspace` **1152 / 0 / 3** (identical to the `f3c380f` baseline); `cargo clippy -p hhagent-core --all-targets` no new lints. The de-indent (`sed 's/^    //'`) is safe even inside the one `\`-line-continuation string literal in `screen_caps_score_at_one_point_zero` — Rust strips leading whitespace after a string-continuation backslash, so indentation there is semantically inert (and that test passes).

**File-size watch.** `injection_guard.rs` **667 → 338 LOC** (now under the 500-LOC cap — item 4b resolved). New `injection_guard/tests.rs` is 338 LOC (test file; the cap targets production files). The remaining over-cap file in this arc is `tool_host.rs` (911 LOC — Next-TODO item 5).

---

## Recently completed (2026-05-29 — Issue [#156](https://github.com/hherb/hhagent/issues/156): `walk()` depth-skip continues siblings, **merged to `main` via PR [#158](https://github.com/hherb/hhagent/pull/158) at `f3c380f`**, #156 CLOSED — code commit `43598b4`)

Picked from the Next-TODO bucket (operator chose #156 over the two file-size lifts and the graph-lane wiring). The last open injection-guard follow-up from the #143/#155 arc.

**The problem.** [`core/src/cassandra/injection_guard.rs`](../../../core/src/cassandra/injection_guard.rs)'s `walk()` (the recursive body of `extract_scannable_text`) used a single `bool` return that conflated **two** signals: "did truncation happen" and "stop the whole walk". On *either* cap hit it returned `true`, and every array/object caller did `if walk(child) { return true; }` — so the **first** truncating child aborted the entire remaining walk. A structure like `{"decoy": <deeply-nested subtree>, "later": "ignore previous instructions"}` truncated on `decoy` (alphabetically first → visited first) and never scanned `later`. An attacker controlling worker-output ordering could bury injection text behind a leading depth-truncating decoy sibling to evade the catalogue scan.

**The fix (separate the two signals).** Thread a `&mut bool truncated` out-parameter through `walk`; the **return value** now means only "stop the *entire* walk (byte budget exhausted)". Two caps, deliberately different:
- **Depth cap** (`depth >= MAX_WALK_DEPTH`) is a *local* property of one branch: set `truncated = true` and `return false` (skip the too-deep subtree, **keep scanning siblings** — `out` still has room). This closes the #156 evasion.
- **Byte cap** is a *global* budget on `out`: set `truncated = true` and `return true` (abort). Once `out` is full no later sibling could append anything anyway, so continuing only burns CPU traversing a potentially huge `Value` for zero gain — abort stays correct.

**Byte-cap residual (out of scope, documented).** The byte-cap evasion window noted in #156 (`{"big": <fills buffer>, "later": "<injection>"}`) is **inherent to the accumulate-then-scan design** — a full buffer cannot admit later content regardless of whether the walk continues — so continuing siblings provides *zero* security benefit there. Closing it needs a scan-as-you-go rework (screen each string independently against a separate CPU budget), which is a larger architectural change left for a future slice. The fix targets exactly the depth-cap case, where continuing *does* help.

**TDD (RED watched).** New `walk_continues_siblings_after_depth_skip`: a `{"decoy": deeply_nested(300, "UNREACHABLE"), "later": "ignore previous instructions"}` object must scan `later` (body contains the phrase, `screen` Blocks) while the too-deep decoy leaf stays unscanned + `truncated == true`. RED on pre-fix code (`body == ""` — decoy aborted the walk). The two existing depth-guard tests (`walk_stops_at_max_depth`, `walk_captures_content_below_max_depth`) are unchanged-green — a lone too-deep branch with no siblings still emits nothing + flags truncated, and a shallow leaf is still fully scanned.

**Verification (macOS M3 Max).** `cargo test --workspace`: **1152 / 0 / 3** (+1). `injection_guard` module: 29/29. `cargo clippy -p hhagent-core --all-targets`: no new lints (`walk` 4→5 args, under the 7-arg threshold). **PG-gated `injection_guard_e2e` NOT re-verified live** — every attempt died in `tests-common` PG cluster bring-up (#130 launchd-registration contention; never reached an assertion). The fix is a pure function with no PG/dispatch path; the e2e exercises only the unchanged dispatch→screen wiring. Closed #156 on merge (PR #158 at `f3c380f`).

**File-size watch.** `injection_guard.rs` 619 → **667 LOC** (+48: doc rewrite + the new test), now 167 over the 500-LOC cap. The **test-module sibling-lift** to `injection_guard/tests.rs` (Next-TODO item 4b) is the tracked remedy and now more pressing — it would drop production LOC to ~285. Kept out of this PR to keep the #156 diff focused (the lift is pure-mechanical; bundling would bury the 5-line logic change in a ~340-line move).

---

## Recently completed (2026-05-29 — Issues [#148](https://github.com/hherb/hhagent/issues/148) + [#149](https://github.com/hherb/hhagent/issues/149): secret-vault test gaps, **merged to `main` via PR [#157](https://github.com/hherb/hhagent/pull/157) at `53e68ed`**, both issues CLOSED)

Picked the Next-TODO item 3 (secret/injection-guard follow-ups). Both gaps were surfaced by `/code-review` on PR [#146](https://github.com/hherb/hhagent/pull/146) (Item 31) and deferred for want of a fault-injection seam. Operator approved a refined plan over the issues' literal suggestions: lower call-site churn for #148, a PG-free unit test for #149.

**#149 — `Vault::materialize`'s `Entry::Occupied` (ref-collision) arm was untested.** A ref collision (vanishingly rare under OsRng over the 2^32 namespace) must return `VaultError::RefCollision`, never silently rebind the existing `SecretRef` to a different plaintext (its next `redeem` would then yield the wrong secret). **Fix:** extracted the locked map-insert critical section (step 4 of `materialize`) into a private `Vault::insert_fresh(&self, secret_ref, entry) -> Result<SecretRef, VaultError>` — behaviour-preserving; `materialize` now calls it. New PG-free unit test `insert_fresh_rejects_ref_collision_without_overwriting` in [`core/src/secrets/vault/tests.rs`](../../../core/src/secrets/vault/tests.rs) calls `insert_fresh` twice at the same ref and asserts the second returns `RefCollision` and the original plaintext is intact. **Why not the issue's `_test_materialize_at_ref` seam:** that needs a live PG pool (full materialize path) → integration test + a test-only public backdoor. `insert_fresh` is a legitimate internal factoring (it's production-used, so no `#[cfg(test)]`), enables a true unit test, and isolates exactly the branch #149 targets.

**#148 — the two best-effort secret-ref audit rows swallow insert failures.** `secret.redeemed` (one per substituted ref, after `worker.call`) and `secret.redemption_failed` (on substitution miss, before `worker.call`) both log via `tracing::error!` and continue, so a transient `audit_log` outage can't change the dispatch outcome. That swallow path is unreachable with a real pool. **Fix:** new `AuditSink` trait + `PgAuditSink` prod impl in [`core/src/tool_host/audit_sink.rs`](../../../core/src/tool_host/audit_sink.rs) (`#[async_trait]`, matches the `RecallBuilder` pattern). `dispatch` split into a thin `dispatch(pool, …)` **wrapper** over `dispatch_with_sink(&dyn AuditSink, …)`; `dispatch`'s 4 `audit::insert(pool, …)` calls now route through the sink (`pool` was used *only* for audit). **Wrapper preserves the public signature → all 18 existing call sites (1 prod + 17 test) are untouched** — only the two new tests pass a sink. A `MockAuditSink` (inline in the e2e file) records every `(actor, action)` attempt and fails one chosen action. Two integration tests in [`core/tests/secret_vault_e2e.rs`](../../../core/tests/secret_vault_e2e.rs): `dispatch_swallows_redeemed_audit_insert_failure` (returns Ok + tool row still written) and `dispatch_swallows_redemption_failed_audit_insert_failure` (returns `Err(SecretRedemptionFailed)` + no tool row; PG-free, since the empty vault fails substitution before any pool use).

**Why a `pub` seam on the chokepoint (and why it's safe).** The redeemed-path test needs a *real spawned worker*, so it lives in the separate `core/tests/` crate → `dispatch_with_sink` + `AuditSink` + `PgAuditSink` must be `pub`. A misused sink could drop audit rows, so production code keeps calling `dispatch` (hard-wired to `PgAuditSink`); a doc-comment on `dispatch_with_sink` states it exists for fault-injection tests only. The seam touches only *where audit rows are sent* — the spawn/jail trust boundary stays sealed behind `WorkerCommand` / `SupervisedWorker::call`. **This `pub`-seam cost is identical under the issues' verbatim approach** (which also threads a sink through); the refinement only lowers blast radius.

**Not forced TDD on #148.** The swallow logic already exists and is correct, so there was no genuine RED — these are regression-coverage tests (they lock the best-effort posture so the planned `tool_host.rs` sibling-lift, item 5, can't silently turn a swallow into a hard-fail). Both are meaningful: test 10 panics at `.expect("…Ok…")` if the redeemed insert stops being swallowed; test 11's `match err` hits the panic arm if the audit error masks `SecretRedemptionFailed`.

**Verification (macOS M3 Max).** `cargo test --workspace`: **1151 / 0 / 3** (+3 over 1148). Live against Postgres.app v18: `secret_vault_e2e` **11/11**. `cargo clippy -p hhagent-core --all-targets`: no new lints. Closes #148 + #149 on merge.

**File-size watch.** `vault.rs` 273 → 284 LOC (under cap); new `tool_host/audit_sink.rs` 59 LOC (under cap); `tool_host.rs` 893 → 911 LOC (+18 from the wrapper + doc; still 411 over the 500 cap — the Items 30+31 sibling-lift, Next-TODO item 5, is the tracked follow-up and would now also lift the audit-sink wiring); `secret_vault_e2e.rs` 611 → 810 LOC (integration test file).

---

## Recently completed (2026-05-29 — Issue [#143](https://github.com/hherb/hhagent/issues/143): `injection_guard::walk()` recursion-depth guard, **merged to `main` via PR [#155](https://github.com/hherb/hhagent/pull/155) at `6e82252`**, #143 CLOSED)

Picked from the Next-TODO "injection-guard limits" bucket (item 4). The operator chose to **defer [#142](https://github.com/hherb/hhagent/issues/142)** (chat-template false-positives) per its author's own recommendation — that decision needs real data from a `web-fetch`/`mcp` worker that doesn't exist yet — and ship **#143 only** this session.

**The problem.** [`core/src/cassandra/injection_guard.rs`](../../../core/src/cassandra/injection_guard.rs)'s `walk()` (the recursive body of `extract_scannable_text`) descended once per array/object nesting level with no cap. A pathologically deep worker JSON-RPC result (`{"a":{"a":…}}` 100k deep) would overflow the dispatcher thread's stack before hitting `SCAN_BYTE_CAP`. **Unreachable today** — serde_json's parser rejects nesting past 128 and the protocol frame-size limit bounds wire depth — so this is the defense-in-depth backstop if either upstream limit is ever removed (exactly as the issue framed it).

**The fix (~5 production lines + const).** New `pub const MAX_WALK_DEPTH: usize = 256` (2× serde_json's 128 default, so any worker-parsed `Value` passes well under it; bounds the recursion far below the stack-overflow threshold). Added a `depth: usize` param to `walk` — `extract_scannable_text` seeds it at `0`, each descent into an array/object passes `depth + 1`, and at function entry `if depth >= MAX_WALK_DEPTH { return true; }` bails and signals truncation. **Reuses the existing `truncated` bool channel** rather than threading a new signal: hitting the cap requires adversarial input, for which an audit row marked truncated (and no crash) is the right forensic posture. `extract_scannable_text`'s public `(String, bool)` signature is unchanged.

**Why depth-cap over the issue's alternative "iterative walk with explicit stack".** Both were offered in #143. Depth-cap is the smaller, lower-risk change (it preserves the readable recursive shape and all existing truncation semantics); the iterative rewrite would have reopened the approved design for no extra safety at our scale.

**TDD (RED watched for both behavioural tests).** `walk_stops_at_max_depth` — leaf nested 300 deep is left unscanned + `truncated == true` (RED on unguarded code: walked to 300, `truncated == false`). `walk_captures_content_below_max_depth` — leaf 100 deep still fully scanned (guard doesn't touch normal nesting). `max_walk_depth_is_256` — const drift pin, matching the existing `block_threshold_*` / `scan_byte_cap_*` pins. **No literal 100k-deep overflow test:** any `Value` deep enough to overflow `walk` *also* overflows Rust's recursive `Drop` glue at test teardown (`serde_json::Value` has no iterative `Drop`), and such a `Value` can never reach `walk` in production — so the test would test drop glue on an unconstructable-in-prod input. Overflow-safety is instead a corollary of the ≤256-frame bound that `walk_stops_at_max_depth` pins; documented inline. The crash *was* observed once (SIGABRT) against unguarded code to confirm the vulnerability is real, then the test was removed.

**Verification (macOS M3 Max).** `cargo test --workspace`: **1148 / 0 / 3** (+3 over 1145). `injection_guard` module: 28/28. `cargo clippy -p hhagent-core --all-targets`: no new lints (10 pre-existing lib warnings unchanged). Closed #143 on merge (PR #155 at `6e82252`).

**File-size watch.** `injection_guard.rs` 527 → 615 LOC in the fix, then → **619 LOC** when the merge carried the #156 sibling-skip note (commit `40e2911`); now 119 over the 500 cap. The bulk is the module doc + catalogue + inline tests. A **test-module sibling-lift** (`injection_guard/tests.rs`, the established repo pattern) would drop production LOC to ~285 — flagged as a fresh Next-TODO follow-up (item 4b), kept out of this PR to keep the diff focused.

**Also in this session (separate commit `97add3a`):** reconciled the stale HANDOVER/ROADMAP after PR [#152](https://github.com/hherb/hhagent/pull/152) merged to `main` at `560d845` (header still said "not yet merged"); issues #144 + #150 are closed.

---

## Recently completed (this session, 2026-05-29 — Bugs [#150](https://github.com/hherb/hhagent/issues/150) + [#144](https://github.com/hherb/hhagent/issues/144): clippy gate + Linux build, branch `fix/issue-144-150-linux-build-clippy-gate`, **merged to `main` via PR [#152](https://github.com/hherb/hhagent/pull/152) at `560d845`**)

Both were the "real errors, not enhancements" entries at the top of the previous Next-TODO (rule 5 — fix breakage before features). Picked together because both are small and both unblock a broken gate.

**#150 — `cargo clippy --workspace --all-targets` hard-errored.** [`sandbox/tests/fixtures/mem_burner.rs:46`](../../../sandbox/tests/fixtures/mem_burner.rs) did `Vec::with_capacity(bytes)` then `unsafe { buf.set_len(bytes) }`; `clippy::uninit_vec` is now deny-by-default, so the whole `--all-targets` clippy run failed before reaching any real lint. Fix: `let mut buf: Vec<u8> = vec![0u8; bytes];` and drop the `unsafe`. **Correctness note worth keeping:** the OOM-kill smoke test relies on the page-touch loop *below* the allocation to force residency. `vec![0u8; n]` can hand back demand-zero pages (calloc-style) that don't count against `memory.max` until written — but the loop dirties one byte per page regardless, so residency is still forced and the test's behaviour is unchanged. Rewrote the SAFETY comment accordingly. **Verify:** `cargo clippy --workspace --all-targets` → 0 errors (warnings remain, non-fatal).

**#144 — `hhagent-core` did not compile on Linux** (the primary host is the DGX = Linux, so this was the higher-impact bug). [`core/src/workers/gliner_relex.rs`](../../../core/src/workers/gliner_relex.rs)'s `container_mode_entry` referenced the macOS-only `SandboxBackendKind::Container` variant unconditionally, and `gliner_relex_entry` dispatched to it on every target. Fix mirrors the **established cfg pattern already in [`core/src/sandbox_health.rs`](../../../core/src/sandbox_health.rs)** (`#[cfg(target_os = "macos")]` / `#[cfg(not(...))]` paired):

- `#[cfg(target_os = "macos")]` on `container_mode_entry` + the two container-only consts (`CONTAINER_IMAGE_DEFAULT`, `CONTAINER_BINARY`).
- In `gliner_relex_entry`, the `if env.use_container_backend { return container_mode_entry(env); }` branch is `#[cfg(target_os = "macos")]`; `host_mode_entry` is the only reachable path on Linux.
- In `resolve_env`, `use_container_backend` is computed from the env var on macOS but is a **compile-time `false`** on non-macOS (the env var isn't even read). An operator who sets `HHAGENT_GLINER_RELEX_USE_CONTAINER=1` on Linux silently gets host-mode + bwrap. (bwrap already provides containment on Linux; Apple `container` is the macOS-only memory-enforcement opt-in.)
- Gated the **6 container-specific unit tests** in [`core/src/workers/gliner_relex/tests.rs`](../../../core/src/workers/gliner_relex/tests.rs) to macOS (`entry_container_mode_*` ×2 + `resolve_env_*container*` ×4). Container tests in the *other* crates (`worker_lifecycle/manager/tests.rs`, `gliner_relex_e2e.rs`, `lifecycle_container_routing_e2e.rs`) were already macOS-gated — `gliner_relex.rs` was the sole ungated site.

**Why a CI workflow for the regression guard.** #144 can't be verified on the dev Mac: cross `cargo check -p hhagent-core --target aarch64-unknown-linux-gnu` dies in a C dependency's build (`cc-rs` can't find `aarch64-linux-gnu-gcc`), not in our code. There was no CI in the repo. Added [`.github/workflows/linux-check.yml`](../../../.github/workflows/linux-check.yml): `cargo check --workspace --all-targets` + `cargo clippy --workspace --all-targets` on `ubuntu-latest`. `--all-targets` is deliberate — it type-checks `#[cfg(test)]` code too, so a future un-gated container test reference can't sneak back in. Scope is compile-only (no `cargo test` — integration tests need live PG + the bwrap AppArmor profile; full runs stay DGX/operator-driven). x86_64 is sufficient: the break was `target_os`-conditional, not arch-conditional.

**Verification (macOS M3 Max).** `cargo test --workspace`: **1145 / 0 / 3** (unchanged — the gated tests still run on macOS; #150 only touched a fixture binary). `cargo clippy --workspace --all-targets`: 0 errors. **Linux: CONFIRMED GREEN** — the `linux-check` CI job on PR [#152](https://github.com/hherb/hhagent/pull/152) passed `cargo check` + `cargo clippy` (`--workspace --all-targets`) on `ubuntu-latest` (1m17s), proving `hhagent-core` compiles on Linux again and no other Linux-only breakage exists.

---

## Recently completed (this session, 2026-05-29 — Issue [#147](https://github.com/hherb/hhagent/issues/147): redact secret plaintext in the tool audit row, branch `fix/issue-147-redact-tool-req-plaintext`, **merged to `main` via PR [#151](https://github.com/hherb/hhagent/pull/151) at `54e8885`**)

**The problem.** Opaque secret references slice 1 (Item 31, PR #146) deliberately scoped the secret-plaintext privacy invariant to `actor='policy'` rows only — mirroring the injection-guard slice 1 precedent (commit `45627fd`). The cost: `tool_host::dispatch` snapshotted `req_for_audit` AFTER `substitute_refs_in_params` ran, so the `tool:<name>` row's `payload.req` carried the **redeemed plaintext**. Any operator (or anyone) with read access to `audit_log` could recover every materialized secret by reading the corresponding tool row — a much weaker operational posture than a naive reader of the design spec would assume. Surfaced by `/code-review` on PR #146 and filed as Issue #147.

**The fix (one moved clone).** Snapshot `req_for_audit = params.clone()` BEFORE the substitution block in [`core/src/tool_host.rs`](../../../core/src/tool_host.rs). At that point `params` still carries the opaque `secret://<8-hex>` refs exactly as the planner issued them, so the redeemed plaintext never reaches `audit_log`. The worker still receives the real plaintext via the mutated `params` passed to `WorkerCommand::new` — only the audit snapshot is taken pre-substitution.

**Why snapshot-before over the issue's "substitute back" proposal.** Issue #147 suggested using the `redemption_events` vector to substitute the plaintext back to the ref string in `req_for_audit`. Snapshot-before is strictly better: (1) it's a one-line move, not a reverse-substitution walker; (2) it can't accidentally redact a coincidental plaintext match elsewhere in `params`; (3) it records the request *as the planner issued it* — the most faithful audit shape. The `redemption_events` vector is untouched (still drives the `secret.redeemed` rows).

**Scoped invariant — `payload.result` is out of scope.** A worker that is legitimately handed a secret may echo it in its own output (the `printf %s` test worker does exactly this). That lands in `payload.result`, which is the worker's response, not the request. The invariant is and remains: no *redeemed plaintext* in the request snapshot (`req`) or in any `policy` row. The tool row's `result` is the worker's own data.

**TDD.** Widened test 7 (`policy_rows_contain_no_substring_of_redeemed_plaintext`) in [`core/tests/secret_vault_e2e.rs`](../../../core/tests/secret_vault_e2e.rs): after the policy-row loop it now fetches the `tool:shell-exec` row and asserts `payload.req` (serialized) contains no plaintext marker. Added focused test 9 `tool_row_req_shows_opaque_ref_not_plaintext` — negative (no plaintext) **and** positive (the opaque `secret://<ref>` IS present in `req`, and stdout still carries the plaintext so the worker did receive it). Both confirmed RED on pre-fix code, GREEN after.

**Verification (macOS M3 Max).** `cargo test --workspace` without PG: **1145 / 0 / 3** (1144 → 1145, +1 from new test 9). `secret_vault_e2e` live against Postgres.app v18: **9 / 9 passed**. `cargo clippy -p hhagent-core --all-targets`: 10 warnings, all pre-existing.

**Drive-by filed, not fixed (Issue [#150](https://github.com/hherb/hhagent/issues/150)).** `cargo clippy --workspace --all-targets` now hard-**errors** on `sandbox/tests/fixtures/mem_burner.rs:46` (`set_len()` after `with_capacity` — `clippy::uninit_vec` promoted to deny-by-default). Untouched by this change (different crate); `cargo build`/`cargo test` unaffected. One-line fix (`vec![0u8; bytes]`) kept out of this PR to keep the diff focused.

**File-size watch.** `core/src/tool_host.rs`: net **+0 production LOC**. Still 867 LOC, 367 over the 500-LOC cap — the Items 30+31 sibling-lift bundle remains the tracked follow-up.

---

## Recently completed (this session, 2026-05-29 — ★ Opaque secret references slice 1, branch `feat/opaque-secret-refs-slice-1`, **merged to `main` via PR [#146](https://github.com/hherb/hhagent/pull/146) at `bc36e4c`** + post-review polish `5885e26` + `9348e9c` — closes HANDOVER Item 31)

The planner context is the widest LLM-visible surface in the system. Today a tool calling path that includes an API key, a bearer token, or any named secret passes that value verbatim through `tool_host::dispatch` into the worker's `params`, through the JSON-RPC call, and — if the planner logs it — into the audit log and transcript replay. This slice closes that leak surface for secrets the operator explicitly stages: once a secret is materialized into the in-process `Vault`, the planner sees only an opaque 8-hex ref string (`secret://7c9f2e`), never the plaintext.

The chokepoint pattern mirrors the injection guard (HANDOVER Item 30): substitution runs BEFORE `worker.call` in `tool_host::dispatch`, fail-closed on any miss. The two guards sit at opposite ends of the same dispatch call: injection guard screens worker OUTPUT on the way back; secret substitution resolves opaque refs in worker INPUT on the way in.

**What shipped (4 substantive commits TDD-ordered):**

1. **`b58cd55` — Task 1: module skeleton + types + const pins.** New `core/src/secrets/mod.rs` with `SecretRef` opaque newtype, `RedeemResult` enum (`Hit(String)` / `Expired` / `NotFound`), `SECRET_REF_PREFIX = "secret://"`, `SECRET_REF_HEX_LEN = 8`, `DEFAULT_TTL = 3600s`. +7 unit tests.
2. **`3bc66a3` — Task 2: Vault impl.** New `core/src/secrets/vault.rs` with `Vault` (TTL'd `std::sync::RwLock<HashMap>`, lazy GC, `OsKeyringProvider` trait). `materialize` mints a ref + writes the `policy / secret.materialized` row hard-fail (no materialized-but-unaudited ref can exist). `redeem` returns `RedeemResult` with lazy GC. +9 unit tests.
3. **`7b1e3c5` — Task 3: substitution walker + FakeVault.** New `core/src/secrets/substitute.rs` with `substitute_refs_in_params` — recursive `serde_json::Value` walker; exact-match `SECRET_REF_PREFIX` check; fail-closed on Expired/NotFound. `FakeVault` test seam. +16 unit tests.
4. **`19eebd6` — Task 4: dispatch wiring + scheduler plumbing + main.rs bootstrap + 8 integration tests + classifier test fix.** Substitution wired into `tool_host::dispatch` before `worker.call`: on Ok, one `policy / secret.redeemed` row per ref (best-effort); on Err, `policy / secret.redemption_failed` row fail-closed, worker not called, no tool row. `ToolHostError` widened to `#[non_exhaustive]`; classifier maps the new variant to `POLICY_DENIED`. `main.rs` reads `HHAGENT_BOOTSTRAP_SECRETS` and materializes each via `OsKeyringProvider` at startup.

**Verification (macOS M3 Max):** with PG live **636 passed / 2 pre-existing flakes** (baseline 603 on `main` at `c505b36`); without PG **1137 / 0 / 3** (+41 over 1096 on `main` at `62905ae`). No new clippy warnings.

**Architectural notes worth keeping:**

- **Substitution BEFORE injection guard.** The two dispatch guards are ordered: secret substitution on INPUT, injection guard on OUTPUT. Neither knows about the other; they compose at the chokepoint.
- **Asymmetric audit posture.** Materialize-time audit is hard-fail (no materialized-but-unaudited ref can exist). Redeem-time audit is best-effort (plaintext already in process memory; failing the audit there would discard a successfully-substituted value with no security benefit).
- **Tool row `payload.req`** is allowed to carry the substituted plaintext per the original slice — **corrected by Issue #147 this session** (see entry above; now snapshots the opaque ref pre-substitution). The `policy / secret.redeemed` row carries only `{ref_hash, tool, actor}`, never the value.
- **Exact-match substitution only.** A JSON string that IS `"secret://…"` is replaced entirely; embedded (`"Bearer secret://…"`) is NOT. Deferred to Slice 2.

**File-size watch:** new `secrets/mod.rs` ~130 / `vault.rs` ~260 / `substitute.rs` ~290 LOC (all under cap). `tool_host.rs` 767 → 867 LOC (+100; 367 over cap — bundle the sibling-lift with Item 30).

**Open follow-ups for future slices:** no CLI surface yet (Slice 2 = `hhagent-cli secrets materialize` + CLI↔daemon IPC); per-task vault scoping; embedded substitution; revocation (Slice 3); binary secrets; `tool_host.rs` sibling-lift (bundle Items 30 + 31). Filed: [#148](https://github.com/hherb/hhagent/issues/148) (audit-insert failure paths untested), [#149](https://github.com/hherb/hhagent/issues/149) (`Vault::materialize` ref-collision branch untested — needs RNG injection).

**Post-review polish (`5885e26` + `9348e9c`):** manual `Debug` for `SecretRef` (prints `ref_hash=<sha256>` only, never the ref string; pinned by `secret_ref_debug_never_leaks_ref_string`); `materialize` collision-safety via `Entry::Vacant`-or-`VaultError::RefCollision`; `gliner-relex::Client::extract` shares a `OnceLock<Vault>` static instead of allocating per call; walker partial-walk behaviour promoted from "unspecified" to contract.

---

## Recently completed (this session, 2026-05-28 — ★ Worker-output prompt-injection guard slice 1, **merged to `main` via PR [#141](https://github.com/hherb/hhagent/pull/141) at `62905ae`** — closes HANDOVER Item 30)

Sandboxing contains code; it does not contain text. Today every successful worker result flows verbatim back into the planner's conversation history via `core::tool_host::dispatch`. A poisoned tool output (a hostile file the worker dutifully `cat`'d, a malicious web page once `web-fetch` lands, a coerced MCP response once that lands) can rewrite the planner's instructions on the next turn. The open-loop risk exists **today** via shell-exec reading any FS-allowed file — this is not a hypothetical attack surface.

**Slice scope (deliberately narrow — Slice 1):**

- Two-tier verdict (`Allow` / `Block`) — defer the 0.45–0.70 Review tier per YAGNI; `InjectionDecision` is `#[non_exhaustive]` so it slots in later without breaking callers.
- 22-entry English-substring catalogue across 4 attack classes (instruction_override, role_hijack, secret_exfiltration, unsafe_tool_coercion). Substring matching post-`normalize` (lowercase + zero-width strip). No regex, no leetspeak fold, no multilingual coverage.
- Per-rule weights summed (cap 1.0); `BLOCK_THRESHOLD = 0.70`. Catalogue invariant pinned: every class has at least one entry with weight ≥ `BLOCK_THRESHOLD`.

**What shipped (4 substantive commits TDD-ordered):** `536d23a` Task 1 module skeleton + const pins; `11a0a8e` Task 2 `extract_scannable_text` (recursive `serde_json::Value` walk, UTF-8-aware truncation at `SCAN_BYTE_CAP = 64 KiB`); `eae2a6f` Task 3 `normalize` + 22-entry catalogue + `screen`; `4e32988` Task 4 wire into `tool_host::dispatch` + 6 integration tests. On Block, the worker result is replaced with a placeholder JSON and a SECOND audit row (`actor='policy'`, `action='injection.blocked'`) carries SHA-256 + byte length + truncation flag + score + class codes; the raw scanned body is **never** persisted (pinned by `policy_audit_row_contains_no_substring_of_blocked_body`). Errors are NOT screened.

**Verification (macOS M3 Max):** `cargo test --workspace` **1096 / 0 / 3** (+31 over 1065, re-verified post-merge on `main` at `62905ae`). The 6 integration tests skip-as-pass without PG. No new clippy warnings.

**Architectural notes worth keeping:**

- **The screen runs at the chokepoint, not at the call site** (Option M's sealed `WorkerCommand`); one wiring point, no bypass.
- **The Err path is not screened** — errors are not text-channel content; the scheduler short-circuits on Err.
- **Block returns `Ok(placeholder)`, not `Err`** — the planner sees a tool result it can react to, not a failure that burns the retry budget.
- **Two audit rows on Block** — the `tool:<name>` row carries the placeholder; the `policy / injection.blocked` row carries forensic data; correlate by timestamp/tool/SHA-256.
- **`#[non_exhaustive]` on `InjectionDecision`** is the forward-compat seam for the Review tier.

**File-size watch:** new `cassandra/injection_guard.rs` ~430 LOC (under cap). `tool_host.rs` 708 → 763 LOC (was already over cap; now 263 over). Filed: [#142](https://github.com/hherb/hhagent/issues/142) (chat-template tokens may false-positive on technical docs once web-fetch/MCP land), [#143](https://github.com/hherb/hhagent/issues/143) (`extract_scannable_text` walk has no explicit depth guard; reachable only if upstream serde_json/protocol limits are removed).

**Deferred to future slices:** Review tier; heuristic/combinatorial scoring; multilingual; per-tool policy; `hhagent-cli policy review` operator surface; `tool_host.rs` sibling-lift.

---

## Earlier history (summary)

One bullet per session. Full reasoning lives in the archive snapshots: sessions 2026-05-10 → 2026-05-29 in [`archive/handover_20260529_pre-prune.md`](archive/handover_20260529_pre-prune.md); sessions 2026-05-06 → 2026-05-09 in [`archive/handover_20260510_pre-prune.md`](archive/handover_20260510_pre-prune.md).

- **2026-05-28 — `idle_timeout/release.rs` sibling-lift + Issue #89 `/tmp` tmpfs pin:** pure-mechanical lift of `release_idle_timeout_worker` + `abort/replace_idle_teardown_handle` into a new `worker_lifecycle/idle_timeout/release.rs` (`idle_timeout.rs` 647 → 490 LOC, under cap), merged via PR [#138](https://github.com/hherb/hhagent/pull/138) at `5fc5fee`. Plus a new Linux `linux_smoke` test pinning that `--tmpfs /tmp` is per-spawn ephemeral + writable + listing-real (PR [#139](https://github.com/hherb/hhagent/pull/139) at `504094e`), and docs-only PR [#140](https://github.com/hherb/hhagent/pull/140) at `6fd82bf` importing 7 openhuman design patterns (flagged Items 30 + 31).
- **2026-05-27 — worker_lifecycle hardening + test-infra slices:** debounce pending-acquires queue-depth warn ([#136](https://github.com/hherb/hhagent/issues/136), PR #137 `fed0f21`); hardening trio [#84](https://github.com/hherb/hhagent/issues/84)+[#85](https://github.com/hherb/hhagent/issues/85)+[#86](https://github.com/hherb/hhagent/issues/86) (`#[non_exhaustive]` Lifecycle variant + one-teardown-per-slot abort/respawn + `pending_acquires` AtomicU32 + warn threshold, PR #135 `7f98ee4`); `bring_up_pg_cluster_with_timeout` sibling ([#131](https://github.com/hherb/hhagent/issues/131), PR #133 `8655319`); test-module lift quadruple (PR #132 `162f71f`, −940 LOC off 4 parents); `bring_up_pg_cluster` polling-cap lift 15s→30s + 5-site consolidation −1062 LOC (PR #129 `4e94e42`, filed [#130](https://github.com/hherb/hhagent/issues/130)).
- **2026-05-26 — graph + entity-upsert slices:** `walk_*_edges` diamond-dedupe via `DISTINCT ON` + combined `walk_edges_around` UNION ALL ([#114](https://github.com/hherb/hhagent/issues/114)+[#115](https://github.com/hherb/hhagent/issues/115), PR #128 `bb32cab`); `HHAGENT_PG_BIN_DIR` test-fixture env override (Item 29, PR #126 `7adc582`, filed [#127](https://github.com/hherb/hhagent/issues/127)); entity-upsert Layer B full-batch unnest + per-row attribution fallback ([#95](https://github.com/hherb/hhagent/issues/95), PR #125 `dac0dcd` — new `entity_extraction/batch_upsert.rs`).
- **2026-05-25 — Slice 2.5 + lifts:** Slice 2.5 follow-up triple [#120](https://github.com/hherb/hhagent/issues/120)+[#121](https://github.com/hherb/hhagent/issues/121)+[#122](https://github.com/hherb/hhagent/issues/122) (PR #124 `e93997e`); `gliner_relex.rs` test-module lift 1547 → 811 LOC (PR #123 `920e0c9`); GLiNER-Relex Slice 2.5 Containerfile + macOS image build, `--init` always-on closes [#107](https://github.com/hherb/hhagent/issues/107) (PR #118 `a9e3385`, container e2e empirically PASSED in 12.58s).
- **2026-05-23 — CLI splits + relations show:** Item 23(a) test-module sibling lifts (PR #117 `919882d`); Item 22 kinds-CLI shared lift + over-cap CLI file splits, closes [#111](https://github.com/hherb/hhagent/issues/111)+[#112](https://github.com/hherb/hhagent/issues/112) (PR #116 `1abb061`); `hhagent-cli relations show <id> [--depth N] [--format]` graph-edge introspection (PR #113 `9a46e18`).
- **2026-05-22 — kinds CLIs + container Slice 2:** `entities kinds {add,remove,list}` (PR #110 `a65bb4a`); `relations kinds {add,remove,list}` + `connect_admin_pool` (PR #109 `f234d0c`); `MacosContainerBackend` Slice 2 — `SandboxBackendKind` enum + `SandboxBackends` resolver + per-worker `ToolEntry.sandbox_backend` (PR #108 `1b86f84`). **(NB: the unconditional `Container` reference added here is what breaks the Linux build — [#144](https://github.com/hherb/hhagent/issues/144).)**
- **2026-05-21 — macOS container backend + GLiNER macOS:** `MacosContainer` Slice 1 (PR #106 `cc0b0de`, filed [#107](https://github.com/hherb/hhagent/issues/107)); Issue #55 Apple `container` discovery spike — verdict COMMIT (PR #105 `56456da`); GLiNER-Relex macOS device decision tree (PR #103 `9220f40`); audit_tail tempdir-collision flake fix [#101](https://github.com/hherb/hhagent/issues/101); relation-label vocabulary migration 0017 + `RelationKindsCache` (PR #100 `5bcd060`); defer hhagent-cli tokio runtime ([#97](https://github.com/hherb/hhagent/issues/97), PR #98 `dbee0ac`).
- **2026-05-20 — quarantine CLI + cli split:** `hhagent-cli.rs` 1933 LOC → per-subcommand-module directory ([#66](https://github.com/hherb/hhagent/issues/66), PR #96 `2704468`); entity-upsert Layer A round-trip reduction ([#90](https://github.com/hherb/hhagent/issues/90), PR #94 `3ab94f6`); operator quarantine-review CLI `entities {list,show,approve,reject,merge}` (PR #93 `028e541`).
- **2026-05-19 — entity extraction v2:** memory-write-time `memory_entities` auto-linker (PR #92 `d58ecc9`); Entity Extraction v2 design + plan + full GLiNER-Relex implementation + migration 0016 (PR #91 `f12b460`).
- **2026-05-18 — worker lifecycle + GLiNER worker:** GLiNER-Relex Slice 2 Rust manifest + e2e (PR #88 `715a882`); tech-debt batch (PR #87 `665901d`); GLiNER-Relex Slice 1 Python worker (`dfb1126`); worker-lifecycle slices 1+2 (`SingleUse`/`IdleTimeout`/`Composite` managers, +30 tests); `inner_loop.rs` 1214 → 655 LOC split ([#81](https://github.com/hherb/hhagent/issues/81)); L1 promotion writer (PR #82 `eb6b8a8`); post-merge spec landings.
- **2026-05-17 — recall-lane wiring** (PR #79 `7553404`): recall wired into the production scheduler path.
- **2026-05-16 — memory plumbing:** prompt-assembler L0+L1 `build_system_prompt` (PR #74); L0 seed-data loader (PR #77); runner rejects producer-supplied `agent_raised` provenance ([#71](https://github.com/hherb/hhagent/issues/71)); automatic classification-floor inference (PR #70 `4ddfe3b`).
- **2026-05-15 — first CASSANDRA rules + harness:** L1 memory-layer storage primitive migrations 0013+0014 (PR #68 `b1c63e2`); first real `DeterministicPolicy` rule; first real `ConstitutionalGuard` prompt screen (PR #67 `67d29a0`); rule-iteration harness `observation::replay` + CLI (PR #65 `9c01e30`); audit-payload bump on `agent/plan.formulate` (PR #61 `67f2dac`).
- **2026-05-14 — observation + refusal state:** observation-phase first capture run + `parse_plan_lenient` (PR #60); constitutional refusal `tasks.state='refused'` + migration 0012 ([#23](https://github.com/hherb/hhagent/issues/23), PR #59 `f1fea54`); batch issue cleanup #5/#6/#17/#20/#40/#47/#50 (PR #54 `25c312c`); per-tool argv allowlist DB + `tools allowlist` CLI; producer-cancelled-pending `task.finalize` row; Option G CPU-quota/rlimit enforcement.
- **2026-05-13 — audit rows + seal:** crashed-task `task.finalize` row; observation-phase fixture captures; `WorkerCommand` seal tightened ([#16](https://github.com/hherb/hhagent/issues/16)); CLI `task.submitted` + `task.cancelled` producer rows; graph lane in `memory::recall` (PR #41 `76fe940`).
- **2026-05-12 — Phase 1 foundations:** `hhagent-tests-common` shared dev-dep crate ([#15](https://github.com/hherb/hhagent/issues/15), PR #38 `97f2743`); `task.crashed` crash-recovery sweep row; spec §7 task-lifecycle audit rows; scheduler short-circuit audit rows; split `memory.rs` into submodules ([#30](https://github.com/hherb/hhagent/issues/30)); Option O embedding router + first `actor='llm:router'` row.
- **2026-05-11 — scheduler online:** Task 4.4 `cli_ask_e2e` full-chain pin; Task 3.2.bis wire `ToolHostStepDispatcher` to `tool_host::dispatch`; mock-HTTP coverage for `formulate_plan`; `tasks_lifecycle_e2e` deadlock fix; scheduler/CASSANDRA Phases 2–5 landed.
- **2026-05-10 — chokepoint + recall skeleton:** Options M (sealed `WorkerCommand` + dispatch chokepoint) + N (`memory::recall` pgvector+tsvector RRF skeleton); Option J LLM-router stub; secrets-at-rest AES-256-GCM + OS keyring + migration 0004; Option I dispatcher chokepoint + audit_log NOTIFY trigger + JSONL mirror + `audit tail`; Option L non-superuser runtime role + GRANT split.
- **2026-05-09 — cgroup v2 caps:** `linux_cgroup.rs` wraps every bwrap call in `systemd-run --scope` with `MemoryMax`/`MemorySwapMax=0`/`CPUQuota`/`TasksMax`; `cgroup_probe()` fail-closed; mem_burner OOM-kill test. Plus C2.2 schema + sqlx migrations + Graph trait + probe + e2e.
- **2026-05-08 — supervisors + Phase-0 polish:** Linux `systemd_user` + macOS `LaunchAgents` backends (pure `build_unit_file`/`build_plist` + validate + atomic write); per-task scratch `Workspace` with RAII; wall-clock watchdog + the `kill(-1)` fanout fix.
- **2026-05-06/07 — Phase 0 sandbox core:** `workers/prelude` Landlock + seccomp lock_down; macOS Seatbelt backend (`build_profile` → `.sb`); initial AGPL workspace + Linux bwrap backend + protocol crate + shell-exec worker + tool_host + first e2e. Full detail in the 20260510 archive.

---

## Key design decisions locked in

- **Vendor-neutral, AGPL-compatible deps only.** AGPL project; all third-party deps must be AGPL-compatible (Apache-2.0, MIT, BSD, MPL, LGPL, (A)GPL all fine).
- **Cross-platform first-class.** Linux (DGX Spark primary) + macOS (Apple Silicon and Intel). No Linux-only code without a macOS counterpart of equivalent guarantee.
- **Rust core, Python workers.** Rust for core (no eval/dynamic surface); Python only inside sandboxed tool workers. shell-exec is Rust because it's a thin execve wrapper — Python's first appearance will be `python-exec` in Phase 4 (or possibly `web-fetch` earlier).
- **Hybrid LLM with policy routing.** Local-first via OpenAI-compatible HTTP (vLLM/SGLang on Linux, llama.cpp/Ollama on macOS). Frontier (Claude/OpenAI) only via the Phase-5 policy gate, through the egress proxy.
- **Single-host deployment via OS-native user-level supervisor.** `systemd --user` (Linux) / `launchd` LaunchAgents (macOS). No k3s.
- **Fixed core tools, sandbox-bound agent-authored Python.** Critical workers are human-curated and shipped with the binary. Agent-authored code only runs inside `python-exec`'s strict sandbox; named/persisted skills get an optional human-approve gate (Phase 4).
- **JSON-RPC 2.0 over stdio.** MCP-stdio compatible. Lets us swap in a richer MCP client later without changing the trust boundary.

---

## Next TODO (pick one)

Phase 0 is complete; Phase 1 (memory recall + scheduler loop + end-to-end step dispatch) is on `main` and pinned by `cli_ask_e2e`. The recent slices closed the two openhuman-imported security items (Item 30 injection guard, Item 31 opaque secret references). The list below is an **operator-picks bucket** — sized roughly one session each, with file paths and the verification step.

**Done this session (branch `feat/l3-skill-crystallisation`, off `main` at `98a5be0`, tip `0505986`, PR [#173](https://github.com/hherb/hhagent/pull/173) OPEN):** the **L3 skill crystallisation writer** — the engineering-pickup "L3 skill crystallisation — spec" item. Brainstormed → spec → plan → 14-task subagent-driven development. Writer-only, non-executable, `trust:"untrusted"`; `Plan.l3_skill` agent-emitted template; `drain_lane` hook with canonical-SHA-256 dedup; `dispatch_count >= 1` grounding gate; `hhagent-cli memory l3 {list,remove}`; DB e2e 7/7 + CLI e2e 4/4; workspace **1157 → 1177 (+20)**. `l3_crystallise.rs` is 670 LOC (~458 prod / ~212 tests) — over cap; clean test-lift available. **NOTE:** the spec's "total_steps_executed" grounding accumulator was implemented by reusing the existing `dispatch_count` counter (behaviourally identical; no separate accumulator). See Recently-completed. **Done last session (branch `refactor/inner-loop-test-module-lift`, off `main` at `30aa52e`, code commit `cb561fd`, merged to `main` via PR [#172](https://github.com/hherb/hhagent/pull/172) at `98a5be0`):** the **`core/src/scheduler/inner_loop.rs` over-cap test-lift — closes issue [#81](https://github.com/hherb/hhagent/issues/81)** (its second slice). A full census of inline-test fractions (the prior "no clean test-lift-only candidate remains" claim was scoped only to the active arc) found `inner_loop.rs` (655 LOC, ~437 prod / ~217 inline tests) was a CLEAN test-lift that *also* closes a tracked issue. Lifted the remaining inline `#[cfg(test)] mod tests` block (state-machine / floor-raise pins) verbatim (de-indented one level, re-indent byte-identical) to a re-exported-by-declaration sibling `inner_loop/tests.rs` (230 LOC); the parent keeps `#[cfg(test)] mod tests;` (the audit-payload pins stay with their builder in the existing `inner_loop_audit.rs` sibling). Production **655 → 438 LOC** (under cap — **#81 CLOSED**). Zero behaviour change (production region L1–437 byte-identical; workspace **1157/0/3** unchanged; `scheduler::inner_loop`/`inner_loop_audit` unit 22/22 from new location; clippy `-p hhagent-core -D warnings` exit 0; doc-link set matches `main` exactly — 21 pre-existing, diff vs a fresh `main` worktree → empty). PG-free unit tests; no live PG run needed. See Recently-completed. **Done last session (branch `refactor/replay-rs-test-module-lift`, off `main` at `4e401cc`, code commit `adb29cd`, merged to `main` via PR [#171](https://github.com/hherb/hhagent/pull/171) at `30aa52e`):** item 9b — the **`core/src/observation/replay.rs` over-cap test-lift**. At 804 LOC (~420 prod / ~384 inline tests) it was the next CLEAN test-lift candidate: lifting the inline `#[cfg(test)] mod tests` block alone lands the parent under cap (no pure-helper seam needed). Moved the block verbatim (de-indented one level, re-indent byte-identical) to a sibling `observation/replay/tests.rs` (390 LOC); the parent keeps `#[cfg(test)] mod tests;`. Production **804 → 422 LOC** (under cap). Zero behaviour change (production region L1–420 byte-identical; workspace **1157/0/3** unchanged; replay unit 20/20 from new location; clippy `-p hhagent-core -D warnings` exit 0; doc-link set matches `main` — 25 pre-existing, zero replay/observation). PG-free unit tests; no live PG run needed. See Recently-completed. **Done (branch `refactor/tool-dispatch-split-result-mapping`, off `main` at `e1be537`, code commit `4c1241e`, merged to `main` via PR [#170](https://github.com/hherb/hhagent/pull/170) at `4e401cc`):** the **`core/src/scheduler/tool_dispatch.rs` over-cap split** — at 828 LOC the biggest remaining over-cap file with a *clean* lift available. Test-lift + a re-exported pure-helper seam (mirrors `audit.rs`/`launchd_agents.rs`): the two pure result-mapping fns `rpc_code_name`+`map_dispatch_result` → re-exported sibling `tool_dispatch/result_mapping.rs` (88 LOC, byte-identical move), the inline test module → `tool_dispatch/tests.rs` (339 LOC, verbatim de-indented). Parent **828 → 442 LOC** (under cap); keeps the registry/dispatcher + the dispatcher-coupled audit-payload helpers. Zero behaviour change (workspace **1157/0/3** unchanged; clippy `-D warnings` clean; doc-link set matches `main` exactly — 25 pre-existing, zero new; live `scheduler_step_dispatch_e2e` 1/1 on Postgres.app v18). See Recently-completed. **Done (branch `refactor/memories-rs-split-write-search`, code commit `39e5cae` + style polish `7b41008`, merged to `main` via PR [#169](https://github.com/hherb/hhagent/pull/169) at `e1be537`):** the **`db/memories.rs` over-cap split** — at 961 LOC (~863 prod) the largest remaining over-cap production file. Its ~98 LOC of inline tests exercise parent-resident items, so a test-lift can't get under cap — a **real production-logic split** along the read/write seam into re-exported siblings `memories/write.rs` (259 LOC: inserts/layer-writers/delete/seed_meta/link) and `memories/search.rs` (400 LOC: semantic/lexical/graph search + fetch_by_ids + load_layer/load_active_l0); the parent (360 LOC) keeps the shared vocabulary (`MemoryLayer`, `Memory`, consts, `check_embedding_dim`, `limit_as_i64`, `vector_literal`) + their tests + the re-exports, so every `db::memories::<name>` path holds byte-for-byte. Production **961 → 360 LOC** (under cap — resolved); zero behaviour change (moved bodies byte-identical vs HEAD; workspace **1157/0/3** unchanged; clippy `--workspace -D warnings` clean; broken-doc-link set matches `main` — 2 new `EMBEDDING_DIM` links repointed at `super::`). Live (Postgres.app v18): `postgres_e2e` 57/57, `memory_recall_e2e` 13/13, `memory_l1_promote_e2e` 9/9, `memory_l0_seed_e2e` 2/2, `embedding_recall_e2e` 4/4. See Recently-completed. **Done (branch `refactor/launchd-agents-split`, code commit `d195d63`, merged to `main` via PR [#168](https://github.com/hherb/hhagent/pull/168) at `5bf010b`):** the **`launchd_agents.rs` over-cap lift** — at 1093 LOC the largest remaining over-cap production file. A test-only lift would leave production ~686 (still over cap), so (mirroring the `audit.rs` precedent) split into siblings `launchd_agents/builders.rs` (pure `build_plist` + `xml_escape` + `validate_service_name` + consts + their tests, re-exported so `launchd_agents::{build_plist, validate_service_name}` paths hold) and `launchd_agents/tests.rs` (driver + launchctl-helper tests). Production **1093 → 485 LOC** (under cap — resolved); zero behaviour change (workspace **1157/0/3** unchanged; supervisor unit 52/52; clippy `--workspace -D warnings` clean; doc-link set matches `main`). See Recently-completed. **Done (branch `refactor/audit-rs-split-extract-entities`, code commit `3bb0cc7`, merged to `main` via PR [#167](https://github.com/hherb/hhagent/pull/167) at `79fcc27`):** the **bigger over-cap file lift** — split `scheduler/audit.rs` (1106 LOC, the largest over-cap file; pure functions → lowest-risk) into siblings `audit/extract_entities.rs` (v2 entity-extraction const + builder + 2 tests, re-exported so public paths hold) and `audit/tests.rs` (main `mod tests` body). Production **1106 → 500 LOC** (at cap); zero behaviour change (region A L1–489 byte-identical; workspace **1157/0/3** unchanged; clippy `--workspace -D warnings` clean). See Recently-completed. **Done (branch `fix/issue-99-cli-with-runtime`, merged to `main` via PR [#166](https://github.com/hherb/hhagent/pull/166) at `75e9039`):** Next-TODO item 9 / Issue [#99](https://github.com/hherb/hhagent/issues/99) — migrated `ask.rs` + `observation_replay.rs` to `common::with_runtime`, so all six `hhagent-cli` dispatchers share one idiom; pure consistency cleanup, zero behaviour change (workspace **1157/0/3** unchanged; clippy `-D warnings` clean); #99 CLOSED. See Recently-completed. **Done (branch `refactor/macos-container-test-module-lift`, merged to `main` via PR [#165](https://github.com/hherb/hhagent/pull/165) at `48c0396`):** the `macos_container.rs` sibling test-module lift — moved the ~494-line inline `#[cfg(test)] mod tests` block into a new sibling `sandbox/src/macos_container/tests.rs`; production **983 → 491 LOC** (under cap). Pure-mechanical, zero behaviour change (byte-identical production diff; workspace **1157/0/3** unchanged; clippy `-D warnings` clean). Also reconciled the start-of-session #164 docs staleness (header said PR #164 OPEN; it's merged at `091e53d`, #130+#163 CLOSED) in the same commit. See Recently-completed. **Done last session (merged to `main` via PR [#164](https://github.com/hherb/hhagent/pull/164) at `091e53d`):** Next-TODO **item 6 / [#130](https://github.com/hherb/hhagent/issues/130)** — reentrant `serial_lock` (`parking_lot::ReentrantMutex`) taken around the launchd window in `bring_up_pg_cluster` (macOS); fixes within-binary launchd-registration contention. Bundled **[#163](https://github.com/hherb/hhagent/issues/163)** (separate, found during live validation): `injection_guard_e2e` over-long data label overflowed macOS `sun_path` → shortened labels + added a `check_socket_path_fits` guard. Live: `injection_guard_e2e` 6/6, `secret_vault_e2e` 11/11, `postgres_e2e` 57/57, zero timeouts. See Recently-completed. **Done (merged to `main` via PR [#162](https://github.com/hherb/hhagent/pull/162) at `a83be4a`):** Next-TODO **item 12** — found already-implemented (Slice F, 2026-05-19); reconciled the stale note + added a RouterAgent-layer regression test pinning the graph-lane seed thread (`formulate_plan` → `build_with_seeds` + `FormulationMeta.graph_seed_*`); zero production change. See Recently-completed. **Done (merged to `main` via PR [#161](https://github.com/hherb/hhagent/pull/161) at `12b080c`, code commits `d5db4ba` + `ca827d7`):** Next-TODO item 8b / [#153](https://github.com/hherb/hhagent/issues/153) — cleared every workspace clippy warning and flipped `linux-check` to `-D warnings`; **#153 CLOSED**; see Recently-completed. **Done (merged to `main` via PR [#160](https://github.com/hherb/hhagent/pull/160) at `fd7dd7a`, code commit `379f77a`):** Next-TODO item 5 — `tool_host.rs` sibling-lift (911 → 519 LOC; watchdog + lockdown_env + seal tests lifted to child modules; trust-boundary core stays in parent; see Recently-completed). **Done (merged to `main` via PR [#159](https://github.com/hherb/hhagent/pull/159) at `1106145`):** Next-TODO item 4b — `injection_guard.rs` test-module sibling-lift (667 → 338 LOC, under cap; see Recently-completed). **Done last session (merged to `main` via PR [#158](https://github.com/hherb/hhagent/pull/158) at `f3c380f`):** Issue [#156](https://github.com/hherb/hhagent/issues/156) — `walk()` depth-skip now continues scanning siblings; #156 CLOSED. **Also merged (PR [#157](https://github.com/hherb/hhagent/pull/157) at `53e68ed`):** Issues [#148](https://github.com/hherb/hhagent/issues/148) + [#149](https://github.com/hherb/hhagent/issues/149) — secret-vault test gaps closed; both CLOSED. **Done earlier:** PR [#155](https://github.com/hherb/hhagent/pull/155) (#143 `walk()` depth guard) merged to `main` at `6e82252`; #143 closed; that merge filed [#156](https://github.com/hherb/hhagent/issues/156) (still OPEN). PR [#152](https://github.com/hherb/hhagent/pull/152) (#144 + #150) merged at `560d845`; `linux-check` CI green. Still optional (operator action, no code): a full `cargo test --workspace` on the DGX — CI is compile-only, so the Linux *test* baseline is still the stale `990`-at-`1abb061`.

**Secrets / injection-guard follow-ups (continue the Item 30/31 arc):**

4. **[#142](https://github.com/hherb/hhagent/issues/142) — injection-guard chat-template false-positives.** **Deferred this session per the issue author's recommendation** (`#143` shipped; see Recently-completed). Chat-template tokens (`<|im_start|>`, `<|system|>`) will false-positive on technical docs once `web-fetch`/MCP workers land — pick a fix (per-tool exemption hook, AND-heuristic, or catalogue tightening) *with real data* once such a worker exists, not before. `#143` (`walk()` depth guard) is **done** (`MAX_WALK_DEPTH = 256`).
4b. ~~**`injection_guard.rs` test-module sibling-lift.**~~ **DONE — merged to `main` via PR [#159](https://github.com/hherb/hhagent/pull/159) at `1106145`** (code commit `c0bb178`): lifted the inline `#[cfg(test)] mod tests` into a sibling `core/src/cassandra/injection_guard/tests.rs`; production LOC 667 → 338, under the cap. See Recently-completed.
5. ~~**`tool_host.rs` sibling-lift.**~~ **DONE this session** (branch `refactor/item-5-tool-host-sibling-lift`, code commit `379f77a`, PR [#160](https://github.com/hherb/hhagent/pull/160)): lifted the watchdog subsystem → `tool_host/watchdog.rs`, lockdown-env derivation → `tool_host/lockdown_env.rs`, and the seal tests → `tool_host/tests.rs`; production LOC **911 → 519**. The dispatch chokepoint, `WorkerCommand` seal, and `SupervisedWorker` stay in the parent (trust boundary). 519 is 19 over cap — the irreducible trust-boundary residual. See Recently-completed.

**Test-infra / refactor bucket:**

6. ~~**[#130](https://github.com/hherb/hhagent/issues/130) — parallel-launchd bring-up contention under `HHAGENT_PG_BIN_DIR`.**~~ **DONE this session** (branch `fix/issue-130-launchd-bringup-serialization`): `bring_up_pg_cluster` now takes a reentrant `serial_lock` (`parking_lot::ReentrantMutex`) around the launchd window on macOS, serializing within-binary registration. Also fixed bundled [#163](https://github.com/hherb/hhagent/issues/163) (over-long-`data_label` `sun_path` overflow in `injection_guard_e2e`). See Recently-completed. **Residual:** cross-process serialization (issue Option 2, flock) deferred — `cargo` runs binaries sequentially so it wasn't needed; revisit only if a workspace flake persists.
7. **[#134](https://github.com/hherb/hhagent/issues/134) — `tests-common`: revise the `bring_up_pg_cluster` doc example** or ship a real `_with_timeout` caller (the demonstrative Homebrew-detection caller deferred from #131).
8. **`HHAGENT_GLINER_RELEX_REQUIRE_E2E=1` CI knob** — turn the container e2e's skip-as-pass into a hard fail for any CI runner with PG + container + image + weights staged.
8b. ~~**[#153](https://github.com/hherb/hhagent/issues/153) — clear clippy warnings, then flip `linux-check` to `-D warnings`.**~~ **DONE — merged to `main` via PR [#161](https://github.com/hherb/hhagent/pull/161) at `12b080c`; #153 CLOSED** (branch `fix/issue-153-clippy-clean-gate`, commits `d5db4ba` + `ca827d7`): cleared the **whole workspace** (not just hhagent-core lib — the CI command is `--workspace --all-targets`) and flipped the CI clippy step to `-- -D warnings`. The gate is now enforced on both macOS (clippy 1.96, exit 0) and Linux (PR #161 `linux-check` green). See Recently-completed.
9. ~~**[#81](https://github.com/hherb/hhagent/issues/81) — split `core/src/scheduler/inner_loop.rs`**~~ **DONE this session — #81 CLOSED** (branch `refactor/inner-loop-test-module-lift`, code commit `cb561fd`): the second slice — inline `mod tests` lifted to `inner_loop/tests.rs`; production 655 → 438 LOC, under cap. See Recently-completed. ~~**[#99](https://github.com/hherb/hhagent/issues/99)** migrate `ask.rs`+`observation_replay.rs` to `common::with_runtime`~~ **DONE — merged to `main` via PR [#166](https://github.com/hherb/hhagent/pull/166) at `75e9039`** (branch `fix/issue-99-cli-with-runtime`; all six `hhagent-cli` dispatchers now share the `with_runtime` idiom; zero behaviour change; see Recently-completed); **`db/src/graph.rs` walk-impl split** (Item 23(b), deferred until a second `WalkedEdge` consumer materialises); **`KindsCli<T>` shared-generic lift** (Item 24, deferred until a third kinds-style CLI appears).
9b. **Next over-cap file split (refactor bucket).** After `inner_loop.rs` (done this session, #81 closed) and `replay.rs`/`tool_dispatch.rs` (prior sessions), a **full census** of every over-cap production file shows the remaining candidates split into two buckets. **(a) Clean test-lifts** (inline `mod tests` block large enough that lifting it alone lands the parent under cap — re-census the exact split before picking): `core/src/memory/l0_seed.rs` (730, `mod tests` at L462 → ~461 prod), `core/src/observation/capture.rs` (715, L373 → ~372 prod), `core/src/memory/recall.rs` (622, L406 → ~405 prod), `sandbox/src/macos_seatbelt.rs` (604, L332 → ~331 prod). **(b) Need a real prod split or a re-exported pure-helper seam** (a test-lift alone leaves the parent over cap): `db/secrets.rs` (848, ~645 after a test-lift), `core/src/cli_audit.rs` (771, ~670 after a test-lift), `core/src/main.rs` (729, ~690 prod, almost no inline tests), `core/workers/gliner_relex.rs` (861, tests already lifted), `db/graph.rs` (926, the design-gated Item 23b walk-impl split — deferred until a 2nd `WalkedEdge` consumer materialises), `supervisor/systemd_user.rs` (798, ~502 prod — basically at cap). Same pure-mechanical pattern where a seam exists; the existing suite is the regression pin.

**Engineering pickups (need a spec/design first):**

10. ~~**L3 skill crystallisation — spec.**~~ **DONE this session (branch `feat/l3-skill-crystallisation`, off `main` at `98a5be0`, tip `0505986`, PR [#173](https://github.com/hherb/hhagent/pull/173) OPEN)** — spec + plan + 14-task subagent-driven implementation. L3 writer shipped: agent emits `Plan.l3_skill` → `drain_lane` hook validates/dedups → stores at `layer=3`; writer-only / non-executable / `trust:"untrusted"`. See Recently-completed. **NOTE:** the spec's "total_steps_executed" grounding accumulator was implemented by reusing the existing `dispatch_count` counter (behaviourally identical; no separate accumulator). **Follow-ups (new Next-TODO candidates):**
   - ~~**(a) `l3_crystallise.rs` test-lift**~~ **DONE this session** (branch `refactor/l3-crystallise-test-module-lift`, code commit `34bc262`): lifted the inline `#[cfg(test)] mod tests` block to a re-exported sibling `core/src/memory/l3_crystallise/tests.rs`; production **676 → 467 LOC** (under cap). Pure-mechanical, zero behaviour change (production region L1–464 byte-identical; workspace **1177/0/3** unchanged; 20 unit tests pass from the new location; clippy `-D warnings` exit 0; doc-links 21 = `main`'s). See Recently-completed. Prerequisite for further L3 slices now cleared.
   - ~~**(b) Skill trust enum + approval gate**~~ **DONE this session — the GATE half** (branch `feat/l3-skill-approval-gate`, tip `71188e3`, **PR [#176](https://github.com/hherb/hhagent/pull/176) OPEN**): `SkillTrust` enum (`Untrusted|UserApproved|Pinned`, fail-safe parse); pure `evaluate_approval` gate (structural re-validation + `secret://`-ref scan + tool-existence vs the latest `registry.loaded` snapshot, fail-closed); layer-guarded db `set_skill_trust`; `hhagent-cli memory l3 {approve,revoke}` + `l3.approved`/`l3.approve_rejected`/`l3.revoked` audit rows. Trust flips → `user_approved` ONLY on `Approve`; rejects audited + leave trust untrusted. **No execution.** `Pinned` defined but command-less. Tool-existence is the `registry.loaded`-snapshot best-effort check; the LIVE re-check is deferred to invocation (TOCTOU). The Next-TODO note's "policy hash" was interpreted as the template `body_sha256` recorded in the `l3.approved` payload. See Recently-completed. **(b-next, the DOOR — still open):** L3 invocation/execution — substitute `{{params}}`, re-validate every tool against the **live** `ToolRegistry` at dispatch, run the template steps through the sandbox; only `UserApproved`/`Pinned` skills runnable. Needs (c) first (the planner must see the skill to invoke it).
   - ~~**(c) Recall surfacing**~~ **DONE this session** (branch `feat/l3-skill-recall-surfacing`, tip `ed226e1`, **PR [#177](https://github.com/hherb/hhagent/pull/177) OPEN**): a `<skills>` prompt block surfacing only `UserApproved`/`Pinned` L3 rows to the planner at context-assembly time, analogous to `<l1_insights>` (L0 → L1 → skills → recalled → base). New `core/src/memory/l3_surface.rs` (`SurfacedSkill` projection + fail-safe parse + `is_surfaceable` gate + `render_skill_entry` + `cap_surfaced` + `load_l3_skills_default`); `assemble_system_prompt` `<skills>` block; `skill_count` threaded through `AssembledPrompt`/`FormulationMeta` + emitted in the `plan.formulate` audit payload; prompt documents the block reference-only/no-invoke. **Surfacing-only — no invocation.** Final review caught + fixed a param-description `</skills>` breakout (now guarded in `validate_l3_skill`). Workspace 1220→1243. See Recently-completed. **The remaining L3 arc is now just (b-next), the DOOR:** invocation/execution — substitute `{{params}}`, re-validate every tool against the **live** `ToolRegistry` at dispatch, run the template steps through the sandbox; only `UserApproved`/`Pinned` runnable; the `pin` command lands with it.
11. **Worker manifest plumbing — design slice.** Spec open question 1 (TOML files vs Rust consts) is unresolved; slices today ship `Lifecycle` directly on `ToolEntry`.
12. ~~**Production caller wiring for the graph lane.**~~ **ALREADY DONE — discovered stale this session.** The wiring shipped in the 2026-05-19 entity-extraction-v2 / "Slice F" arc (PR #91): [`RouterAgent::formulate_plan`](../../../core/src/scheduler/agent.rs#L122) calls `entity_extractor.extract(&ctx.instruction)` every iteration and threads `&seeds.ids` into `recall_builder.build_with_seeds(...)` (graph lane ON via `RecallParams::with_seeds`); [`main.rs`](../../../core/src/main.rs#L174-L260) wires the real `GlinerRelexExtractor` (NoOp fallback only when the worker is disabled). The Option-P note below was written *before* Slice F and never reconciled. This session closed the one genuine residual — a RouterAgent-layer regression test pinning the seed thread (see Recently-completed). **Remaining (separate, NOT item-12 wiring):** the quarantine review gate means freshly-extracted entities default `quarantine=TRUE` and [`graph_search`](../../../db/src/memories.rs#L469-L500) filters `quarantine=FALSE`, so seed entities don't surface memories until an operator un-quarantines them (open issue [#40](https://github.com/hherb/hhagent/issues/40) tracks the graph-default policy question).

**Operator actions (no code):** recapture observation fixtures against the current daemon (`cargo test -p hhagent-core --test observation_capture -- --ignored --nocapture`); real-model relation-extraction validation (`HHAGENT_GLINER_RELEX_ENABLE=1 cargo test … entity_extraction_e2e`); Linux DGX full `cargo test --workspace` re-verification once #144 is fixed; DGX verification of the #89 `/tmp` tmpfs pin (pass-on-current-code + catches-drift via temporary `--tmpfs`→`--bind-try` mutation).

---

## Design notes for parked work

### Option P — entity↔memory linkage + graph lane (Phase 1 cont.)

The `memory_entities` join table (P1) shipped; the graph lane is wired into `recall`. **The production caller wiring (former Next-TODO item 12) is DONE** — it shipped in the 2026-05-19 entity-extraction-v2 / "Slice F" arc (PR #91): `RouterAgent::formulate_plan` populates `seed_entity_ids` from `entity_extractor.extract(&ctx.instruction)` on every iteration; `main.rs` wires the real `GlinerRelexExtractor`. (This note previously described that work as parked — it predated Slice F and was reconciled 2026-05-30.) For a query carrying `seed_entity_ids`, the lane traverses outbound 1-hop to a candidate entity set, then `SELECT memory_id FROM memory_entities WHERE entity_id = ANY($1)` ranked by neighbour count. **Remaining parked work is the quarantine review gate, not the wiring:** freshly-extracted entities default `quarantine=TRUE` and `graph_search` filters `quarantine=FALSE`, so seed entities surface no memories until an operator un-quarantines them (open issue [#40](https://github.com/hherb/hhagent/issues/40) tracks the graph-default policy question). Secondary deferral: `entities.embedding` is NULL for all entities; a populated column would seed an entity-similarity lane. Deferred until observation phase; the `vector(1024)` column already exists.

### Option K — cross-platform exponential restart backoff

Currently `Restart=on-failure RestartSec=5` is a constant 5 s. systemd 252+ supports `RestartSteps`/`RestartMaxDelaySec`; macOS launchd's `KeepAlive=true` has no operator-controllable throttle. Cross-platform shape: extend `ServiceSpec` with `restart_backoff: Option<RestartBackoff>` (max delay + step count); the systemd backend wires it into the unit file, the macOS backend logs a warning at install time and falls back to launchd's default. Filed but parked; no immediate need.

---

## Open follow-up issues (filed but not picked)

Only currently-open issues are listed; closed-issue detail lives in the archive snapshots and git history.

- [#3](https://github.com/hherb/hhagent/issues/3) — drop `SYS_SENDFILE`/`SYS_FADVISE64` shim once libc exposes them on aarch64.
- [#4](https://github.com/hherb/hhagent/issues/4) — bump Last-commit + test-count fields whenever a Recently-completed entry is added (process hygiene).
- [#8](https://github.com/hherb/hhagent/issues/8) — collapse `default_probe`/`default_supervisor` cfg-ladder duplication once a third entry point or backend OS appears.
- [#13](https://github.com/hherb/hhagent/issues/13) — write a migration numbering / rename hygiene checklist (sqlx fingerprints version+slug; a rename on a shipped migration silently breaks startup on existing clusters).
- [#14](https://github.com/hherb/hhagent/issues/14) — replace the brittle `wait_for_log_match("database probe succeeded")` in `supervisor_e2e.rs` with a real readiness signal or a public constant.
- [#20](https://github.com/hherb/hhagent/issues/20) — `agent_prompts` PK on sha256 means renamed prompt files lose their original name *(note: 0011 changed the PK to `(sha256, name)`; issue tracks any residual)*.
- [#21](https://github.com/hherb/hhagent/issues/21) — scheduler per-iteration cancellation poll could be a `watch::Receiver` instead of a DB round-trip.
- [#24](https://github.com/hherb/hhagent/issues/24) — deployment: `HHAGENT_PROMPTS_DIR` has a cwd-relative fallback; production unit files must set it explicitly.
- [#37](https://github.com/hherb/hhagent/issues/37) — scheduler crash-recovery sweep+audit is unoptimised for high crash counts.
- [#39](https://github.com/hherb/hhagent/issues/39) — tests-common optional hardening (PgCluster.sup access, internal self-tests).
- [#40](https://github.com/hherb/hhagent/issues/40) — design: should `RecallParams::new()` default to graph-off until an entity-extraction step lands? *(partially addressed by `with_seeds`; tracks the residual design question.)*
- [#42](https://github.com/hherb/hhagent/issues/42) — `deleted_memories` AFTER DELETE trigger uses `SECURITY INVOKER`; a future role with DELETE-on-memories but no INSERT-on-deleted_memories would silently break DELETE. Deferred until a second DELETE-capable role is proposed.
- [#47](https://github.com/hherb/hhagent/issues/47) — observation/capture: distinguish 'no verdict row' from a real Approve verdict *(SCHEMA_VERSION 2 made `verdict_today` Optional; tracks residual.)*
- [#50](https://github.com/hherb/hhagent/issues/50) — unify finalize-payload provenance signal across crashed/producer-cancelled/runtime emitters *(`provenance` field shipped; tracks residual unification.)*
- [#55](https://github.com/hherb/hhagent/issues/55) — macOS Apple `container` micro-VM backend *(discovery spike shipped + Slices 1/2/2.5 shipped; issue tracks the broader rollout.)*
- [#62](https://github.com/hherb/hhagent/issues/62) — audit-payload truncation can silently nuke `agent/plan.formulate` fields.
- [#63](https://github.com/hherb/hhagent/issues/63) — e2e gap: classification_floor plumbing from `tasks.payload` to the `agent/plan.formulate` audit row.
- [#73](https://github.com/hherb/hhagent/issues/73) — scheduler/runner e2e integration test + TaskContext-construction reminder for producer-side floor-source validation.
- [#76](https://github.com/hherb/hhagent/issues/76) — prompt-assembly: verify PromptAssembly error retry semantics in scheduler.
- [#78](https://github.com/hherb/hhagent/issues/78) — prompt-assembly: global token cap with priority drop for the assembled system prompt.
- ~~[#81](https://github.com/hherb/hhagent/issues/81) — split `core/src/scheduler/inner_loop.rs` (still over cap)~~ **CLOSED — fixed (2nd slice: inline `mod tests` lifted to `inner_loop/tests.rs`; 655 → 438 LOC, under cap), branch `refactor/inner-loop-test-module-lift`, code commit `cb561fd`.**
- ~~[#99](https://github.com/hherb/hhagent/issues/99) — migrate `ask.rs` + `observation_replay.rs` to `common::with_runtime` for consistency~~ **CLOSED — merged to `main` via PR [#166](https://github.com/hherb/hhagent/pull/166) at `75e9039`.**
- [#104](https://github.com/hherb/hhagent/issues/104) — audit the pid+nanos tempdir pattern across the workspace (follow-up to #101).
- [#107](https://github.com/hherb/hhagent/issues/107) — `MacosContainer` PID-1 signal-handling posture *(closed in code by always-on `--init` in Slice 2.5; verify end-to-end before long-lived workers migrate).*
- [#127](https://github.com/hherb/hhagent/issues/127) — env-var save/restore RAII helper for the `pg_bin_dir_candidates_with_env_override` tests.
- ~~[#130](https://github.com/hherb/hhagent/issues/130) — parallel-launchd bring-up contention under `HHAGENT_PG_BIN_DIR` override~~ **CLOSED — fixed (reentrant `serial_lock` around the launchd window in `bring_up_pg_cluster`), branch `fix/issue-130-launchd-bringup-serialization`.** ~~[#163](https://github.com/hherb/hhagent/issues/163) — `injection_guard_e2e` over-long `data_label` overflows macOS `sun_path`~~ **CLOSED — fixed in the same PR (shortened labels + `check_socket_path_fits` guard).**
- [#134](https://github.com/hherb/hhagent/issues/134) — tests-common: revise `bring_up_pg_cluster` doc example or ship a real `_with_timeout` caller.
- [#142](https://github.com/hherb/hhagent/issues/142) — injection_guard: chat-template tokens will false-positive on legitimate technical docs (see Next-TODO item 4).
- ~~[#153](https://github.com/hherb/hhagent/issues/153) — CI: clear clippy warnings, then flip `linux-check` to `-D warnings`~~ **CLOSED — fixed (whole-workspace clippy clean + `-- -D warnings` gate), merged via PR [#161](https://github.com/hherb/hhagent/pull/161) at `12b080c`.**
- ~~[#143](https://github.com/hherb/hhagent/issues/143) — injection_guard `walk()` has no recursion depth limit~~ **CLOSED — fixed (`MAX_WALK_DEPTH = 256`), merged via PR #155 at `6e82252`.**
- ~~[#148](https://github.com/hherb/hhagent/issues/148) — secrets: cover audit-insert failure paths with fault injection~~ **CLOSED — fixed (`AuditSink` seam + `dispatch_with_sink`), merged via PR #157 at `53e68ed`.**
- ~~[#149](https://github.com/hherb/hhagent/issues/149) — secrets: `Vault::materialize` ref-collision branch is untested~~ **CLOSED — fixed (`insert_fresh` extraction + unit test), merged via PR #157 at `53e68ed`.**
- ~~[#156](https://github.com/hherb/hhagent/issues/156) — injection_guard: `walk()` truncation aborts the whole walk, skipping later siblings~~ **CLOSED — fixed (depth-skip now continues siblings; byte-cap residual documented as inherent to accumulate-then-scan), merged via PR #158 at `f3c380f`.**

---

## Open questions parked for later

(From the design plan, restated here so they're surfaced when relevant.)

1. Embedding model on-device — bge-m3 vs nomic-embed-text vs ColBERT (Phase 1)
2. ~~Channel approval — passcode pairing vs static contact allowlist (Phase 2)~~ **Resolved 2026-05-06:** pairing flow with WebAuthn-or-OTP fallback, modeled on ZeroClaw's `security/{pairing,webauthn,otp}.rs`.
3. ~~Egress proxy as separate worker vs in-process in `tool_host`~~ **Resolved 2026-05-06:** separate worker, with the credential-leak scanner co-located.
4. Skill review workflow for *named* agent-authored Python (Phase 4) — see Phase 4 line items: trust enum + per-level capability ceiling.
5. Worker keep-alive vs spawn-per-call (idle-timeout lifecycle shipped for GLiNER-Relex; revisit for other workers when latency matters).
6. Worker binary discovery in production (currently `target/debug/...` for tests; need a stable install location convention).

## Inspirations / things to read before each milestone

Two adjacent OpenClaw-derived projects ship code we can read (Apache-2.0/MIT, AGPL-compatible) before each new milestone — convergent prior art saves design time:

- **ZeroClaw** ([`zeroclaw-labs/zeroclaw`](https://github.com/zeroclaw-labs/zeroclaw), 100% Rust): read [`crates/zeroclaw-runtime/src/security/`](https://github.com/zeroclaw-labs/zeroclaw/tree/main/crates/zeroclaw-runtime/src/security) — has working `bubblewrap.rs`, `landlock.rs`, `seatbelt.rs`, `firejail.rs`, `pairing.rs`, `webauthn.rs`, `leak_detector.rs`, `workspace_boundary.rs`. Architectural drawback vs us: tools run as in-process Rust traits, OS sandbox wraps the runtime — weaker boundary than our process-per-worker. Don't copy the in-process tool model.
- **IronClaw** ([`nearai/ironclaw`](https://github.com/nearai/ironclaw)): read its dispatcher chokepoint pattern (`ToolDispatcher::dispatch()` is the single audit/safety-validation funnel for *every* action, regardless of caller). Drawbacks: WASM-as-boundary is software-only containment; Postgres+libSQL dual backend is overkill at our stage.

The *defining* architectural difference: hhagent enforces **one OS process + one bwrap/Seatbelt jail per worker**. Both reference projects retreated from that. Don't.

---

## How to update this document at session end

**Header first, prose last.** The header is what the next session reads first
and treats as authoritative; stale header fields silently mislead future
sessions even when the prose is correct. Follow the steps in this order:

1. **Bump header fields at the top — before writing any prose:**
   - `Last updated:` → today's date.
   - `Last commit on <branch>:` → the hash of the most recent shipped commit.
     Confirm with `git log --oneline -1`.
   - `Session-end verification:` → re-run `cargo test --workspace` and copy
     the **passed / failed / ignored / `[SKIP]`** counts into this line.
   - **Every test-count number embedded elsewhere in the doc that changed
     this session** (e.g. the headline test count, "Test count delta" lines
     in Recently-completed entries). A fresh agent grep-finds them and will
     trust whatever is there.
2. **Move "Next TODO" → "Recently completed (this session)"** if the picked option shipped, with enough detail that the next session can understand the decision (file paths, why-not-X, gotchas, test-count delta).
3. **Write a fresh "Next TODO (pick one)"** with options sized for one session each — include file paths, gotchas, and the verification step.
4. **Refresh "Working state"** — anything new under stubs, anything that became real.
5. **Tick the matching items off in [`../ROADMAP.md`](../ROADMAP.md)** with the commit hash.
6. **Commit both files together** with a `docs(handover): ...` message.

### Pruning convention

The handover should stay focused on **what the next session needs to act on**: the current state, the last 2–3 sessions in detail, and the next TODO. Older session entries get compressed into the "Earlier history" summary or dropped entirely once they're no longer load-bearing.

When HANDOVER.md grows past the point where the next session can absorb it cold (rough rule of thumb: more than a couple of screens of "Recently completed"), prune it:

1. **Snapshot first.** Copy the current HANDOVER.md to `archive/handover_<YYYYMMDD>[_<slug>].md` (e.g. `handover_20260510_pre-prune.md`). The archive is the audit trail — never edited after the fact, never deleted.
2. **Keep verbatim:** the header, "Read these first," "Working state" (current truth), the most recent 1–2 sessions of "Recently completed," "Key design decisions," "Next TODO," "Open follow-up issues," "Open questions," "Inspirations," and this section.
3. **Compress everything else** into a single "Earlier history" section: one bullet per session, naming the slice + the headline change + a pointer to the archive snapshot for full reasoning.
4. **Cross-link** from the compressed bullets to the archive snapshot so anyone who needs the full reasoning can find it.
5. **Commit the prune separately** with `docs(handover): prune older sessions, archive pre-prune snapshot` so the diff is reviewable.

The archive directory is the historical record; HANDOVER.md is the working brief.
